# Project Single Source of Truth — the registry

**One index, many owners.** This is the project-wide map of *where the truth for any fact lives*.
It does **not** copy the data — it points to the one authoritative owner of each domain and states
the rule: **reference, never restate.** A number that appears in two places without a reconcile
guard is a latent drift bug (this is Law 1 of the Deliverable Excellence Standard, and the
"correspondence rules" of ISO/IEC/IEEE 42010 applied at project scale).

> Why not one mega-file? Because these domains have different update cadences (code = every commit;
> assessment facts = every collection; memory = every session; version = every release), formats
> (AST graph vs JSON vs markdown vs TOML), and producers. Consolidating the *storage* would drift
> the instant any owner updates. So we consolidate the **index** (this file) and federate the
> **storage** (the owners below), with a mechanical guard on the facts that legitimately live in
> two homes.

## The map — for any fact, read it from its one owner

| Domain of truth | Authoritative owner | How to read it (never re-derive) | Enforcement |
|---|---|---|---|
| **Atlas detached generated-PDF visual review** — authenticated page-by-page visual evidence for one immutable generated-PDF family; it is not a release-gate transition | verifier = `master-reference/release/pdf_review.py`; schemas = `master-reference/release/schemas/pdf-review.schema.json`, `pdf-review-signature.schema.json`, `pdf-reviewer-key-policy.schema.json`, and `pdf-review-result.schema.json`; immutable subject owners = `release-manifest.json`, `family-attestation.json`, `pdf-gate.json`, and `master-reference.pdf` | run `python -m cli verify-pdf-review` with the family manifest plus four separately custodied inputs. The payload binds exact source/manifest/attestation/gate/PDF identities, the fixed 144-DPI `pdftoppm` render profile, and strictly ordered page rows `1..page_count`, each with rendered byte count, SHA-256 and review disposition. Public helpers own the non-circular review-evidence digest and domain-separated signing material. The CLI verifies family receipts before and after, reloads all external evidence, and emits only canonical `pdf-review-result/1` bytes to stdout | v1 accepts only `generated_visual_review_pending`; the signed PNG hashes and render profile are authenticated reviewer assertions, not mechanically recomputed PDF-to-PNG provenance because PNG bytes are not inputs. It does not discover reviewer trust, place evidence inside the exact-census family, or infer a signature from prior QA. Even an authenticated all-page PASS is `verified_visual_pass_not_promoted`; current/global gates, family mutation, owner-manifest signature, accessibility, binary privacy and publication authority remain fixed false. No accepted signed PDF-review envelope currently exists. Focused gates: `master-reference/tests/release/test_pdf_review.py` and `master-reference/tests/release/test_release_pipeline.py` |
| **Assessment headline facts** — device/collected/endpoint/VLAN counts, health posture, lifecycle bands, #design-decisions | `cisco_toolkit/ssot.py :: CANONICAL_FACTS` → published at `executive_brief.scale/posture`, `lifecycle_risk.summary`, `design_blueprint.summary` | `ssot.canonical_facts(snap)` | ✅ **CI-enforced** — `ssot.reconcile()` + cross-surface/dashboard/webapp locks. Detail contract: [`docs/ssot-contract.md`](ssot-contract.md) |
| **Coverage census** (J3) — per-section coverage map: what the snapshot actually SAW vs a blind spot (SuzieQ `describe` analog) | `cisco_toolkit/ssot.py :: compute_schema_census` → published at `snap['schema_census']` (`schema_census/1`) | `ssot.compute_schema_census(snap)`; each section's `state` reuses `ssot.abstention_reason` | derives from `abstention_reason` (the 3-state token owner); non-volatile (presence/absence + shape) → frozen in `tests/golden/snapshot.json`; logic pinned by `tests/test_schema_census.py` |
| **Runtime protocol assessability** — the current-run device × seven-family evidence denominator, capture/input state, sparse-health-row witness, and abstention reason | family/state contract + producer = `cisco_toolkit/analyze.py :: PROTOCOL_ASSESSABILITY_FAMILIES`, `PROTOCOL_ASSESSABILITY_STATES`, and `compute_protocol_assessability` → published at `snap['protocol_assessability']` (`protocol_assessability/1`) | read the published receipt; do not reconstruct it from sparse `protocol_health` rows. It reports capture/parser reachability, not configured-protocol applicability or expected-neighbor intent | exact family/cell totality, capture precedence, phase failure, privacy, workbook, pipeline, and cross-language workspace binding are pinned by `tests/test_protocol_assessability.py`, `tests/test_pipeline_inprocess.py`, and `tests/test_master_reference_protocol_depth.py`; deterministic snapshots are frozen in `tests/golden/snapshot.json` and `webapp/sample_data/sample_fleet.snapshot.json` |
| **VTP safety gate** — the bounded local `show vtp status` attempt/subject denominator, coverage census, and pre-cutover disposition for high-revision Server observations | contract/producer/validator = `cisco_toolkit/vtp_safety.py :: compute_vtp_safety_subject_scope`, `scope_vtp_safety_subjects`, `compute_vtp_safety_baseline`, `validate_vtp_safety_baseline`, and `embedded_vtp_safety_baseline` → published at `snap['vtp_safety_baseline']` (`vtp_safety_baseline/1`) | consume the validated receipt; do not reconstruct VTP safety from sparse `protocol_health`, `protocol_intelligence`, raw captures, or UI copy. `summary.by_status` owns subject-row counts; `summary.by_coverage_status` owns the distinct producer fleet census and `coverage[]` is per-host detail only. A VTP Server revision of 100 or higher is a conservative `REVIEW` heuristic requiring explicit disposition, not proof that overwrite or propagation will occur; matching it is **NOT ACCEPTANCE**. Missing capture/parser custody is `NOT VERIFIED`. Current-run decisions require the process-local marker; JSON audit copies are `embedded_unverified`, and a serialized `current_run_source_bound` claim cannot authorize a decision. `NOT_APPLICABLE` means no positive local VTP-status subject was identified, not platform or network absence | exact scoping, parsing, source custody, semantic/census/digest reconciliation, hostile/deep input, casefold collision, marker-loss, high-revision, and explicit-no-subject behavior are pinned by `tests/test_vtp_safety_baseline.py`. The **VTP Safety** workbook, runbook, and Explorer retain every `REVIEW` and `NOT VERIFIED` subject plus the first 50 assessed subjects, preserve separate health/intelligence detail, fail closed without reflecting rejected leaves, and render no raw capture, hash, source locator, or path; this is pinned by `tests/test_vtp_operator_surfaces.py` and `tests/test_master_reference_protocol_depth.py`. No VLAN-database equality or contents, advertisement/per-VLAN propagation, intended version/compatibility, pruning, password/authentication, freshness, revision-reset safety, failover, or cutover authorization is proved |
| **Source-bound VTP/VLAN database evidence and change assurance** — additive Release-1 local observations and before/after safety transitions; the protected `vtp_safety_baseline/1` owner above remains unchanged | evidence producer/validator = `cisco_toolkit/vtp_extended.py :: compute_vtp_extended_evidence`, `validate_vtp_extended_evidence`, and `embedded_vtp_extended_evidence` → published at `snap['vtp_extended_evidence']` (`vtp_extended_evidence/1`); transition owner = `cisco_toolkit/protocol_deltas.py :: compute_vtp_safety_delta` (`vtp_safety_delta/1`) | consume the strict additive receipt together with the protected v1 co-owner. It binds each snapshot-device subject to exact `show vtp status`, `show vlan brief`, and full `show running-config` source custody and owns mode/domain/version/revision, domain+version database identity, a canonical local VLAN-database digest/count, pruning state, and authentication configured-presence only. It never serializes an authentication secret or a device-reported authentication digest. Same-database digest/authentication contradictions and high-revision Server state are current unsafe conditions. Every healthy movement defaults to `review`; current unsafe state remains `block`, including appeared or unchanged degradation. A revision decrease remains only an observation unless an exact-subject `cutover_change_intent/1` row explicitly carries `intent_kind=revision_reset`; wildcard, generic exact-subject movement, and inferred reset are forbidden. Missing, renamed, truncated, malformed, contradictory, or detached evidence is `coverage_lost`/`not_comparable` and cannot PASS. The native delta owns no score or overall verdict; `cutover_gate/1` remains the sole decision owner | real IOS/NX-OS-shaped sources, secret suppression, command/source/custody mutations, semantic reseals, legacy fail-closed behavior, exact reset-intent reconciliation, appeared/current unsafe gate behavior, pipeline publication, canonical compare receipts, portfolio subject caps/complete export, and generic Protocol Family Changes workbook parity are pinned by `tests/test_vtp_extended_evidence.py`, `webapp/tests/test_vtp_compare_api.py`, `webapp/tests/test_protocol_portfolio.py`, `tests/test_pipeline_inprocess.py`, `tests/test_protocol_deltas.py`, and `tests/golden/snapshot.json`. Scope is bounded local observed safety on IOS/IOS-XE/NX-OS live or offline import; it does not prove advertisement delivery, simultaneous distributed state, propagation, reset execution, convergence, or service survival |
| **IPv6 routing adjacency gate** — the bounded observed-runtime OSPFv3 and IPv6-unicast BGP adjacency denominator for default/global routing, plus its distinct route-summary/OSPFv3/BGPv6 host-input coverage census | contract/producer/validator = `cisco_toolkit/ipv6_routing.py :: compute_ipv6_routing_adjacency_baseline`, `validate_ipv6_routing_adjacency_baseline`, `embedded_ipv6_routing_adjacency_baseline`, and `compute_ipv6_routing_subject_scope` → published at `snap['ipv6_routing_adjacency_baseline']` (`ipv6_routing_adjacency_baseline/1`) | consume the validated receipt; do not reconstruct IPv6 acceptance from legacy `ipv6_routing`, sparse `protocol_health`, raw captures, or UI copy. `summary.by_status` owns observed adjacency-row counts; `summary.by_coverage_status` alone owns the producer fleet census, while `coverage[]` is host-family/input drilldown only. `FULL`/`2WAY` OSPFv3 and `Established` BGPv6 are bounded observed states; every `degraded`, `review`, and `not_verified` row is a blocker, and matching a degraded state is **NOT ACCEPTANCE**. Current-run decisions require process-local source custody; JSON audit copies are `embedded_unverified`, and a serialized `current_run_source_bound` claim fails closed | exact parser/semantic/census/digest/custody behavior is pinned by `tests/test_ipv6_routing_adjacency_baseline.py`. The **IPv6 Routing** workbook and runbook accept only structurally valid embedded receipts and retain every degraded/review/not-verified row plus the first 50 assessed rows. The Explorer initially renders at most 200 blocker rows plus the first 50 assessed rows, reports exact rendered/total/omitted counts, and exports every validated blocker row through a bounded safe JSON projection. These surfaces use coverage-only subjects for Explorer host drilldown, take fleet coverage only from `summary.by_coverage_status`, and render bounded commands/custody plus exact observed peer/state without raw captures, SHA values, source locators, or paths; this is pinned by `tests/test_ipv6_routing_operator_surfaces.py` and `tests/test_master_reference_protocol_depth.py`. The denominator is observed default/global runtime adjacency only: it proves no configured/expected peers, VRF/other-AF completeness, route/prefix correctness, RIB/FIB/path selection, policy/best path, convergence, freshness, simultaneity, interoperability, or cutover authorization. Route summary is a point-in-time scope census; prefix counts are informational. The before/after `protocol_adjacency_delta/1` owner remains IPv4 OSPF/BGP/EIGRP-only in this minimum slice; the current-state IPv6 receipt still reaches the shared current-baseline gate |
| **Observed single-snapshot STP consistency baseline** — the bounded current-run inconsistent-port truth used by migration readiness, Cutover Validation, the current-baseline gate, and NRFU | producer = `cisco_toolkit/analyze.py :: summarize_stp_consistency_baseline` (`stp_consistency_baseline/1`); subject scoping and receipt/health reconciliation stay inside that owner | consume the shared summary; do not parse the STP health prose or reconstruct cleanliness from sparse rows. Assessed/degraded consistency requires usable `state` and `inconsistent_ports` inputs plus exactly one matching well-formed STP health row. `blocked_ports` is disclosed independently as observed or not collected, and missing blocked-port evidence must never be rendered as zero; `topology_changes`/detail remains optional | a positive L2/STP subject with incomplete or contradictory consistency evidence becomes review/not verified, prevents a readiness pass, and survives as an INDETERMINATE/BLOCKED current-baseline HOLD through Validation/NRFU and the operator surfaces. A host with no positive L2/STP subject is omitted without a health or applicability claim. This is not configured-STP applicability, complete VLAN/MST-instance, timer, convergence, interoperability, or cutover-authorization proof. Focused gates: `tests/test_stp_consistency_truth.py`, `tests/test_stp_validation_surfaces.py`, `tests/test_pipeline_inprocess.py`, and `tests/test_master_reference_protocol_depth.py` |
| **Observed single-snapshot STP topology baseline and before/after delta** — the namespace-aware local PVST/MST root, per-port role/state, forwarding/blocked-path, and per-instance topology-change-counter evidence used by Release-1 cutover assurance | observation/baseline producer and validator = `cisco_toolkit/stp_topology.py :: produce_stp_topology_observation`, `compute_stp_topology_baseline`, `validate_stp_topology_observation`, and `validate_stp_topology_baseline` (`stp_topology_observation/1`, `stp_topology_baseline/1`) → published at `snap['stp_topology_observations']` and `snap['stp_topology_baseline']`; transition owner = `cisco_toolkit/protocol_deltas.py :: compute_stp_topology_delta` (`stp_topology_delta/1`) | consume the strict co-published observation/baseline and its owner-produced delta; do not reconstruct roles or counters from legacy interface ranges, collapse `pvst_vlan` and `mst_instance`, treat a missing counter as zero, or reinterpret a legacy snapshot as typed evidence. The baseline reconciles exact host/namespace/instance identities to the unchanged `stp_roots` projection and the snapshot device roster. The delta gains comparison authority only from the admitted exact before/after source-byte binding | valid complete evidence emits explicit root, path, and counter transitions without synthetic role/counter gaps. Missing, malformed, renamed, duplicated, or truncated required leaves remain `coverage_lost`/`not_comparable`; an unchanged current invalid role/state and an increased topology-change counter block. Root or role movement remains reviewable intent, never inferred safe. `l2_failure_rehearsal/1` consumes the same owner output only as current root-loss precondition coverage and explicitly proves neither loop freedom, convergence, simultaneous state, service continuity, nor failure survival. Legacy snapshots stay fail-closed and the protected STP consistency/v1 owners are unchanged. Focused gates: `tests/test_stp_topology_depth.py`, `tests/test_parsers.py`, `tests/test_parser_examples.py`, `tests/test_l2_failure_rehearsal.py`, `tests/test_protocol_deltas.py`, `webapp/tests/test_protocol_portfolio.py`, and `tests/test_pipeline_golden.py` |
| **Observed single-snapshot routing baseline** — the bounded OSPF, BGP, and EIGRP peer state that Cutover Validation and NRFU may ask an operator to preserve | producer = `cisco_toolkit/analyze.py :: summarize_routing_baseline`; state normalization owner = `normalize_routing_adjacency_state` (`routing_adjacency_baseline/1`) | consume the shared summary in Validation and NRFU; never rewrite observed peers as ideal `FULL`/`Established`, accept an exact baseline without a valid assessed `protocol_assessability/1` cell, or infer configured/expected peers from missing rows | degraded states, malformed/legacy/contradictory receipt abstention, source locators, `embedded_unverified` projection custody, downstream blocker retention, and Validation/NRFU parity are pinned by `tests/test_routing_cutover_truth.py`, `tests/test_routing_nrfu_truth.py`, `tests/test_routing_validation_surfaces.py`, and `tests/test_pipeline_inprocess.py` |
| **Configured BGP peer gate** — the literal configured-peer denominator for default/global IPv4 unicast and its reconciliation to current summary evidence | contract/producer/validator = `cisco_toolkit/bgp_intent.py :: compute_bgp_configured_peer_baseline`, `validate_bgp_configured_peer_baseline`, and `embedded_bgp_configured_peer_baseline` → published at `snap['bgp_configured_peer_baseline']` (`bgp_configured_peer_baseline/1`) | consume the validated receipt; do not reconstruct intent from sparse `routing_neighbors`, raw configuration, or UI copy. `summary.by_status` owns peer-row counts; the closed `summary.by_coverage_status` map owns the fleet host-coverage census, while `coverage[]` is host detail only. A configured-active peer absent from usable summary evidence is degraded; ambiguity is review and evidence/custody loss is not verified. `NOT_APPLICABLE` means no in-scope literal peer subject was identified; it is not proof that BGP is absent or that configuration coverage is complete. `CLEAR` is bounded to literal default/global IPv4-unicast peers | parser/reconciliation/custody/hostile-input/secret-suppression, exact coverage-map reconciliation, and missing-peer behavior are pinned by `tests/test_bgp_configured_peer_baseline.py`; uncapped workbook projection plus runbook/Explorer blocker retention, 50-row ordinary cap, exact scope/custody and `NOT_APPLICABLE` copy, distinct peer/host counts, fail-closed malformed census, configured-only and coverage-only subject drill-down, and Master Reference binding are pinned by `tests/test_bgp_operator_surfaces.py` and `tests/test_master_reference_protocol_depth.py`. VRFs, IPv6, VPNv4/EVPN, peer groups/templates, dynamic peers, policy, routes, best path, RPKI, convergence, freshness, interoperability, and cutover authorization remain explicitly excluded |
| **Configured FHRP group gate** — the direct-literal local HSRP/VRRP/GLBP group denominator for default/global IPv4, its reconciliation to subtype runtime evidence, and bounded sequential exact-candidate election-consistency review | contract/producer/validator = `cisco_toolkit/fhrp_intent.py :: compute_fhrp_configured_group_baseline`, `validate_fhrp_configured_group_baseline`, and `embedded_fhrp_configured_group_baseline` → published at `snap['fhrp_configured_group_baseline']` (`fhrp_configured_group_baseline/1`) | consume the validated receipt; do not reconstruct group intent or cross-host election consistency from sparse `protocol_health`, interface prose, raw configuration, or UI copy. `summary.by_status` owns concrete configured-group row counts; the closed `summary.by_coverage_status` map owns the exact host × subtype census (one HSRP, VRRP, and GLBP cell per inspected host), while `coverage[]` is host detail only. A configured-active local group absent from usable subtype runtime evidence is degraded; ambiguity is review and evidence/custody loss is not verified. Across distinct hosts, only complete, healthy rows sharing the exact default/IPv4 + subtype + normalized interface + group + equal configured/runtime VIP candidate identity are reconciled: zero or multiple observed leaders marks every matching existing row `review` with `election_no_leader_observed` or `election_multiple_leaders_observed`. This is a sequential-capture consistency review, not proof that the roles were simultaneous; it neither invents peer rows nor infers an expected peer/member count. One observed leader with matching healthy backups, or a lone candidate, remains only bounded local-state evidence. `NOT_APPLICABLE` means no in-scope literal local group subject was identified; it is not proof that FHRP is absent or that configuration coverage is complete. `CLEAR` is bounded to direct-literal local default/global IPv4 groups | parser/reconciliation/custody/hostile-input/secret-suppression and exact three-cells-per-host coverage reconciliation remain pinned by `tests/test_fhrp_configured_group_baseline.py`; sequential multiple/no-leader review, single-candidate non-inference, exact-identity isolation, deterministic ordering, tamper resistance, and embedded-custody behavior are pinned by `tests/test_fhrp_election_reconciliation.py`; additive Validation/current-gate/NRFU consumption is pinned by `tests/test_fhrp_decision_consumer_parity.py`. Uncapped **FHRP Group Intent** workbook projection plus runbook/Explorer blocker retention, generic Validation/MOP/AssessHub projection, ordinary-row caps, exact scope/custody and `NOT_APPLICABLE` copy, distinct group-row/host × subtype counts, fail-closed malformed census, configured-only and coverage-only subject drill-down, current-baseline marker compatibility, and Master Reference binding are pinned by `tests/test_fhrp_configured_group_operator_surfaces.py`, `tests/test_fhrp_election_operator_surfaces.py`, `tests/test_explorer_parse_yield.py`, `tests/test_mop.py`, and `tests/test_master_reference_protocol_depth.py`. VRFs, IPv6, templates/inheritance/dynamic constructs, secondary VIPs, expected peer/member count or identity, simultaneous election health, timers, authentication, preemption, tracking behavior, failover, convergence, freshness, interoperability, and cutover authorization remain excluded; NX-OS configured-group parsing is limited to nested HSRP |
| **FHRP redundancy-domain composition gate** — the authoritative join of current normalized SVI members to the validated configured-group receipt inside exact observed IPv4 domains | contract/producer/validator = `cisco_toolkit/fhrp_redundancy.py :: compute_fhrp_redundancy_domain_baseline`, `validate_fhrp_redundancy_domain_baseline`, `embedded_fhrp_redundancy_domain_baseline`, and `scope_fhrp_redundancy_domains` → published at `snap['fhrp_redundancy_domain_baseline']` (`fhrp_redundancy_domain_baseline/1`) | consume the validated receipt; do not reconstruct domain composition from VLAN-only interface prose, sparse health, or the legacy `snap['fhrp']` compatibility view. Domain identity is exact VLAN + normalized VRF + observed IPv4 subnet; candidate identity inside it is protocol + group + virtual IP. A bounded upstream configured/runtime local fault remains `degraded`. A same-domain SVI with zero positive HSRP/VRRP/GLBP participation beside a positive candidate is `review` because intended membership is unresolved—not a proven unprotected gateway or failure. Missing subtype capture/parser evidence is `not_verified`. Multiple groups remain assessable only when participating member candidate sets reconcile; disjoint or subset sets require review. `NOT_APPLICABLE` means no subject was identified, not absence or complete intent. Matching unresolved composition is not acceptance | the current-run receipt binds a canonical normalized-SVI projection digest and the validated upstream receipt under `current_run_projection_bound`; this is process-current projection custody, not cryptographic raw-capture custody. Serialization deliberately changes custody to `embedded_unverified`. Exact-domain isolation, zero/positive participation, multi-group candidate sets, digest/receipt tamper resistance, totality/caps, static identity-only fallback, operator uncapped-blocker retention, hostile-input suppression, and Master Reference binding are pinned by `tests/test_fhrp_redundancy_domain_baseline.py`, `tests/test_fhrp_redundancy_domain_engine.py`, `tests/test_fhrp_domain_operator_surfaces.py`, `tests/test_pipeline_inprocess.py`, and `tests/test_master_reference_protocol_depth.py`. Expected/off-scan members, intended member count, simultaneous roles, timers, authentication, tracking, failover behavior, convergence, freshness, interoperability, and cutover authorization remain excluded |
| **Observed single-snapshot EtherChannel baseline** — the bounded local group/member flags that Cutover Validation and NRFU may ask an operator to preserve | projection + receipt-gated owner = `cisco_toolkit/analyze.py :: compute_etherchannel_projection` and `summarize_etherchannel_baseline` (`etherchannel_projection/1`, `etherchannel_baseline/1`) | consume the shared baseline in Validation and NRFU; never reconstruct operational `(P)` state from `interfaces.*.port_channel` associations. Config/interface associations establish a subject only; supported non-forwarding flags block acceptance, and ambiguous or unobserved state abstains | exact flag preservation, group/member collision handling, assessed/degraded/review/not-verified classification, source locators, `embedded_unverified` projection custody, no partner/min-links/hash/MLAG intent inference, downstream blocker retention, and Validation/NRFU parity are pinned by `tests/test_etherchannel_cutover_truth.py`, `tests/test_etherchannel_nrfu_truth.py`, `tests/test_etherchannel_validation_surfaces.py`, and `tests/test_pipeline_inprocess.py` |
| **Source-bound single-chassis EtherChannel operational evidence and change assurance** — additive Release-1 configured/runtime identity, capacity, counters, and local member-loss eligibility; the protected `etherchannel_projection/1` and `etherchannel_baseline/1` owners above remain unchanged | evidence producer/validator = `cisco_toolkit/etherchannel.py :: compute_etherchannel_operational_evidence`, `validate_etherchannel_operational_evidence`, and `embedded_etherchannel_operational_evidence` → published at `snap['etherchannel_operational_evidence']` (`etherchannel_operational_evidence/1`); transition owner = `cisco_toolkit/protocol_deltas.py :: compute_etherchannel_delta` (`etherchannel_delta/1`) | consume the strict additive receipt together with both protected v1 co-owners. It binds IOS/IOS-XE and NX-OS exact summary, full/scoped running-config, LACP-neighbor, interface-status, and physical-counter bytes; owns exact `active`/`passive`/`desirable`/`auto`/`on` member modes, configured/runtime membership, explicit remote LACP system plus operational aggregation key, min-links, count/bandwidth capacity, configured hashing, bounded counters, and local one-forwarding-member-loss arithmetic. Missing/renamed/truncated/malformed leaves abstain. Current runtime, partner, min-links, member, or counter faults and an unsafe appeared group remain producer blocks that expected intent cannot clear; healthy configuration movement defaults to review | `tests/test_etherchannel_operational_evidence.py` pins real IOS/NX-OS-shaped source variants, strict contract mutation, custody erasure, capacity/min-links failure, appeared-degraded blocking, and no service-path promotion. `tests/test_protocol_deltas.py`, `tests/test_l2_failure_rehearsal.py`, `webapp/tests/test_protocol_portfolio.py`, `tests/test_pipeline_inprocess.py`, and the snapshot golden pin delta/gate/rehearsal/portfolio/pipeline parity. The local rehearsal proves neither convergence, hashing distribution, remote forwarding, traffic continuity, nor service-path survival. It never asserts multichassis peer-pair or dual-homed attachment identity; `multichassis_lag_domain_baseline/1` remains that distinct owner |
| **Observed protocol adjacency change gate** — before/after preservation of baseline-observed OSPF, BGP, and EIGRP peers across receipt-assessed device-family cells | producer = `cisco_toolkit/html.py :: compute_protocol_adjacency_delta` → published at `compute_snapshot_delta(...)["protocol_adjacencies"]` (`protocol_adjacency_delta/1`) and reused by campaign endpoint/step comparisons | consume the published block; do not diff raw state strings or infer an expected-peer inventory. Parsed healthy→unhealthy state is a regression; disappearance and evidence loss require review/abstention | the gate is source-bound when caller hashes exist, but its embedded routing-neighbor projection custody remains explicitly unverified. Identity, normalization, last-peer abstention, partial-evidence precedence, workbook/campaign projection, Explorer parity, and Master Reference binding are pinned by `tests/test_protocol_adjacency_delta.py`, `tests/test_explorer_parse_yield.py`, and `tests/test_master_reference_protocol_depth.py` |
| **Current-baseline cutover gate** — whether the reconciled current validation plan already carries a degraded, review, or not-verified observed baseline that must survive a clean before/after delta | decision producer = `cisco_toolkit/analyze.py :: compute_current_baseline_gate` (`current_baseline_gate/1`) over `validation_plan`; additive complete presentation owner = `cisco_toolkit/protocol_assurance.py :: current_baseline_blocker_export` (`current_baseline_blocker_export/1`) | consume the published gate; do not rescan free-form UI copy or treat a delta-only CLEAN as current-state acceptance. Degraded evidence is `BLOCKED`; review/not-verified or malformed evidence is `INDETERMINATE`; absent/empty evidence is `NOT_ASSESSED`; `CLEAR` means only that no blocker was emitted in reconciled validation scope. The v1 gate retains its 50-row presentation cap; decision code uses its reconciled totals, while the additive export owns the complete non-decision blocker projection | item↔wave↔summary reconciliation, hostile/malformed abstention, typed routing/EtherChannel/FHRP/BGP blocker classification, legacy marker compatibility, unchanged-degraded compare enforcement, AssessHub plan/execution refusal, and cap/export reconciliation are pinned by `tests/test_current_baseline_gate.py`, `tests/test_compare_cutover_gate_cli.py`, the focused AssessHub backend/frontend tests, `tests/test_mop.py`, `tests/test_explorer_parse_yield.py`, and `tests/test_master_reference_protocol_depth.py`. The diff workbook retains **Current Baseline Gate** and adds **Current Baseline Export** with exact rendered/total/omitted disclosure; the comparison JSON is the complete Campaign sink. The MOP and Explorer Waves view consume the same global gate and render HOLD unless it is `CLEAR` |
| **Source-bound protocol comparison admission and receipt** — the exact engagement/campaign/snapshot pair, source hashes and byte counts, local subject identities, decision-owner versions, implemented support profiles, and expected-change intent admitted to one comparison | contracts = `cisco_toolkit/protocol_assurance.py :: comparison_admission`, `normalize_change_intent`, `protocol_support_profiles`, and `receipt_envelope` (`protocol_comparison_admission/1`, `cutover_change_intent/1`, `protocol_support_profile/1`, `protocol_receipt_envelope/1`); shared composition owner = `cisco_toolkit/comparison.py :: compare_bound_pair`; AssessHub adapter = `webapp/backend/engine.py :: compare_bound_pair` | every decision surface consumes the one complete comparison composed from `BoundSnapshot` inputs. Do not infer engagement identity from names/paths, runtime support from catalog presence, or source custody from re-serialization. AssessHub binds the exact immutable `snapshots.snapshot_json` blob it parsed (original upload bytes are not retained); offline Compare requires explicit `offline_comparison_context/1` before its exact file bytes are admitted. Cross-engagement/campaign comparison is refused; incompatible semantics is `not_comparable`, missing scope is `coverage_lost`, and malformed custody/identity/intent cannot authorize PASS | process-local `BoundNativeProtocolDeltaSet`, `BoundProtocolFamilyChangeSet`, canonical decision-input authority, and `BoundComparison` authority prevent partial applicability lists, cross-pair input mixing, detached/rehashed gates, external-context rewriting, and post-composition mutation from crossing the gate/workbook trust boundary; portable wire receipts remain JSON audit records. Exact vocabularies, subject collisions, duplicate-key JSON, intent, custody-leaf mutation, stored-byte hashing, artifact forgery, API/CLI/workbook/execution parity, and cross-boundary refusal are pinned by `tests/test_bound_snapshot_source.py`, `tests/test_protocol_assurance_contracts.py`, `tests/test_compare_cutover_gate_cli.py`, `tests/test_l2_failure_rehearsal.py`, `webapp/tests/test_comparison_core.py`, and `webapp/tests/test_compare_execution_receipts.py`. The closed support set contains only the ten executable comparison owners; the separate 39-profile Capability Catalog is planning/reference inventory and never upgrades runtime support |
| **Native protocol-family change composition** — verdict-free before/after transitions for IPv6 routing, configured BGP, STP consistency/topology, single-chassis EtherChannel, VTP safety, configured FHRP groups/domains, and multichassis LAG, composed additively with the unchanged IPv4 adjacency delta | native owners = `cisco_toolkit/protocol_deltas.py` (`ipv6_routing_adjacency_delta/1`, `bgp_configured_peer_delta/1`, `stp_consistency_delta/1`, `stp_topology_delta/1`, `etherchannel_delta/1`, `vtp_safety_delta/1`, `fhrp_configured_group_delta/1`, `fhrp_redundancy_domain_delta/1`) and `cisco_toolkit/multichassis_lag.py :: compute_multichassis_lag_delta`; composition owner = `cisco_toolkit/protocol_assurance.py :: compute_native_protocol_deltas`, `protocol_family_change_set` (`protocol_family_change_set/1`) | consume each producer's exact transition/effect over its complete rows. Expected intent may neutralize a producer-owned `review`, never a `block` or `not_verified`; persistent current degradation still blocks. Missing/malformed/custody/identity loss is explicit `coverage_lost`/`not_comparable`. Every owner coverage denominator and positive local subject is reconciled to the admitted `snapshot.devices` roster before a healthy or rowless-not-applicable transition is allowed. Only `compute_native_protocol_deltas` may mint completeness authority from that exact before/after applicability pass; `protocol_family_change_set` carries gate authority only when it consumes the unchanged set. Ordinary or partial lists remain inspectable reference composition but are non-authoritative, without imposing a fixed family roster or inferring applicability from catalog/runtime support. Routed scope is `observed_state_preservation`; VRF/AFI/SAFI/process-depth remains outside this release | `/api/compare`, CLI, execution, Trend, and `write_diff_workbook` consume the same native object from their exact bound pair; the gate and uncapped **Protocol Family Changes** sheet consume it while **Protocol Adjacency Delta** remains. Empty-estate grafts, off-roster subjects, configured-BGP loss, FHRP role movement versus redundancy loss, current-fault clean delta, capture loss, owner mutations, and workbook reconciliation are pinned by `tests/test_protocol_deltas.py`, `tests/test_protocol_assurance_contracts.py`, `tests/test_compare_cutover_gate_cli.py`, `webapp/tests/test_comparison_core.py`, and the focused AssessHub tests |
| **Multichassis LAG identity and redundancy assurance** — local vPC/MLAG observations, reciprocal peer pairs, local attachment legs, and reconciled dual-homed attachments remain four distinct subject classes | owners = `cisco_toolkit/multichassis_lag.py` (`multichassis_lag_subject_scope/1`, `multichassis_lag_domain_baseline/1`, `multichassis_lag_delta/1`) | a domain/vPC ID is only an attribute. A pair requires explicit reciprocal switch identities; an attachment requires the proven pair, exactly one local leg per peer, and matching remote LACP system plus aggregation/key identity. One-sided peers, reused cross-site domains, missing legs/partner identity, malformed/unknown states, and capture loss abstain or degrade; they never become confirmed healthy | executable scope is NX-OS live/offline and EOS offline import only. Real-shaped fixtures, substring-negative states (`not formed`, `not alive`), reused domains, mismatched LACP, one-leg and custody mutations are pinned by `tests/test_multichassis_lag.py`, `tests/test_review_round2.py`, and `tests/test_explorer_render_safety.py`. Explorer legacy shared-domain combinations are labeled only unverified candidates |
| **L2 failure-rehearsal projection and observed local trial** — bounded operator-facing STP, single-chassis EtherChannel, multichassis LAG, and reused service/path evidence, plus one optional source-bound failure trial | base composer/validator = `cisco_toolkit/l2_rehearsal.py :: compute_l2_failure_rehearsal`, `validate_l2_failure_rehearsal` (`l2_failure_rehearsal/1`); additive observed owner = `compute_observed_l2_failure_evidence`, `validate_observed_l2_failure_evidence` (`observed_l2_failure_evidence/1`) over exact pre-failure, induced-failure, and recovery snapshot bytes plus bounded `l2_failure_witness/1`; reused owners = `failover_readiness/1`, native STP/EtherChannel/multichassis evidence, and stored `traffic_assurance_set/1` → `cutover_sim/1`; comparison projection = `cisco_toolkit/protocol_assurance.py :: cutover_operator_evidence` | consume the complete receipts; do not rerun or invent a path engine in a UI. Base STP rows are election candidates; EtherChannel and multichassis rows project only their exact local subjects/capacity and current faults. `service_path` applicability is false only when the traffic owner is absent or a valid bounded set has no requested failure; a present malformed/oversized owner is applicable `not_verified`, and exact requested intent+failure-target scenarios lost across before/after remain coverage gaps. An observed trial requires one closed source owner/context, distinct hash- or snapshot-derived phase identities, source-owned aware `collected_at`, independent upload/mtime order, exact witness subject/scenario/target, device-state failure proof, and recovery equal to the comparison NEW source | arithmetic/simulation stays `not_verified` and proves no hashing, convergence, traffic, service, or path survival. A valid observed survival raises only the exact covered local scenario to `local_safety_preservation`; it may clear one exactly matching projected risk or a base `not_verified` row only when that row's sole gap was the missing trial, never current faults, unrelated subjects, service-path evidence, or coverage loss. Observed refutation blocks; supplied invalid/incomplete evidence is INDETERMINATE; coherent synthetic preservation stays nonblocking but explicitly unobserved. Execution persists and remints all phase/witness sources under one transaction, applies a monotone exact-subject re-trial ratchet, and replays append-only comparison history from integer-anchored exact bytes rather than mutable state mirrors. Offline Compare exposes all-or-none `--l2-trial-pre`, `--l2-trial-post`, and `--l2-trial-witness`; NEW is recovery. `cutover_gate/1` remains the sole overall decision owner. Focused gates: `tests/test_l2_failure_rehearsal.py`, `tests/test_observed_l2_failure_evidence.py`, `tests/test_observed_l2_failure_cli.py`, `webapp/tests/test_compare_execution_receipts.py`, and Campaign/Execution/Planner frontend tests |
| **Single-snapshot Protocol Assurance receipt and portfolio** — source custody plus family/subject evidence for one snapshot, without a before/after verdict | source owner = `webapp/backend/protocol_portfolio.py :: build_protocol_single_snapshot_bundle` (`protocol_single_snapshot_receipt/1`, `protocol_single_snapshot_export/1`); exact source variants = persisted `snapshots.snapshot_json` bytes or engine-emitted assessment snapshot file bytes; shared portable renderer = `cisco_toolkit/protocol_receipt_surfaces.py`; normal CLI finalization owner = `COLLECT_PARSE_V3_23_0.py :: _stage_finalize`; AssessHub route = `GET /api/snapshots/{id}/section/protocol_assurance`; complete export route = `GET /api/snapshots/{id}/protocol-assurance/export` | consume only the validated receipt/export pair; do not interpret absence as health, derive support from the catalog, or hash a parsed mapping inside a renderer. AssessHub and its Explorer bind the exact persisted blob. A normal CLI run emits and rereads `<stem>.snapshot.json` through the existing atomic-first writer with its disclosed fallback, freezes those exact bytes, builds the owner portfolio from that `BoundSnapshot`, emits canonical uncapped `<stem>.protocol-assurance.json`, and only then atomically replaces the pre-pass `NOT VERIFIED` workbook, Explorer, runbook, and gate-permitted MOP with BOUND projections after revalidating source + owner rebuild + export bytes before and after replacement and again before the manifest. The local source variant carries label/script owner but no invented AssessHub engagement/campaign/snapshot IDs or endpoint. Direct/legacy renderer/finalizer calls remain explicit `NOT VERIFIED`. NRFU remains an AssessHub-only writer; there is no normal-engine CLI NRFU artifact to refresh | every family coverage/positive-host denominator is reconciled to `snapshot.devices`; mismatch erases subjects and becomes `not_verified`. Post-freeze optional renderer failures are custody/timing/manifest facts and cannot mutate bound snapshot bytes; a failed refresh may leave the prior `NOT VERIFIED` bytes recoverable on disk, but that path is explicitly unregistered and excluded from the current-run manifest (or no artifact remains registered). Exact persisted/emitted custody, detached/tampered/rehashed sidecars, stale source/export mutation, interruption rollback, ten-family roster reconciliation, rendered/total/omitted disclosure, uncapped export hashing, AssessHub/Explorer/workbook/runbook/MOP/NRFU parity within their actual producer channels, and hostile input are pinned by `tests/test_custody_pipeline.py`, `tests/test_pipeline_inprocess.py`, `tests/test_protocol_receipt_surfaces.py`, `webapp/tests/test_protocol_portfolio.py`, `webapp/tests/test_backend.py`, the Explorer safety suites, and `webapp/frontend/src/pages/Snapshot.test.tsx` |
| **Combined before/after cutover decision** — one operator verdict across admitted comparison custody, the observed snapshot delta (including protocol-adjacency and current-baseline gates), native protocol-family effects, bounded local L2 rehearsal dispositions, and the bounded FIB/path-intent Pre-Change Certificate | `cisco_toolkit/html.py :: compute_cutover_gate` (`cutover_gate/1`); canonical composition = `cisco_toolkit/comparison.py :: compare_bound_pair`; `/api/compare`, Trend, execution comparison, and `write_diff_workbook` render/return that exact receipt | consume the receipt; never promote the certificate verdict, a clean delta, local simulation arithmetic, or a capped presentation array as the overall result. A supplied non-admitted comparison withholds authorization as `INDETERMINATE`; otherwise proven family blocks/regressions fail, family or applicable-L2 coverage loss abstains, unexpected family change or projected L2 risk requires review, and only this gate owner may emit PASS. When `comparison_admission` is supplied, the process-local decision-input authority minted by `compare_bound_pair` must bind the exact delta, certificate, admission, family set, operator evidence, and family source pair; missing, mutated, caller-crafted, or cross-pair inputs force `INDETERMINATE`. Direct legacy no-admission two-argument callers retain v1 behavior | workbook/terminal/API/Trend/execution identity, orphan-risk REVIEW, rehearsal mutation refusal, forged-gate and rewritten-context rejection, certificate-scope disclosure, current-baseline enforcement, default artifact-compatible exit 0, opt-in `--fail-on-compare-gate` exit 2, routed-only nonapplicability, legacy abstention, complete family/rehearsal schema-count reconciliation, and source-admission withholding are pinned by `tests/test_l2_failure_rehearsal.py`, `tests/test_compare_cutover_gate_cli.py`, `tests/test_protocol_assurance_contracts.py`, `webapp/tests/test_comparison_core.py`, and `webapp/tests/test_compare_execution_receipts.py` |
| **Execution post-change comparison record** — append order, immutable full comparison receipts, latest canonical gate, and the success authority for new execution runs | persistence/CAS owner = `webapp/backend/storage.py :: execution_comparisons` and `append_execution_comparison_if_unchanged`; run outcome owner = `webapp/backend/execution.py :: _derive_outcome`; API = `POST /api/executions/{id}/compare` | each new `cutover_execution/2` freezes its exact before binding, start time, snapshot high-water mark, and implementation-step state. The after snapshot must be a later upload whose source-owned aware `collected_at` is after the completed implementation evidence. Receipt append re-reads both exact blobs, recomputes the complete comparison under `BEGIN IMMEDIATE`, and atomically moves the latest marker only if execution state is unchanged. `SUCCESSFUL` requires the newest immutable receipt to remain bound to the current implementation record and carry `cutover_gate/1 = PASS`; re-actioning a step invalidates that acceptance. Legacy runs are not backfilled or reinterpreted | reverse/stale/missing/future capture times, pre-implementation evidence, later step action, compare/finish races, source deletion/mutation, wrong pair wiring, fully rehashed forgery, non-PASS outcomes, legacy/finished refusal, API/UI parity, and PIR before/after custody are pinned by `webapp/tests/test_compare_execution_receipts.py`, `webapp/tests/test_backend.py`, the focused Execution/Cutover Planner tests, and `webapp/tests/test_pir_docx_render.py` |
| **Campaign adjacent comparison receipt set** — canonical before/after decisions for every original oldest-to-newest adjacent pair, kept distinct from directional trend scoring | web adapter = `webapp/backend/engine.py :: campaign_trend` (`campaign_adjacent_comparison_set/1`); CLI context = `offline_trend_context/1`; workbook projection = `cisco_toolkit/html.py :: write_campaign_workbook` | web Trend produces receipts only from a complete ordered set of exact persisted `BoundSnapshot` inputs and never bridges a missing middle snapshot. CLI Trend strictly parses every snapshot and requires engagement/campaign/ordered IDs/exact hashes before composition. Missing context or any missing pair is `not_verified`, `complete=false`; cross-campaign/engagement/repeated identity is `not_comparable` and the trajectory verdict is `INDETERMINATE` | web cards render server gates/receipts without recomputation and disclose expected/produced/rendered/omitted/complete; CLI writes the uncapped `.trend-comparisons.json` plus the capped **Adjacent Comparison Receipts** sheet. Missing-middle races, duplicate JSON keys, context/hash/order mutations, web `/api/compare` parity, cap wording, and CLI standalone-compare parity are pinned by `webapp/tests/test_campaign_trend_receipts.py`, `tests/test_trend_comparison_cli.py`, `tests/test_campaign_trend.py`, and the focused Campaign frontend tests |
| **Proof-carrying transition structural contract (Release 2.0 candidate)** — exact Evolution IR, canonical `TransitionCase`, evidence/applicability/temporal semantics, receipt-specific pack/TCB custody, authoritative four-state gate, invalidation, a bounded executable declarative-DSL conformance prototype, a standalone runtime-closure `/2` evidence/review protocol boundary, a Windows live-discovery lane that can emit only non-authoritative `COLLECTED_INCOMPLETE` evidence, a standalone representative-workload external-review authority boundary, and non-promoting retrospective conformance-vector replay of pinned Release 1 code | case and denominator owners = `cisco_toolkit/transition_contract.py :: bind_transition_case_bytes`, `validate_transition_case`, `validate_qualification_denominator`; pack/TCB and external qualification owners = `cisco_toolkit/transition_pack.py :: bind_pack_manifest_bytes`, `bind_tcb_manifest_bytes`, `verify_qualification_evidence`, `qcp_001_must_remain_experimental`; executable prototype owner = `cisco_toolkit/transition_dsl.py :: bind_packaged_dsl_prototype_bytes`, `run_bound_pack_abi`; detached budget-review authority owner = `cisco_toolkit/transition_tcb_review.py :: verify_tcb_budget_review_evidence`; runtime-closure protocol owner = `cisco_toolkit/transition_runtime_closure.py :: validate_transition_runtime_closure_evidence`, `bind_transition_runtime_closure_evidence_bytes`, `verify_transition_runtime_closure_review`, `require_verified_transition_runtime_closure_review`; incomplete-only Windows live-discovery owner = `cisco_toolkit/transition_runtime_discovery.py :: RuntimeClosureDiscoverySubject`, `CapturedIncompleteRuntimeClosureEvidence`, `validate_windows_runtime_discovery_trace`, `validate_windows_execution_environment_manifest`, and `capture_windows_runtime_closure_incomplete`; representative-workload evidence/review owner = `cisco_toolkit/transition_workload_review.py :: bind_transition_workload_evidence_bytes`, `verify_transition_workload_review`, `require_verified_transition_workload_review`; decision owners = `cisco_toolkit/transition_verifier.py :: verify_transition_case`, `map_authoritative_gate`, `compute_invalidation_receipt`; legacy reference adapter = `cisco_toolkit/transition_legacy.py :: verify_release1_semantic_bundle`, `adapt_release1_comparison_bytes`, `replay_release1_comparison_bytes`; structural projections = `cisco_toolkit/schemas/atlas-transition-contract-v1.schema.json`, `cisco_toolkit/schemas/atlas-r2-structural-tcb-census-v1.schema.json`, `cisco_toolkit/schemas/atlas-r2-execution-evidence-v1.schema.json`, `cisco_toolkit/schemas/atlas-r2-transition-runtime-closure-v2.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-runtime-discovery-v1.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-execution-environment-manifest-v1.schema.json`, `cisco_toolkit/schemas/atlas-r2-transition-workload-review-v1.schema.json`; packaged evidence = `cisco_toolkit/data/qcp-001.experimental.json`, `atlas-r2-structural-tcb-census.v1.json`, `atlas-r2-dsl-prototype-denominator.v1.json`, `atlas-r2-dsl-prototype-input.v1.json`, `atlas-r2-dsl-prototype-pack.experimental.json`, `atlas-r2-dsl-prototype-program.v1.json`, `atlas-r2-dsl-prototype-tcb.v2.json`, `atlas-r2-dsl-prototype-measurements.v1.json`, `atlas-r1-executable-bundle.json`, `atlas-r1-source-bundle.json`, `atlas-r1-retrospective-before.json`, `atlas-r1-retrospective-after.json`, and `atlas-r1-retrospective-comparison.json` | consume only strict canonical bytes and complete exact identity/scope/time/dependency bindings. Trust policy is an independent input; a producer cannot mint its own qualification or independent review. Applicability and gate disposition use separate closed three-state vocabularies; evidence, qualification, temporal outcomes, and authoritative gates each use their separate closed four-state vocabularies. Incomplete temporal observation stays `INCONCLUSIVE`, and no receipt autonomously authorizes GO. The synthetic conformance prototype is `EXPERIMENTAL` + `CONTRACT_ONLY`, performs a same-checkout self-check only, and cannot execute QCP-001. QCP-001 remains fixed `EXPERIMENTAL` + `CONTRACT_ONLY`; the Release 1 capsule is `REFERENCE_NOT_REWRITE` + `AUDIT_ONLY`, with a null Release 2 gate and no authenticated historical fixture | the executable DSL prototype supplies measured hostile/resource-boundary evidence, not a Wasm runtime, sandbox claim, qualified/GA pack, or promotion authority. Its TCB runtime dependency inventory is explicitly `PARTIAL_NONPORTABLE_PROTOTYPE`; `COMPLETE_EXACT_RUNTIME_CLOSURE` remains an engineering blocker to freeze. The runtime-closure `/2` module and schema are protocol structure only. Windows live discovery emits only non-authoritative `COLLECTED_INCOMPLETE` evidence after its bounded target actually runs and at least one dynamic trace is nonempty. Its parent-runtime selection anchor is the resolved executable image reported by Windows. On a Windows virtual environment, the image must reconcile to base CPython and differ from the short-lived venv redirector. Both manifest sides bind the selected path and their separately read file bytes; they do not prove the already-loaded image bytes, persistent file identity, the installed distribution, or the parent collector. Its separate digest-only parent-expected/target-observed environment manifest may set only `execution_environment_argv_cwd_and_inputs_bound=true`; every other closure boolean remains false. It supplies no closure-capable collector, capture corpus, or authority. No trust policy, reviewer key, signature, or signed closure-review receipt is bundled. The protocol and incomplete-only discovery lane do not change the runtime-inventory `/1` roster or its `PARTIAL_NONPORTABLE_PROTOTYPE` state, and freeze `/1` remains unchanged and blocked. Representative-workload evidence is non-authoritative and its evidence state is never `ADEQUATE` in R2.0. No representative-workload corpus, reviewer key, trust policy, or signed review receipt is bundled. The existing freeze `/1` remains explicitly blocked by representative-workload adequacy evidence absence. This boundary does not make runtime inventory `COMPLETE`, qualify a pack, authorize promotion, or implement Release 3. Numeric pack/TCB budgets, reviewed resource ceilings, independent review evidence, and selected commit remain pending/null; the machine census retains its R2.0 completion blocker. Sealed independent replay, persistence, and supported API/CLI/UI surfaces are not supplied. No R2.1+ or Release 3 capability is implemented. Canonical mutation, evidence teleportation, sampled-as-continuous claims, fifth statuses, uncertified positive claims, unqualified promotion, deterministic replay, package provenance, and schema closure are pinned by `tests/test_transition_contract.py`, `tests/test_transition_adversarial.py`, `tests/test_transition_verifier.py`, `tests/test_transition_temporal_semantics.py`, `tests/test_transition_pack_abi.py`, `tests/test_transition_dsl.py`, `tests/test_transition_tcb_review.py`, `tests/test_transition_runtime_discovery.py`, `tests/test_transition_workload_review.py`, `tests/test_transition_r1_compatibility.py`, and `tests/test_transition_schema_assets.py` |
| **Release 2.0 Windows debug-event capture tranche (`/2`, incomplete only)** — one fixed synthetic execution with process lifetime coverage from before the first debug continuation through the final descendant, plus deliberately incomplete image-event observation | public owner = `cisco_toolkit/transition_runtime_discovery.py :: capture_windows_debug_runtime_closure_incomplete`, `validate_windows_debug_runtime_discovery_trace`, `validate_windows_debug_execution_environment_manifest`; spawned-helper supervisor, deadline, canonical IPC, and parent `GO` gate = `cisco_toolkit/transition_runtime_discovery.py`; private helper-main-thread event engine = `cisco_toolkit/_transition_runtime_debug.py :: WindowsDebugEventSession`; projections = `cisco_toolkit/schemas/atlas-r2-windows-debug-runtime-discovery-v2.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-execution-environment-manifest-v2.schema.json` | consume only the sealed 12-artifact envelope. The helper must receive the parent `GO` gate before launch and return exactly one bounded canonical frame with a zero exit; the canonical Job `NEW_PROCESS`/`EXIT_PROCESS`/`ACTIVE_PROCESS_ZERO` ledger must exactly equal DEBUG_PROCESS create/exit sets, the root Job assignment must precede the first debug-event pump, every created process must reach its initial breakpoint and normal exit, and image rows must be the exact projection of received create/load/unload/exit events. Only `process_tree_captured_before_first_instruction_through_final_descendant` and `execution_environment_argv_cwd_and_inputs_bound` may be true | always `COLLECTED_INCOMPLETE` / `MEASURED_INCOMPLETE`; the spawned helper, Job, and debugger backstops are lifecycle containment, not a sandbox or execution policy. Image history, persistent file identity/loaded bytes, event-stream continuity, start/end reconciliation, static/loader/crypto closure, allow-set/enforcement, platform/boot state, collector TCB, and denominator closure remain false or unknown. OS loss counters remain null; runtime inventory `/1` remains `PARTIAL_NONPORTABLE_PROTOTYPE`. No budget, authority, signature, qualification, promotion, R2.1+, or Release 3 effect. Pinned by `tests/test_transition_runtime_debug.py`, `tests/test_transition_runtime_discovery.py`, `tests/test_transition_schema_assets.py`, and distribution replay/audit tests |
| **Release 2.0 Windows target-endpoint reconciliation tranche (`/3`, incomplete only)** — the `/2` process-lifetime lane plus target-only stable double-read K32 START/END slot reconciliation for the same fixed synthetic execution | public owner = `cisco_toolkit/transition_runtime_discovery.py :: capture_windows_debug_runtime_closure_v3_incomplete`, `validate_windows_debug_runtime_discovery_v3_trace`, `validate_windows_debug_execution_environment_v3_manifest`; suspended START observation seam = `cisco_toolkit/_transition_runtime_debug.py :: WindowsDebugEventSession.pump(before_continue=...)`; projections = `cisco_toolkit/schemas/atlas-r2-windows-debug-runtime-discovery-v3.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-execution-environment-manifest-v3.schema.json` | consume only the sealed 12-artifact `/3` envelope. Exactly two target checkpoints are required: START while the target is suspended at its handled initial breakpoint before continuation, and END after the payload is received but before the parent releases the STOP gate. The END fact does not assert a specific target-thread wait state. Each checkpoint retains two equal normalized K32 reads; received target image events are replayed through that checkpoint's collector-local append ordinal and their active mapping-slot set must exactly equal the K32 set. Later target teardown image activity remains serialized and debug-projected but is outside END checkpoint reconciliation. Mapping epochs remain sequence-qualified while a mapping-slot token is stable across base reuse. `target_start_end_snapshot_reconciled=true`, `collector_sequence_kind=LOCAL_APPEND_ORDINAL`, `collector_ledger_contiguous=true`, and `collector_sequence_gap_count=0` are only target/local collector facts | always `COLLECTED_INCOMPLETE` / `MEASURED_INCOMPLETE`. Broad `event_stream_contiguous=false` and `start_end_snapshot_reconciled=false`; `os_event_sequence_available=false`, `os_loss_counter_available=false`, and OS/global loss counters remain null. Endpoint equality cannot detect an omitted balanced load/unload pair; K32 can race with loader changes or omit `LOAD_LIBRARY_AS_DATAFILE` mappings. Complete mapping history, manual/anonymous mappings, mapped/loaded bytes, static/loader/crypto closure, allow-set/enforcement, platform/boot state, collector TCB, and denominator closure remain false or unknown. Only `process_tree_captured_before_first_instruction_through_final_descendant` and `execution_environment_argv_cwd_and_inputs_bound` may be true in the runtime-closure envelope. Runtime inventory `/1` remains `PARTIAL_NONPORTABLE_PROTOTYPE`; `/2` remains unchanged. No budget, authority, signature, qualification, promotion, R2.1+, or Release 3 effect. Pinned by `tests/test_transition_runtime_debug.py`, `tests/test_transition_runtime_discovery.py`, `tests/test_transition_schema_assets.py`, `tests/test_ssot_registry.py`, and distribution replay/audit tests |
| **Release 2.0 Windows debug-file identity/on-disk-byte tranche (`/4`, incomplete only)** — the unchanged `/3` process/image/checkpoint semantics plus a borrowed debug-event image-handle trace for the same fixed synthetic execution | public owner = `cisco_toolkit/transition_runtime_discovery.py :: capture_windows_debug_runtime_closure_v4_incomplete`, `validate_windows_debug_runtime_discovery_v4_trace`, `validate_windows_debug_execution_environment_v4_manifest`; borrowed-handle observation seam = `cisco_toolkit/_transition_runtime_debug.py :: WindowsDebugEventSession.pump(before_event_file_close=...)`; projections = `cisco_toolkit/schemas/atlas-r2-windows-debug-runtime-discovery-v4.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-execution-environment-manifest-v4.schema.json` | consume only the sealed 13-artifact `/4` envelope. This fixed capture fails closed unless every received CREATE_PROCESS or LOAD_DLL image row supplies a non-null debug-event `hFile` and joins one-to-one by source debug sequence and mapping tokens to the file-identity trace while the debuggee is suspended and before the engine closes its event-owned handle. The observer borrows but never closes, retains, or transfers that handle. Each row binds `FILE_ID_INFO` (lowercase 64-bit volume serial plus 128-bit file ID) and stable file size before/after to exactly two equal SHA-256 whole-file reads from offset zero through that same handle. File identifiers are machine-local and can be reused over time. Raw paths and filenames are not disclosed. Collection ceilings (128 MiB per file, 1 GiB total, 1 MiB chunks, two passes) are protective guards only, never approved budgets | always `COLLECTED_INCOMPLETE` / `MEASURED_INCOMPLETE`. This proves stable handle-addressed **on-disk** bytes only for non-null debug-event image handles observed in this bounded run; it does not prove mapped or loaded memory bytes. Debug-event image handles can be null outside this fail-closed fixed capture. `persistent_file_identity_and_loaded_bytes_bound=false` and `mapped_or_loaded_memory_bytes_bound=false` remain fixed. The `/3` target-only/local facts and limitations remain: broad `event_stream_contiguous=false` and `start_end_snapshot_reconciled=false`; OS/global sequence and loss authority remain false/null; balanced omissions, manual/anonymous mappings, complete mapping history, static/loader/crypto closure, allow-set/enforcement, platform/boot state, collector TCB, and denominator closure remain unresolved. Only `process_tree_captured_before_first_instruction_through_final_descendant` and `execution_environment_argv_cwd_and_inputs_bound` may be true in the runtime-closure envelope. Runtime inventory `/1` remains `PARTIAL_NONPORTABLE_PROTOTYPE`; `/2` and `/3` remain unchanged. No budget, authority, signature, qualification, promotion, R2.1+, or Release 3 effect. Pinned by `tests/test_transition_runtime_debug.py`, `tests/test_transition_runtime_discovery.py`, `tests/test_transition_schema_assets.py`, `tests/test_ssot_registry.py`, and distribution replay/audit tests |
| **Release 2.0 Windows event-coincident mapped-image-byte tranche (`/5`, incomplete only)** — the unchanged `/4` process/image/checkpoint and handle-addressed on-disk-byte semantics plus a suspended-event read of each received image's committed `MEM_IMAGE` bytes for the same fixed synthetic execution | public owner = `cisco_toolkit/transition_runtime_discovery.py :: capture_windows_debug_runtime_closure_v5_incomplete`, `validate_windows_debug_runtime_discovery_v5_trace`, `validate_windows_debug_execution_environment_v5_manifest`; pre-continue observation seam = `cisco_toolkit/_transition_runtime_debug.py :: WindowsDebugEventSession.pump(before_event_image_memory_read=...)`; projections = `cisco_toolkit/schemas/atlas-r2-windows-debug-runtime-discovery-v5.schema.json`, `cisco_toolkit/schemas/atlas-r2-windows-execution-environment-manifest-v5.schema.json` | consume only the sealed 13-artifact `/5` envelope. Every received CREATE_PROCESS or LOAD_DLL row must still supply the non-null event-owned `hFile` required by `/4`; while that event is suspended, the engine also creates a fresh non-inheritable least-privilege query/read duplicate of the retained CREATE_PROCESS debug-event `hProcess` for that process, passes both handles to one observer, then closes the duplicate and event-owned file handle before `ContinueDebugEvent`. The collector reads the complete handle-addressed disk file twice, parses one bounded AMD64 PE32+ layout, and walks exactly `[mapping_base, mapping_base + SizeOfImage)` twice with `VirtualQueryEx`. Each pass must independently form a contiguous same-allocation-base partition of readable non-guarded `MEM_COMMIT` + `MEM_IMAGE` regions and every `ReadProcessMemory` call must return its exact requested bytes; the two complete passes must have equal whole-span SHA-256 digests and equal retained PE-header prefixes, and the normalized disk/memory PE layouts must match. Windows copy-on-write or protection bookkeeping may split region topology between passes, so topology, protection, and per-region digest stability are not claimed. No raw path, filename, process handle, or runtime address is serialized. Collection ceilings (512 MiB per image, 4 GiB total image memory, 1 MiB chunks, two passes, 1 MiB PE header, 96 sections, 512 memory regions per image read pass, 16,384 total emitted memory regions across both passes) are protective guards only, never approved budgets | always `COLLECTED_INCOMPLETE` / `MEASURED_INCOMPLETE`. Only inside the `/5` file/memory trace, `mapped_or_loaded_memory_bytes_bound=true` and `event_coincident_mem_image_bytes_bound=true` are scoped to every received image event at its suspended pre-continue instant; `persistent_file_identity_and_loaded_bytes_bound=false` remains fixed, and the runtime-closure envelope does not gain a broad loaded-byte closure bit. PE-layout reconciliation is not disk/memory byte equality; relocations, imports, copy-on-write, runtime writes, and loader transformations are uninterpreted, and the point reads do not prove allocation exhaustion, lifetime immutability, or complete mapping history. Balanced omissions, manual/anonymous mappings, global sequence/loss authority, static/loader/crypto closure, allow-set/enforcement, platform/boot state, collector TCB, and denominator closure remain unresolved. Only `process_tree_captured_before_first_instruction_through_final_descendant` and `execution_environment_argv_cwd_and_inputs_bound` may be true in the runtime-closure envelope. Runtime inventory `/1` remains `PARTIAL_NONPORTABLE_PROTOTYPE`; `/2`, `/3`, and `/4` remain unchanged. No budget, authority, signature, qualification, promotion, R2.1+, or Release 3 effect. Pinned by `tests/test_transition_runtime_debug.py`, `tests/test_transition_runtime_discovery.py`, `tests/test_transition_schema_assets.py`, `tests/test_ssot_registry.py`, and distribution replay/audit tests |
| **L3 segmentation posture** — gateway-SVI count, gateway-ACL coverage, and how many VRF *buckets* actually carry a gateway SVI (the basis of every "the fabric is flat / segmented" claim) | `cisco_toolkit/analyze.py :: compute_segmentation` → published at `snap['segmentation']` (`summary` + `gateway_acl` + `vrfs`) | **`ssot.segmentation_facts(snap)`** — owner-first, with an interface-derived fallback for pre-`segmentation` snapshots (`source` says which) and `None` rather than a fabricated 0 when neither is available | ✅ `tests/test_ssot_reconciliation.py` (accessor contract + totality + the archreview SEC-2 grade + a design/CRD cross-surface lock). ⚠️ **Two different facts, kept apart on purpose:** `gateway_vrfs` = non-default VRFs that CARRY a gateway SVI (what segments user traffic; `(global)` is the owner's unsegmented bucket label, never a VRF) vs `other_vrfs` = non-default VRFs configured elsewhere on the box (management / vPC-keepalive), which segment nothing. design/crd/archreview each recomputed the second and rendered it under the first's label — "4 VRF(s) in use" beside the runbook's "1 VRF(s) across 231 gateway SVI(s)" off one snapshot, and it downgraded archreview's SEC-2 from `deviation` to `advisory` on a fabric the owner calls FLAT |
| **Capture-finding status vocabulary + severity order** — which capture-integrity statuses exist and which is WORST for a host (what a coverage artifact must cite as its evidence) | `cisco_toolkit/capture_integrity.py :: STATUS_ORDER` (lower == worse; also the key `_package` sorts findings by, and the basis of the `summary.n_*` counters incl. `n_unreadable`) | `from cisco_toolkit.capture_integrity import STATUS_ORDER` — import it, never restate it | ✅ `tests/test_coverage_matrix.py` reconciles `coverage_matrix._CAPTURE_WORST` to it (membership AND order) + a behavioural lock that the capture row quotes the status the owner calls worst. It was a hand-written copy and had rotted twice: it never learned `unreadable` (the owner's worst status, so it ranked as unknown = least severe) and it inverted `empty` vs `unverified_prompt` |
| **BPDU-Guard port state** — is edge protection ON for this port, OFF, or NOT ASSESSED | producer vocabulary: `cisco_toolkit/parse.py :: parse_run_config_interfaces` + `build.py` write exactly `"Enable"` / `"Disable"` / `""` into `interfaces[host][port].stp_bpduguard`; the consumer-side decision owner is **`cisco_toolkit/textutils.py :: bpduguard_state`** | `textutils.bpduguard_state(value)` → `True` / `False` / `None`; count "protected" with `is True`, an OBSERVED gap with `is False`, and put `None` in a not-assessed bucket (Law 3) | ✅ `tests/test_archreview.py` (L2-2 reads the producer's own tokens; both consumers agree port-for-port). archreview L2-2 and `design_advisor._signals` each hand-rolled a token set and NEITHER matched the producer: archreview's disabled-list omitted `"Disable"`, so an explicitly-unprotected edge port counted as GUARDED there and UNGUARDED in the design blueprint — same field, same snapshot, opposite verdicts (6 ports on the Meridian reference fleet) |
| **Fact lineage** (J2) — attribute-level provenance for the headline facts: WHERE each canonical number comes from (path + basis + coverage state) | `cisco_toolkit/ssot.py :: compute_fact_lineage` → published at `snap['fact_lineage']` (`fact_lineage/1`) | `ssot.compute_fact_lineage(snap)`; one row per `CANONICAL_FACTS` entry, `value` read via `ssot.canonical_facts` | reuses the `CANONICAL_FACTS` contract + `abstention_reason` (no parallel store); date-relative values → golden-excluded, logic pinned by `tests/test_fact_lineage.py` |
| **Raw assessed evidence** — the ~60 analysis blocks (routes, acls, endpoints, health_scores, architecture_coverage, cable_map, move_groups, device_dossiers, …) | the snapshot JSON (`*_AUTOFILLED_*.snapshot.json`; current: `Migration_Assessment_AUTOFILLED_20260613_063201.snapshot.json`) | `snap['<block>']`; re-analyze offline with `--no-collect --collection-dir` | producer = the engine (`COLLECT_PARSE_V3_23_0`) |
| **Code + docs structure** | graphify (`graphify-out/graph.json`); bounded Graphifyy 0.9.51 JSON/report correction owner = `tools/graphify_guarded.py`; guarded-refresh bookkeeping = `graphify-out/.guarded_refresh.json` | on the owner host, read with `py -3.12 -m graphify query \| explain \| affected \| path`; source-extract only with guarded `update <local-path>`, `watch <local-path>`, or `extract <local-path> --code-only` forms; treat `GRAPH_REPORT.md` as a derivative, never an owner | The Stop hook requires Python 3.12+, isolated mode, bytecode-write suppression, and the tracked guard's exact-version/source/path/alias probe before re-extract; it fails closed for graph mutation while remaining fail-open for the user turn. It triggers on dirty state or committed-HEAD drift from the last complete clean receipt, marks pending before mutation, and finalizes only when its pre/post HEAD and porcelain-status reads match. A dirty/pending receipt never suppresses the next clean refresh. `built_at_commit` records the last topology write, while the local guarded receipt records a wrapper exit 0, matching observed HEAD/status endpoints, and the final graph digest; it is not signed source-to-output provenance, does not close producer warnings or corpus completeness, and cannot exclude a concurrent writer changing and restoring source bytes during extraction; topology-neutral Markdown-body/unsupported changes intentionally may not advance the former. The in-memory corrections prevent unrelated JSON arrays from becoming `extends` and exclude structural-only communities from the report's shown count. The launcher fixes AST extraction to one process, bypasses every case variant of ambiguous 0.9.51 JSON AST-cache entries without deleting them, sanitizes inherited `GRAPHIFY_*`/`GIT_*` controls and relative/repository-local executable lookup before the first Graphify import, canonicalizes the effective root, serializes every guarded producer through one OS-backed output lock, rejects linked worktrees/Git subdirectories and recursive reparse-point or hard-link output indirection, and does not modify the installed package or attest the rest of the wheel/transitive chain. The closed producer surface rejects semantic/generic extract, partial-output and corpus-broadening overrides, live/global sources, output redirection, hidden `.graphify_root` reuse, and update/watch bootstrap without an established owner graph. The upstream-generated `.git/hooks/post-commit` bypass remains outside this tracked guarantee until a separately authorized local replacement/reactivation. Installed Graphifyy 0.9.51 has two exact corpus/coverage residuals: its special saved-memory scan overrides explicit ignores, and its hard-coded noise pruning omits authored directories named `build`; `tests/test_graph_invariants.py` bounds both categories, so graph queries cannot prove absence across them. `py -3.12 -m tools.verify_graph_report graphify-out/graph.json graphify-out/GRAPH_REPORT.md` independently reconciles graph-derived counts, label membership, and default plain-label navigation. The two corpus BLOCKs withhold corpus completeness/privacy. The guarded reporter has no allowed report residual; an older/unpatched report remains BLOCK until the verifier returns exact PASS, while the validated in-scope graph remains available |
| **Atlas whole-repository census and semantic projection** — which tracked paths, safe nonblank lines, symbols, parser-owned structural roots, routes, components, tests, workflows, datasets, binaries, dependencies and Graphify records are represented for one source state | primary owner = the exact clean Git tree; deterministic derivative = compiler corpus contract `1.2.0` in `master-reference/compiler/` with schemas in `master-reference/schema/` | compile with `python -m compiler --repo-root .. --output <new-dir>` from `master-reference/`; read `manifest.json` + `completeness.json`, then follow record IDs back to their named live owners. Exact-clean source/digests come from raw selected-commit Git blobs (not checkout-filtered bytes); full-exposure worktree bytes are read separately only to detect concurrent/local change. Each successfully parsed safe source owns exactly one typed `structural_entities` root, and every safe nonblank line resolves to a same-file symbol or root. File census IDs cannot satisfy Level 1. The projection is a cache and never overrides source code, tests, manifests, runtime evidence, Graphify provenance, or this registry | compiler refuses an unclassified path, unsafe content, parser failure, census mismatch, changed-during-read file, dirty tracked tree, missing/duplicate parser root, orphan semantic target, or any safe line below Level 1 by default; `--allow-dirty-preview` is explicitly non-releaseable. Restricted payloads remain unread and metadata-only. Focused gates: `master-reference/tests/compiler/` |
| **Atlas bounded curated-content consequential-claim census** — the exact field-atomic candidate denominator within the five curated content owners; it is not a sentence-level or global claim census | contract owner = `master-reference/governance/consequential-claim-contract.json`; evaluator = `master-reference/governance/consequential_claims.py`; schemas = `master-reference/schema/consequential-claim-contract.schema.json`, `master-reference/schema/atlas-records.schema.json`, and the `consequential_claim_denominator` definition in `master-reference/schema/completeness-ledger.schema.json`; external authenticated-review verifier = `master-reference/release/authenticated_review.py` with its four independently versioned schemas in `master-reference/release/schemas/` | read `completeness.json :: consequential_claim_denominator` and the manifest-bound `chunks/consequential_claim_facets/` group. The evaluator binds the reviewed rule grammar and all five sources to selected-commit raw Git blobs, classifies every covered object property, and emits per-source rule-set/candidate receipts plus the aggregate candidate-set digest. The group emits one payload-omitting fingerprint record per candidate; candidate IDs bind semantic record identity and facet path while source pointers bind concrete locations. Read the group receipt for the exact count rather than restating it. `python -m cli verify-claim-review` may verify a separately stored canonical payload/signature against an external trust policy and public key, rejoin every signed row, and emit a schema-validated stdout-only non-promoting receipt. Payload records are lexical by facet ID; the public signing-material builder owns the domain-separated exact bytes | the emitted group contains no claim values, but its unsalted fingerprints are staleness bindings rather than confidentiality and low-entropy values may be dictionary-recoverable. External review inputs are absent by default and never discovered from the repository or release family. Authentication is relative to the exact caller-supplied policy/key snapshot; this verifier does not establish that snapshot's external authority, freshness, revocation history, or custody. Even a complete authenticated bounded review leaves `current_gate_promoted=false` and `global_gate_closed=false`; it supplies neither rendered-sink-universe closure nor release, recovery, or publication authority. No accepted review envelope currently exists, so the bounded census stays `declared_incomplete`, the reviewed count remains zero, and the global `consequential_claim_denominator_closed` gate remains unconditionally false. Focused gates: `master-reference/tests/governance/test_consequential_claims.py`, `master-reference/tests/compiler/test_compiler.py`, and `master-reference/tests/release/test_authenticated_review.py` |
| **Atlas Open Horizon rendered-sink lineage slice** — exact accounting for the existing Horizon candidate facets and safety boundaries in the PDF Horizon section and private `/gaps` route; it is not the global rendered-claim universe | contract = `master-reference/governance/rendered-sink-lineage-contract.json`; evaluator = `master-reference/governance/rendered_sink_lineage.py`; schema = `master-reference/schema/rendered-sink-lineage.schema.json`; sink producers = `master-reference/release/pdf_report.py` and `master-reference/app/atlas/horizonLineage.ts` | expand the contract against the compiler's `consequential_claim_facets`, the exact selected-commit `open-horizon-register.json` blob, and actual renderer observations. Every in-scope facet receives exactly one per-sink disposition; only the contract may declare omissions. The PDF gate carries the digest-only mechanical receipt, while actual PDF text extraction independently checks visible slots | this bounded slice deliberately leaves other curated sources, routes, PDF sections, template/computed/joined claims, branches, locale/layout, and runtime instances outside its universe. Payloads are omitted, but unsalted fingerprints are not confidentiality. A local mapping PASS supplies no semantic acceptance, independent review, claim evidence, publication authority, or global closure; `independently_reviewed=0`, all global candidates remain unresolved, and `closes_global_gate=false` are fixed. Focused gates: `master-reference/tests/governance/test_rendered_sink_lineage.py`, `master-reference/tests/release/test_pdf_report.py`, and the Horizon web content/rendered tests |
| **Atlas Capability Catalog rendered-sink lineage slice** — exact accounting for the existing capability-state and current-scope candidate facets plus the declared source-contract boundaries in the PDF Closed Capability Catalog and canonical unfiltered private `/capabilities` route; it is additive to, not a replacement for, the Horizon slice | contract = `master-reference/governance/rendered-sink-lineage-capability-contract.json`; evaluator = `master-reference/governance/capability_sink_lineage.py`; schema = `master-reference/schema/rendered-sink-lineage-capability.schema.json`; sink producers = `master-reference/release/pdf_report.py` and `master-reference/app/atlas/capabilityLineage.ts` | expand the contract against the compiler's `consequential_claim_facets`, the exact selected-commit `capability-catalog.json` blob, and actual renderer observations. The declared default sinks must account exactly once for every in-scope facet and bind every named source-contract input; PDF text and web DOM checks independently reconcile the visible slots | filtered/query/hydrated web states, other routes and PDF sections, other curated sources, computed counts, labels, joins, fixed prose, responsive/print/locale variants, and runtime instances remain outside this bounded universe. Payload-omitting unsalted fingerprints are not confidentiality. A local mapping PASS supplies no semantic acceptance, independent review, claim evidence, publication authority, or global closure; the global reviewed count remains zero, every global candidate remains unresolved, and `closes_global_gate=false` is fixed. Focused gates: `master-reference/tests/governance/test_capability_sink_lineage.py`, `master-reference/tests/release/test_pdf_report.py`, and the capability web content/rendered tests |
| **Atlas Core outcome rendered-sink lineage slice** — source-wide accounting for the Atlas Core candidate-facet denominator with a deliberately narrow visible subregion in the PDF Product Purpose and Outcomes section and `/product#core-outcome-contracts`; it is additive to the Horizon and Capability slices | contract = `master-reference/governance/rendered-sink-lineage-core-contract.json`; evaluator = `master-reference/governance/core_sink_lineage.py`; schema = `master-reference/schema/rendered-sink-lineage-core.schema.json`; sink producers = `master-reference/release/pdf_report.py` and `master-reference/app/atlas/coreOutcomeLineage.ts` | expand the contract against the compiler's `consequential_claim_facets`, the exact selected-commit `atlas-core.json` blob, and actual renderer observations. Each declared sink maps every Core candidate exactly once: the nine outcome `success_signal` facets are visible, while all other Core facets are explicit contract-owned omissions. PDF text and scoped web DOM checks independently reconcile the nine slots | the surrounding `/product` page, other routes and PDF sections, repeated business-value wording, titles, labels, joins, fixed/template/computed claims, branches, responsive/print/locale states, and runtime instances remain outside this bounded sink universe. Compiler source-grounding fallback is reported separately from renderer fallback; neither is independent review. The slice has no declared safety-field denominator and cannot infer safety from field names. Local mapping PASS supplies no semantic acceptance, claim evidence, publication authority, or global closure; the global reviewed count remains zero, all global candidates remain unresolved, and `closes_global_gate=false` is fixed. Focused gates: `master-reference/tests/governance/test_core_sink_lineage.py`, `master-reference/tests/release/test_pdf_report.py`, and the Core web content/rendered tests |
| **Atlas tracked-binary privacy review evidence** — exact identity, automated format evidence and claimed contextual verdict evidence for every binary in the selected Git tree | evidence owner = `master-reference/governance/tracked-binary-review.json`; schema = `master-reference/schema/tracked-binary-review.schema.json`; recomputing evaluator and compiler-owned authentication custody = `master-reference/compiler/binary_review.py` | join the evidence one-to-one to the selected commit's binary paths and Git blob IDs, then recompute each raw SHA-256/byte count/media type and bounded PNG or single-member gzip-TSV evidence from those Git blobs. PNG text/profile/EXIF and every private or unknown ancillary channel are rejected as automated-format pending; the two social PNGs contain only `IHDR`/`IDAT`/`IEND`, retain independently matched decoded-pixel evidence, and are automated-format passes. Gzip optional metadata flags are rejected. Accepted ancillary and decoded TSV text are scanned for both high-confidence secrets and generic local-home identities. The basis commit may precede the self-containing evidence commit only while it remains an ancestor and every reviewed blob identity is unchanged. Binary payload projection remains metadata-only | reviewer kind, independence and evidence-reference strings inside the tracked file are unauthenticated claims. Compiler contract `tracked-binary-review/1` has no trusted public key or detached signature and therefore fixes custody to `pending_trusted_external_attestation`, authenticated/accepted files to zero, and the semantic gate to false; invented strings cannot promote it. Compiler and release-bundle validators also fail closed on an empty, missing, duplicate, orphan, stale, wrong-blob, malformed or format-invalid evidence set. Completeness owns the exact denominator, automated passed/pending counts, claimed-vs-authenticated contextual counts and receipt/binary-set digests. Focused gates: `master-reference/tests/compiler/test_compiler.py` and `master-reference/tests/release/test_release_pipeline.py`. Generated PDF inspection, container/vulnerability assessment, signature and publication authority remain separate gates |
| **Atlas closed capability denominator, gaps, decisions and open horizon** — the scope against which the private reference says current/partial/missing/gated/excluded/unknown | `master-reference/content/capability-catalog.json`, `delivery-governance.json`, and `open-horizon-register.json`; bounded current facts inside them remain cited caches of the live owners named in each record | import through `master-reference/app/atlas/data.ts`; never use the hosted page as an owner and never promote an advisory horizon signal into a product-support claim | `master-reference/tests/content/catalog-validation.test.mjs` enforces denominator/disposition/owner relations; compiler/release freshness binds the content to its exact source tree |
| **Atlas release-member denominator** — canonical names and conditional/external disposition for every Master Reference output | `master-reference/content/output-contract.json` | UI reads the same contract; `master-reference/release/pipeline.py` refuses a generated family whose exact member set differs | release schemas + `master-reference/tests/release/test_release_pipeline.py`; the detached owner signature remains external |
| **Atlas image-metadata dependency boundary** — exact Vinext parser replacement, source/lock topology and unresolved vendored-parser release state | dependency owner = `master-reference/package.json` + `package-lock.json`; bounded local source = `master-reference/vendor/bounded-image-size/`; inventory/source-receipt owner = `master-reference/release/sbom.py`; gate owner = `master-reference/release/pipeline.py` | resolve `image-size` from Vinext's own module location and read the exact local-link → local-package → source-receipt edges from the SBOM. The local package accepts only bounded PNG metadata and an SVG root marker; it does not provide general image decoding, bound Vinext's prior file read, or supply external VEX authority | `master-reference/tests/source/bounded-image-size.test.mjs` ratchets the Git-owned JS/TS source denominator, rejects static/metadata-route/Next-image entry points, joins real assets and exact lock resolution, and uses subprocess watchdogs. `master-reference/tests/release/test_sbom.py` and `test_release_pipeline.py` bind the scoped override, exact Vinext identity and local-source digest. Next 16.2.12 still embeds compiled parser code outside npm override/SBOM component resolution, including an image-size implementation for which exact ICNS/JXL zero-progress inputs remain mutation-effective evidence; the related detector bundle is present but its runtime reachability is not asserted. Therefore `blocked_next_vendored_image_parser_and_external_review_required` and the external applicability/VEX BLOCK remain honest. Offline `npm audit` zero is not closure |
| **Atlas projection gzip representation contract** — the fixed RFC 1952 header, producer emission helper and portable receipt-bound expansion shared by projection members and the three Sites packaging receipts | header owner = `master-reference/build/gzip-contract.js :: CANONICAL_GZIP_HEADER_BYTES`; implementation owner = `master-reference/build/deterministic-gzip.mjs :: RECEIPT_BOUND_GZIP_ALGORITHM / deterministicGzip / expandReceiptBoundGzip` | import the owners in `build/compress-projection.mjs`, `build/deployment-manifest.mjs`, and `worker/index.ts`; never restate production bytes. Producer emission is pinned to deflate level 9, memLevel 8 and `Z_FILTERED`, with the exact receipt algorithm identifier owned beside the helper. Canonical raw projection, compression and outer deployment JSON remain the conceptual digest basis; `dist` retains only `.json.gz` representations. Compression binds raw and physical projection receipts. Outer deployment schema v1.2 directly hashes every non-projection-module physical member, including both inner receipt representations, and binds the compression-derived projection-module ledger once through its authority representation path plus exact count/bytes/full canonical-ledger digest; overall count/bytes/digest reconstruct the sorted union, while only the outer representation is self-excluded and included separately in the 248 MiB check. Loaders use same-handle bounded reads, exactly one fixed-header member, bounded inflate, EOF after independently checked CRC/ISIZE, strict UTF-8/canonical JSON, and exact raw/representation SHA-256 joins; exact pre/post full physical-tree metadata snapshots close mutation windows between member phases. Receipts bind bounded Node/zlib producer-version strings as provenance but do not require equality to the verifier runtime: deflate bytes are not cross-zlib canonical, and a fully reauthored chain remains self-authorable at the selected-commit build-custody boundary. The Worker prefix-checks body-bearing module GET responses only when the asset binding omits `Content-Encoding`; already-encoded responses remain an unread trusted-ASSETS boundary, and metadata-only HEAD refuses the ambiguous JavaScript/no-encoding tuple | focused source tests pin the exact producer profile and deterministic lossless representation, single-member framing, portable receipt-bound alternate-deflate expansion, raw/physical joins, same-handle and inter-phase races, the exact noncircular reconstructed census and hostile summary/overlap/rechain boundaries; `master-reference/tests/rendered-html.test.mjs` reconciles the built receipt/module census, expanded Sites limit, exact replay, malformed/plain/locked/failing streams, HEAD abstention, both physical metadata shapes, and Workerd's browser-decoding boundary. Runtime prefix proof is narrower than build/deployment full-member SHA-256 and bounded-inflate proof |
| **Atlas architecture and agent-continuity authority** — component ownership, allowed/forbidden static edges, protected actions, task scope and completion receipts | `master-reference/governance/architecture.json`; evaluators in `master-reference/governance/architecture.py` and `master-reference/continuity/` | `python -m continuity ...` is read-only and exact-compiler-bound; static candidates never become runtime truth | governance/continuity tests plus the compiler architecture receipt; device/Vault/client-ingest/public-publish constraints are unwaivable |
| **Design decisions** (→ HLD/LLD/CRD/deck) | `snap['design_blueprint'].decisions` | read the block; **count via fact #1, never restate it** | decision **count** reconciled; bodies producer-owned |
| **Architecture coverage** (per-architecture detector map) | `snap['architecture_coverage']`; owner = `cisco_toolkit/design_advisor.py :: _ARCH_COVERAGE_REGISTRY` (authoritative class count — don't hardcode it) | read the block | webapp SSOT lock |
| **Governed unknown evidence** — aggregate parser exceptions, suspicious zero-yield, unregistered parser/coverage axes, and unsupported diagnostic shapes without copying client evidence | `cisco_toolkit/unknown_evidence.py :: compute_unknown_evidence` → published at `snap['unknown_evidence']` (`unknown_evidence/1`, containing `unknown_evidence_event/1` records) | read the block; every event is aggregate-only, candidate-classified, `needs_triage`, and mapped to the current capability/gap IDs. Missing or partial input remains explicit and can never produce an unqualified no-unknowns claim | `tests/test_unknown_evidence.py` (counterexamples, registry reconciliation, deterministic aggregation, bounded-empty semantics, and forbidden-content injection) + `tests/test_pipeline_inprocess.py` publish/recompute lock |
| **Traffic Assurance result and evidence custody** — one bounded synthetic IPv4 TCP/UDP five-tuple verdict across requested forward/return paths, stateless interface ACLs, observed per-hop MTU, ECMP consistency and an optional single node/site/link failure; plus the positive command/capture/parser, exact-type content, forwarding-gate syntax, and scoped-route denominators required before absence can be evidence | result/custody owner = `cisco_toolkit/traffic_assurance.py :: assess_flow / assess_flows / build_traffic_evidence_custody`; route-projection receipt owner = `cisco_toolkit/build.py :: ScopedRouteProjection / scope_routes`; forwarding-gate syntax owner = `cisco_toolkit/parse.py :: FORWARDING_GATE_SYNTAX_REGISTRY`; published at conditional `snap['traffic_assurance']` (`traffic_assurance_set/1`) and always-present `snap['traffic_evidence_custody']` (`traffic_evidence_custody/1`) | During the producing run, call `assess_flows(snap, intents)` exactly once while the in-memory current-run custody marker, matching `scoped_route_projection/1` scope/digest receipt, and exact content bindings exist. A registry-matched but unmodeled gate is a categorical abstention; the registry is explicitly bounded and does not certify all vendor syntax. Persisted custody and projection fields are audit evidence only: JSON serialization intentionally destroys the trust marker, so a loaded snapshot cannot self-certify a fresh positive calculation. When redaction is requested, compute first and apply the one global redaction transform; JSON and Excel project the transformed precomputed result, while MCP performs an exact lookup of that result only and tells a miss to rerun `cisco-assess --traffic-intents` from original evidence. No delivery surface recalculates assurance | `tests/test_traffic_assurance.py` owns counterexamples, current-run/JSON-boundary, content-binding, gate-registry, and route-receipt abstention; `tests/test_traffic_assurance_excel.py` proves workbook projection without recalculation; `tests/test_mcp_server.py` proves lookup-only behavior; `tests/test_pipeline_inprocess.py` and `tests/test_redact_e2e.py` lock the one-call, post-redaction cross-surface join |
| **Domain skill-packs** — which domain lens (DC/Enterprise/SP/Security) a snapshot loads (D6: retrieval-selected, NOT standing agents) | pack→class map in `cisco_toolkit/domain_packs.py :: PACKS`; knowledge/checklists in `docs/packs/*.md` | `cisco_toolkit.domain_packs.select_packs(coverage)` (loads a pack iff its class was OBSERVED); CLI `python -m cisco_toolkit.domain_packs <snap>` | pinned to `_ARCH_COVERAGE_REGISTRY` by `tests/test_domain_packs.py` (every mapped class in the registry; every registry class has a pack; every pack doc exists) |
| **Per-detector descriptors** — what each analysis detector checks, its healthy value/threshold, the snapshot fields it reads, and the coverage-honest `abstains_when` guard ("not-observed != healthy" as a schema property) | `cisco_toolkit/detector_schema.py :: compute_detector_schema` → published at `snap['detector_schema']` (+ the 'Detector Schema' workbook sheet) | read the block; it DESCRIBES detection, never re-runs it | `tests/test_detector_schema.py` (every evidence-gated detector has a non-empty `abstains_when`; cited_fields are real dotted snapshot keys) |
| **Project decisions / current cross-agent state / lessons / how-to-work** | live Git + the current/reconciled sections of `CLAUDE.md`; durable verified engine lessons live in `docs/quality/learnings.md` | enter through root `AGENTS.md`, then read the live owner; Claude/Codex chats and `.claude/.../memory/*.md` are platform-specific caches, never the sole owner | ⚠️ current status still requires live verification; caches may lag or contradict tracked owners |
| **Operating doctrine / PPDIOO gates / guardrails / entrypoints** | `CLAUDE.md` (shared owner for every agent surface) | Claude Code loads it directly; Codex enters through root `AGENTS.md` and reads it completely | convention + shared bootstrap |
| **Engagement gate state** (P0-3/DEC-003) — PPDIOO document-gate approvals (Assess→Design needs `assessment_approved`; Design→MOP needs `lld_approved` + `baseline_captured`) + the who/when/why audit trail for BOTH dispositions — the `--override-gate` bypass **and** the refusal that withheld a deliverable | `docs/engagement-state.json` — **per-engagement**, resolved under `--gate-root` (default: the working directory) next to that engagement's artifacts; created by the first `approve` into an **existing** root (a root that is not a directory REFUSES everywhere — enforce/record/show — rather than reading as brownfield; note this catches only a path that does not exist, NOT a wrong-but-existing one such as `D:\Engagements` for `D:\Engagements\ACME`, which still reads as brownfield). ABSENT store = ungated brownfield, warn-and-proceed, so a caller that re-homes the engine to a scratch cwd and omits `--gate-root` silently ungates EVERY gate: a fail-OPEN path, unlike an unreadable store which fails closed. **Ownership is DECLARED, never inferred (ADR-0006).** A ledger may carry an append-only top-level `engagement` identifier naming what it governs, and a run may declare the engagement it is for (`--engagement`, or `gate_state bind <id>` to bind a ledger once). A declaration that the ledger contradicts (different id) or cannot confirm (unbound ledger) REFUSES, non-overridably — `--override-gate` is consent to skip a *known* gate, and neither case has located one. Declaring nothing preserves the historical proximity behaviour exactly, so no existing engagement's gate DECISIONS change — scoped deliberately: the ledger FILE is not inert, because a `pending` refusal appends a `refuse` row to any readable ledger, bound or not (see the audit-array owner below; ADR-0006's Correction of 2026-07-25 records the same distinction). ⚠️ Auto-discovery of "the ledger for this collection" remains impossible BY DESIGN and is not a goal: two proximity heuristics (cwd; walk-up-from-the-collection) were built and refuted end-to-end, the second reporting engagement ACME's approvals for a run of GLOBEX. The identifier is human-minted and opaque because the snapshot has none to derive — no section names a client, `--redact` pseudonymizes the IP/MAC/serial fields a fingerprint would use, and the fleet is deliberately replaced across the Assess→PIR span the ledger must survive. The `audit` array is the ONE owner of the refusal record: `enforce` returns a `GateVerdict` and appends a durable `refuse` row (who/when/why + the missing approvals + the engagement it was written against) for a refusal over MISSING APPROVALS — the only refusal class meaning a located gate said no — so the SAFE path is as auditable as the override — previously only the override left a trace, making an absent `design.docx` indistinguishable from a `--no-design` run. It is the cross-run home BY DESIGN: a refusal provokes a re-run, which overwrites the per-run `.run_manifest.json`, so any manifest copy is a cache that must cite this array — and now does: the sealed row carries the `engagement` identifier (the join key back to its owning ledger) plus `recorded`, so a per-run copy can never assert a durable trail the ledger does not hold. The absolute store path stays UNsealed, which would make `chain_root` machine-specific. Five statuses cannot be recorded and report `GateVerdict.recorded=False` rather than implying a write — `bad_root`/`unreadable`/`ungated` (nowhere to write, or writing would destroy evidence / silently enrol an unenrolled engagement) and `ownership_mismatch`/`ownership_unbound` (the ledger governs another engagement and this run has no standing to append to it). `STATUSES` is the single status vocabulary shared by `enforce` and `pending_approvals`, so deciding and disclosing can never disagree about one ledger. Schema + enforcement owner: `cisco_toolkit/gate_state.py :: DOC_GATES / GENERATOR_REQUIRES / ENGAGEMENT_KEY / STATUSES / GateVerdict / enforce / _record_refusal / ownership_error` (append-only gate keys, same contract as `engagement.py :: GATE_SEQUENCE` — the per-wave T-minus cadence in `webapp/backend/gates.py` is a DIFFERENT axis, wave execution vs document approvals, and the two records stay deliberately distinct: a document approved once is a different fact from a wave signed at each T-minus, so each keeps one owner under Law 1. They share only the `engagement` vocabulary, which is what a future join would key on). Read-only disclosure (never writes, never raises) via `gate_state.pending_approvals`; `is_revoked`/`revoked_requirements` separate a human's positive WITHDRAWAL of approval from a gate nobody ever signed — only the former is strong enough to withhold a deliverable | `python -m cisco_toolkit.gate_state show` (board + audit tail + the engagement it governs, or a loud `UNBOUND`; `approve`/`revoke` record a human gate, `bind` declares ownership) | ✅ **engine refusal** — `COLLECT_PARSE_V3_23_0` skips the design/MOP write on a missing upstream approval unless an explicit `--override-gate "<reason>"` appends an audit line, and records the refusal in the ledger either way. The run still exits **0** — a refusal is a correct run that withheld a document, and every wrapper here reads non-zero as a hard failure (`webapp/backend/ingest.py` raises, which `serve.py` renders to a field engineer as "redaction FAILED … Treat anything already written as UNREDACTED"); opt in with `--fail-on-gate-refusal` (exit 2, matching `holdout.py`/`scorecard.py`). Pinned by `tests/test_gate_state.py` (refusal + override-audit + brownfield contract + a mis-set `--gate-root` refusing + the synthetic-cwd caller inventory). ⚠️ **Enforcement — i.e. WITHHOLDING a deliverable — is the CLI only.** `webapp/backend/deliverables.py::generate` renders design/MOP without ever refusing, but it is no longer silent: since 2026-07-22 it consults the gates and DISCLOSES the posture on the response (`X-Gate-Status: <status>:<missing>`, set by the route in `app.py`) via `gate_disclosure`, which reads the token from `pending_approvals` rather than deriving its own — it derived its own until 2026-07-25 and reported `ungated` for a ledger whose approval had been REVOKED. `Atlas.exe --redact-folder` is deliberately UNGATED (refusing there would withhold two renderers while the same `design_blueprint` ships in the snapshot/explorer/deck; and the 7 DOCX writers already stamp a draft Status row via `docmeta.add_document_control` — NB the workbook, explorer and deck do not) |
| **Release version** | `pyproject.toml :: version` (currently 3.32.1) — bump at **tag time only** | — | ✅ this cached value reconciled to the owner by `tests/test_registry_freshness.py` |
| **Schema version** | `cisco_toolkit.__version__` (currently 3.23.0) — **decoupled from release by design** | — | intentional decouple (do **not** reconcile equal to release); the cache itself pinned by `tests/test_registry_freshness.py` |
| **Release history / what shipped** | `CHANGELOG.md` | read it | convention |
| **Run provenance / chain-of-custody** | `cisco_toolkit/manifest.py` (hash-chained ledger; append-only **within one sealed manifest**, NOT across runs) | the sealed run-manifest `<base>.run_manifest.json` — a PER-RUN seal of the artifact set beside it, replaced by a re-run to the same `--output` exactly as its artifacts are | corruption/careless-edit evident by an **unkeyed** hash chain, not producer-authenticated; written crash-safely by `write_json_file` (tmp+fsync+`os.replace`, with a **logged** in-place fallback when the destination is locked — see `tests/test_run_manifest_durability.py`). It DOES record the per-run PPDIOO gate verdicts: `build_run_manifest` seals `gate_state.verdicts()` as the `{"stage": "gate"}` step, so a deliverable withheld by a gate is sealed and not merely a console line (`missing: null` = never evaluated, NOT "nothing missing"). The manifest is a PER-RUN cache of those verdicts; the durable cross-run record of human gate decisions remains `gate_state`'s audit array, which it must cite as owner. The pipeline finalizer re-opens the written manifest and verifies its chain, sealed metadata and every current-run artifact byte before reporting success. A receiver repeats the safe-default full check with `python -m cisco_toolkit.manifest verify` (or `Atlas.exe --verify-manifest`); `--metadata-only` is the explicit opt-out only for a deliberately separated manifest. Retain the full `chain_root` out of band and pass `--expect-root` when binding a delivered set to one run matters |
| **Deliverable quality bar + reusable prompts** | `…\Desktop\_Deliverable_Excellence_Kit\` (Standard + Master Prompt Library) | read the kit | the verify-gate ratchet on generated docs |
| **Generated-deliverable excellence** (furniture every docx carries) | `cisco_toolkit/docmeta.py :: add_excellence_front / add_glossary / add_document_control` + plan `docs/deliverable-excellence-plan.md` | the 7 writers call them; read the plan | ✅ **CI-enforced** — `tests/test_deliverable_excellence.py` fails the build if any deliverable lacks the At-a-Glance / Glossary / Document-Control / acceptance furniture |
| **Artifact lifecycle and deliverable-set completeness** — pre-cutover family membership, engine CLI filenames, AssessHub snapshot downloads, producer/dependency/media metadata, and the conditional post-execution PIR | `cisco_toolkit/docmeta.py :: ARTIFACT_SPECS`; `FAMILY`, `CLI_ARTIFACT_SUFFIX`, `WEB_ONLY_KINDS`, the AssessHub catalogue and portable renderer imports are compatibility/consumer views derived from it. PIR is conditional post-execution and deliberately has no CLI suffix | `docmeta.cli_artifacts(stem)` for the complete engine-run denominator; `artifact_family_metadata()` for UI/portable denominators; `artifact_spec(key)` for producer/surface metadata. `webapp/backend/ingest.py :: run_redaction_folder` verifies only the registry's CLI-produced set | ✅ **CI-enforced** — `tests/test_docmeta_cli_artifacts.py` rejects empty/duplicate/orphaned contracts, reconciles mapped suffixes against actual engine writer literals, resolves every AssessHub writer, and proves the web catalogue is the registry view; `tests/test_atlas_bundle.py` mutates the dependency-owner seam to prove portable hidden imports follow it |
| **Deliverable quality trend** — per-cycle QA verdicts + golden-eval scores (the "provably improves over time" signal) | `docs/quality/scorecard.jsonl` (append-only), written by the `SubagentStop` appender (`cisco_toolkit/scorecard.py`) + the golden-snapshot eval harness (`cisco_toolkit/eval_harness.py`) | `cisco_toolkit.scorecard.read_rows()` / `summarize_trend()`; render via `python -m cisco_toolkit.scorecard trend` — never hand-edit; schema in `docs/quality/README.md` | append-only; parser + trend pinned by `tests/test_scorecard.py`, harness by `tests/test_eval_harness.py`; empty renders "no entries" (coverage-honest, never "healthy"). **PROVISIONAL judge-verdicts (P0-6/DEC-004):** an APPROVE with `judge_tnr` null/below `scorecard.JUDGE_TNR_FLOOR` (the constant owns the 0.25 figure) is machine-marked `provisional: true` = advisory, non-gating — enforced by `scorecard.append_row` + `selfcheck.check_judge_trust` |
| **Self-check guard roster** — WHICH test suites the immune system verifies non-vacuous (the guards' guard) | `cisco_toolkit/selfcheck.py :: GUARD_FILES` (the authoritative membership — read it, never restate a count) | `from cisco_toolkit.selfcheck import GUARD_FILES` | ✅ exact membership pinned by `tests/test_selfcheck.py :: test_guard_files_exact_pin` (P0-6 — closes the "drop the entry AND its file, stay green" hole); runtime non-vacuity by `check_guards_nonvacuous` in the nightly self-check |
| **PIR-outcome calibration** — predicted pre-cutover readiness vs. actual PIR outcome (the "is the scorer calibrated" signal, Phase 1) | `docs/quality/pir_outcomes.jsonl` (append-only), derived by `cisco_toolkit/calibration.py` | `cisco_toolkit.calibration.read_outcomes()`; report via `python -m cisco_toolkit.calibration --report` — never hand-edit; schema in `docs/quality/README.md` | **D11-gated: descriptive-only until N≥5, propose-only never auto-applied**; pinned by `tests/test_calibration.py`. **Distinct from** `analyze.compute_calibration_report` (prospective within-snapshot band diagnostic) |
| **D10 eval pre-registration** — the Phase-2 falsification thresholds, committed BEFORE any eval run (P2-0d; owner-doc §7 discipline + DEC-006 corpus recording) | `docs/quality/d10-eval-thresholds.json` (values frozen at registration 2026-07-10; adjusting one after a run is a protocol violation) | read the JSON; the registration of record is `docs/d10-retrieval-eval-design-2026-07-08.md` §7 | ✅ exact-value pin `tests/test_d10_thresholds.py` — any edit must move the file AND the pin together in a reviewed diff; a light prose-reconcile stops JSON↔doc drift |
| **D10 eval set + sealed anchor key** (P2-1) — the frozen Phase-2 60-query fixture (20 identifier rows extracted deterministically from the graph / 20 semantic / 10 multi-hop with declared edges / 10 negative) with graded 0–3 qrels, plus the 15-anchor judge-screening pair | `docs/quality/d10-eval-set.jsonl` (queries + qrels; header records graph provenance + corpus contract) + `docs/quality/d10-anchors.jsonl` (judge-visible half: aid/query/doc ONLY) + `docs/quality/d10-anchor-key.jsonl` (**sealed** expected-grade bands — scorer-only, never in a judge prompt; the `cisco_toolkit/defect_panel.py` separation pattern); extractor + validators `cisco_toolkit/d10_eval_set.py`; strata counts owned by the P2-0d pre-registration row above | read the JSONL (first line is `_meta`); validate via `python -m cisco_toolkit.d10_eval_set --verify` (graph-dependent checks run on the owner machine and skip elsewhere, saying so) | ✅ `tests/test_d10_eval_set.py` — counts reconcile to `query_set`, every non-identifier query must cite grounding, semantic rows synonym-stripped, anchor whitelist (the seal) + exact SHA-256 pins on all three fixtures (an edit must move file + pin together in a reviewed diff); frozen at registration — P2-2 pool grades land in a SEPARATE file, this one is never edited |
| **Holdout policy (DEC-007 / P1-2)** — WHEN the sealed 70/30 optimisation⁄holdout split over the REAL PIR rows activates + HOW the holdout may be read | `docs/quality/holdout-contract.md` (the policy owner — pre-registered BEFORE any REAL data exists); mechanics + figures `cisco_toolkit/holdout.py` (`ACTIVATION_FLOOR` owns the N≥50-REAL trigger, `HOLDOUT_PCT` the split; seal = the `manifest.py` hash chain; REAL-vs-surrogate discriminator shared with `calibration.py`) | read the contract; activation state via `python -m cisco_toolkit.holdout status`; holdout rows ONLY via `python -m cisco_toolkit.holdout read --who <name>` — every access appends who/when to the append-only `docs/quality/holdout_access.jsonl` | refusal-below-floor + seal→tamper→verify-fails + access-logged pinned by `tests/test_holdout.py` (which also reconciles the contract's cached figures to their code owners); **audit trail, NOT proof of non-use (none exists — the contract says so plainly)**; no manifest is committed while dormant — the seal is created only at activation (architect-plan §6 frozen set) |
| **Nightly-run ledger** — the clock's audit trail: per-run outcome + metered spend + actions (Phase 2 rails) | `docs/quality/nightly_runs.jsonl` (append-only), written by a nightly wrapper via `cisco_toolkit/clock.py :: append_run` | `cisco_toolkit.clock.read_runs()`; go/no-go via `python -m cisco_toolkit.clock --preflight`; spend-vs-action via `--report` — never hand-edit | rails only (3-fail breaker + 30m cooldown + daily-spend ceiling D13); pinned by `tests/test_clock.py`. **Scheduler registration + `claude -p` invocation are NOT wired — human-gated (spend + system change)** |
| **Real-query mix** (P2-0a) — the log of REAL retrieval/`/ask` queries that D10's dense-lane precondition classifies ("classify 30 real queries by type FIRST" — `docs/d10-retrieval-eval-design-2026-07-08.md` §5; before P2-0a no such log existed, so that precondition was unrunnable) | `docs/quality/query_log.jsonl` (**append-only, gitignored/owner-machine** — like the vault digest: a real query may name client tokens from another engagement, two-store rule ADR 0001; per-command opt-out `CISCO_RECALL_NO_LOG=1`; human decision 2026-07-10 resolving the #319/#321 design split), written by `cisco_toolkit/recall.py :: log_query` from the REAL surfaces only: the recall CLI (`surface: recall`) and the `/ask` command's mandatory first step (`--log-only --surface=ask`) | read the JSONL — one object per line, exactly `date`/`query`/`surface`, nothing else; never hand-edit; synthetic queries (`--eval`, the labeled experiment, unit tests) are deliberately NOT logged, so the mix stays real | fail-open by contract — a logging failure never breaks retrieval or an `/ask` turn; schema + no-eval-pollution + registration + `/ask` wiring pinned by `tests/test_query_log.py` |
| **External advisory intel** — sanitized, SHA-256 hash-sealed PSIRT/advisory feed (Phase 5 "eyes"; **unauthenticated**) | `docs/intel/feed-*.jsonl`: **produced** by `research_lane/producer.py` (egress-fenced, outside `cisco_toolkit/`; run from a connected worktree, live egress opt-in `--live --url`), **consumed** by `cisco_toolkit.intel_feed` | `cisco_toolkit.intel_feed.load_feeds()`/`verify_feed()` — intake-gated (sanitization claim + strict JSON/IDs + manifest line count + SHA-256 + forbidden-scan); v2 envelopes declare `integrity: sha256`, `hash_scope: entry-lines`, and `authentication: none`; producer recursively scrubs via `research_lane/sanitize.py` — never hand-edit | producer + consumer + HTTP/intake gates built (`tests/test_research_lane.py`, `tests/test_intel_feed.py`, `tests/test_research_http_guard.py`); live intake is public-HTTPS-only with DNS/address pinning, environment proxies disabled, host-confined redirects, and byte/count/deadline budgets. **First live sweep landed 2026-07-07** — tracked `feed-2026-07-07.jsonl` has 93 verified CISA KEV entries under the current v2 envelope; it is corruption-evident, never source-authenticated. Contract in `docs/intel/README.md` + `research_lane/README.md`. Empty → "no intel feed (gated)", never "no advisories". Status + entry-count cache reconciled by `tests/test_registry_freshness.py` |
| **Vault digest** — one-way, sanitized, read-only distilled domain facts for recall (D3/D4, ADR-0001 Amendment 1) | `docs/vault-digest/digest-*.jsonl`: **produced** by `research_lane/vault_digest.py` (vault-fenced, outside `cisco_toolkit/`; run from a vault-connected session, explicit `--vault`), **consumed** by `cisco_toolkit.recall` | `cisco_toolkit.recall.load_vault_digest()` — intake-gated via `intel_feed.verify_feed` (sanitization claim + strict structure/count + SHA-256 + forbidden-scan), ranked **lexically** (`vault_digest_rank`) + optional local-Ollama re-rank (`ollama_recall.py`, subprocess, gated); Rule-3-scrubbed via `research_lane/sanitize.py`, hash-sealed like the intel feed with `authentication: none` — never hand-edit | producer + consumer + lexical RRF fusion **built** (`tests/test_vault_digest.py`); **first digest produced 2026-07-07** (`digest-2026-07-07.jsonl`) — **owner-machine only**: digests are gitignored by design (personal-vault-derived), so clean clones / worktrees / CI carry none and recall there honestly falls back to graph+docs; Ollama optional + gracefully-degrading (absent ⇒ lexical); Ollama + `nomic-embed-text` installed locally, semantic path validated (`/api/embeddings`) — read the version live via `ollama --version`, never cache it here (a pinned "0.31.1" had already drifted to 0.31.2 by 2026-07-10). Contract in `docs/vault-digest/README.md`; never-tracked + owner-machine status guarded by `tests/test_registry_freshness.py` |
| **Distilled engine learnings** — verifiable, cited facts about the codebase/engine, surfaced at SessionStart | `docs/quality/learnings.md` (discipline: <100 lines · every entry cited · no self-assessment) | read it; lint via `cisco_toolkit.learnings.lint_file`; surfaced by `.claude/hooks/session-brief.sh` | ✅ `tests/test_learnings.py` enforces the discipline on the real file. **Distinct from** `docs/log.md` (raw `/retro` source) and the career/domain vault (promoted via `/ingest`) |
| **Brand design tokens** — print navy + severity (deck/DOCX/workbook) · digital dark/light palette (explorer/webapp) · **application identity** (the portable app's name/byline — ADR-0004 D1) | print → `cisco_toolkit/brand_tokens.py :: BRAND_NAVY_RGB` (canonical `#1F3864`); digital → `cisco_toolkit/blast_radius_explorer.html` `:root`; app identity → `cisco_toolkit/brand_tokens.py :: APP_NAME/APP_BYLINE/APP_TITLE` ("Atlas" — a rename is ONE edit there) | import the token — never hardcode a hex / `RGBColor` / the app name | ✅ **CI-enforced** — `tests/test_brand_tokens_reconcile.py` (zero stray print-navy literal) + `tests/test_theme_tokens_reconcile.py` (theme.css must not drift from the explorer; 33 shared tokens) + `webapp/tests/test_atlas_entry.py` (app-identity values pinned) |
| **Side engagements** (own records, *not* this repo — artifacts untracked by design) | reference DC design bundle (`Reference_DC_Design\`) and reference CCTV bid (`Reference_CCTV_PS_Proposal\`) are fictional aliases only; real client names and absolute owner-machine paths are intentionally omitted from pushable source | read the private artifacts from the **main checkout** only when separately authorized — untracked means no worktree / fresh-clone / CI copy | privacy boundary: this registry records the ownership rule without publishing client identity or local paths; validate real pointers against the owner-machine private inventory, never a public-repo string |

> **Runtime-closure authority consumption rule:** the `/2` review signature authenticates the
> canonical receipt, not its trust policy. The policy remains a replaceable authorization and
> revocation input. Every authority use must obtain the current canonical policy bytes and their
> exact digest from authenticated external custody, then call
> `require_verified_transition_runtime_closure_review` again. The digest argument proves only exact
> equality to the caller's selection; the repository verifier cannot establish external selection,
> policy issuer/namespace/succession, authenticity or custody, reviewer-key identity/custody,
> trusted time, global freshness or anti-rollback, revocation-history completeness, organizational
> independence, artifact semantic truth, or real-world capture completeness. Any authority gate
> must consume only that fresh return value and
> independently compare and bind its `bindings_digest`, `policy_digest`, and `evaluated_at` to the
> gate-selected commit/tree, evidence digest and states, and mapped evidence digests; a retained
> `.complete` value is historical state, not authority. No such current policy, key, signature, or
> receipt is bundled.

> **Representative-workload authority consumption rule:** the workload-review `/1` signature
> authenticates the canonical receipt, not its replaceable trust policy. Every authority use must
> obtain the current canonical workload-policy bytes and exact digest from authenticated external
> custody, then call `require_verified_transition_workload_review` again. The verifier rejects an
> evaluation-time rollback and fully rechecks current key authorization, subject authorization,
> receipt lifetime, and revocation, but it cannot authenticate policy selection, succession,
> custody, trusted time, global anti-rollback, revocation-history completeness, or organizational
> independence. A consuming gate must use only the fresh return value and independently bind its
> `bindings_digest`, `policy_digest`, and `evaluated_at` to the selected candidate. Retained
> `.adequate` is historical state, not authority. No current workload policy, key, signature,
> receipt, or representative-workload corpus is bundled.

> **TCB-budget authority consumption rule:** the budget-review `/2` signature authenticates its
> canonical receipt, not the replaceable trust policy, and does not by itself make the signed
> budget decision current authority. Every authority use
> must obtain the current canonical TCB-budget policy bytes and exact digest from authenticated
> external custody, then call `require_verified_tcb_budget_review` again. The verifier rejects an
> evaluation-time rollback and fully rechecks current key authorization, source-subject
> authorization, receipt lifetime, and revocation, but it cannot authenticate policy selection,
> namespace or succession, custody, trusted time, global anti-rollback, revocation-history
> completeness, reviewer independence, or the truth of reviewed evidence. A consuming gate must
> use only the fresh return value and independently bind its `bindings_digest`, `policy_digest`,
> and `evaluated_at` to the selected candidate. A serialized freeze retains the decision-time
> `review_trust_policy_digest`; its separately held current review is ephemeral authorization and
> must never rewrite those historical bytes. No current TCB-budget policy, key, signature, receipt,
> independently approved budget, or positive freeze is bundled.

> **Snapshot-vintage note (coverage-honest):** the three newest evidence blocks — `cable_map`,
> `architecture_coverage`, `coverage_matrix` — are published by the *current* engine
> (`COLLECT_PARSE_V3_23_0.py:2485/2624/2629`) but are **absent from the on-disk 20260613 snapshot**,
> which predates them. Regenerate the snapshot to populate them; the owner paths above are the
> contract regardless of that file's vintage.

## Facts that live in two homes — which one wins

| Fact | Authoritative owner | Legitimate copy (a *cache*) | Rule |
|---|---|---|---|
| Fleet counts (303 inventoried / 253 collected / 50 not) | `ssot.canonical_facts(snap)` | memory `canonical-reference-fleet.md` | Snapshot wins. Memory is a human-readable cache — regenerate it *from* the snapshot; never edit the number in memory alone. (They agree today.) |
| Release vs schema version | `pyproject.toml` vs `__version__` | — | **Not the same fact.** Decoupled on purpose; do not force-equal. |
| Self-verification badge: published FACTS vs reconcile CHECKS | `ssot.summary(snap)` → `n_facts` (canonical facts PUBLISHED) and `n_checked` (reconcile checks that RAN) | the rendered "Single source of truth" line in every DOCX (`docmeta.add_excellence_front`) | **Not the same fact, and not the same unit.** A fact can carry several independent bases (`n_devices` reconciles against BOTH `len(health_scores)` and `collection_completeness.summary.inventory`) and `reconcile` also verifies non-canonical siblings (`lifecycle_risk.summary.by_band`), so `n_checked > n_facts` is NORMAL (20 vs 14 on the Meridian reference fleet). They must never share an "X of Y" frame — rendered that way the badge read "✓ 20 of 14 headline figures self-verified" in all seven deliverables. `verified` additionally requires `n_checked > 0` (an empty violation list over zero checks is not a pass). Guarded by `tests/test_ssot_reconciliation.py::test_excellence_badge_never_claims_more_figures_than_published`; the fact-count slot is also pinned by `eval_harness._check_citations_rendered` (L1) |
| Native-VLAN-1 exposure: switchport-mode count vs live count | switchport-mode figure → `design_advisor._signals` (`native1_trunks`/`native1_switches`, via the shared `textutils.is_trunk_mode` predicate; archreview L2-3 co-renders the same basis) · live figure → `analyze.compute_operational_drift` item 3 (via `textutils.is_live_trunk_status`) | the rendered decision / punch-list / archreview texts, the `l2-native-vlan-1` descriptor, `design_kb` observable | **Not the same fact.** Two deliberate measures of one exposure (QA deliverables_M0_20260718: 3100 vs 1777); do not force-equal. Every render names its unit+basis from the `textutils.NATIVE1_*` tokens (one owner), and the numbered-unit vocabularies stay disjoint across bases — guarded by `tests/test_design_blueprint.py::test_native_vlan1_design_and_punchlist_texts_are_self_disambiguating`. |
| Any headline number rendered in a deliverable | fact #1 (`canonical_facts`) | the rendered DOCX/XLSX/HTML/PPTX | Already mechanically reconciled by the cross-surface lock. |
| Digital design tokens (dark/light palette) | `blast_radius_explorer.html` `:root` | `webapp/frontend/src/theme.css` (mirror; cites it, line 1) | Explorer wins. Guarded by `test_theme_tokens_reconcile.py` — the 33 shared tokens must match. |
| Non-negotiable safety guardrails (read-only, proposer≠verifier, coverage-honest, one-source, no-egress, no-bypassPermissions, human PR+CAB) | `CLAUDE.md` guardrails + operating-doctrine section | the **protected memory tier** `protected-constraints.md` (`protected: true` — pinned, never-compressible; D12) | CLAUDE.md wins. The pin is a labelled cache reconciled to it by `cisco_toolkit/memory_guard.py :: reconcile_constraints` (guarded by `tests/test_memory_guard.py`); memory consolidation must **never** compress a `protected: true` entry (the monthly-consolidation skill's protected-tier rule). |

## The federation rule (how to keep it consolidated)

1. **Reference, don't restate.** If a fact has an owner above, READ it from the owner; don't hardcode a copy.
2. **A copy is a cache and must be labelled.** If you must cache a fact for offline/human convenience
   (memory, an executive summary), cite the owner path so a reader knows where to reconcile.
3. **Two computations of one fact = a bug.** The second one is where drift will appear. Delete it, or
   guard it with a reconcile check.

## Adding a new source of truth (the ratchet)

1. **Register the domain here** — name the one owner path and how to read it.
2. **If it overlaps an existing fact**, add a `check(...)` to `ssot.reconcile` (for assessment facts)
   or the relevant guard, so the overlap can't drift.
3. **Never introduce a second computation** of a fact that already has an owner.
4. A review finding that a fact lived in two un-reconciled places → add the guard *permanently*. The
   registry's floor only rises.

---
*This registry is itself a deliverable: answer-first, complete, and coverage-honest (it names its
own weakest link — memory is the one hand-maintained owner). Governed by the Deliverable Excellence
Standard, Law 1.*
