# Pre-registered decision rule — spare rung (rung 6), written BEFORE reading the rung-5 gate result

Recorded 2026-07-11 ~09:05Z while the rung-5 gate run (qwen3:8b, instrument v2, commit 42008fb)
is still in flight; committed before its output exists to read.

- Gate PASSES (kappa >= 0.70 AND accuracy >= 0.80): rung 6 NOT spent. Ship; D10 re-run unblocked
  (human-owned, overnight ~13 h measured ETA).
- Gate FAILS with accuracy >= 10/15 AND clear-irrelevant still 5/5: mechanism (excerpt coverage)
  CONFIRMED, residue plausibly excerpt semantics -> spend rung 6, ONE variable: a wrapper-prompt
  line stating the excerpt is selected windows of a larger document and absence from the excerpt
  is not evidence about the document. Re-gate from scratch; record either way.
- Gate FAILS with accuracy <= 9/15 OR any clear-irrelevant anchor lost: rung 6 NOT spent (a third
  same-family prompt tweak against a flat/regressed result is noise-chasing). Record the failure;
  next-value forks are human-owned: mid-size cross-family model (EGRESS), GPU/RAM host, or accept
  the pre-registered PARTIAL protocol.

This file is the pre-commitment artifact for the ladder log; the ladder entry will cite it.
