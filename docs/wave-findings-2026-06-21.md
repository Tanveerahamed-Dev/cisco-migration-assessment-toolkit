# Wave findings backlog — wf_875b803c-1cf (2026-06-21)

150 confirmed (adversarially verified). Synthesis agent OOM-failed; this is the raw ranked set.
Severity: {'High': 31, 'Medium': 77, 'Low': 33, 'Info': 9}

## [High/high] R2-1-01
- file: COLLECT_PARSE_V3_23_0.py:1720  (engine pipeline -> snapshot -> all lifecycle-bearing deliverables)
- claim: compute_lifecycle_risk is the engine's per-device EoL/end-of-support classifier. Its sole production call site COLLECT_PARSE_V3_23_0.py:1720 (`lifecycle_risk = _run_phase("Lifecycle risk", compute_lifecycle_risk, _dev_lifecycle, ...)`) passes NO asof argument. analyze.py:4764 declares `asof: Optional[object] = None`; analyze.py:4781 makes `_to_date` fall back to `date.today()` for None/unparseable input; analyze.py:4782 sets `today = _to_date(asof)` = wall-clock today. The Past-LDoS / Near-LDoS / Past-EoS boundaries (analyze.py:4806 `today > ldos`, 4808 `yrs <= 1.0`, 4810 `today > eos`) therefore re-evaluate against the calendar day the pipeline RUNS, not the day the data was collected. Consequently the canonical 'frozen' snapshot and every regenerated deliverable change their Critical/High EoL counts with NO change in input. PROVEN empirically: feeding the snapshot's own 303-device set to compute_lifecycle_risk yields Past-LDoS=137/Near-LDoS=76 at asof=2026-04-30 but Past-LDoS=152/Near-LDoS=61 at asof=2026-05-01 (15 devices flip Near->Past in ONE wall-clock day) and Past-LDoS=213/Near-LDoS=0 at asof=2027-04-30 — same input, different numbers. The driver is the Nexus 5600 fleet whose ldos=2026-04-30 (eoldb.py:60) crosses the strict `>` boundary. The canonical file is named '...20260613_063201...' (June-13 collection) yet its persisted lifecycle_risk.asof='2026-06-21' AND generated_at='2026-06-21T06:58:07' — i.e. it was regenerated on the audit day and the bands belong to 2026-06-21, NOT to the June-13 collection, with no field recording which date the bands describe. This is a known/self-documented hazard: test_pipeline_golden.py:62-64 excludes lifecycle_risk from the frozen golden BECAUSE 'bands/years shift relative to today', and determinism is pinned ONLY in tests via test_lifecycle.py:9 `ASOF = "2026-06-07"`; production never pins it.
- fix: Freeze asof to the COLLECTION instant, not wall-clock-at-regen, and persist it as provenance. (1) In COLLECT_PARSE_V3_23_0.py derive a single `collection_asof` from the collection evidence (e.g. the collection-dir timestamp / the earliest device 'show clock' / the run's started_at) and thread it explicitly: `compute_lifecycle_risk(_dev_lifecycle, asof=collection_asof)`. (2) On a `--no-collect --collection-dir <dir>` re-analyze, asof MUST come from the ORIGINAL collection (e.g. read it back from the prior snapshot or the dir stamp) so re-rendering an old collection reproduces the original bands byte-for-byte. (3) Stamp the chosen date as explicit provenance everywhere the bands are shown (runbook.py:297, excel.py:2048) — e.g. 'Lifecycle bands as of collection date YYYY-MM-DD' — so a reader knows which calendar day the Past/Near classification belongs to. Keep `date.today()` only as an explicit last-resort with a visible 'asof defaulted to today (no collection date available)' note.
- test: Add tests/test_lifecycle_provenance.py: (a) reproducibility — `compute_lifecycle_risk(devs, asof='2026-06-13')` called on two different machine-clock days returns identical by_band/n_past_ldos/n_near (monkeypatch date.today() to two values, assert outputs equal because asof is pinned). (b) drift-guard — assert by_band at asof='2026-04-30' differs from asof='2026-05-01' for the Nexus-5600 set (locks the boundary behaviour so a future refactor can't silently neutralise it). (c) pipeline-provenance — run COLLECT_PARSE with --no-collect on the June-13 dir and assert the produced snapshot's lifecycle_risk.asof equals the collection date (e.g. 2026-06-13), NOT date.today(); regression-fails today since it emits 2026-06-21.

## [High/high] R2-4-01
- file: COLLECT_PARSE_V3_23_0.py:1150-1160, 1938-1944, 2047-2053, 2084  (pipeline / snapshot assembly)
- claim: _run_phase (L1150-1160) catches every Exception, logs '[SKIP]' to the LOGGER ONLY, and returns the caller's _default. For the executive brief the call at L1938-1944 passes _default={}, so when compute_executive_brief raises (e.g. a KeyError/TypeError while reading any axis summary), snap_dict['executive_brief'] is set to {} at L2047. The SSOT n_vlans injection at L2051-2053 is then silently skipped because {}.get('scale') is None (isinstance(_ebscale, dict) is False), so the snapshot is written (L2084) with the canonical scale/posture/axes/top_gating/posture_statement block ENTIRELY ABSENT. No integrity check runs after assembly and no disclaimer is stamped into the snapshot or any artifact — the '[SKIP]' line lives only in the logfile a downstream reader of the .xlsx/.docx never sees. This is the assembly analogue of the device-level false-health class: a compute failure becomes 'nothing to report' instead of 'synthesis could not be produced'. I reproduced the exact mechanism: _run_phase('Executive brief', boom, _default={}) returns {}, _ebscale resolves to None (injection skipped), and every consumer key (scale/posture/axes) is gone.
- fix: After the brief phase, detect the failed-compute sentinel and make it loud + visible in the artifact, not just the logfile. Minimal: distinguish 'failed' from 'legitimately empty' by passing a sentinel default (e.g. _default={"_unavailable": True}) for the brief phase, then (a) log a WARNING at assembly that the executive brief could not be synthesised, and (b) write a machine-readable flag into the snapshot (snap_dict['assessment_integrity'] = {'executive_brief': 'compute_failed'}) so every downstream consumer can surface 'cross-axis synthesis unavailable — re-run' instead of rendering zeros/blanks. Do NOT let a {} brief pass as a normal empty result.
- test: Add tests/test_pipeline_failopen.py: monkeypatch cisco_toolkit.analyze.compute_executive_brief to raise, run the pipeline in-process (mirror test_pipeline_inprocess.py harness), load the snapshot, and assert that the failure is DISCLOSED — i.e. snap['assessment_integrity']['executive_brief']=='compute_failed' (and a WARNING was logged), rather than the brief silently being {} with no marker. Guard against regression to the silent-{} behaviour.

## [High/high] DET-syslog-1
- file: cisco_toolkit/analyze.py:4161  (engine detector (compute_syslog_intelligence))
- claim: The login-fail detector keys on `if "LOGIN" in fac and "FAIL" in mn:` (analyze.py:4161), which only matches the IOS facility/mnemonic %SEC_LOGIN-4-LOGIN_FAILED. On the AJ fleet (NX-OS core + Catalyst) the real authentication-failure signal is %AUTHPRIV-3-SYSTEM_MSG 'pam_aaa:Authentication failed for user ...' (156,598 lines, 37 devices) and %AAA-3-BADSERVERTYPEERROR (292,268 lines, 163 devices) — facility AUTHPRIV/AAA contains no 'LOGIN', mnemonic SYSTEM_MSG/BADSERVERTYPEERROR contains no 'FAIL'. Grep of the corpus: %SEC_LOGIN-4-LOGIN_FAILED occurs 0 times. The AJ snapshot's syslog summary.by_kind has NO 'login-fail' key, i.e. the detector never fires fleet-wide despite this being the single largest error-band class. This is the same IOS-literal-blinds-NX-OS regression class the project already fixed once for show logging facilities (ETHPORT/VSHD/L2FM). A senior engineer greps for failed logins / broken AAA first; the axis silently returns 'healthy' here — a false-health gap before cutover.
- fix: Broaden the auth-failure capture to the NX-OS/AAA families, mirroring the family-startswith style already used for environment/link. e.g. replace the single line with: `if (("LOGIN" in fac and "FAIL" in mn) or (fac in ("AUTHPRIV","DAEMON") and "AUTHENTICATION FAILED" in ev["raw"].upper()) or (fac.startswith("AAA") and any(t in mn for t in ("BADSERVER","AUTH")) ) or (fac.startswith("TACACS") and "ERROR" in mn)): login_fails += 1`. Consider splitting into two kinds: 'login-fail' (repeated user auth failures = AUTHPRIV pam_aaa / SEC_LOGIN) and a new 'aaa-server' kind (High) for %AAA-3-BADSERVERTYPEERROR / %TACACS-3-* (a broken AAA/TACACS config, not user error) so the doctrine/severity differ. Keep the >= _SYSLOG_LOGIN_FAIL_MIN threshold for the user-failure kind.
- test: Add to tests/test_syslog_intelligence.py: feed 6x '2026 Jun 9 ... nx1 %AUTHPRIV-3-SYSTEM_MSG: pam_aaa:Authentication failed for user x from 10.0.0.1 - sshd[1]' and assert 'login-fail' in _kinds(si); feed 3x '%AAA-3-BADSERVERTYPEERROR: Cannot process accounting server type tacacs+' and assert the new aaa-server kind fires (severity High). Lock the IOS form too (regression guard).

## [High/high] DET-syslog-2
- file: cisco_toolkit/analyze.py:4134-4135  (engine detector (compute_syslog_intelligence))
- claim: The 'environment' detector fires only when `("ENV" in fac or "THERMAL" in fac) or any(t in mn for t in ("FAN","TEMP","THERM","PSU","POWER"))` and sev<=3 (analyze.py:4134-4135). Real NX-OS/Catalyst power-supply failures in the AJ corpus use facilities with no 'ENV'/'THERMAL' and mnemonics that contain neither 'PSU' nor 'POWER': %PFMA-2-PS_FAIL (172), %NOHMS-2-NOHMS_DIAG_ERR_PS_FAIL (172), %NGWC_PLATFORM_FEP-1-FRU_PS_SIGNAL_FAULTY (213, severity 1 = critical), %PLATFORM_FEP-1-FRU_PS_SIGNAL_FAULTY (73), %NGWC_PLATFORM_FEP-1-FRU_PS_ACCESS (622). The mnemonic 'PS_FAIL'/'FRU_PS_*' contains the substring 'PS_' not 'PSU'/'POWER'. Executed: these lines produce NO detection. The detector's own doctrine says 'environmental failures void redundancy assumptions' and 'replace the failing FRU before the migration window' -- yet a failed PSU on the core/access is invisible. 47 distinct devices carry a PS_FAIL/FRU_PS_SIGNAL_FAULTY in their log buffer. Same NX-OS-blind class as DET-syslog-1.
- fix: Add the platform power/FRU families to the environment branch. e.g. extend the condition to also match `fac.startswith(("PFMA","PLATFORM_FEP","NGWC_PLATFORM_FEP","NOHMS")) and any(t in mn for t in ("PS_FAIL","FRU_PS","PFM_ALERT","PS_CAPACITY"))`, and broaden the mnemonic token set from ('FAN','TEMP','THERM','PSU','POWER') to also include 'PS_FAIL','FRU_PS','SUPPLY'. Guard against the *_OK / *GREEN_THRESHOLD / PS_FANOK recovery variants so they are not scored as alarms (e.g. exclude mnemonics ending in OK or containing GREEN/RECOVERY).
- test: tests/test_syslog_intelligence.py: assert '%PFMA-2-PS_FAIL: Power supply 1 failure' and '%NGWC_PLATFORM_FEP-1-FRU_PS_SIGNAL_FAULTY' each yield kind=='environment' (severity High); assert '%PLATFORM-5-PS_STATUS ... PS_FANOK' and '%THERMAL-1-THERMAL_GREEN_THRESHOLD' do NOT produce an environment detection (recovery/OK suppression).

## [High/high] R2-6-01
- file: cisco_toolkit/analyze.py:5015-5019 (axis); 4807 (status string); eoldb.py:9-11,17-78  (executive_brief lifecycle axis + posture_statement)
- claim: compute_executive_brief() publishes the 'Hardware lifecycle (EoL)' axis at severity Critical with headline '152 past end-of-support, 61 within 1yr (70%)' and detail only 'Reference dates from the offline KB.' I verified against the live AJ snapshot that ALL 152 Past-LDoS devices carry conf='derived' (per_device conf-distribution within Past-LDoS = {'derived': 152}) and that every non-active eoldb row is conf='derived' (KB conf distribution = {'derived':20,'active':9}). A derived LDoS = EoS+5yr (eoldb.py:9-11), i.e. a heuristic, not a Cisco-published last-day-of-support. The single most load-bearing number in the assessment (and the posture_statement's 'primary driver / 70% past-near') is presented as fact with no confidence qualifier on the headline.
- fix: Surface the confidence basis on the headline/detail wherever the count is the lead fact. In compute_executive_brief's lifecycle axis, when all (or n of) past-LDoS rows are conf='derived', append to detail e.g. 'N of M rest on a derived EoS+5yr rule — confirm exact LDoS on Cisco's EoL portal.' Compute the derived-count from lc per_device (e.g. summary could expose n_past_ldos_derived). Do not change the count itself — it is a legitimate worst-case driver — only its epistemic framing.
- test: In tests/test_executive_brief.py, build a fleet whose Past-LDoS members are all conf='derived', call compute_executive_brief, and assert the lifecycle axis detail contains a 'derived'/'EoS+5' caveat (and a derived-count). Add the inverse: a confirmed-LDoS fixture must NOT emit the caveat.

## [High/high] R2-7-01
- file: cisco_toolkit/analyze.py:126-204 (compute_move_groups); 162,172-173 build a singleton component per host); design_advisor.py:1759-1800 (_wave_plan, n_move_groups=len(groups)); design.py:620-634 (HLD §5.4 render)  (Engine compute_move_groups + design_advisor._wave_plan -> design_blueprint.target_state.wave_plan -> HLD §5.4 (design.py) -> deck/explorer/webapp)
- claim: compute_move_groups builds union-find over `parent = {h: h for h in all_interfaces}` (analyze.py:162) and collects one component per host (172-173). all_interfaces carries ALL 303 inventoried hosts, with the 50 not-collected hosts present but holding EMPTY interface dicts (verified: snapshot interfaces map = 303 hosts, exactly 50 with 0 interfaces, and those 50 == collection_completeness.devices not-collected set). Result: the 50 not-collected devices each become a singleton move-group. Snapshot move_groups = 53 groups whose switch memberships sum to 303 (== inventoried), decomposing as 1×251-switch coupled group + 2 collected singletons (AS01-IT03-SANDBOX-ARDOH, AS10-BC-CA01A14-BCDOH) + 50 not-collected singletons (0 endpoints each). _wave_plan (design_advisor.py:1765-1766) keeps every group whose `switches` is non-empty -- all 50 not-collected singletons survive (they each have 1 switch) -- and bin-packs them into 'independent-batch' waves (snapshot wave_plan: n_move_groups=53, 9 waves, 7 coupled-subwave + 2 independent-batch, 303 switches placed). The HLD §5.4 (design.py:624) then states '53 L2-coupling move-group(s) ... → 9 candidate wave(s)' and the LLD migration-approach dimension (design_advisor.py:1952) states '53 move-group(s) ... across <move_switches> switch(es)' -- presenting the 50 not-collected devices as proven-independent migration units. This contradicts the blueprint's OWN coverage caveat rendered two lines later (design.py:633-634 -> ts['coverage']['caveat'] = 'not-collected devices ... their role and redundancy are not assumed'). The wave-plan note (design_advisor.py:1794-1799) never discloses that 50 of the 53 groups are zero-evidence singletons. A reader sizing the cutover sees 53 groups / 9 waves and would batch 50 devices in parallel whose actual L2 coupling was never measured -- a make-before-break sequencing error if any of those 50 in fact share VLANs with collected switches.
- fix: In _wave_plan (design_advisor.py:1759), partition groups into evidenced vs not-collected: read the not-collected host set from snap['collection_completeness']['devices'] (or detect singleton groups whose lone switch has an empty interface map / band 'Insufficient Data') and EXCLUDE them from n_move_groups / the bin-packed independent-batch waves; surface them in a separate 'n_uncollected_singletons' field with a note that their coupling is UNKNOWN and they cannot be wave-sequenced until collected. Update HLD §5.4 (design.py:624) and the LLD migration-approach line (design_advisor.py:1952) to read 'N evidenced move-group(s) (+M not-collected device(s) whose L2 coupling is unknown -- collect before sequencing)'. The conservation invariant should be: evidenced_group_memberships == n_collected (253), with the 50 carried as an explicit unknown bucket.
- test: Extend tests/test_design_blueprint.py (the existing 'every switch placed exactly once' assertion at line 687): build a snap with 3 hosts where 1 has an empty interface map AND appears in collection_completeness.devices as not-collected; assert wave_plan['n_move_groups'] counts only the 2 evidenced hosts (or that the not-collected host is reported under a distinct 'n_uncollected'/unknown field and NOT inside any 'independent-batch' wave). A mutation that lets the not-collected singleton into n_move_groups / a batch wave must turn the test red.

## [High/high] R2-11-1
- file: cisco_toolkit/analyze.py:4982  (analyze.py compute_executive_brief() -> posture.avg_health (SSOT, read by deck/runbook/explorer/webapp/design))
- claim: avg = round(sum((x.get('score') or 0) for x in hs) / n) averages over EVERY health_scores row with no band filter. A device whose evidence was not collected accumulates zero deductions (compute_health_scores, analyze.py:1090 -> score=100-total), so it carries a near-perfect numeric score and is only flagged via band='Insufficient Data' (analyze.py:1097-1098 changes the BAND, never the SCORE). Those inflated scores are folded into the canonical posture.avg_health that every other surface reads. This is absent-evidence-as-favorable: a missing device raises the headline average instead of being excluded.
- fix: In compute_executive_brief, mirror compute_calibration_report: compute avg over only scored, evidence-bearing rows — `scored=[x for x in hs if isinstance(x.get('score'),(int,float)) and x.get('band')!='Insufficient Data']; avg = round(sum(x['score'] for x in scored)/len(scored)) if scored else 0`. Keep n_devices/scale at the full inventoried count (303) for honesty, but stop letting no-evidence boxes raise avg_health. Optionally surface n_insufficient alongside so the average's denominator is auditable.
- test: Add to tests/test_compute.py (or a new test_executive_brief): build health_scores with two real rows (score 40,60, normal bands) + one row {score:90, band:'Insufficient Data'}; assert compute_executive_brief(...)['posture']['avg_health'] == 50 (the mean of 40 and 60), NOT 63 (which would prove the Insufficient-Data 90 leaked into the average). Reverting the fix must turn it red.

## [High/high] EX-archreview-1
- file: cisco_toolkit/archreview.py:905 (compute_architecture_review, LC-1 critical branch); n_eos defined L893-894 from lifecycle_risk.summary.n_past_eos  (archreview)
- claim: In the critical (n_ldos) branch, LC-1 unconditionally interpolates n_eos into the observed text: f"{n_ldos} device(s) are past LAST-DAY-OF-SUPPORT (and {n_eos} past end-of-sale)". The engine's lifecycle bands are MUTUALLY EXCLUSIVE (_LIFECYCLE_BAND_RANK at analyze.py:4761; a device past LDoS is classified Past-LDoS, NOT Past-EoS — analyze.py:4806-4811), so n_past_eos counts ONLY devices past end-of-SALE but not yet past LDoS. On the canonical AJ fleet n_past_ldos=152 and n_past_eos=0, so LC-1's shipped observed text reads literally: '152 device(s) are past LAST-DAY-OF-SUPPORT (and 0 past end-of-sale)'. That is a false customer-facing fact: every one of the 152 past-LDoS devices is necessarily past end-of-sale too (LDoS is later than EoS). Sibling generators were corrected for exactly this conflation: engagement.py:146-150 emits the EoS sentence only under a separate gate (never '0 past end-of-sale'), and deck.py:283-293 carries an explicit comment that Past-LDoS is the no-TAC band ALONE and keeps Past-EoS as its own distinct segment. archreview LC-1 is the lone surface still printing the conflated, contradictory phrase.
- fix: In LC-1's n_ldos branch, drop the unconditional '(and {n_eos} past end-of-sale)' clause (it is always misleading because past-LDoS implies past-EoS). Either render only the LDoS count, or — to keep the EoS signal honestly — phrase it as a strict superset, e.g. '... past LAST-DAY-OF-SUPPORT' + (f' (plus {n_eos} more past end-of-sale but not yet LDoS)' if n_eos else ''). Mirror engagement.py's conditional pattern so '0 past end-of-sale' can never print.
- test: Extend tests/test_archreview.py: build a snap with lifecycle_risk.summary {n_past_ldos:152, n_past_eos:0, n_near:0}; assert compute_architecture_review(snap) check LC-1 verdict=='critical' AND 'and 0 past end-of-sale' NOT in observed AND '0 past end-of-sale' not in observed; add a second snap with n_past_eos>0 asserting the EoS count, when present, is described as a superset/separate count, never folded as '(and N past end-of-sale)' alongside the LDoS total.

## [High/high] HON-archreview-passbysilence-HIER2
- file: cisco_toolkit/archreview.py:230-249 (not-assessable guard L230; conforms branch L245)  (archreview.py compute_architecture_review · D1 Hierarchy & modularity · check HIER-2)
- claim: HIER-2's only not-assessable guard is `if not interfaces:` (L230). The check's actual evidence is the in-fleet adjacency map `adj`, built purely from per-port `cdp_neighbor` fields in `_fleet_model` (L154, L170). When `interfaces` is present but no port carries a `cdp_neighbor` (e.g. an NX-OS device whose `show cdp neighbors` failed while `show interface` succeeded), `adj` is empty, `chains` (L228) is empty, and execution falls through to the `else` at L244-249, emitting verdict 'conforms' with observed text 'Every observed access uplink lands on the L3 tier.' Absence of neighbour evidence is thereby scored as a clean pass. This is provably inconsistent with RES-1 (L253-276), which reads the SAME `adj`/neighbour data but correctly returns 'not-assessable' (`if not homed:` L255) when no uplink neighbours were observed. The module's own doctrine (file header L17-19: 'A check whose evidence was not captured returns NOT-ASSESSABLE') is violated here. A conforms verdict also contributes weight 1.0 to the domain/grade rollup (_V_WEIGHT L46), inflating the conformance grade on a blind spot.
- fix: Gate HIER-2 on the presence of the evidence it actually consumes, mirroring RES-1. Compute whether any in-fleet CDP/LLDP adjacency was observed for the access tier (e.g. `any(adj.get(h) for h in l2_hosts)` or a fleet-wide `any_neighbor = any(adj.values())`) and, when false, emit 'not-assessable' ('No in-fleet neighbour/CDP data was captured for the access tier — daisy-chaining cannot be judged.', recommend 'Re-collect with CDP/LLDP output included.') instead of falling to the conforms `else`. Only return 'conforms' when at least one access uplink adjacency was actually seen and none of them land on another access switch.
- test: Add to tests/ an archreview honesty test: build a snapshot with interfaces present but all ports missing `cdp_neighbor`; assert HIER-2 verdict == 'not-assessable' (currently 'conforms'). Add the symmetry assertion that HIER-2 and RES-1 are both 'not-assessable' on that same neighbour-blind snapshot, and that HIER-2 only becomes 'conforms' when at least one in-fleet access→L3 cdp_neighbor adjacency is present and no access-to-access link exists.

## [High/high] HON-archreview-passbysilence-L2-3
- file: cisco_toolkit/archreview.py:423-459 (not-assessable guard L432; conforms branch L455)  (archreview.py compute_architecture_review · D3 Layer-2 design · check L2-3)
- claim: L2-3's only not-assessable guard is `if not interfaces:` (L432). Its evidence is the per-port `vlan` (access) and `trunk_native_vlan` fields (L428, L430). When `interfaces` is present but no port carries those sub-fields (a NOS/parse path where the access-VLAN or native-VLAN column was not captured), `v1_access` and `v1_native` stay empty and execution falls to the `else` at L454-459 emitting 'conforms' with observed 'No access port and no trunk native VLAN uses VLAN 1.' / 'VLAN 1 is confined to its default control-plane role.' That is a customer-facing hardening pass asserted from absence of evidence. This is inconsistent with sibling per-port checks in the same domain: L2-2 BPDU Guard (L391-421) gates on a `bpdu_data` capture counter (`if n_access_ports==0 or bpdu_data==0` L403) and L2-5 trunk-pruning (L499-533) gates on a `pruned_data` counter (L513) — both correctly return 'not-assessable' when their field is absent. Aggravating factor making this a FALSE-NEGATIVE, not merely a silence-pass: per build.py:477 an access port's `vlan` is only set when `access_vlan` is present, and build.py:487 only sets `trunk_native_vlan` `if v.get('native_vlan')`. A port implicitly on VLAN 1 (no explicit `switchport access vlan` / no explicit `native vlan` line — the common default) therefore keeps `vlan=''`/`trunk_native_vlan=''` and is NOT counted, so even on fully-collected configs L2-3 can report 'conforms' while default-VLAN-1 user/trunk ports exist — exactly the CIS condition the check is meant to catch.
- fix: Add an evidence-capture counter as L2-2/L2-5 already do: count ports for which the relevant field was actually populated (e.g. `vlan_data = sum(1 for ...,d in ... if str(d.get('vlan') or '').strip())` for access and a `native_data` counter for trunk native-VLAN). When neither dimension has any captured field, emit 'not-assessable' ('Per-port access-VLAN / trunk native-VLAN assignment was not captured — VLAN-1 hygiene cannot be judged.', recommend collecting `show interfaces switchport` / running config). Additionally treat an explicit-default access port as on VLAN 1 (the parser should record the resolved access VLAN, defaulting to 1 when the port is access-mode and no explicit vlan is set, or the check should infer VLAN 1 for access ports with empty vlan) so the conforms branch is only reached when the access/native VLAN is positively known to be non-1.
- test: Honesty test: snapshot with interfaces present but ports missing `vlan` and `trunk_native_vlan` → assert L2-3 verdict == 'not-assessable' (currently 'conforms'). False-negative test: snapshot where an access port is switchport_mode=access with vlan='' (implicit default) → assert L2-3 does NOT report 'conforms' (it should flag VLAN-1 use or, at minimum, be not-assessable rather than asserting VLAN 1 is unused).

## [High/high] EX-crd-001
- file: cisco_toolkit/crd.py:71  (crd.py _evidence_facts)
- claim: _evidence_facts builds n_devices = len(devices) and n_vlans = len(vlan_inventory(snap)) by recomputing locally, never reading the published single source executive_brief.scale.n_devices / scale.n_vlans first. design.py:124-130 explicitly fixed this same pattern ('SSOT scale: prefer the canonical executive_brief.scale ... the HLD was the last surface still recomputing scale, a latent drift risk even though they coincide today (A5 SSOT fix)'), reading eb.get('scale') canonical-first with len() only as fallback. crd.py is now the surface still recomputing. The asymmetry is even within crd.py itself: line 77 reads scale.n_endpoints canonical-first ('the published single source ... with the access-port tally as fallback') but lines 71 do NOT do the same for devices/VLANs. Empirically verified on the canonical AJ snapshot: scale={n_devices:303, n_vlans:202, n_endpoints:5127} and the CRD recompute today coincides (303 / 202 / 5127), so this is a LATENT drift risk, not a current wrong fact (hence High, not Critical). The values feed §1 title meta, §1 engagement-context evidence scope, §2 current-environment table, REQ-T-LAN-001, and §6 growth baseline — every customer-facing scale statement in the CRD.
- fix: In _evidence_facts read the canonical scale first, mirroring design.py: `eb = (snap.get('executive_brief') or {}).get('scale') or {}`; then `'n_devices': eb.get('n_devices') or len(devices)` and `'n_vlans': eb.get('n_vlans') or len(vlan_inventory(snap))`. Keep len()/vlan_inventory only as the fallback. Leave any line that renders the actual VLAN LIST on len(vlan_inventory) per the design.py convention.
- test: Add test_crd_scale_reads_canonical_executive_brief: build a snapshot where executive_brief.scale DISAGREES with the local recompute (e.g. devices={2 hosts} but scale.n_devices=303, vlan_inventory=>3 but scale.n_vlans=202), render the CRD, and assert the §2 Current Environment table shows 303 and 202 (canonical), not 2 and 3 — a regression to len() recompute turns it red. Mirror the mutation-discriminating style already used in test_crd_future_plans_anchors_to_observed_scale (which proves scale.n_endpoints=4242 is read).

## [High/high] HON-crd-overclaim-1
- file: cisco_toolkit/crd.py:151, 193-194  (CRD .docx (title-page meta + §1 Engagement Context "Evidence scope (this assessment)" row))
- claim: The title page renders "{ev['n_devices']} devices in evidence scope" and §1's "Evidence scope (this assessment)" row renders "{ev['n_devices']} devices, ...". ev['n_devices'] = len(snap['devices']) (crd.py:71) = the full INVENTORY (devices.json) = 303 on the canonical AJ snapshot. But canonical n_collected = 253; 50 of those 303 produced ZERO evidence (collection_completeness.summary.not_collected = 50). Calling all 303 the "evidence scope" / "devices in evidence scope" over-claims coverage: the actual evidence scope is the 253 collected. §2's own "Collection completeness" row (crd.py:216-218) renders "253 complete / 0 partial / 50 not collected", and §2's lead-in says "what the assessment actually observed" — so the document internally contradicts its own title/§1 wording. This is the coverage-honesty class the engine elsewhere guards (inventoried 303 vs collected 253 are deliberately distinct everywhere else).
- fix: Distinguish inventoried from collected in the scope wording. Title (crd.py:151): f"{ev['n_devices']} inventoried / {ev['coll'].get('complete','?')} collected"; §1 row (crd.py:193-194): f"{ev['coll'].get('complete', ev['n_devices'])} of {ev['n_devices']} devices collected, {ev['n_vlans']} VLANs, {ev['endpoints']} evidenced endpoints" (and rename the label to "Evidence scope (collected) / Inventory"). Read the canonical pair (executive_brief.scale.n_devices = inventoried; collection_completeness.summary.complete = collected) rather than implying all inventory was observed.
- test: In tests/test_crd.py, build a snapshot whose devices has N hosts but collection_completeness.summary.not_collected > 0 (e.g. 3 inventoried, 1 not collected); render the CRD; assert the extracted DOCX text does NOT contain a bare "3 devices in evidence scope" and DOES surface the collected count (e.g. contains "2" alongside "3" / "collected"), so the inventory count is never presented as the evidence scope.

## [High/high] EX-deck-01
- file: cisco_toolkit/deck.py:158-160  (deck.py (executive PPTX) — Title slide (1))
- claim: The ONLY place the deck renders fleet scale (devices/endpoints) is the title-slide subtitle at L158-160: `sub = eb.get('posture_statement') or (f"{n_devices} devices · {n_endpoints} endpoints assessed")`. Because the canonical brief ALWAYS publishes a non-empty `posture_statement` (analyze.py:5123-5126), the right-hand scale fallback NEVER fires in practice, so the endpoint total never appears. `n_vlans` (202) is read by NO line in deck.py at all. Runtime proof: generating the deck from the canonical AJ snapshot and scanning every text run yields 0 hits for '5127'/'5,127' and 0 hits for the fleet VLAN count '202'; the title slide shows only the posture sentence. For a steering-committee readout, scope-of-assessment (N endpoints, N VLANs) is table-stakes on the opening slide and is silently absent. This contradicts the lane requirement that headline numbers equal canonical 303/253/5127/202.
- fix: Make the title subtitle (or a dedicated stat row on the Fleet-posture slide) ALWAYS render the canonical scale in addition to the posture statement — e.g. concatenate: `sub2 = f"{scale.get('n_devices')} devices · {scale.get('n_endpoints')} endpoints · {scale.get('n_vlans')} VLANs"` as a second line under `posture_statement`. Read canonical-first from `eb['scale']` (n_devices/n_endpoints/n_vlans); never recompute.
- test: In tests/test_deck.py add n_vlans to the fixture scale and assert the rendered text contains str(n_endpoints) AND str(n_vlans) even when posture_statement is present (the current fixture has n_endpoints:840 + a posture_statement and asserts neither renders — lock the opposite).

## [High/high] HON-deck-headline-1
- file: cisco_toolkit/deck.py:190  (deck)
- claim: The 'Fleet posture' slide (the deck's Executive summary, header at deck.py:167) renders 'HEADLINE BY AXIS' from `for a in axes[:4]:` (deck.py:190). The canonical `executive_brief.axes` list is severity-sorted at the source (analyze.py:5107, `axes.sort(key=lambda a: _APP_SEV_RANK...)`), so the 4 shown are the worst 4 by severity. On the canonical AJ snapshot (Migration_Assessment_AUTOFILLED_20260613_063201.snapshot.json) there are 13 axes; the 4 shown are all Critical, and the cap drops FIVE 'High' axes — Application domains, Segmentation (flat L3, 3 on-air-critical domains not isolated), Multicast/timing (1 MAC clash, 13/13 PTP dormant), Operational logs (3788 critical events on 253 devices), and Software risk (109 exposed advisory surfaces) — plus Cutover/QoS/Platform/Remediation. Unlike the punch-list slide (deck.py:217-219, '+ N more...') and the lifecycle slide, the axis loop ends at deck.py:195 with NO '+N more axes' affordance, so a steering-committee reader sees only the 4 Criticals and gets no signal that 5 further High-severity concerns exist. This is a coverage-honesty / false-completeness gap in the stakeholder exec summary: it can mislead a reader about the breadth of the fleet's posture. (Not Critical because the source pre-sorts by severity, so no lower-severity axis is ever shown ahead of a higher one; the issue is purely the missing 'more exist' breadcrumb when >4 Critical/High axes are present.)
- fix: After the `for a in axes[:4]:` loop in deck.py (after L195), add a '+N more' breadcrumb consistent with the punch-list/lifecycle slides, e.g.: `more = len(axes) - 4`; if `more > 0`, render a muted italic line `f"+ {more} more axis headline(s) (incl. {n_more_high} more at High or above) — full set in the workbook Executive Brief."` where `n_more_high = sum(1 for a in axes[4:] if a.get('severity') in ('Critical','High'))`. This preserves the 4-row visual budget while making the omission honest. Optionally bump the cap to show all Critical/High axes (they are already severity-sorted) and only truncate Medium/below.
- test: Add a test in the deck test module (e.g. tests/test_deck.py) that builds a snapshot whose executive_brief.axes contains 4 Critical + 3 High axes (severity-sorted), calls write_executive_deck_pptx to a temp .pptx, re-opens it with python-pptx, and asserts that some text run on the Fleet-posture slide contains 'more' AND the count '3' (the dropped-High count) — i.e. the breadcrumb is present. Add the inverse: with exactly 4 axes total, assert NO '+N more' breadcrumb is rendered (no false 'more' when nothing is dropped).

## [High/high] R2-4-02
- file: cisco_toolkit/deck.py:152-176  (deliverable: Executive presentation deck (PPTX))
- claim: When executive_brief={} (R2-4-01), deck.py reads eb.get('scale')||{} and eb.get('posture')||{} with NO independent fallback. The title slide subtitle (L158-160) falls through posture_statement='' to `f"{scale.get('n_devices','—')} devices · {scale.get('n_endpoints','—')} endpoints assessed"` → renders '— devices · — endpoints assessed'. Worse, the Fleet-posture slide (L172-175) renders avg=posture.get('avg_health','—') → '—' but n_critical=posture.get('n_critical', 0) → a hard '0 Critical-band switches' stat callout presented as fact, and the 'HEADLINE BY AXIS' section (L187-195, axes=eb.get('axes')||[]) is silently empty. A stakeholder reading the deck sees a confident '0 Critical' with zero indication the cross-axis synthesis crashed — the classic false-health read ('not observed' silently becomes 'healthy'). I reproduced: subtitle='- devices . - endpoints assessed', avg='-', n_critical=0, axes rows=0. Unlike the explorer (R2-4 note) the deck has no '??'/'or' recompute and no guard.
- fix: Make the deck honest when the brief is absent/failed. (1) Gate the title subtitle and posture stats on a populated brief; when missing, either recompute n_critical from snap['health_scores'] band counts (the deck already computes band_counts at L168-171 — reuse it: n_critical=band_counts.get('Critical',0)) AND show 'cross-axis synthesis unavailable' instead of em-dash scale, or (2) consume the R2-4-01 integrity flag and stamp a visible 'Executive synthesis could not be generated — see workbook' banner on the posture slide. Never print '0 Critical' sourced from an empty brief.
- test: Extend tests/test_deck.py with a fixture whose snapshot has executive_brief={} (or omitted) but health_scores containing 2 Critical-band switches; build the deck and assert the rendered posture text does NOT contain a false '0' Critical (assert it reflects the 2 real Critical switches OR carries an explicit 'unavailable' marker). Today test_deck.py only ever supplies a fully-populated brief (test_deck.py:15-16), so this path is unverified.

## [High/high] SSOT-posture-deliv-001
- file: cisco_toolkit/design.py:196 and 263  (design (HLD/LLD docx))
- claim: design.py renders `("FHRP gateway groups", len(fhrp))` at L196 (§1 As-built) and L263 (§2.4 Resilience). `fhrp = snap.get("fhrp")` (design.py:103) is the engine's FHRP-CONSISTENCY array — analyze.py:2375-2403 builds it as "VLANs with >=2 gateways, flag FAKE redundancy"; excel.py:2424 names the sheet "FHRP Consistency" ("fake redundancy that fails silently at failover") and excel.py:3733 titles each punch-list item "Fake FHRP redundancy on VLAN {vid}". On the canonical AJ snapshot this array = 52 groups, and ALL 52 carry a "N gateways but no FHRP — no first-hop redundancy" issue (l3_forwarding fhrp distribution = {'none':231}; 0 groups have any member actually running FHRP). So the design doc tells the customer "FHRP gateway groups: 52" when canonical posture is 0 FHRP groups running / FHRP redundancy = 0 of 209. This is the exact false-redundancy class MEMORY says was fixed in crd.py:51 and runbook.py:336 but it survives in design.py — and it is the lone surface that inverts the absence of FHRP into an apparent presence (CRD says 0 protected, runbook says "231 of 231 have no FHRP peer", workbook says "Fake FHRP redundancy").
- fix: Relabel both rows so the count reads as an exposure, not a working-redundancy count. At design.py:196 and :263 replace `("FHRP gateway groups", len(fhrp))` with the count of groups that actually run FHRP, e.g. compute `n_real_fhrp = sum(1 for g in fhrp if not g.get("issues"))` and emit `("FHRP groups configured / fake-redundancy VLANs", f"{n_real_fhrp} / {len(fhrp)}")`; or rename to `("Multi-gateway VLANs with NO/fake FHRP", len(fhrp))` to mirror the workbook's 'FHRP Consistency' framing. §2.4 already has "FHRP groups with a consistency issue: {n_fhrp_issue}" (L264) and "Single-gateway VLANs (no FHRP peer): {n_single_gw}" (L265), so make L263 the configured/clean count to avoid the internal contradiction (52 "groups" of which 52 have issues).
- test: Add to tests/test_design.py: build a snap whose `fhrp` array is all-issue (e.g. two groups each with members fhrp=False and issues=['2 gateways but no FHRP — no first-hop redundancy']), render the design docx text, and assert the As-built/Resilience tables do NOT present a bare count implying working FHRP — i.e. assert the rendered string contains a label like 'fake'/'no FHRP'/'consistency issue' adjacent to that count, and that a configured-FHRP count of 0 is shown. Mirror test_crd.py's FHRP=0 assertion.

## [High/high] DET-vpc-02
- file: cisco_toolkit/design_advisor.py:327-333 (mc!='success' branch) + 341 (vpc_unhealthy sum) + 945-947 (summary string)  (design_blueprint decision summary + evidence.count (=842), propagated to every consumer (HLD/deck/explorer/webapp/punch-list))
- claim: The detector reports '174 member leg(s) carry a type-1/type-2 consistency mismatch (traffic on the affected VLANs is blocked until reconciled)' but this is false for the AJ fleet. A cross-tab of (status, consistency) over all 1350 legs proves EVERY one of the 174 'inconsistent' legs is also a down* leg (status=down*: 171 consistency='not' + 3 consistency='failed'), and ZERO up legs carry any consistency mismatch. The 'not' value is the parser truncating NX-OS 'Not Applicable' to its first token (parse.py:555 captures consistency as a single \S+). Per Cisco NX-OS, the consistency check is NOT performed on a down vPC member leg - it prints 'Consistency Check Not Performed' / 'Not Applicable' BY DESIGN (verified against the raw show_vpc.txt and Cisco troubleshooting docs). So these legs are not 'blocked by a type-2 mismatch'; they are down* and consistency is N/A *because* they are down. Two concrete defects: (a) customer-facing wrong fact - a benign expected state is reported as a traffic-blocking consistency mismatch; (b) double-count - each such leg is added to BOTH vpc_legs_down and vpc_legs_incons, so vpc_unhealthy/evidence.count=842 over-states the distinct unhealthy-leg population (honest distinct count = 668; +0 genuine up-leg mismatches on this fleet).
- fix: In design_advisor.py _signals vpc loop: only count a leg as a genuine consistency mismatch when it is NOT already down* AND its consistency is a real failure. Replace the unconditional 'if mc and mc != "success": vpc_legs_incons += 1' (line 331-333) with a guard that (a) excludes legs already counted as down*, and (b) treats the parsed 'not' token as the benign 'not applicable' case rather than a mismatch. Concretely: keep vpc_legs_down for st!='up'; for the consistency arm use `if st == 'up' and mc and mc not in ('success','n/a','not','not-applicable'): vpc_legs_incons += 1` so only UP legs with a real failed/inconsistent value count. Separately fix parse.py:555 to capture the full multi-word consistency value (or at least normalize 'Not Applicable' -> 'n/a') so the snapshot stops storing the truncated 'not'. Update the _d_vpc_health summary string (945-947) so the consistency-mismatch sentence only renders when there is a genuine up-leg mismatch, and keep count = distinct unhealthy legs (down* + genuine up-leg mismatch + dom_bad) to remove the double-count.
- test: Add to tests/test_design_blueprint.py: build a snap with vpc where one leg is {'status':'down*','consistency':'not'} (the AJ 'Not Applicable' case) and another is {'status':'up','consistency':'failed'} (a real mismatch). Assert the decision summary says exactly ONE consistency mismatch (the up leg), that evidence.count == down*_legs + 1 (no double-count of the down* 'not' leg), and that the summary does NOT claim the down* 'not' leg is a blocked consistency mismatch. Add a parser test in tests/test_vpc.py with a real down* member row 'Po32 down* Not Applicable Consistency Check Not Performed -' asserting parse_vpc captures consistency as 'not applicable'/'n/a' (not the truncated 'not').

## [High/high] DET-portchannel-001
- file: cisco_toolkit/design_advisor.py:292-300, 926-929  (design_advisor._d_bundle_health (→ HLD/LLD §4.2 decision cards, deck.py, explorer ✎Design, webapp /design))
- claim: The restore-degraded-portchannel detector fires correctly and is severity-gated, but its decision text reports the wrong quantity as "members." sig['bundle_degraded'] = len(pi_bad) (design_advisor.py:297) counts protocol_intelligence records, and compute_protocol_intelligence (analyze.py:1525-1539) emits ONE record per (switch, protocol, observed-state-token) — collapsing every same-state member on a switch into a single record. So on the canonical AJ snapshot the live decision reads '84 EtherChannel/port-channel member anomalie(s) across 75 switch(es) -- members are down/suspended/standalone ... Restore full membership' (count=84), but the true number of degraded member instances is 751 (730 D-down + 19 s-suspended + 2 I-standalone). Example: ACS01-BC-CA05R30 has protocol_health summary '6 bundle(s), 16 member(s); 8 not bundled' with detail 'Te1/1(D);...;Te1/8(D)' — 8 down ports — yet contributes exactly 1 to the 84. The figure 84 is neither members (751) nor switches (75); it is an intermediate (switch×distinct-state) record count mislabeled as members. A reader sizing physical remediation (cabling/SFP/errdisable/LACP per port) scopes ~84 ports when ~751 member ports are actually degraded. This is a customer-facing High decision; the wording propagates verbatim to every surface that renders evidence.summary (HLD §4.2, deck, explorer, webapp). The switch count (bundle_degraded_nsw=75) is correct; only the member figure is wrong.
- fix: Stop labeling the record count as members. protocol_health IS in the snapshot (670 rows; verified) and its EtherChannel rows carry a parseable 'N not bundled' summary plus a per-member detail string. In _signals(), in addition to sig['bundle_degraded']=len(pi_bad) (keep as the per-(switch,state) advisory-row count), compute sig['bundle_degraded_members'] by summing the degraded members from snap['protocol_health'] EtherChannel rows for the SAME switches in pi_bad (parse detail with re.findall(r'\(([sDI])\)', detail) — restrict to the same hard states the severity gate uses, so the two figures reconcile). Then reword _d_bundle_health (design_advisor.py:926) to e.g. '{members} degraded EtherChannel/port-channel member port(s) (down/suspended/standalone) across {bundle_degraded_nsw} switch(es) in {bundle_degraded} affected bundle-group(s)/state(s)' so the headline number is the true member count (751) and the record count is described as bundle/state groups, not members. Keep severity gating unchanged (reads protocol_intelligence severity SSOT).
- test: Extend tests/test_design_blueprint.py::test_v2_bundle_health_detector_is_severity_gated (or add test_v2_bundle_health_counts_members_not_records): build a _snap_v2 fixture where ONE switch has a single protocol_intelligence record state='D'/severity='High' AND a matching protocol_health EtherChannel row whose detail lists 3 down members ('Te1/1(D); Te1/2(D); Te1/3(D)'). Assert the decision's member figure == 3 (not 1) and that the summary text says 'member port' reflecting 3. This fails on the current code (which would render 1 'member anomaly') and passes after the fix. Guards the multi-member-per-record case the current 1-member-per-record fixtures never exercise.

## [High/high] R2-3-01
- file: cisco_toolkit/docmeta.py:153 (consumes); COLLECT_PARSE_V3_23_0.py:1038 (source); :1355 (no-collect path)  (All 7 DOCX deliverables (design, crd, mop, runbook, engagement, archreview, ops) via add_document_control)
- claim: docmeta.py:153 renders the field `generated_at` under the label "Snapshot captured". But `generated_at` is set to `datetime.now().isoformat()` at the COLLECT_PARSE run (COLLECT_PARSE_V3_23_0.py:1038, build_run_manifest), and that line fires unconditionally even on a `--no-collect` offline re-analysis (the no-collect branch at :1355 only reuses already-collected files; it does not preserve the original collection time, and build_run_manifest still restamps now()). Proven against the on-disk AJ data: snapshot `generated_at`='2026-06-21T06:58:07.924237' yet the evidence was collected on 2026-06-13 (collection dir `migration_collection_20260613_063201`, member files mtime Jun 13). All 7 regenerated covers therefore print "Snapshot captured 2026-06-21" for a snapshot whose evidence is 6 days old. This is a customer-facing wrong fact: a reader takes the cover to mean the network state was sampled on the 21st, when nothing was collected that day — the data is from the 13th.
- fix: Separate collection-time from generation-time. In build_run_manifest (COLLECT_PARSE_V3_23_0.py:1035) add a distinct field, e.g. `collected_at`, that on a live run equals now() but on `--no-collect` is derived from the collection dir (parse the `YYYYMMDD_HHMMSS` suffix of --collection-dir, or earliest member-file mtime) and is NOT overwritten on re-analysis; keep `generated_at` as the (honest) generation timestamp. Then change docmeta.py:153 to render `collected_at` under "Snapshot captured" (falling back to generated_at when absent), and add a separate "Document generated" row for generated_at so the two moments are explicit and never conflated.
- test: Extend tests/test_docmeta.py: build a snapshot dict with collected_at='2026-06-13' and generated_at='2026-06-21 06:58', call add_document_control, assert the rendered text contains 'Snapshot captured' adjacent to '2026-06-13' (NOT 2026-06-21) and a distinct 'generated' row with 2026-06-21. Add a pipeline test (tests/test_pipeline_*) asserting that a `--no-collect` re-run over migration_collection_20260613_063201 yields collected_at starting '2026-06-13', not today's date.

## [High/high] EX-workbook-001
- file: cisco_toolkit/excel.py:2921-2936  (workbook · Executive Summary sheet)
- claim: The workbook's landing/scorecard sheet (write_executive_summary_sheet) consumes only brief['axes'], brief['posture_statement'] and brief['top_gating']; it never reads brief['scale'] or brief['posture']. The 'Fleet posture' block recomputes from raw arrays and shows ONLY 'Switches assessed' = len(health_scores)=303, 'Average health score', and band counts. The canonical n_endpoints (5127), n_vlans (202) and the collected count (253) are absent from the scorecard, even though the explorer + webapp are test-locked to read exactly these from executive_brief.scale. A reader of the primary, always-produced deliverable cannot see the endpoint or VLAN census totals on the summary tab.
- fix: In write_executive_summary_sheet, after the 'Fleet posture' block, add a 'Scope / scale' sub-section that reads the canonical brief: sc = (brief or {}).get('scale') or {}; emit _kv('Devices inventoried', sc.get('n_devices')), _kv('Endpoints (evidenced)', sc.get('n_endpoints')), _kv('VLANs in use', sc.get('n_vlans')) (guarding None). Because n_vlans is injected into snap['executive_brief'].scale only AFTER the workbook write (COLLECT_PARSE_V3_23_0.py:2053), either (a) move that injection above the Phase 30e workbook write, or (b) have the sheet derive it via analyze.vlan_inventory(snap) if absent. n_endpoints/n_devices are already present at write time.
- test: Extend tests/test_pipeline_inprocess.py (or a new test_excel_scorecard.py): build the workbook in-process for the AJ fixture, load the 'Executive Summary' sheet, and assert the rendered cells contain the canonical strings for n_endpoints and n_vlans from snap['executive_brief']['scale'] (i.e. '5127' and '202' for the fixture), mirroring the existing explorer-embed lock at lines 115-119 so the scorecard cannot silently drop canonical scale.

## [High/high] R2-2-01
- file: cisco_toolkit/html.py:99-101  (--compare diff workbook (CLI) + /api/compare (webapp))
- claim: compute_snapshot_delta computes switches={'old':len(od),'new':len(nd)} (od/nd = raw snapshot['devices'] dicts) and interfaces=sum(len(v) for v in oi.values()) directly from the raw devices/interfaces arrays, never reading the published canonical executive_brief.scale.n_devices. The sibling _trend_point (same module, lines 307) was deliberately made canonical-first and is regression-locked (webapp test_trend_point_honors_canonical_scale_posture_over_recompute), but the delta path was not. Proven empirically: on a snapshot whose canonical scale.n_devices=42 while len(devices)=3, compute_snapshot_delta returns switches.old/new = 3/3 (raw) whereas _trend_point on the SAME snapshot returns n_switches=42 (canonical). The brief derives n_devices from len(health_scores)=len(all_interfaces) (analyze.py:4978, compute_health_scores hosts=sorted(all_interfaces) analyze.py:1031) while the delta derives from the DevicePhysical-sourced devices dict (snapshot_state html.py:483) — distinct populations that CAN diverge. Note: on the canonical AJ snapshot len(devices)==len(health_scores)==303, so this is currently LATENT (no visible mis-number) but structurally real and the contract is considered real enough that the trend reader was locked against exactly this.
- fix: In compute_snapshot_delta, prefer the canonical published count when present, falling back to the raw len only for pre-brief snapshots — mirroring _trend_point's pattern. E.g. `def _scale_n(s): return ((s.get('executive_brief') or {}).get('scale') or {}).get('n_devices')` and set switches old/new = `_scale_n(old) if _scale_n(old) is not None else len(od)` (same for new). Keep added/removed as the raw set-difference (those are identity lists, not the headline count) but consider labeling the count vs the membership consistently. Then update write_diff_workbook Summary 'Switches' row (html.py:149) to use delta['switches']['old']/['new'] instead of re-deriving len(od)/len(nd) at line 149 so the workbook can't drift from the delta.
- test: Add to tests/test_snapshot_delta.py: build old=new snapshot with executive_brief.scale.n_devices=42 but devices dict of size 3; assert compute_snapshot_delta(s,s)['switches']['old']==42 and ['new']==42 (canonical), and a second snapshot WITHOUT executive_brief still yields ==3 (fallback). This makes a regression to the raw recount turn red, matching the webapp-side lock.

## [High/high] HON-mop-precision-1
- file: cisco_toolkit/mop.py:95-96, 109, 338-339  (MOP §x.1 Scope & risk table (per-wave))
- claim: The readiness record's n_fail/n_warn are per-GROUP counts of failed/warning checklist checks (binary per-group determinations from compute_migration_readiness, analyze.py:1347 statuses.count('fail')/('warn')). mop.py:95-96 apportions them by the sub-wave's switch-share and round()s the result, while the readiness VERDICT (mop.py:98-99, 109) is taken as the worst un-apportioned value. For a coupled-subwave that is a small slice of a large group, the apportioned fail/warn collapses to 0 while the verdict stays NOT READY/CAUTION. The §x.1 scope table then prints 'Readiness verdict: NOT READY' (mop.py:338) directly above 'Blocking / warning checks: 0 / 0' (mop.py:339) — an internal contradiction that hides that the wave is blocked and gives no signal as to which sub-wave holds the failing switch. Empirically proven: a 40-of-251-switch sub-wave of a NOT-READY group with n_fail=1 yields readiness='NOT READY', n_fail=0, n_warn=0. This is reachable on the canonical AJ fleet (the 1x251-switch coupled move-group, _WAVE_CAP=40 in design_advisor.py:37 -> 7 sub-waves each ~40/251 share). The existing test test_mop_join_apportions_fail_warn_to_subwave (tests/test_mop.py:345) only uses clean-dividing counts (4,8 over 4 switches) and never exercises the round-to-zero collapse.
- fix: Do not apportion n_fail/n_warn by switch-share (a blocking CHECK is not a divisible quantity — a single err-disabled/sole-gateway check failing applies to the wave that contains the offending switch, not a fraction of every sub-wave). Either (a) keep the parent group's full n_fail/n_warn on every sub-wave it slices and relabel the scope row 'Blocking / warning checks (parent move-group)', or (b) recompute per-wave by intersecting each failing readiness check's host set with the wave's switches so the count is exact for THIS wave's members. As a minimum guard, never let the rendered count round below 1 while the verdict is worse than READY — i.e. assert invariant: if verdict in {'NOT READY','CAUTION'} then displayed n_fail+n_warn >= 1.
- test: Add tests/test_mop.py::test_mop_subwave_notready_never_shows_zero_blockers: build _join_group_records(['G1'], first_40, {'G1':{'switches':[251 hosts],'endpoints':5000,'n_fail':1,'n_warn':1,'readiness':'NOT READY'}}, {},{},{}) and assert NOT (r['readiness']=='NOT READY' and r['n_fail']==0 and r['n_warn']==0). Also render write_mop_docx on a wave_plan slicing that group and assert the §x.1 table never co-locates 'NOT READY' with 'Blocking / warning checks: 0 / 0' in the same wave section.

## [High/high] HON-mop-precision-2
- file: cisco_toolkit/mop.py:458, 465, 540-543  (MOP §x.5 Cutover procedure and §x.7 Rollback)
- claim: The cutover strategy is derived as `strategy = (seq.get('sequence') or scen.get('recommended_scenario') or '').lower()` (mop.py:458) and the make-before-break branch is selected by `if 'make-before-break' in strategy or 'parallel' in strategy` (mop.py:465 for the procedure, mop.py:540 for the rollback). For a MIXED move-group, compute_wave_sequencing emits the sequence string '<N> hard cutover (single-homed, schedule a window) + <M> make-before-break (dual-homed)' (analyze.py:925-926). That string CONTAINS the literal 'make-before-break', so the whole wave — including its single-homed switches — takes the make-before-break path. The MBB rollback then asserts (mop.py:541-543) 'Because the legacy path was never torn down, this is non-disruptive' with success criterion 'all endpoints re-home onto the legacy leg with no loss', and the MBB procedure (mop.py:474-476) says 'keep the legacy leg as the live fallback' — both FALSE for a single-homed switch, which has no second leg to keep live and whose cutover genuinely needs an outage window (the engine itself labeled it 'schedule a window'). The reader is told a portion of the wave rolls back with zero loss when those endpoints will in fact drop. Empirically proven: feeding the real mixed string through the same condition selects the make-before-break branch.
- fix: Stop classifying on a free-text substring. Drive the branch off the structured split the engine already provides: read wave_sequencing's hard_cutover[]/make_before_break[] (or scen.recommended_scenario which is a clean enum {phased, parallel-run, big-bang}). Treat a wave as make-before-break ONLY when it has zero hard_cutover members; when a wave is mixed, either emit BOTH a make-before-break sub-procedure for the dual-homed switches and a hard-cutover sub-procedure (with outage + disruptive rollback) for the single-homed ones, or fall to the conservative hard-cutover branch for the whole wave. Never assert a non-disruptive rollback for a wave containing a single-homed switch.
- test: Add tests/test_mop.py::test_mop_mixed_homing_wave_not_marked_nondisruptive: set wave_sequencing[group].sequence to '3 hard cutover (single-homed, schedule a window) + 37 make-before-break (dual-homed)' (and hard_cutover=['x','y','z']), render write_mop_docx, and assert the §x.7 Rollback text for that wave does NOT contain 'non-disruptive' / 'with no loss' (or asserts it explicitly carries a hard-cutover rollback for the single-homed members). A regression that keeps the substring classifier renders 'non-disruptive' and fails.

## [High/high] DET-aaa-01
- file: cisco_toolkit/parse.py:1066, 1132-1134  (parse_security() / security[host].findings[no-aaa] -> mgmt-secure-protocols-and-rbac decision -> CRD/archreview/explorer/workbook)
- claim: The no-aaa CIS check tests ONLY the IOS-specific token `aaa new-model` (parse.py:1066 `aaa = has(r"^aaa new-model\b")`). NX-OS has no `aaa new-model` command — AAA is always-on and configured via `aaa authentication login default group <grp>`. So on every NX-OS device the check FAILS and emits the finding `status:fail, detail:"no 'aaa new-model' -- authentication is local-only / line-based."` even though TACACS+ centralized AAA is fully configured. Proven on the canonical AJ fleet: exactly 61 devices have NO `aaa new-model` but DO have `aaa authentication login default group ACS` + `tacacs-server host ...`, and the snapshot records all 61 as no-aaa=FAIL with that 'local-only' wording (snapshot security['10GSW01-BC-CA11F17'] no-aaa finding verbatim). The remediation it prints — "Enable 'aaa new-model'" — is also an invalid command on NX-OS. This false 'no centralized AAA' claim then propagates: it is one of the fail-ids feeding the Critical `mgmt-secure-protocols-and-rbac` design decision (evidence.summary lists 'no-aaa' for 253 devices).
- fix: Make the AAA detection NOS-aware in parse_security: treat AAA as present if EITHER `^aaa new-model` (IOS/IOS-XE) OR an NX-OS/IOS-XR central-AAA construct is configured, e.g. `^aaa authentication login default group\b` (and/or `^aaa group server (tacacs+|radius)\b`). Reword the fail detail/remediation to be NOS-neutral ('no centralized AAA — authentication falls back to local/line') and stop prescribing `aaa new-model` on platforms that don't have it. This collapses the 61 false fails.
- test: Add tests/test_parse_security_aaa.py: (a) feed an NX-OS config containing `aaa authentication login default group ACS` (no `aaa new-model`) and assert the no-aaa finding status=='pass'; (b) feed an IOS config with `aaa new-model` and assert pass; (c) feed a config with neither and assert fail. Lock the count on the AJ collection: assert the number of no-aaa fails over migration_collection_20260613_063201 is 0 for NX-OS devices that carry `aaa authentication login default group`.

## [High/high] DET-intf-errors-002
- file: cisco_toolkit/parse.py:parse.py:1936-1979 (parse_show_interface_counters)  (engine: parse -> physical_health[] -> _d_phys_remediation)
- claim: parse_show_interface_counters extracts ONLY input_errors, crc, output_drops, last_input, last_output (parse.py:1948-1966). It does NOT extract output errors, late collisions, runts, or giants - all four are named explicitly in this lane as dirty-L1 / failing-optic symptoms. These ARE present in the collected `show interfaces` dumps and are parseable, and on the real AJ fleet they are non-zero on many ports: 1349 ports with non-zero output errors, 82 with non-zero late collisions, 382 with runts, 278 with giants (grep over migration_collection_20260613_063201/*/show_interfaces.txt). Because none reach physical_health[], the dirty-L1 detector _d_phys_remediation (design_advisor.py:838-854) - whose predicate is crc_errors>0 OR input_errors>0 OR duplex==half OR err-disabled (design_advisor.py:238-241) - never counts them. Late collisions in particular are a textbook duplex-mismatch / failing-cable signature and output-error/runt/giant bursts are classic marginal-optic symptoms, so a genuinely dirty port that happens to show clean CRC/input-error counts (e.g. drops only on TX) is reported as healthy and migrated as-is.
- fix: In parse_show_interface_counters add regexes for `(\d+) output errors`, `(\d+) late collision`, `(\d+) runts`, `(\d+) giants` and surface them on the record as output_errors/late_collisions/runts/giants. Carry output_errors and late_collisions onto the physical_health[] record in write_physical_health_sheet (excel.py:3090-3094) and add them to the _phy_dirty predicate in design_advisor.py (oper-up gated, matching the existing error-rate-high logic) plus the _d_phys_remediation summary string and its sources list. Treat late_collisions>0 as High (duplex/cable fault), output_errors/runts/giants>0 as Medium when oper-up.
- test: tests/test_parsers.py: feed an interface block containing '5 output errors', '3 late collision', '7 runts', '2 giants' and assert each maps onto the record. tests/test_design_blueprint.py: seed physical_health with a port that has crc_errors=0,input_errors=0 but late_collisions=3 (status connected) and assert _d_phys_remediation now counts it in phy_dirty and the summary mentions late-collision; add a regression asserting a port with output_errors=9 but otherwise clean is counted.

## [High/high] DET-intf-errors-001
- file: cisco_toolkit/parse.py; cisco_toolkit/excel.py; cisco_toolkit/design_advisor.py:parse.py:2082 (parse_interface_phy, no optics); excel.py:3015-3094 (write_physical_health_sheet record has no optics field); design_advisor.py:236-248,840-854 (_d_phys_remediation reads no optics)  (engine: collection -> parse -> physical_health[] SSOT -> dirty-L1 detector)
- claim: The lane's central question - is transceiver/optics health (Rx/Tx power, DOM) collected and feeding a failing-optic detector - is answered NO. `show interface transceiver` IS in the collected command set (COLLECT_PARSE_V3_23_0.py:481,537) and the AJ run captured 506 transceiver files; 197 devices carry a real DOM table with Tx/Rx power in dBm (e.g. migration_collection_20260613_063201/AAS01-BC-CAR7R03/show_interfaces_transceiver.txt: `Te1/49  29.4  3.30  -2.3  -2.1`). But there is NO parser for it: `grep '^def parse' parse.py | grep -i trans|optic|dom|power` returns only PoE/env-power parsers. The optics output is never read into physical_health[] (record keys are exactly switch/port/status/speed/duplex/media/input_errors/crc_errors/output_drops/port_channel/poe/risk/severity - verified on the AJ snapshot, union of all keys has no rx_power/tx_power/optic/dom). Consequently no detector can fire on a failing/marginal optic. textutils.py:73 detect_link_type() classifies a transceiver TYPE string but is never fed the collected output and never reads DOM power. An optic at e.g. Rx -18 dBm near the sensitivity floor - exactly the 'failing optic' the lane names - is invisible today, so it is silently carried across the migration on the same bad optic and the post-cutover NRFU baseline is corrupted.
- fix: Add parse_interface_transceiver(output) in parse.py to extract per-port {present, type, serial, rx_power_dbm, tx_power_dbm, temp_c, voltage_v} from both NX-OS `show interface transceiver [details]` and IOS `show interfaces transceiver` (the DOM table 'Temperature Voltage Tx Power Rx Power' block). Carry rx_power_dbm/tx_power_dbm onto the physical_health[] record in write_physical_health_sheet (read via _load_cmd_output(c2f,'show interface transceiver','show interfaces transceiver')). Add an engine_actionable detector _d_optic_health in design_advisor.py that flags ports whose rx_power_dbm is below a conservative threshold (e.g. <= -14 dBm for 10G-SR or within ~1 dBm of the type's documented Rx sensitivity floor) or whose tx_power is out of the type's range, priority High, driver 'replace marginal optic before cutover so NRFU certifies a clean L1'. NOTE the AJ collection used `show interface transceiver` WITHOUT the `detail`/`details` keyword on NX-OS, so NX-OS files carry type/serial but not the DOM power table; add `show interface transceiver details` (NX-OS) / `show interfaces transceiver detail` (IOS-XE) to the collected command lists so Rx/Tx power is captured fleet-wide, not only on the 197 platforms that print it by default.
- test: In tests/test_parsers.py add a case feeding a sample IOS `show interfaces transceiver` DOM block (Te1/49 ... -2.3 -2.1) and asserting parse_interface_transceiver()['Te1/49']['rx_power_dbm']==-2.1. In tests/test_design_blueprint.py seed physical_health with one port rx_power_dbm=-18.0 (and one healthy -2.1) and assert the new _d_optic_health decision fires with count 1 and the host listed; assert it does NOT fire when all optics are within range (coverage-honest: absent rx_power must not flag).

## [High/high] SSOT-device-deliv-01
- file: cisco_toolkit/runbook.py:217 (n_dev defined L138)  (Network Assessment & Migration Runbook §2 Scope & Data Completeness)
- claim: runbook.py:138 sets `n_dev = len(devices)` where `devices = snap_dict.get("devices")` (L126) is the INVENTORY dict (303 on the canonical AJ snapshot). Line 217 then renders: "In scope: the {n_dev} Cisco switches present in the collected dataset…" = "303 … present in the collected dataset". But only 253 were collected: the immediately-following §2.1 (L240-244) reads `collection_completeness.summary` and states "Of 303 inventory device(s): 253 fully collected … 50 NOT collected … assessment blind spots". So §2 asserts 303 are in the collected dataset while §2.1 says 50 of those were NOT collected — an internal contradiction and a false-collected claim. Verified against the snapshot: collection_completeness.summary={inventory:303, complete:253, not_collected:50}, and health_scores has exactly 50 rows banded 'Insufficient Data' whose set equals the 50 blind hosts.
- fix: Do not call the inventory total the 'collected dataset'. Either reword to inventory/scope — e.g. "In scope: the {n_dev} Cisco switches inventoried for this engagement…" — or, to keep the 'collected' wording, render the collected count: read `cs = (snap_dict.get('collection_completeness') or {}).get('summary') or {}` (already loaded at L235-236) and use `cs.get('complete', n_dev)` so the sentence reads "the 253 … present in the collected dataset (of 303 inventoried)".
- test: In tests/ (e.g. test_runbook.py / test_deliverables) build the runbook from the AJ snapshot, extract paragraph text, and assert that no paragraph contains both the inventory total and the phrase 'present in the collected dataset' unless the number shown equals collection_completeness.summary.complete; concretely assert the §2 scope sentence contains str(complete) and not f"{inventory} Cisco switches present in the collected dataset".

## [High/high] R2-1-02
- file: cisco_toolkit/runbook.py:297  (runbook DOCX + Lifecycle Risk workbook sheet)
- claim: Two deliverable surfaces render the drifting asof as if it were an authoritative assessment date: runbook.py:297 emits `f"As of {lr.get('asof', '')}, of {ls.get('n_devices', 0)} device(s): ..."` and excel.py:2048 emits `f"As of {L.get('asof', '')}: {s.get('n_past_ldos', 0)} past end-of-support ..."`. Because asof = date.today() at regen (see R2-1-01), regenerating the SAME June-13 collection on a later day changes BOTH the printed date AND the printed n_past_ldos/n_near with no input change and no indication that the figures describe a different calendar day than the collection. For the canonical AJ snapshot this prints 'As of 2026-06-21: 152 past end-of-support' even though the data was collected 2026-06-13; re-rendering tomorrow could print a higher number while still labelling it against the data collected June 13. This violates evidence-grounded / coverage-honest provenance: a customer reading the runbook cannot tell which date the EoL classification is anchored to, and two regenerations of one engagement disagree.
- fix: Once asof is frozen to the collection date (R2-1-01 fix), label these lines explicitly as the collection/assessment date rather than a bare 'As of <today>' — e.g. runbook.py:297 -> 'As of the assessment date <collection_asof>, of N device(s): ...' and excel.py:2048 -> 'Lifecycle bands as of collection date <collection_asof>: ...'. This makes the date load-bearing provenance instead of an incidental wall-clock stamp, and ties the counts to a stable anchor so successive regenerations of the same collection agree.
- test: In the existing runbook/excel deliverable tests, build a snapshot whose lifecycle_risk.asof is a FIXED date distinct from today (e.g. '2026-06-13') and assert the rendered runbook paragraph / workbook cell contains that exact date string and the matching n_past_ldos, and assert it does NOT contain date.today().isoformat() — locking that the deliverable reflects the frozen collection asof rather than wall-clock.

## [High/high] R2-11-2
- file: cisco_toolkit/runbook.py:804  (runbook.py §8 Shared Infrastructure & Consolidation — 'Spare port capacity (consolidation candidates — lowest port utilisation)' DOCX table)
- claim: low_util = sorted([c for c in cap if isinstance(c.get('port_util'),(int,float))], key=lambda c: c.get('port_util') or 0)[:10] selects the lowest-utilisation switches as the best consolidation targets. But port_util is computed as 100.0*active/total when total>0 (excel.py:574); when a device's per-interface STATUS was not observed, active_ports collapses to 0 (build.py:342-343 counts only status in ('connected','up'); dp.active_ports or 0 -> 0) while total survives from the parsed interface list (build.py:341). The result is port_util=0.0 with a BLANK active_ports — an absent-evidence device that ranks #1 as '0% used / all ports free, consolidate first', when in fact its load was simply never collected.
- fix: Two layers. (a) excel.py compute_capacity: only emit a numeric port_util when active_ports is genuinely observed — guard `putil = round(100.0*active/total,1) if (total and dp.active_ports is not None) else ''` (or require active>0 / treat 'active status not parsed' as unknown rather than 0). (b) runbook.py §8: additionally filter the consolidation list to rows where active_ports is a real number, e.g. `low_util = sorted([c for c in cap if isinstance(c.get('port_util'),(int,float)) and isinstance(c.get('active_ports'),(int,float))], ...)`, so absent-evidence devices cannot top the 'most spare' list. Label genuinely-zero (collected but idle) distinctly from not-observed.
- test: In tests/test_runbook.py, add a capacity row {hostname:'PHANTOM', total_ports:38, active_ports:'', port_util:0.0} alongside a real low-util row {active_ports:5,total_ports:48,port_util:10.4}; render the runbook and assert 'PHANTOM' does NOT appear in the §8 consolidation table while the real 10.4% row does. (And/or unit-test compute_capacity: a DevicePhysical with total_ports=38, active_ports=0-from-unobserved-status yields port_util=='' not 0.0.)

## [High/high] EX-nrfu-001
- file: webapp/backend/nrfu_docx.py:59,140-174  (webapp NRFU/ATP DOCX generator (nrfu_docx.write_nrfu_docx))
- claim: The engine publishes `design_nrfu.items[]` where EACH item is a fully-formed acceptance test carrying `setup` (preconditions), `description` (what to verify, with concrete show-commands), `pass_criteria` (explicit pass/fail), `devices` (the exact devices to verify) and `principle_citation` (compute_design_nrfu, design_advisor.py:2250-2261). On the canonical AJ snapshot this is 30 items, every one carrying setup+pass_criteria (verified: design_nrfu items=30, all has setup=True/has pass=True). The explorer renders all of this — one card per decision with Setup:/description/Pass: lines (blast_radius_explorer.html:6386-6389). But the webapp DOCX NRFU writer reads `design_nrfu` ONLY to build a `phase_by` lookup (nrfu_docx.py:162) and emits each design decision as a SINGLE traceability row `(title, priority, 'Tested — <phase>')` in §2.1 (lines 163-167). The setup/description/pass_criteria/devices/citation never appear in the formal ATP DOCX. The customer-facing acceptance document is therefore materially thinner than the explorer and omits the per-test pass/fail criteria and preconditions for every design-driven test — exactly the 'per-test setup/preconditions, pass/fail criteria, design-decision coverage' an ATP must carry.
- fix: Add a dedicated section (e.g. §2.2 'Design-driven acceptance tests' or fold into a phase-grouped table) that renders, per design_nrfu item, an ID (e.g. NRFU-D-001), the title/priority, the `setup` (preconditions), `description` (verification + commands), `pass_criteria` (pass/fail), and the device list — mirroring what the explorer already shows. Keep §2.1 as the compact traceability summary, but stop discarding the per-test body the engine authored.
- test: Extend webapp/tests/test_backend.py::test_nrfu_carries_design_traceability_and_scope_limits: seed a design_nrfu item with a distinctive setup ('Replacement hardware is on site') and pass_criteria ('no past-LDoS device appears in show cdp neighbors'); assert BOTH substrings appear in the rendered DOCX paragraphs/cells (currently they do not).

## [High/high] SSOT-webapp-backend-001
- file: webapp/backend/storage.py:138  (webapp/backend (storage.py add_snapshot) -> _meta_row -> GET /api/snapshots/{id} & list_snapshots -> frontend Snapshot.tsx:245 / Campaign.tsx:216)
- claim: Store.add_snapshot persists the snapshots.n_devices DB column as `len(snapshot.get("devices") or {})` (storage.py:138) — a second, server-side recompute of fleet scale that does NOT read the canonical executive_brief.scale.n_devices. _meta_row (storage.py:306-309) returns this column as a top-level `n_devices` field, which the frontend renders directly as the device count in Snapshot.tsx:245 (`{meta!.n_devices} devices`) and Campaign.tsx:216 (`{s.n_devices} dev`), and types in api.ts:27/186. The SIBLING headline on the same payload, summary.n_switches, IS canonical (summary.py:108/119 -> engine.trend_point -> html.py _trend_point:307 reads scale.n_devices first). So the two co-located fields drift whenever len(devices) != scale.n_devices. The engine sets scale.n_devices = len(health_scores) (analyze.py:5128 with n=len(hs) at :4978), the INVENTORIED count (the project's 303-inventoried vs 253-collected model). The repo's own test asserts these differ and that canonical must win: test_backend.py:622 (`'303' in rows ... canonical scale.n_devices, not len(devices)=2`) and _divergent_snap (test_backend.py:972-992) makes len(devices)=3 while scale.n_devices=42 — under that snapshot summarize() yields n_switches=42 (test_backend.py:1008) but the n_devices column persists 3, so the cockpit card would read '42 switches' while the page subtitle reads '3 devices' for the same snapshot. On the current AJ snapshot they coincide (303==303, verified: len(devices)=303, health_scores=303, scale.n_devices=303), so it is not visibly wrong today, but it is structurally a non-canonical recompute and is provably wrong for any inventoried!=len(devices) snapshot.
- fix: Source the persisted column from canonical scale, falling back to len(devices) only pre-brief — mirroring the C9 fix already applied in nrfu_docx.py:50/91 and the summary.py:119 default. In Store.add_snapshot, replace `len(snapshot.get("devices") or {})` with a canonical-first value, e.g. compute `n_dev = ((snapshot.get("executive_brief") or {}).get("scale") or {}).get("n_devices")` and pass `n_dev if isinstance(n_dev, int) else len(snapshot.get("devices") or {})`. (The canonical value is also already available as the passed-in `summary.get("n_switches")`, since summary=summarize(snap) is computed in the same call site app.py:239/268/595 — using summary['n_switches'] with the len() fallback is equally valid and avoids re-reading the blob.) No schema/migration change needed; the column stays INTEGER.
- test: Add to webapp/tests/test_backend.py, reusing _divergent_snap(): create a campaign, add_snapshot(_divergent_snap()), then assert the returned meta['n_devices'] == 42 (canonical scale.n_devices) and == meta['summary']['n_switches'] — i.e. the top-level column and the summary headline agree and both equal the canonical 42, NOT len(devices)=3. This is the storage-layer twin of the existing test_summarize_projects_the_canonical_headline (test_backend.py:1004) and would currently fail (returns 3).

## [Medium/high] DET-errdisable-001
- file: COLLECT_PARSE_V3_23_0.py:475-584; cisco_toolkit/analyze.py:475-584 (command lists); physical_health record keys  (engine: collection/parse + physical_health)
- claim: The `errdisable recovery` configuration (which causes auto-recover, and the recovery interval/timer) is present in the already-collected `show running-config` text on 177 of the AJ devices, but no parser extracts it and no snapshot field carries it. Consequently the engine's err-disabled detection has NO awareness of whether a stuck port will auto-recover. A port err-disabled for a cause that has `errdisable recovery cause <x>` configured self-heals after the interval; one whose cause is NOT in the configured recovery set stays down until a manual shut/no-shut — an operationally critical distinction the engine cannot currently make. The exemplar (`_aci_conv.txt:221-222`) names `errdisable recovery interval(seconds)=300` + recovery cause as a baseline parameter, confirming a Cisco-AS deliverable is expected to report this posture.
- fix: In COLLECT_PARSE, add a small parser (e.g. parse_errdisable_recovery(running_config_text) -> {'interval': int|None, 'causes_enabled': [..]}) that scans the already-collected `show running-config` for `errdisable recovery interval <N>` and `errdisable recovery cause <cause>` lines (no new device command needed — data is already on disk). Attach the per-device result to the snapshot (e.g. under a new `errdisable_recovery` map or onto the device record), and join it into each physical_health err-disabled record so a port carries `recovery_configured: bool` for its disabling cause when derivable. Treat absence-of-config honestly as 'not observed', never as 'recovers'.
- test: tests/test_compute.py (or a new tests/test_errdisable_recovery.py): feed a synthetic running-config with `errdisable recovery interval 300` + `errdisable recovery cause bpduguard` and assert the parser returns interval==300 and 'bpduguard' in causes_enabled and 'link-flap' NOT in it; feed a config with NO errdisable-recovery lines and assert causes_enabled==[] / interval is None (coverage-honest, not silently 'enabled').

## [Medium/high] SSOT-device-deliv-06
- file: cisco_toolkit/analyze.py:5128 (scale dict) — root cause behind findings 01-05  (SSOT contract — executive_brief.scale (consumed by design/crd/runbook/deck/engagement/ops + explorer + webapp))
- claim: compute_executive_brief returns `scale = {n_devices: n, n_domains, n_endpoints}` (analyze.py:5128) where `n = len(health_scores)` (analyze.py:4978) = inventoried (303). `n_vlans` is injected at assembly, but there is NO `n_collected` field on `scale` anywhere (grep for 'n_collected' across design/crd/mop/deck/engagement/archreview returns nothing). Because the canonical SSOT publishes only the inventoried count and no collected count, each deliverable that needs 'collected/assessed/evidence-scope' must independently re-derive it from collection_completeness — and several instead reuse the inventoried 303 under a 'collected/assessed/evidence scope' label (findings 01-05). The fix-once is to add the collected count to the canonical scale so all surfaces read one number rather than recompute or mislabel.
- fix: In compute_executive_brief, accept the collection_completeness summary (or compute it) and publish `scale["n_collected"] = complete` and optionally `scale["n_inventoried"] = n` so the inventoried/collected split is explicit and canonical. Then point findings 01-05 at `scale.n_collected` for any 'collected/assessed/evidence-scope' wording. Keep it out of the frozen golden the same way n_vlans is excluded, to avoid golden churn.
- test: Extend test_pipeline_inprocess (which already locks scale.n_vlans==len(vlan_inventory)) to assert scale.n_collected == collection_completeness.summary.complete and scale.n_devices == len(health_scores); then a per-deliverable test that any 'collected/assessed/evidence scope' device count equals scale.n_collected.

## [Medium/high] DET-stp-001
- file: cisco_toolkit/analyze.py:1366-1372, 1420-1427  (engine: analyze.compute_protocol_health + design_advisor detectors)
- claim: `show spanning-tree detail` is collected for all 253 AJ devices (raw files present: 253 `show_spanning-tree_detail.txt`) and `_parse_stp_tcn` (analyze.py:1366) extracts (max, total) topology-change counts. But the ONLY consumer is analyze.py:1420-1425, which appends `; max TCN {maxt}` to the `protocol_health[STP].summary` DISPLAY string and DISCARDS `_tot`. Severity is set solely by inconsistent-port count (`sev = "High" if ninc else "Info"`, line 1422) — TCN never affects severity. No design_advisor detector reads TCN (the 4 STP detectors `_d_stp_lag`/`_d_stp_det`/`_d_stp_mst_scale`/`_d_stp_root_determinism` key on blocked/mode/root only), no punch-list entry and no design decision keys on it. Grep for `tcn|topology.change|_parse_stp_tcn` across cisco_toolkit/*.py confirms analyze.py:1420 is the sole call site and it terminates in a summary string. This is a textbook collected-but-unused axis: a high cumulative topology-change count is one of the strongest migration-risk signals (chronic L2 instability that will reconverge unpredictably in the cutover window), yet it is invisible to the design blueprint. On the canonical AJ snapshot it would fire on 22 devices at >=100,000 changes (32 at >=10,000), including the distribution core DS-VSS-CAR3-R13-ARDOH (533,347) and AS04-BC-CAR3R13-AJA (569,075) — none of which is surfaced by any of the 4 existing blueprint STP decisions (verified: blueprint decision ids are dc-stp-root-determinism/-determinism-edge-protection/-multichassis-lag-over-stp/-mst-instance-scale, and no decision's evidence keys on TCN; the lone 'topology change' phrase in dc-bound-layer2-failure-domain is generic rationale prose, evidence fields are move_groups[].spanning_vlans, count 58).
- fix: Add a firing engine_actionable detector `_d_stp_topology_instability` in cisco_toolkit/design_advisor.py and register it in `_DETECTORS` (design_advisor.py:1076-1085). Build the signal mirroring the existing summary-parse pattern (cf. `_stp_blocked` at design_advisor.py:528 which does `re.search(r"(\d+)\s+blocked", summary)`): in the signal block (~design_advisor.py:179-186) add `sig["stp_unstable"]`/`sig["stp_unstable_hosts"]` by extracting `re.search(r"max TCN (\d+)", x.get("summary",""))` for STP rows and thresholding (default >=100000 cumulative; expose the constant like `_STP_TCN_THRESHOLD`). The detector fires when `sig["stp_unstable"] > 0`, priority High, axes ["availability","convergence","manageability"], evidence fields ["protocol_health[STP].summary (max TCN)"], driver 'Stabilise the chronically-reconverging L2 trees (root/edge/flap source) before the cutover window — a tree that has reconverged hundreds of thousands of times will reconverge unpredictably under migration load.' Add a matching engine_actionable=True principle to design_kb.py with citation. RIGOR IMPROVEMENT (recommended, separable): also stop discarding recency — extend `_parse_stp_tcn` to also capture 'last change occurred <X> ago' so a high count with a RECENT last-change is distinguished from aged churn, and publish a structured `max_tcn`/`tcn_source_port` field on the protocol_health[STP] row so the detector and every downstream surface read a number, not a regex over a display string (SSOT-cleaner than re-parsing the summary).
- test: Add `test_stp_topology_instability_fires_and_honest` to tests/test_design_addenda.py following the existing _by(...) pattern (cf. test_stp_root_determinism_fires_and_honest at line 34). FIRE case: `_by({"protocol_health":[{"switch":"ds1","protocol":"STP","summary":"mode rapid-pvst; 0 blocked, 0 inconsistent; max TCN 250000"}]})` must contain the new decision id with evidence count==1 and devices==["ds1"] and "protocol_health[STP].summary" in evidence.fields. CLEAN cases (no fire): a low count `max TCN 12`; a row with no `max TCN` token; a non-STP protocol row. Lock engine_actionable: assert the new principle in design_kb has `engine_actionable=True` (mirrors the per-detector honesty tests). Optionally a pipeline-level assertion that on the AJ fixture the decision is present (the snapshot proves 22 devices fire at the 100k threshold).

## [Medium/high] DET-cdp-lldp-001
- file: cisco_toolkit/analyze.py:228-260 (compute_topology_links); 980-1015 (compute_collection_completeness); design_advisor.py:1076-1085 (_DETECTORS)  (engine analyze.py / design_advisor.py detectors; snapshot; deliverables)
- claim: CDP/LLDP neighbor evidence IS mined for cross-end mismatch defects (native-VLAN, duplex/speed) and one-end-only links, but the set of infrastructure (Switch/Router) neighbors a collected switch advertises that are absent from the inventory (devices.json) is captured yet never surfaced as an actionable finding or design decision. compute_topology_links() deliberately keeps a genuinely off-scan neighbor under its raw advertised name (analyze.py:243) and _is_infra_neighbor() (analyze.py:213) already classifies these as infra, but no consumer flags 'this infra device is wired to us and is out of assessment scope'. compute_collection_completeness() (analyze.py:980) only diffs inventory-vs-collected and never reads neighbor names, so an entirely un-inventoried adjacent device is a structural blind spot. For a brownfield migration this is the classic undocumented-topology / unknown-blast-radius gap: every infra device a to-be-migrated switch touches must be in scope or explicitly accepted as out-of-scope before cutover.
- fix: Add a new engine_actionable detector _d_undocumented_neighbors(snap, sig) in cisco_toolkit/design_advisor.py and register it in _DETECTORS (design_advisor.py:1076). In _signals() (design_advisor.py:105) iterate snap['interfaces'] exactly like _d_native_vlan does (design_advisor.py:266-270): for each port with a non-empty cdp_neighbor whose endpoint_type in {'Switch','Router'} (reuse the _is_infra_neighbor rule), canonicalize via the same _canon_host logic, and count those whose canon is NOT in the set of canon(inventory_hostnames) and NOT in canon(collected hosts). Store sig['undoc_nbr_count'], sig['undoc_nbr_names'] (distinct, capped 12), sig['undoc_nbr_hosts'] (collected switches that see them). The detector returns a _decision(pid='topology-bring-undocumented-neighbors-into-scope', priority='Medium' (raise to 'High' if any undocumented neighbor is a Router or carries a core naming token like CS/DS/CORE), driver='Scope completeness: every infra device wired to a migrating switch must be assessed or explicitly out-of-scoped before cutover', fields=['interfaces[host][port].cdp_neighbor','interfaces[host][port].endpoint_type'], devices=sig['undoc_nbr_hosts']) with a matching design_kb principle. Because compute_topology_links already resolves off-scan names, an even cleaner option is to publish the resolved link map into the snapshot (it is NOT currently emitted; only link_phy/trunk_native are at COLLECT_PARSE_V3_23_0.py:2037-2038) and have the detector read links where confirmation startswith 'One end' AND b_host canon is not inventoried. Keep it engine_actionable=True so it reaches the blueprint, punch-list and explorer like the other firing detectors.
- test: Add tests/test_design_addenda.py case test_d_undocumented_neighbors_fires: build a snap with interfaces={'sw1':{'Gi1/0/1':{'cdp_neighbor':'CORE-OFFSCAN-X','endpoint_type':'Switch'}, 'Gi1/0/2':{'cdp_neighbor':'sw2','endpoint_type':'Switch'}}, 'sw2':{...}} and an inventory/collected set containing only sw1,sw2; assert compute_design_blueprint(snap) yields a decision id 'topology-bring-undocumented-neighbors-into-scope' with evidence.count==1 and 'CORE-OFFSCAN-X' in evidence.devices-or-summary, and that an all-in-scope snap (every neighbor inventoried) yields no such decision (gate returns None). Add a coverage-honesty assert mirroring the existing addendum tests that the principle is engine_actionable. Optionally lock the AJ count with a smoke assert (>=1 undocumented infra neighbor) against the canonical snapshot.

## [Medium/high] DET-qos-copp-001
- file: cisco_toolkit/analyze.py:4212-4239 (_QOS_DOCTRINE); 4311-4356 (qos_audit checks)  (engine: analyze.compute_qos_audit / design_advisor detectors)
- claim: qos_audit DESCRIBES MQC/auto-QoS/mls-qos/trust posture but has no CoPP angle. The Cisco AS exemplar checks a CoPP policy (rates + class coverage) as a control-plane-protection control. On the AJ fleet, 154 of 253 collected devices are classic IOS 15.1/15.2 (PIDs incl. WS-C3850/WS-C3560X) that carry L3 SVIs yet have NO `control-plane` stanza and no service-policy anywhere -> CoPP is opt-in on classic IOS and is entirely absent, so their CPUs are unprotected against control-plane floods/DoS. No firing decision, no doctrine finding, and no CIS check (security has 11 ids, none CoPP) surfaces this. The only CoPP-aware code is analyze.py:4331-4336, which EXCLUDES NX-OS copp-system-*/default-* names so the default policy is not mis-flagged as a dangling policy-map — i.e. the engine knows CoPP exists but treats it purely as noise to suppress, never as a posture signal.
- fix: Add a CoPP posture fact to parse_qos_config (parse.py:2185) — e.g. `copp = {present: bool, policy_name, n_classes, policer_seen: bool}` extracted from the `control-plane` block (the service-policy under it + whether the referenced policy-map has a `police`), platform-aware (classic-IOS = opt-in so absence is actionable; NX-OS default = implicit, treat absence-in-plain-config as not-assessable not as a gap). Surface it in compute_qos_audit via a new `_QOS_DOCTRINE` kind `copp-absent` (severity High for L3 classic-IOS, since unprotected control plane is a hardening/availability risk) and a firing `_d_copp` detector (engine_actionable=True) that fires when a collected, L3-capable, classic-IOS device has no control-plane service-policy. Cite `qos_audit.per_device[].copp` / `qos_audit.findings[kind=copp-absent]` in the design requirement and CRD so the fix lands on every consumer surface via the SSOT.
- test: tests/test_qos_audit.py: (1) parse_qos_config on a classic-IOS config WITHOUT a control-plane stanza but WITH `interface Vlan10 / ip address ...` -> copp.present is False and compute_qos_audit emits a `copp-absent` finding for that host; (2) an IOS-XE config with `control-plane / service-policy input system-cpp-policy` + a defined `policy-map system-cpp-policy` -> copp.present True, NO copp-absent finding; (3) an NX-OS config (feature lines) without control-plane in plain config -> NOT flagged (not-assessable, asserts the platform guard); (4) a design_advisor test that the new _d_copp detector carries engine_actionable=True and FIRES on a fixture matching case (1) but is silent on cases (2)/(3).

## [Medium/high] DET-syslog-3
- file: cisco_toolkit/analyze.py:4130-4146  (engine detector (compute_syslog_intelligence))
- claim: The detector chain (analyze.py:4130-4146) has no signature for three high-frequency error-band link/optic classes present on the AJ fleet: %ETH_PORT_CHANNEL-3-LACP_ERROR (15,848), %SFF8472-3-THRESHOLD_VIOLATION (2,277 = optic DOM Tx/Rx power/temp crossing -- a transceiver going bad), %ETHPORT-3-IF_UNSUPPORTED_TRANSCEIVER (670). These are exactly the 'fix the media before cutover' / 'flapping links invalidate baselines' situations the link-flap doctrine already articulates, but the symptom here is the optic/bundle itself, not a flap. LACP errors are EtherChannel bundle health that bears directly on the vPC/port-channel redundancy the migration relies on. Executed each verbatim-shape line through compute_syslog_intelligence: all return NO DETECTION. So a senior engineer reading the syslog axis would see no optic/LACP finding even though the buffers shout it.
- fix: Add doctrine keys + branches: 'optic-degraded' (Medium): mnemonic startswith 'SFF8472' threshold-violation or 'IF_UNSUPPORTED_TRANSCEIVER' -> 'A pluggable optic crossed a DOM threshold or is unsupported; failing optics cause silent errors -- replace before the cutover baseline'; 'lacp-error' (Medium/High): fac.startswith('ETH_PORT_CHANNEL') and 'ERROR' in mn (or 'LACP' in mn) -> 'A LACP bundle reported errors; verify member/peer config and resolve before relying on this port-channel/vPC for cutover redundancy'. Wire both into the existing kind chain and the doctrine dict.
- test: tests/test_syslog_intelligence.py: assert '%SFF8472-3-THRESHOLD_VIOLATION: Te1/1 Rx power low' -> 'optic-degraded'; '%ETHPORT-3-IF_UNSUPPORTED_TRANSCEIVER' -> 'optic-degraded'; '%ETH_PORT_CHANNEL-3-LACP_ERROR' -> 'lacp-error'. Assert each carries a non-empty recommendation.

## [Medium/high] HON-analyze-falsehealth-1
- file: cisco_toolkit/analyze.py:3924-3935, 3935 (hosts = sorted(rc)), 4007-4008 (summary)  (Golden-Config Drift workbook sheet (excel.py:1567 write_golden_drift_sheet, row 2 'N device(s)'); runbook §6.9 (runbook.py:608-616); snapshot golden_drift.summary)
- claim: compute_golden_drift only iterates hosts that HAVE a captured running-config (analyze.py:3935 `hosts = sorted(rc)`) and has NO `all_hosts` parameter — unlike its sibling running-config axes compute_qos_audit (analyze.py:4242, takes all_hosts) and compute_software_risk (analyze.py:4495, takes all_hosts). The pipeline (COLLECT_PARSE_V3_23_0.py:1483-1485) excludes not-collected hosts from all_run_configs via `if rc_text:`, so a device that is inventoried but whose running-config was not captured is SILENTLY ABSENT from the golden-drift report rather than declared 'not assessed'. The summary's `n_devices` (analyze.py:4007) therefore counts only config-bearing hosts. On the canonical AJ snapshot this is 253, while 303 are inventoried — 50 inventoried devices vanish from coverage with no 'not assessed' line. Every sibling running-config axis surfaces exactly these 50 (qos n_not_assessable=50, swrisk n_config_not_assessable=50, syslog n_not_collected=50) with the host list; golden_drift.summary has no equivalent key. This is the coverage-honesty side of the false-health class: 'not observed' becomes invisible, and a reader reasonably reads '253 device(s) · avg compliance 77%' as the whole fleet.
- fix: Give compute_golden_drift an `all_hosts: Optional[List[str]] = None` parameter (mirroring compute_qos_audit / compute_software_risk). Compute `report_hosts = sorted(set(all_hosts or []) | set(rc))`; for a host with no config text, append a per_device row marked not-assessable ({'host':h,'assessable':False,'compliance_pct':None,'n_missing':None,'note':'Running-config not captured — not assessed.'}) and EXCLUDE it from the avg/drift math. Add `n_assessable` and `n_not_assessable` (+ `hosts_not_assessable[:20]`) to the summary, exactly like the siblings. Wire it at COLLECT_PARSE_V3_23_0.py:1827-1828 by passing `sorted(all_syslogs)` (or the inventory host list) as all_hosts. Update excel.write_golden_drift_sheet (row 2) and runbook §6.9 to render 'N assessed · M not assessed (config not captured)'.
- test: Add tests/test_golden_drift.py::test_not_collected_hosts_declared: call compute_golden_drift({'a':'hostname a\nip routing\nservice timestamps\n','b':'hostname b\nip routing\nservice timestamps\n','c':'hostname c\nip routing\nservice timestamps\n'}, all_hosts=['a','b','c','d_uncollected']); assert summary['n_not_assessable']==1 and 'd_uncollected' in summary['hosts_not_assessable'] and the d_uncollected per_device row has assessable is False and compliance_pct is None; assert d_uncollected is excluded from avg_compliance_pct (i.e. avg reflects only a,b,c). Regression-lock: assert 'n_not_assessable' in summary so the coverage key cannot silently disappear.

## [Medium/high] R2-3-03
- file: cisco_toolkit/analyze.py:4782 (asof default date.today()); 4845-4846 (emit); runbook.py:297 (render); excel.py:2048 (render); docmeta.py:153 & cover sites (the other two anchors)  (Runbook §4.1 + workbook lifecycle sheet ('As of …') vs every cover ('Snapshot captured …' / 'Generated …'))
- claim: One assessment carries three separately-derived temporal anchors that nothing reconciles: (1) `generated_at` (regeneration now(), labeled 'Snapshot captured', docmeta.py:153); (2) the per-doc cover 'Generated' now() (the 7 sites in R2-3-02); (3) lifecycle_risk `asof`, defaulted to `date.today()` (compute_lifecycle_risk, analyze.py:4782; emitted 4845-4846) and rendered as 'As of {asof}' in runbook.py:297 and 'As of {asof}' in excel.py:2048. Because asof is computed inside analyze at run time, it is a THIRD wall-clock read, independent of generated_at. They coincide today only because regeneration ran on the same day; the moment generated_at is corrected to reflect true collection time (R2-3-01) the runbook lifecycle banner 'As of 2026-06-21' will visibly disagree with the cover 'Snapshot captured 2026-06-13' on the same document — exactly the cross-section split this lane targets. The lifecycle banding (Past-LDoS/Near-LDoS) is also evaluated against today, not against the collection date, which is the methodologically correct anchor for an as-of-collection assessment.
- fix: Anchor all three to one source. Pass the snapshot's collection timestamp (the `collected_at` introduced in R2-3-01) into compute_lifecycle_risk as `asof` from the pipeline (instead of letting it default to date.today()), so lifecycle banding and the 'As of' banner reflect the assessment/collection date and match the cover. Document in-code (near analyze.py:4764 and docmeta.py:153) that collection-time is the single temporal source of truth for the deliverable family, mirroring the existing scale SSOT pattern.
- test: Extend tests/test_lifecycle.py / add a pipeline test: run the pipeline with a fixed collected_at and assert lifecycle_risk['asof'] equals that collected_at (not today). Add a cross-render assertion: the runbook's 'As of' date equals the cover's 'Snapshot captured' date for the same snapshot.

## [Medium/high] R2-6-02
- file: cisco_toolkit/analyze.py:4805-4813  (compute_lifecycle_risk band logic)
- claim: The band/comparison logic (today > ldos -> Past-LDoS; yrs<=1 -> Near-LDoS) uses rec['ldos'] with no branch on rec['conf']. A derived date is compared exactly like a confirmed one. The only place conf appears is interpolated into the status STRING (line 4807: "...LDoS {rec['ldos']}, {rec['conf']}") and as a passthrough field (4816) — it never affects the band, the Critical severity in risks (4831), or the count. Concretely on AJ: 15 Nexus 5600 (derived LDoS 2026-04-30) and 14 Catalyst 4500-X (derived LDoS 2025-10-31) are classed Past-LDoS=Critical; the Nexus 5600 crossed only ~7 weeks before the asof (years_to_ldos=-0.1) entirely on the EoS+5 rule. A wrong-by-months derived date silently manufactures or removes a Critical.
- fix: Keep the band (worst-case is defensible) but make the confidence first-class so downstream surfaces can show it: add a per-device boolean like 'derived_basis' and roll up summary['n_past_ldos_derived']/['n_near_derived']. Optionally add a near-boundary flag (e.g. |years_to_ldos| <= 0.5 AND conf=='derived') to mark 'verify — derived date near today'. Do not silently demote derived Past-LDoS to a softer band (that would hide a real driver); just expose the basis.
- test: tests/test_lifecycle.py: assert compute_lifecycle_risk summary exposes n_past_ldos_derived equal to the count of Past-LDoS rows with conf=='derived' for a mixed fixture; assert a confirmed-LDoS row is NOT counted in n_past_ldos_derived.

## [Medium/high] EX-archreview-2
- file: cisco_toolkit/archreview.py:342-350 (RES-4 keystones branch — verdict literal 'advisory')  (archreview)
- claim: RES-4 ('No keystone single point of failure') assigns verdict 'advisory' for ANY non-empty keystone set, with no magnitude/threshold escalation, regardless of how catastrophic the blast radius is. On the canonical AJ fleet, 15 devices each strand 2348 endpoints — 2348 of 5127 canonical endpoints = 46% of the entire campus behind a single device — yet the verdict is 'advisory' (the same band as a benign finding). This under-grades the single highest-impact availability exposure the report measures and is internally inconsistent with the module's own doctrine: RES-2 calls a single-gateway VLAN 'the highest-impact availability exposure a campus carries' and grades it 'critical', while a device that strands half the fleet stays advisory. Because the overall grade weights advisory at 0.7 vs deviation 0.35/critical 0.0, a near-fleet-wide SPOF barely dents the conformance score.
- fix: Add a magnitude-based escalation to RES-4: compute the worst keystone's stranded count as a fraction of total endpoints (read executive_brief.scale.n_endpoints SSOT-first, fall back to sum of macs/failure-impact). Escalate to 'deviation' when the worst keystone strands e.g. >=20% of endpoints (or above an absolute floor), keeping 'advisory' only for modest concentrations. Cite the chosen threshold in `reference`.
- test: In tests/test_archreview.py add a snap with failure_impact=[{host:'core',stranded:2348}] and executive_brief.scale.n_endpoints=5127 (or equivalent MAC evidence); assert RES-4 verdict in ('deviation','critical') and the observed names the fraction. Keep an existing low-concentration case asserting it stays 'advisory' to prove the threshold is bounded.

## [Medium/high] EX-archreview-3
- file: cisco_toolkit/archreview.py:289 (RES-2 critical branch suffix); fhrp_issues defined L280  (archreview)
- claim: RES-2 appends '; {len(fhrp_issues)} FHRP group(s) also carry consistency issues' whenever fhrp_issues is non-empty, where fhrp_issues = any fhrp group with a non-empty `issues` list (L280). On the canonical AJ fleet ALL 52 fhrp entries have ZERO members with fhrp=True and every issue string is literally 'N gateways but no FHRP - no first-hop redundancy'. So the shipped RES-2 observed reads '...; 52 FHRP group(s) also carry consistency issues' for a fleet where FHRP does not exist at all (canonical posture: FHRP gateway redundancy = 0 of 209). 'Consistency issue' connotes a configured FHRP group with mismatched priorities/timers/preemption — the opposite of the true state (FHRP entirely absent). A reader of the conformance report can wrongly conclude FHRP is deployed but misconfigured, masking that there is no first-hop redundancy anywhere. It also double-narrates the single-gateway story (single_gw rows and these 52 'no-FHRP' groups are the same absence).
- fix: Distinguish 'group has no FHRP at all' from 'configured FHRP group with a consistency mismatch'. Partition fhrp_issues by whether any member has fhrp truthy: only call the latter 'consistency issues'; describe the former as 'gateway VLAN(s) with multiple candidate gateways but NO FHRP configured' (or fold into the single-gateway count rather than re-counting). Update the suffix wording accordingly so a zero-FHRP fleet never reads as 'consistency issues'.
- test: Add a snap whose fhrp=[{vid:3, issues:['3 gateways but no FHRP - no first-hop redundancy'], members:[{host:'a',fhrp:False},...]}] plus an l3f single-gateway row; assert RES-2 verdict=='critical' and 'consistency issue' NOT in observed (because no group has configured FHRP). Add a second snap with a member fhrp=True + a real priority-mismatch issue and assert 'consistency issue' IS used there.

## [Medium/high] EX-archreview-4
- file: cisco_toolkit/archreview.py:980-984 (top_actions = sorted(...)[:10]) rendered at 1099-1109  (archreview)
- claim: top_actions is the sorted actionable list truncated to the first 10 (actions[:10], L984) with NO guarantee that all critical+deviation items survive the cut — advisories can crowd out deviations only when critical+deviation themselves exceed 10, but on the AJ fleet there are exactly 2 critical + 9 deviation = 11 critical/deviation items, so the cap drops one DEVIATION (OPS-1 — '3 High/Critical drift findings incl. Temporary L2 bridge / PoE fault'). The §1.1 header (L1102-1104) explicitly frames the queue as 'the order a senior engineer would work them', so omitting a material deviation from the working order is misleading even though §3 still lists it. The drop is data-dependent (only bites when crit+dev>10) which makes it easy to miss in tests built on smaller fleets.
- fix: Guarantee all critical+deviation items appear before any advisory is included, and let only advisories overflow the cap. E.g. take all critical+deviation, then fill the remaining slots (up to a higher cap or 'min(10, len)') with advisories; or raise/remove the cap for non-advisory verdicts. Keep the §1.1 note truthful about what is shown.
- test: Construct a snap producing >=11 critical/deviation checks (e.g. many single-gateway VLANs across domains, or stub several deviations); assert every check whose verdict in ('critical','deviation') has its id present in top_actions (no critical/deviation silently dropped), while advisories may be truncated.

## [Medium/high] EX-archreview-5
- file: cisco_toolkit/archreview.py:57-64 (NOT_ASSESSED table omits multicast) + compute_architecture_review L184-956 (no multicast check)  (archreview)
- claim: The module's doctrine (file docstring L16-19) is 'every check synthesizes ALREADY-COLLECTED snapshot sections' and 'a check whose evidence was not captured returns NOT-ASSESSABLE ... rather than silently skip'. The snapshot carries rich multicast evidence in service_map.multicast (igmp_queriers, active_switches, classified_groups, ptp), and the engine itself relies on igmp_queriers for the canonical VLAN count. Yet compute_architecture_review contains zero multicast/IGMP/PIM logic, and the NOT_ASSESSED honesty table (L57-64) lists QoS, Wireless, WAN, control-plane scale, performance, physical plant — but NOT multicast. So multicast is in neither bucket: not reviewed and not declared out-of-scope, violating the module's own coverage-honesty contract. Multicast is a first-class campus design domain and migration-relevant (a missing active multicast VLAN strands multicast at cutover). A real conformance/design review with querier+PIM evidence in hand would at minimum check 'every active multicast VLAN has an IGMP querier' and 'PIM RP redundancy'.
- fix: Either (a) add a multicast check to D2/D4 (e.g. 'every active multicast VLAN has an IGMP querier', escalate when active multicast VLANs lack a querier; advisory PIM-RP-redundancy when RP data exists), reading service_map.multicast defensively; or (b) at minimum add ('Multicast design (IGMP/PIM/RP redundancy)', 'reason') to NOT_ASSESSED so the report explicitly states it is out of this review's scope. Option (a) is preferred given the evidence is collected.
- test: If adding a check: build a snap with service_map.multicast.igmp_queriers covering VLANs {10,20} and an access VLAN 30 with endpoints but no querier; assert the new multicast check fires (advisory/deviation) and names VLAN 30. If only declaring: assert any('Multicast' in row[0] for row in NOT_ASSESSED) and that the rendered §2.2 table contains 'Multicast'.

## [Medium/medium] EX-archreview-6
- file: cisco_toolkit/archreview.py:251-356 (D2 Resiliency: RES-1..RES-4 — no vpc read); snapshot key 'vpc' unread anywhere in file  (archreview)
- claim: The Resiliency & Availability domain assesses access dual-homing (RES-1, via CDP), FHRP (RES-2), PSU (RES-3) and keystone blast radius (RES-4) but performs NO assessment of vPC / multi-chassis link-aggregation health, even though the snapshot carries vpc (49 domains, each with peer_status/keepalive_status/consistency/vpcs). vPC is the core/aggregation tier's primary redundancy mechanism on the AJ Nexus estate, and the engine's own design_advisor _d_vpc_health detector found 668 down* + 174 inconsistent vPC member legs across 49 domains on the CS01/CS02 core (per project memory). A conformance report whose Resiliency section omits the health of the very mechanism that makes the core redundant is materially incomplete — RES-1 covers only the access edge and RES-3 only power. This is a genuine rigor gap in D2 (archreview is a snapshot-reader and the vpc section is right there in the snapshot it already consumes).
- fix: Add a RES-5 vPC/MLAG-health check to D2 reading snap['vpc'] defensively: deviation/critical when peer_status/keepalive is down or consistency is inconsistent on aggregation nodes; conforms when all domains are peer-adjacent + consistent; not-assessable when 'vpc' is absent. Cite Cisco vPC consistency/peer-keepalive guidance in reference. (Aligns archreview's resiliency view with the existing design_advisor _d_vpc_health detector.)
- test: Add a snap with vpc={'cs01':{peer_status:'peer-down',keepalive_status:'down',consistency:'inconsistent',vpcs:[...]}}; assert the new RES-5 verdict in ('deviation','critical') and names cs01. Add a healthy-vpc snap asserting 'conforms', and an absent-vpc snap asserting 'not-assessable'.

## [Medium/high] SSOT-vlan-deliv-01
- file: cisco_toolkit/crd.py:71  (CRD deliverable (crd.py _evidence_facts -> §2 "VLANs in use", §3 evidence-scope sentence, REQ-T-LAN-001, §6 Future-Plans baseline))
- claim: Of the eight deliverable generators, crd.py is the only one that reports a fleet-wide "VLANs in use" count by INDEPENDENTLY recomputing it rather than reading the published canonical value. crd.py:71 sets `"n_vlans": len(vlan_inventory(snap))` and that value is then rendered on four CRD surfaces: §2 table "VLANs in use" (crd.py:209), the §3 evidence-scope sentence (crd.py:193 `{ev['n_vlans']} VLANs`), REQ-T-LAN-001 "Preserve the {n_vlans} production VLAN IDs" (crd.py:261), and the §6 Future-Plans "Production VLANs" baseline (crd.py:333). It NEVER reads executive_brief.scale.n_vlans. This is the exact pattern design.py was explicitly fixed away from: design.py:128-130 reads `_scale.get("n_vlans") or len(vlans)` (canonical-first) with an in-code comment (design.py:124-127) calling the recompute "a latent drift risk even though they coincide today (A5 SSOT fix)". The CRD's OWN endpoint line one row below (crd.py:77) already does it right — `((snap["executive_brief"]["scale"]).get("n_endpoints")) or endpoints` (canonical-first) — so the n_vlans line is an internal inconsistency within the same _evidence_facts function.
- fix: Mirror the endpoint line (crd.py:77) and the design.py:128-130 pattern: in _evidence_facts, read the published canonical first with the local derivation only as a fallback. Change crd.py:71 from `"n_vlans": len(vlan_inventory(snap)),` to `"n_vlans": (((snap.get("executive_brief") or {}).get("scale") or {}).get("n_vlans")) or len(vlan_inventory(snap)),`. Value stays 202 on AJ today (the published scale is itself len(vlan_inventory) on the same snap), but the CRD now reads the ONE published value, closing the drift seam if the engine's published derivation ever diverges from a bare vlan_inventory() call.
- test: Add tests/test_crd.py::test_crd_vlan_count_reads_published_scale_canonical_first: build a snap whose executive_brief.scale.n_vlans is set to a SENTINEL that DISAGREES with len(vlan_inventory(snap)) (e.g. interfaces/l3_forwarding yield 3 VLANs but scale.n_vlans=777), call write_crd_docx, and assert the rendered §2 "VLANs in use" and REQ-T-LAN-001 show 777 (the published canonical), not 3. The test must FAIL against the current crd.py:71 (which would render 3) and PASS after the canonical-first fix — i.e. it mutation-proves the read path the way webapp _trend_point/summarize is already mutation-proved.

## [Medium/high] SSOT-device-deliv-03
- file: cisco_toolkit/crd.py:151 (title) and 193 (Engagement Context table)  (Customer Requirements Document — title page + §1 Engagement Context 'Evidence scope (this assessment)' row)
- claim: crd.py:71 sets `"n_devices": len(devices)` (devices = inventory dict, 303). The title page (crd.py:151) renders `f"{ev['n_devices']} devices in evidence scope"` and §1 (crd.py:193) renders the row 'Evidence scope (this assessment)' = `f"{ev['n_devices']} devices, …"` = 303 devices. But the prose introducing that row (crd.py:188) defines it: "The evidence scope below is what the assessment actually observed". The assessment actually observed/collected 253 (collection_completeness.summary.complete=253; 50 not collected). So '303 devices in evidence scope' overstates what was observed by 50. The CRD's own §2 'Collection completeness' row (crd.py:216-218) correctly shows "253 complete / … / 50 not collected", so the document self-contradicts: evidence scope=303 devices vs 253 complete/50 not collected.
- fix: Separate inventoried from observed. Keep the 'Devices' row in §2 as the inventory total (303 is fine there), but make the 'Evidence scope (this assessment)' figure the collected count: read `coll = (snap.get('collection_completeness') or {}).get('summary') or {}` (already available as ev['coll']) and use `coll.get('complete', ev['n_devices'])` for the evidence-scope device number, or relabel the title/row to '303 devices inventoried (253 collected)'.
- test: test_crd/test_deliverables: render CRD from AJ snapshot; assert the 'Evidence scope (this assessment)' row's device number equals collection_completeness.summary.complete (253) when complete != inventory, and that no paragraph pairs the literal inventory total with the phrase 'evidence scope' / 'actually observed'.

## [Medium/high] EX-crd-002
- file: cisco_toolkit/crd.py:380  (crd.py §8 Design-Driven Requirements table)
- claim: The §8 table renders columns [REQ-D, Design requirement, Driver/evidence, CCDE basis, Confirmed?] but omits each decision's priority, even though every recommended blueprint decision carries a priority field. On the canonical AJ blueprint the 30 recommended decisions split 5 Critical / 21 High / 4 Medium — yet a customer reading §8 cannot tell the Critical 'introduce first-hop redundancy / secure management' requirements from a Medium one. A real CRD prioritizes requirements (priority / criticality is a standard, expected requirement attribute, orthogonal to the RFC 2119 Class which states obligation strength, not business priority). The data exists in the snapshot and is silently discarded.
- fix: Add a 'Priority' column to the §8 table sourced from d.get('priority') (Critical/High/Medium), e.g. insert it after the requirement text: headers become [REQ-D, Design requirement, Priority, Driver/evidence, CCDE basis, Confirmed?] and the row adds `d.get('priority') or '—'`. Adjust widths to keep the table within page width.
- test: Extend test_crd_gains_design_driven_requirements_section: the fixture already has a Critical recommended decision; assert the §8 table contains a 'Priority' header and that the REQ-D-001 row cell text includes 'Critical', so dropping the priority column fails the test.

## [Medium/high] EX-crd-003
- file: cisco_toolkit/crd.py:351  (crd.py REQ-D id assignment)
- claim: REQ-D IDs are assigned positionally: `req_d = [(f"REQ-D-{i:03d}", d) for i, d in enumerate(rec, 1)]`, where rec is the recommended subset of design_blueprint['decisions']. That list is sorted in design_advisor.compute_design_blueprint by (PRANK[priority], -evidence.count, id) when no requirements are supplied (line 2042-2043). So REQ-D numbering depends on priority bucket AND evidence count AND the firing detector set — all of which change between assessment runs. If a new detector fires (the engine added several recently: the MEMORY notes blueprint 24→32→38→39 decisions across sessions) or an evidence count shifts, the sort reorders and REQ-D-005 silently re-points to a DIFFERENT requirement. Meanwhile each decision already carries a STABLE semantic id (e.g. 'mgmt-secure-protocols-and-rbac') that the CRD never surfaces. A real CRD requires immutable requirement IDs: §7 traceability matrices, customer sign-offs and CRD revisions reference REQ-IDs across time, and a moving ID breaks that contract.
- fix: Derive a stable REQ-D suffix from the decision's immutable id rather than the loop index. Either render REQ-D-<decision.id> directly, or keep the REQ-D-NNN form but pin the numbering by sorting rec on d['id'] (a content key) before enumerate so the same decision always gets the same number for a given decision-set, and surface the decision id in the §8 table so the mapping is auditable. Whichever, the chosen scheme must be deterministic from decision content, not from sort position.
- test: Add test_crd_reqd_ids_are_stable_under_reordering: build two snapshots whose design_blueprint.decisions lists contain the SAME recommended decisions in DIFFERENT orders (e.g. swap two decisions, or add a higher-priority decision that shifts the rest), render both CRDs, and assert the REQ-D id mapped to a given decision id (e.g. 'mgmt-secure-protocols-and-rbac') is identical in both documents — the current positional scheme fails this.

## [Medium/high] HON-crd-overclaim-2
- file: cisco_toolkit/crd.py:213  (CRD .docx §2 Current Environment Summary — "Gateway SVIs / FHRP-protected VLANs" row)
- claim: §2 renders f"{ev['n_l3']} / {len(ev['fhrp_vlans'])}" where ev['n_l3'] = len(l3_forwarding) = 231 and len(fhrp_vlans) = 0 on the canonical AJ snapshot. The canonical posture is "FHRP gateway redundancy = 0 of 209 instances" (sourced from remediation_plan.summary.by_category.FHRP = 209, the gateway-instance denominator carried by the executive brief / remediation plan). The CRD recomputes its own denominator (231 raw l3_forwarding rows) instead of reading the canonical 209, so a reader cross-referencing the CRD against the workbook/remediation plan sees a different base count for the SAME FHRP fact (the FHRP=0 numerator is consistent, but the denominators 231 vs 209 do not reconcile — the §2 lead-in promises "every number here reconciles to it"). Secondary: the label pairs "Gateway SVIs" (counted as SVI rows, 231) with "FHRP-protected VLANs" (counted as distinct VLANs via len(fhrp_vlans)) — two different units printed as "X / Y".
- fix: Either (a) read the canonical FHRP gateway-instance denominator the rest of the surfaces use — e.g. n_fhrp_instances = (snap.get('remediation_plan') or {}).get('summary',{}).get('by_category',{}).get('FHRP') — and render "FHRP-protected gateway instances: {protected} of {n_fhrp_instances}"; or (b) keep the SVI count but fix the units/label to "Gateway SVIs (rows): 231 · FHRP-protected: 0 (0 of 209 gateway instances)" so the SVI-row count, the VLAN count, and the canonical instance denominator are each labeled and the canonical 209 is shown. Do not mix SVI-rows and VLAN-counts in one "X / Y" cell.
- test: In tests/test_crd.py, build a snapshot where l3_forwarding has more rows than remediation_plan.summary.by_category.FHRP (e.g. 5 SVI rows, FHRP instances=4, 0 protected); render the CRD; assert the rendered FHRP statement references the canonical instance denominator (contains "4") and is not a bare "5 / 0" — so the CRD denominator tracks the canonical FHRP base, not len(l3_forwarding).

## [Medium/high] SSOT-device-deliv-02
- file: cisco_toolkit/deck.py:159 and 176  (Executive PPTX — slide 1 title fallback + slide 2 'Fleet posture' stat callout)
- claim: deck.py:159 builds the slide-1 subtitle fallback `f"{scale.get('n_devices','—')} devices · {scale.get('n_endpoints','—')} endpoints assessed"` where `scale.n_devices`=303 (canonical inventoried). deck.py:176 renders the slide-2 KPI `stat(s, …, len(hs), "switches assessed")` where `hs = snap.get('health_scores')` so `len(hs)`=303. Both label 303 as 'assessed', but the engine itself marks 50 of those 303 as un-assessable: health_scores has exactly 50 rows with band=='Insufficient Data' (data_quality 0.0), whose host set equals the 50 not-collected/blind devices. 253 is the assessed/collected count. On the customer-facing deck there is no nearby qualifier, so the headline overstates assessment coverage by 50 devices.
- fix: Relabel to the inventory/scope wording for the 303 figure ("devices in scope" / "switches in scope"), OR show the collected count for an 'assessed' claim. For the KPI, prefer `complete = (snap.get('collection_completeness') or {}).get('summary',{}).get('complete')` and render `stat(…, complete, "switches assessed")` (falling back to len(hs)); for the subtitle change 'endpoints assessed' → 'devices in scope · N endpoints evidenced' or use the collected count.
- test: Add a deck assertion (test_deck / test_deliverables) that the slide carrying the word 'assessed' next to a switch/device count uses collection_completeness.summary.complete (253), not len(health_scores)/scale.n_devices (303): render the deck from the AJ snapshot, pull text frames, assert the 'switches assessed' callout value == complete and != len(hs) when complete != len(hs).

## [Medium/high] EX-deck-02
- file: cisco_toolkit/deck.py:176  (deck.py — Fleet-posture slide (2), stat 'switches assessed')
- claim: Slide 2 labels `len(hs)`=303 as 'switches assessed' (L176), but 50 of those 303 were NOT collected (canonical n_collected=253; the 50 surface only as the 'Insufficient Data' health band on the same slide). The deck never states the collected-vs-inventoried coverage ('253 of 303 collected') as an executive headline, so a reader takes '303 switches assessed' at face value. The canonical scale the lane references (303 inventoried / 253 collected) is only half-represented. Coverage honesty is an explicit guardrail (CLAUDE.md: "'Not observed' never silently becomes 'healthy'"); for an exec deck, the collection gap on the DS/CS core (whose redundancy is therefore UNKNOWN) is exactly the caveat that belongs up front.
- fix: Change the stat label or add a sibling stat that reads canonical coverage, e.g. show `253 collected` (or `len(hs) - band_counts['Insufficient Data']`) with sub-label 'of 303 inventoried' so the coverage ratio is explicit. Source 253 from a canonical field (prefer publishing n_collected into executive_brief.scale and reading it) rather than re-deriving.
- test: Assert the Fleet-posture slide text contains both the inventoried count and the collected count (e.g. '303' and '253') when the snapshot carries an 'Insufficient Data' band, so the coverage gap can't silently collapse to a single 'assessed' number.

## [Medium/high] EX-deck-03
- file: cisco_toolkit/deck.py:190 and 389  (deck.py — Fleet-posture 'HEADLINE BY AXIS' (axes[:4]) + Where-to-start (top_gating[:5]))
- claim: The deck exposes per-axis findings in exactly two places, both truncated: slide 2 renders `axes[:4]` (L190) and slide 8 renders `top_gating[:5]` (L389). On the AJ snapshot the brief publishes 13 axes; the 4 Criticals fill slide 2 and the top 5 gating headlines fill slide 8, so the HIGH-severity axes ranked 5-8 — Segmentation ('flat L3 · gateway-ACL 0.0%'), Multicast/timing ('1 MAC clash · 13/13 PTP dormant'), Operational logs ('3788 critical events on 253 devices'), Software risk ('109 exposed advisory surfaces') — appear on NO slide as named risks. These are material executive risks (flat L3 / no segmentation and a multicast MAC clash are even named in posture_statement) yet are silently dropped. There is no Segmentation/Multicast/QoS/Software/Logs slide (only 9 headers exist).
- fix: Either (a) raise the truncation so every Critical/High axis is represented (e.g. render all axes with severity in {Critical,High} on slide 2, paginating if needed), or (b) add a compact 'All assessment axes' table slide listing each axis + severity chip + headline so no High-severity category is silently dropped from the executive view. Keep reading the canonical `eb['axes']`/`eb['top_gating']` (no recompute).
- test: Build a fixture with ≥6 High-or-Critical axes and assert the rendered deck text contains a token from EACH High/Critical axis headline (e.g. 'gateway-ACL', 'MAC clash', 'advisory surface'), so dropping a gating axis off the deck turns the test red.

## [Medium/high] EX-hld-01
- file: cisco_toolkit/design.py:262-269 (also 197, 266)  (design.py HLD §2.4 Resilience & redundancy)
- claim: The vPC/MLAG resilience design in the HLD is reduced to a single integer `len([h for h,v in vpc.items() if v])`. The snapshot's `vpc` axis is far richer: per-domain `domain_id`, `role`, `peer_status`, `keepalive_status`, `consistency`, `peer_link{port,status,vlans}`, `num_vpcs`, and a per-member `vpcs[]` list with `status`. On the AJ fleet this is 20 vPC domains with 668 down/suspended member legs — a first-order resilience fact for a migration. The real ACI HLD devotes whole sections/tables to the vPC domain: '_aci_conv.txt' line 23-24 'vPC Domain policy (Define the Peer Dead interval) … set by default at 200ms', line 22 'Graceful Convergence – Enables LACP graceful convergence'. Our HLD body never surfaces the peer-link, keepalive, consistency state, or the per-domain member health, even though `design_advisor.py:306-342 _d_vpc_health` already computes vpc_legs_down/vpc_dom_bad/vpc_legs_incons from this same data (it reaches §4 as a decision, but §2 — the architecture-intent body — shows only a count).
- fix: Add an HLD §2.4 sub-table 'vPC / MLAG domains' that, for each vpc domain, lists domain_id, role, peer_status, keepalive_status, consistency, peer-link port/status, num_vpcs, and a derived 'member legs down/susp' count — reading the SAME `sig` that `_d_vpc_health` already produces (or recomputing inline from snap['vpc']) so the §2 body and the §4 decision cannot drift. Keep it count-only when snap['vpc'] is empty (older snapshots degrade gracefully).
- test: Build the design DOCX from the AJ snapshot fixture; assert the rendered text contains a 'vPC' domain table with at least one `domain_id` value and the down/suspended member-leg total (668 on the AJ snapshot), and that with `snap['vpc']={}` the section degrades to the existing count line without raising.

## [Medium/high] EX-hld-02
- file: cisco_toolkit/design.py:296-303  (design.py HLD §2.6 Segmentation & security posture)
- claim: §2.6 describes security posture using ONLY non-default VRF count and ACL-on-SVI count from `_segmentation_facts`. The snapshot's `security` axis is a full CIS hardening register: per host `{findings:[{id,title,severity,status(pass/fail),cis_ref,remediation}], summary}` covering password-encryption (CIS 1.1.1), AAA new-model, SNMPv3, etc. A real AS HLD states the management-/control-plane hardening intent (the exemplar covers CoPP control-plane protection and OOB management hardening). Our HLD body never summarizes how many devices fail which CIS controls, so the architecture-intent document is silent on the hardening baseline even though the data and the `_d_harden` detector (design_advisor.py:628) both exist.
- fix: Extend §2.6 with a hardening-posture summary table built from snap['security']: aggregate per-control fail counts (group by finding id/cis_ref where status=='fail'), e.g. 'CIS 1.1.1 password-encryption — N of M devices fail'. Render only failing/notable controls; cite cis_ref. Read the same security axis `_d_harden` uses so §2.6 and the §4 hardening decision stay one source.
- test: Render the design DOCX from a fixture whose security axis has at least one status=='fail' finding; assert §2.6 lists that control's title + cis_ref + fail count, and that an all-pass security axis yields a 'no hardening gaps observed' line rather than an empty/None table.

## [Medium/high] EX-hld-03
- file: cisco_toolkit/design.py:237-257 (§2.3 Layer-3 design)  (design.py HLD §2.3 Layer-3 design)
- claim: §2.3 reports routing protocols as neighbour counts and lists redistribution edges, but never states convergence / fast-failure-detection design intent. The real ACI HLD's L3 core is built around exactly this: BFD global policy (_aci_conv.txt:25-34, TABLE 96/97 detect-multiplier 3 / 50ms intervals), IS-IS NSF & graceful restart (RFC 3847, line 38-42), LDP graceful restart (line 60-64), BGP HA/graceful-restart (line 70-74). Our `design_blueprint.doctrine` even contains the reference principles for this (domains 'igp','bgp','evpn','segment-routing' are present), but the HLD body never renders a convergence-design subsection that frames target detect/restart intent against the observed protocols (OSPF/EIGRP/BGP). Coverage-honest framing applies (we don't collect BFD timers), but the design INTENT (introduce BFD + graceful-restart on the migrated L3 core) is exactly the kind of reference-doctrine §2.3 should state.
- fix: Add §2.3 sub-paragraph 'Convergence & fast-failure-detection design' that, when dynamic routing is observed, states the target intent to introduce BFD (sub-second detect) + graceful-restart/NSF on the migrated L3 core, pulling the matching reference principles from `bp['doctrine']` (igp/bgp/evpn) so it is grounded in the doctrine KB, clearly labelled design-intent/reference (not asserted-from-evidence, since BFD timers aren't collected).
- test: Render the design DOCX from a fixture with an OSPF or BGP adjacency in routing_neighbors; assert §2.3 mentions 'BFD' and 'graceful restart'/'NSF' as target convergence intent, and that a static-only fixture (no adjacencies) omits the convergence-intent paragraph.

## [Medium/high] EX-hld-04
- file: cisco_toolkit/design.py:200-333 (entire §2 HLD; no §2.x management section)  (design.py HLD §2 (missing management-plane section))
- claim: The exemplar dedicates significant HLD content to the management plane: NTP servers with poll intervals and management EPG (TABLE 38/39), OOB management addressing, SPAN/ERSPAN configuration elements (revision note line 149 'Table 13: SPAN and ERSPAN configuration elements'), and a centralized-orchestration/API matrix (TABLE 373). Our HLD §2 has NO management-plane subsection at all. `design_blueprint` carries a 'management' doctrine domain AND `_d_mgmt` (design_advisor.py:615) produces a `management` decision that reaches §4 — but the architecture-intent body never states the management/monitoring/OOB design. (Per-device NTP/SNMP/AAA telemetry is not in service_map for this snapshot, so the section should be doctrine-/intent-framed, not asserted from absent evidence.)
- fix: Add HLD §2.8 'Management, monitoring & out-of-band' driven by the 'management' doctrine domain (+ any `_d_mgmt` decision), stating the target intent for OOB management, NTP/time sync, SNMP/telemetry, and SPAN/ERSPAN visibility. Where per-device evidence is absent, label it design-intent/reference rather than observed.
- test: Render the design DOCX; assert a 'Management' HLD heading exists and references OOB/NTP/monitoring intent; assert it still renders (from doctrine) when service_map has no ntp/snmp keys, without raising.

## [Medium/medium] EX-hld-05
- file: cisco_toolkit/design.py:259-269 (§2.4) ; 351-371 (§3.2)  (design.py HLD §2.4 / LLD §3.2)
- claim: The exemplar treats physical/port policy as explicit HLD design: errdisable recovery policy (TABLE 51/55/56 — recovery interval 300s, loop-detection), interface debounce intervals (TABLE 59 — debounce 10), and LACP graceful convergence. Our HLD/LLD never states these L1-resilience design choices, even though the engine collects the underlying evidence (snapshot has `physical_health` (list), `link_phy`, `trunk_native`, and MEMORY-noted detectors fired 860 dirty ports / 451 native-VLAN-1-on-trunks / 84 degraded port-channel members via `_d_*` in design_advisor.py). §2.4 resilience is gateway/FHRP/vPC-count only; the port-hardening design intent (errdisable-recovery, native-VLAN discipline, restore degraded LAG members) is absent from the architecture body.
- fix: Add an HLD §2.4 (or LLD §3.x) 'Physical / port-level resilience policy' subsection stating target intent for errdisable auto-recovery, native-VLAN discipline on trunks, and restoration of degraded port-channel members — grounded in the existing physical_health/trunk_native detectors so the design body reflects the L1 findings, not just gateways.
- test: Render the design DOCX from the AJ fixture; assert a physical/port-policy subsection mentions errdisable recovery and native-VLAN/trunk hygiene, and that it summarises the degraded-LAG-member count when snap['vpc']/link_phy carry down members; assert graceful omission when those axes are empty.

## [Medium/high] EX-lld-01
- file: cisco_toolkit/design.py:399-402  (design.py LLD §3.3 Per-device build detail)
- claim: The §3.3 'Per-device build detail' loop hard-caps output at 40 devices (`if shown >= 40: break`). On the canonical AJ fleet, 253 of 303 devices are eligible (carry an SVI, a trunk uplink, or a port-channel), so the LLD silently emits build detail for at most 40 and truncates the remaining 213 (84%) with a one-line 'truncated … full per-port data is in the workbook'. An LLD's entire reason to exist is the per-device build record a re-implementation must reproduce; deferring 84% of the fleet to the workbook means the document is not an LLD for those devices. A real AS LLD (cf. _aci_conv.txt policy-group / per-leaf tables) carries build detail for every in-scope node, not a 40-device sample.
- fix: Remove the 40-device cap (or raise it to cover the whole fleet, as §3.1 already does). If document length is a concern, keep all devices but compress each to a single dense row in a table (Hostname | #SVIs | #Uplinks | #Po) rather than dropping 84% of nodes. The full per-port facts are cheap to render and are exactly the LLD's payload.
- test: In tests/test_design.py, build a snapshot with 60 L3 hosts each owning a distinct SVI (Vlan10 … Vlan69), render the design DOCX, extract text, and assert that the 50th host's hostname (e.g. 'SW050') appears in the §3.3 body — fails today because `shown>=40` breaks before host 50.

## [Medium/high] EX-lld-02
- file: cisco_toolkit/design.py:387-388,397  (design.py LLD §3.3 Port-channels line)
- claim: §3.3 builds the port-channel line from `pos.append(p)` where `p.startswith('Po')`, then renders `', '.join(sorted(set(pos)))` — i.e. just the bundle names (Po1, Po2, …). The snapshot carries, per interface, `port_channel` (which bundle each physical member belongs to) and `port_channel_protocol` (LACP 'Active' vs static 'On') for 4460 member assignments / 4393 with protocol fleetwide, but the LLD never renders member-port→bundle mapping or the bonding protocol. A build engineer cannot reconstruct an EtherChannel from a bare 'Po10' — they need the member interfaces and whether it is LACP or mode-on. _aci_conv.txt makes the bonding/policy-group an explicit per-object table (Table 98 leaf policy-group, Table 59 debounce per speed); our LLD has no equivalent.
- fix: In §3.3, when emitting a port-channel, attach its member interfaces (the ports whose `port_channel`==bundle) and the protocol (LACP/PAgP/static from `port_channel_protocol`), e.g. 'Po10 (LACP): Te1/49, Te1/50'. Optionally fold vPC consistency/peer-link status from snap['vpc'] for L3 nodes.
- test: Build a snapshot where host H has Po10 plus members Te1/49,Te1/50 each with port_channel='Po10', port_channel_protocol='Active'; render and assert the §3.3 text for H contains both 'Te1/49' and 'LACP' (or 'Active') — fails today (only 'Po10' is emitted).

## [Medium/high] EX-lld-03
- file: cisco_toolkit/design.py:385-386,395  (design.py LLD §3.3 Trunk uplinks line)
- claim: §3.3 renders each trunk uplink as `f"{p}→{d.get('cdp_neighbor')}"` — port and CDP neighbor only. The per-interface `trunk_allowed_vlans` (1699 ports) and `trunk_native_vlan` (451 ports) are collected but never rendered. The allowed-VLAN set and native VLAN on every trunk are core LLD build facts: a re-implementation must reproduce exactly which VLANs each uplink carries and what the native VLAN is (a native-VLAN mismatch is a known L2 hazard the canonical posture already tracks). Showing only 'Gi1/0/1→core-sw' is insufficient to rebuild the trunk.
- fix: Add the trunk scope to each uplink line, e.g. 'Gi1/0/1→core-sw [native 1, allowed 10,20,30]', or emit a per-device trunk table (Port | Neighbor | Native | Allowed VLANs). The data is already on each interface dict.
- test: Snapshot with a trunk port (switchport_mode='trunk', cdp_neighbor='core', trunk_native_vlan='99', trunk_allowed_vlans='10,20'); render and assert §3.3 text contains '99' and '10,20' for that host — fails today.

## [Medium/high] EX-lld-04
- file: cisco_toolkit/design.py:334-402  (design.py LLD §3 (entire Low-Level Design))
- claim: A real AS LLD includes a cabling / connectivity matrix (local-device:local-port ↔ remote-device:remote-port, with media/speed) so the physical build can be patched and verified. The snapshot carries `neighbor_port` alongside `cdp_neighbor` for 1421 links (plus `speed`, `link_type`, `neighbor_platform`), but the LLD never emits an A-side/B-side cabling table. §3.3 surfaces uplinks as 'port→neighbor' (local port + remote DEVICE only, no remote PORT), which is not a cabling matrix. Build/NRFU engineers cannot validate patching from this.
- fix: Add an LLD §3.x 'Inter-switch cabling matrix' table: Local device | Local port | Speed/Media | Remote device | Remote port — populated from interfaces[h][p].{cdp_neighbor, neighbor_port, speed, link_type}. Cap/paginate like the other large tables and point to the workbook for the full set.
- test: Snapshot with host A port Te1/1 having cdp_neighbor='B', neighbor_port='Te1/2'; render and assert the design text contains both 'Te1/1' and 'Te1/2' on a cabling line for A↔B — fails today (no cabling table exists).

## [Medium/high] EX-lld-06
- file: cisco_toolkit/design.py:620-629  (design.py §5.4 wave plan (the only move-group surface))
- claim: The only place move-groups appear in the design doc is §5.4 'Candidate migration wave plan', which renders Wave | Kind | n_switches — counts only. `snap['move_groups']` is a list of {switches:[...]} (53 groups on AJ, including one 251-switch L2-coupled group + 52 singletons) and is the LLD-relevant migration cut-detail: an LLD/MOP-feeder must state which devices move together (the L2 adjacency domain that must cut as a unit). The per-group device membership is never tabulated anywhere in the design document.
- fix: Add an LLD move-group detail table (Group | #Switches | Member switches) reading move_groups[].switches, paginated for the large group. This is the migration build-detail an LLD is expected to hand to the MOP. Keep it candidate/requirement-gated consistent with §5.
- test: Snapshot with move_groups=[{'switches':['SWA','SWB']}]; render and assert the design text contains both 'SWA' and 'SWB' on a move-group line — fails today (membership is never emitted, only counts).

## [Medium/high] HON-design-unguarded-1
- file: cisco_toolkit/design.py:178  (HLD/LLD design DOCX (§1 Executive Design Summary, design headline))
- claim: In write_design_doc_docx, line 178 `if bp.get("summary", {}).get("headline"):` uses the dict-default-arg idiom, which only substitutes `{}` when the key is ABSENT. When design_blueprint exists but carries `summary: None` (or any non-dict) — a shape an older/hand-edited/JSON-loaded snapshot can carry — `bp.get("summary", {})` returns None and the chained `.get("headline")` raises AttributeError, aborting the whole document render. This directly contradicts the module docstring (lines 16-18: 'Every snapshot read is defensive (.get / tolerant of missing keys) so an older snapshot degrades gracefully') and is internally inconsistent with line 552 `if (ts.get("summary") or {}).get("headline"):`, which performs the identical `.summary.headline` read on a blueprint-shaped dict but uses the SAFE `or {}` idiom. Every other design_blueprint read in the file (lines 112, 461, 473, 509, 513, 548) uses `or {}`/`or []`; line 178 is the lone outlier. PROVEN: a live call of write_design_doc_docx with `design_blueprint={'summary': None, ...}` raises `AttributeError: 'NoneType' object has no attribute 'get'` with the traceback landing at design.py:178. Blast radius: the webapp deliverables path (webapp/backend/deliverables.py:104-114) catches-then-RE-RAISES, so this becomes an unhandled HTTP 500 on the design-download endpoint; the CLI path (COLLECT_PARSE_V3_23_0.py:2130-2133) catches and logs a warning, so the design DOCX is SILENTLY not produced (a missing deliverable).
- fix: Replace the dict-default-arg form with the file's own `or {}` idiom so a non-dict summary degrades to empty: change line 178-179 to `_sm = bp.get("summary") or {}` then `if _sm.get("headline"):` and use `_sm["headline"]`. This mirrors the safe twin at line 552 and the explorer's `bp.summary||{}`.
- test: Add tests/test_design.py::test_design_tolerates_non_dict_blueprint_summary: build _snap(), set snap['design_blueprint'] = {'summary': None, 'decisions': [], 'tradeoff_scorecard': []}, call write_design_doc_docx(out, snap, 'X') and assert os.path.exists(out) (must not raise). Parametrize summary over None, [], 'str' so the regression locks the truthy-non-dict case too. Mirrors the existing test_design_tolerates_malformed_stp_roots pattern.

## [Medium/high] R2-3-02
- file: cisco_toolkit/design.py:143; crd.py:150; mop.py:192; runbook.py:151; engagement.py:209; archreview.py:1059; ops.py:114; docmeta.py:160  (All 7 DOCX deliverable covers + the shared Revision-history row)
- claim: Each DOCX generator independently calls `datetime.now().strftime('%Y-%m-%d %H:%M')` for its cover 'Generated' line (design.py:143, crd.py:150, mop.py:192, runbook.py:151, engagement.py:209, archreview.py:1059, ops.py:114), and docmeta.py:160 independently stamps `datetime.now().strftime('%Y-%m-%d')` into the shared Revision-history row of every doc. There is no shared 'generation moment' passed into the writers, so each document anchors to its own wall-clock at the instant it is written. In the current on-disk batch all 7 covers happen to read 2026-06-21 06:58 (regen finished inside one minute), so the drift is not presently visible — but it is structurally guaranteed: any multi-minute regen, a slow doc, an interrupted/resumed run, or crossing a minute/midnight boundary produces a SET whose members claim different generation times for one body of evidence. No test locks cover-stamp consistency across the family.
- fix: Make generation-time single-source. Compute the generation timestamp once (prefer reusing the snapshot's generated_at so cover, revision row and the workbook all agree) and thread it through: add a `generated_at`/`now` kwarg to each writer's cover block and to add_document_control's revision-history stamp (docmeta.py:160) instead of recomputing datetime.now() at each site. The orchestrator that emits the set passes one value to all writers.
- test: Add tests/test_temporal_consistency.py that generates the full deliverable set from one snapshot (monkeypatching datetime.now to return increasing values across calls to simulate a multi-second regen) and asserts every doc's cover 'Generated' string and every Revision-history date are identical — proving they read one shared anchor, not per-call now(). Reverting any writer to datetime.now() must turn it red.

## [Medium/high] DET-fhrp-state-01
- file: cisco_toolkit/design_advisor.py:96-102, 562-572  (design blueprint (design_advisor) vs workbook/punch-list)
- claim: compute_fhrp_consistency() (excel.py:2374-2421) DOES reconstruct FHRP state from the behavior strings and detect split-active ('two active routers -- split-brain', L2417-2418), mixed protocols (L2411-2412), different groups (L2413-2414) and same-group-different-VIP (L2415-2416). That output is published to the snapshot as snap['fhrp'] (analyze.py:1791, 2036) and reaches BOTH the punch-list (analyze.py:3722-3736, templated as 'Fake FHRP redundancy', High) and the Excel 'FHRP Consistency' sheet. BUT the design-blueprint signal builder _no_fhrp_vlans() (design_advisor.py:96-102) filters snap['fhrp'] to ONLY issues whose text contains 'no fhrp'/'first-hop'/'first hop'; every other issue type (split-active, mixed-protocol, group-mismatch, VIP-mismatch) is silently dropped. _d_fhrp() (L562-572) therefore fires ONLY on the no-redundancy case. There is no design_kb principle and no _d_* detector for 'fix split-active / consolidate FHRP protocol+group', so the DESIGN layer (HLD/LLD/CRD design decisions) can never recommend remediating a broken-but-present FHRP pair, even though the engine already knows about it. (Verified: grep for split-brain/mixed-protocol/preempt in design_advisor.py returns only an unrelated comment at L305; design_kb.py has no split-active FHRP principle.)
- fix: Add a sibling signal + detector. In _signals(): build sig['fhrp_broken'] = [g for g in snap['fhrp'] if any of its issues contains 'split-brain'/'two active'/'mixed FHRP'/'different FHRP groups'/'different virtual IPs'] (i.e. has-FHRP-but-misconfigured, the complement of _no_fhrp_vlans). Add _d_fhrp_state(snap, sig): if sig['fhrp_broken'] emit an engine_actionable=True DesignDecision (e.g. id 'fhrp-state-consistency', priority High) citing fhrp[].issues / fhrp[].members, recommending 'consolidate to a single FHRP protocol + group + VIP per gateway VLAN and ensure exactly one Active'. Register it in _DETECTORS (L1076) and add a matching engine_actionable principle in design_kb so the blueprint carries it through to HLD/LLD/CRD.
- test: Add tests/test_design_addenda.py case: feed a synthetic snap with snap['fhrp']=[{'vid':10,'issues':['two active routers (A, B) -- split-brain'],'members':[{'host':'A','fhrp':True,'role':'active'},{'host':'B','fhrp':True,'role':'active'}]}] and assert the produced design blueprint contains a decision with engine_actionable True whose id/title references split-active; also assert the legacy _d_fhrp (no-FHRP) is NOT triggered by this input (regression that the two signals are disjoint).

## [Medium/high] DET-ntp-002
- file: cisco_toolkit/design_advisor.py:728-739, 163-164, 33  (engine (design_advisor detector layer))
- claim: The only NTP-aware detector, `_d_timesync` → decision `mgmt-time-sync-logging-baseline` (design_advisor.py:728), derives its signal purely from config-presence findings: `_TIMESYNC_FAIL_IDS = {no-ntp, no-logging}` (L33) feeding `sig['timesync_devices']` (L163-164), which fires ONLY when the `ntp server`/`ntp peer` line is absent. There is NO detector that fires on: (a) clock UNSYNCHRONIZED despite a configured server, (b) stratum-16/insane stratum, or (c) a single (non-redundant) NTP source. Consequently 236 of 253 collected devices that carry an `ntp server`/`ntp peer` line pass `no-ntp` and are scored fine — even though a device whose configured server is unreachable would be running an unsynchronized clock. This is exactly the 'not observed silently becomes healthy' false-health class the CLAUDE.md doctrine forbids, and the detector's own NRFU verify text (design_advisor.py:2118: "...SYNCHRONISED on all devices ('show ntp status' → clock synchronized)"; :2175: "'*' (synced) stratum") asserts a sync verification the engine has no evidence to back.
- fix: Once DET-ntp-001 lands a parsed ntp_state, add an engine_actionable detector `_d_ntp_sync` (design_advisor.py) that FIRES when, among COLLECTED devices with ntp_state present: clock is unsynchronized (synced is False) OR only one usable/selected source exists (no redundant peer). Cite the parsed evidence field (ntp_state.synced / sources). Keep it independent of the config-presence `no-ntp` so a configured-but-unsynced clock is no longer silently healthy. Also soften the `mgmt-time-sync-logging-baseline` NRFU/verify wording (design_advisor.py:2118/2175) so it only claims to verify sync where ntp_state was actually collected.
- test: tests/test_design_addenda.py (or new): build a snapshot where one collected host has `ntp server` configured but ntp_state.synced is False, and another has exactly one selected source → assert a decision with id 'ntp-sync-redundancy' (or chosen id) is emitted, carries engine_actionable True, and lists those hosts in evidence.devices; assert a fully-synced dual-source host does NOT appear.

## [Medium/high] DET-aaa-02
- file: cisco_toolkit/design_advisor.py:615-625 (_d_mgmt); design_kb.py:3326 + 3329-3331  (design_blueprint.decisions / HLD-LLD-CRD-deck design layer)
- claim: There is no AAA-specific detector. The lane's question (is AAA assessed for local fallback / command authorization / dead-time as a DESIGN decision?) is answered No. The only AAA-aware design output is `_d_mgmt` (`mgmt-secure-protocols-and-rbac`), a generic catch-all that fires on ANY of 6 mgmt CIS fail-ids (_MGMT_FAIL_IDS at design_advisor.py:27 includes only the binary 'no-aaa'). Its statement says nothing about TACACS+ servers, server reachability/dead-time, command authorization, or local-fallback/lockout risk. The dedicated AAA design principle `security-aaa-routing-auth-antispoof` (design_kb.py) is explicitly marked engine_actionable:false and listed in `_NOT_YET_AUTO_DETECTED` (design_kb.py:3326, comment 'AAA drives mgmt-secure-protocols; routing-auth/uRPF not collected'), so it never becomes a fired decision (verified: it is absent from the 39 AJ blueprint decisions). Meanwhile the evidence to drive a real AAA design decision IS collected in `show running-config` and thrown away — parse_security extracts only the boolean `aaa new-model`. Across the 253 collected AJ configs: 253 carry `tacacs-server host`/`aaa group server tacacs+`; 193 IOS devices carry `aaa authorization commands` (60 do not); 36 carry an explicit `deadtime`; and 40 devices run `aaa authentication login default group <grp>` with NO trailing `local` keyword = a real AAA design risk (if every TACACS+ server is unreachable those devices have no working login path / lockout). None of {tacacs servers, deadtime, server-private, local-fallback, command-authz} reach the snapshot's parsed evidence (snapshot security subtree: 'tacacs' False, 'deadtime' False; the only 'tacacs'/'local fallback' strings in the whole snapshot live in design_blueprint/design_nrfu doctrine TEXT, not evidence).
- fix: Add an `aaa_audit` axis (parse layer: capture per-device {aaa_present, method (tacacs+/radius/local), n_tacacs_servers, n_radius_servers, has_local_fallback (login default ends in `local`/`local-case`), has_command_authz (`aaa authorization commands`/`config-commands`), deadtime_set, accounting_set}) WITHOUT storing keys (already redacted). Then add a design_advisor detector `_d_aaa` (engine_actionable=True) that FIRES on the real gaps: (1) central AAA configured but NO local fallback => lockout risk (40 AJ devices), (2) admin AAA without command authorization (60 AJ devices), (3) single TACACS server / no dead-time tuning. Flip `security-aaa-routing-auth-antispoof` out of `_NOT_YET_AUTO_DETECTED` once it has a firing detector, or split a narrower `security-aaa-fallback-and-command-authz` principle that is genuinely engine_actionable.
- test: tests/test_aaa_audit.py: (a) unit — config with `aaa authentication login default group ACS` and no `local` => has_local_fallback False; with `... group ACS local` => True; (b) detector — assert `_d_aaa` returns a decision with engine_actionable True when has_local_fallback is False for >=1 device, and returns None when all devices have fallback+command-authz; (c) AJ lock — over migration_collection_20260613_063201 assert the no-local-fallback count == 40 and command-authz-absent count == 60 so the numbers can't silently drift.

## [Medium/high] DET-intf-errors-003
- file: cisco_toolkit/design_advisor.py:design_advisor.py:238-241 (_phy_dirty); 840-854 (_d_phys_remediation)  (engine: physical_health[] -> _d_phys_remediation)
- claim: output_drops IS collected and IS present on every physical_health[] record (excel.py:3056,3093), and on the AJ fleet 3122 ports have output_drops>0. The Physical-Health SHEET correctly flags these as 'error-rate-high' (excel.py:3084 includes _intpos(od)). But the design detector's dirty predicate _phy_dirty (design_advisor.py:238-241) checks only crc_errors, input_errors, duplex and err-disable status - it does NOT include output_drops. So 3122 ports with sustained egress drops (a real pre-migration L1/queue health concern and a classic congested-uplink / marginal-link symptom) are visible in the workbook sheet but contribute 0 to the phy_dirty=860 count that drives the design DECISION and the customer-facing 'remediate L1 before cutover' requirement. This is an internal SSOT inconsistency: the sheet's risk model and the detector's risk model disagree on the same field.
- fix: Either (a) add output_drops>0 (oper-up gated, as a Medium/congestion class) to _phy_dirty and the _d_phys_remediation summary + sources, making the detector consistent with the sheet's own error-rate-high flag, or (b) if output_drops is deliberately excluded as congestion-not-fault, document that decision in a comment at design_advisor.py:235 and keep the sheet/detector definitions reconciled. Prefer (a) for coverage but treat drops as a distinct lower-severity class than CRC/input-errors so the High framing of the decision is not diluted.
- test: tests/test_design_blueprint.py: seed a physical_health port with crc_errors=0,input_errors=0,duplex=full,status=connected,output_drops=500 and assert (under fix a) _d_phys_remediation counts it; add an assertion pinning the intended treatment so the sheet (excel.py error-rate-high) and the detector cannot silently drift on output_drops again.

## [Medium/high] DET-capacity-02
- file: cisco_toolkit/design_advisor.py:251-256, 857-871  (detector (design_advisor._d_capacity))
- claim: COVERAGE GAP (real). _d_capacity sizes 'born full' switches purely on physical-port utilisation (capacity[].port_util) plus PoE (capacity[].poe_util). It never considers MAC/CAM-table or ARP-table occupancy, even though both are collected and parsed for every device: show_mac_address-table.txt (827 lines on AS26, 909 on AS28 — the same two switches the detector flags hot) and show_ip_arp.txt (~100 entries/device) are present in migration_collection_20260613_063201, and parse.py:353 parse_show_mac_address_table already extracts dynamic MACs. CAM/MAC and ARP/adjacency table exhaustion is the canonical data-plane scale limit you hit when CONSOLIDATING switches in a migration — the very decision this detector drives — yet it is invisible. The two port-hot switches also carry the highest MAC counts, so a MAC-occupancy axis would be an independent corroborating capacity signal, not redundant.
- fix: Extend compute_capacity (excel.py:564) to also emit per-device mac_entries (sum of dynamic MACs already parsed) and arp_entries, then add a MAC/ARP-occupancy signal in design_advisor (e.g. cap_mac_hot when mac_entries approaches a per-platform CAM band, or simply surface the top-N highest-MAC switches as a 'forwarding-table scale' consideration in the capacity decision). Keep engine_actionable honest: fire only where the per-platform limit is known or where the figure is in the top decile of the fleet. Add 'capacity[].mac_entries' / 'capacity[].arp_entries' to the decision's fields list at design_advisor.py:868.
- test: Add tests/test_capacity_scale.py: build two DevicePhysical-equivalent capacity rows where one has mac_entries near a configured band and the other low; assert the high-MAC switch is reported and the low one excluded; mirror the existing port_util fire/no-fire pattern in test_design_blueprint.py:1154.

## [Medium/medium] DET-capacity-03
- file: cisco_toolkit/design_advisor.py:857-871; analyze.py:4040-4042, 4137  (detector (design_advisor._d_capacity) + collection scope)
- claim: COVERAGE GAP (real, lower-confidence on remediability). The capacity decision has no TCAM/FIB forwarding-table headroom dimension. The only TCAM awareness in the engine is REACTIVE: analyze.py:4040 ('resource' doctrine, 'TCAM / forwarding-table exhaustion messages recorded') and the syslog classifier at analyze.py:4137 ('TCAM' + EXCEED/EXHAUST/FULL/EXCEPTION) — i.e. it only notices a table problem AFTER the device has already logged an exhaustion event. A migration sizing detector should ask the PROACTIVE question (how full is the FIB/TCAM today, how much runway remains) before consolidating routes/SVIs onto the target. On AJ this currently cannot fire (no 'show platform ... fib/tcam' or 'show ip route summary' route-count is collected — confirmed absent from the collection dir), so this is a measure-then-detect gap, not a silent miss. Flagging at Medium because closing it requires adding collection, not just reading existing evidence.
- fix: Two-step, coverage-honest: (1) add 'show platform hardware fed ... fwd-asic resource ... utilization' (IOS-XE) / 'show hardware capacity forwarding' / 'show system internal forwarding ... utilization' (NX-OS) and 'show ip route summary' to the optional collection command set; (2) parse the FIB/route counts into capacity[].fib_routes / capacity[].tcam_util_pct and add a cap_fib_hot signal that fires only when a real percent-utilisation figure is parsed (engine_actionable=True only on evidence). Until collected, keep the detector silent (do not infer).
- test: Add a parser unit test for the chosen 'show ... utilization' output producing tcam_util_pct, plus a design_advisor test asserting cap_fib_hot fires at >=85% util and is absent when no figure is parsed (mirrors the 'not collected -> no fire' honesty pattern in test_archreview.py).

## [Medium/high] DET-errdisable-002
- file: cisco_toolkit/design_advisor.py:235-248,842-854; cisco_toolkit/analyze.py:322-329,1289-1292,2026-2034:design_advisor.py:245 (phy_errdisable signal); :842 _d_physical decision  (engine: design_advisor detectors + readiness/cross-layer)
- claim: Err-disabled ports ARE detected (8 ports `status:err-disabled` / 4 physical_health err-disabled risk records in AJ), but every consumer treats them identically with NO recovery dimension: design_advisor `_d_physical` folds `phy_errdisable` into a generic L1-remediation decision (design_advisor.py:842-854); analyze.py Findings sheet emits a flat 'Err-disabled (count)' row (:328); migration_readiness flags `no_errdisabled` warn (:1289-1292); cross-layer CL-07 flags an err-disabled port on a gateway switch (:2026-2034); syslog catches the err-disable EVENT (:4027). None of these answer the actionable question 'will this port come back on its own?'. There is no FIRING detector that says: this port is err-disabled AND its cause has no `errdisable recovery` configured → it is permanently down and needs manual intervention before/at cutover. This is the specific actionable gap the exemplar's 'recovery 300s baseline' implies.
- fix: After DET-errdisable-001 publishes recovery config, add an engine_actionable detector (e.g. design_advisor `_d_errdisable_no_autorecovery`, engine_actionable=True) that fires when ≥1 port has status err-disabled and (a) interval/cause cannot be confirmed as recovering that cause, OR globally no `errdisable recovery cause` is configured. Severity High (matches L1W err-disabled weight 8 and the existing 'High' Findings/_d_physical treatment): 'N port(s) are err-disabled and auto-recovery is not configured for their cause → they will not return without manual shut/no-shut; recover the root cause and either configure `errdisable recovery cause <x>` + `errdisable recovery interval 300` or schedule a manual recovery step in the cutover MOP.' Cite physical_health[].status + the new errdisable_recovery field. Keep it coverage-honest: if recovery config was not collected for a host, label 'recovery posture unknown', do not assert 'will recover'.
- test: tests/test_design_blueprint.py refutation-style: snapshot with an err-disabled port + errdisable_recovery={interval:300,causes_enabled:['bpduguard']} where the port's cause is NOT bpduguard → detector FIRES (decision present, engine_actionable True, devices includes the host); same snapshot but causes_enabled covers the cause → detector is SILENT; remove all err-disabled ports → detector DISAPPEARS (mirrors the existing physical_health emit-invariant).

## [Medium/high] DET-dhcp-snoop-01
- file: cisco_toolkit/design_kb.py:3327  (engine: detector / design-advisor + analyze axes)
- claim: There is NO actionable posture detector for the L2 anti-spoofing trust boundary (DHCP-snooping trusted-uplink-vs-untrusted-access, Dynamic ARP Inspection, IP Source Guard). The doctrine principle that owns it, 'security-l2-access-edge-suite' (title: 'Harden the L2 access edge: port-security, DHCP snooping, DAI, IP Source Guard, storm-control, 802.1X'), is High priority but is EXPLICITLY forced engine_actionable=False at design_kb.py:3327 via the _NOT_YET_AUTO_DETECTED set, with the inline reason '# no port-security / BPDU-guard / DHCP-snooping finding collected'. None of the 34 _d_* detectors in design_advisor.py (enumerated 562-1060) emit a DHCP-snooping/DAI/IPSG decision — the nearest, _d_stp_det (design_advisor.py:680-701), covers BPDU-Guard only. analyze.py never consumes the data: a grep for 'dhcp|verify source|arp inspection' over analyze.py returns ZERO matches (the 9 'snoop' hits there are all IGMP/multicast snooping, parse.py:1561/3124). So this trust-boundary axis contributes nothing to any compute_* axis, the executive brief, the punch-list, or the design blueprint.
- fix: Make the trust boundary actionable in two honest steps. (1) The detectable-today slice: the engine ALREADY collects 'show ip dhcp snooping binding' (COLLECT_PARSE_V3_23_0.py:514,568) and parses per-port binding counts (parse.py:2027 parse_dhcp_snooping_binding). Add a _d_dhcp_trust-style detector in design_advisor.py modeled on _d_stp_det (680-701): fire when endpoint-bearing access ports carry DHCP-snooping bindings (or DHCP-server traffic via service_map) yet there is no evidence DHCP snooping/DAI/IPSG is enabled — emitting the 'security-l2-access-edge-suite' decision with engine_actionable=True and a coverage-honest driver. (2) To detect the actual trust state (not just bindings) you must first COLLECT it: add 'show ip dhcp snooping', 'show ip arp inspection interfaces', and 'show ip verify source' to BOTH command lists in COLLECT_PARSE_V3_23_0.py, parse trusted-vs-untrusted per interface into a new per-port field (e.g. dhcp_snoop_trust) — the per-port model at present has NO such field (golden interface record has stp_bpduguard/stp_rootguard/acl_in but no dhcp_snoop_trust/ip_verify_source/arp_inspection) — then the detector fires precisely on untrusted-uplink / missing-DAI/IPSG findings. Until (2) lands, keep the principle's note coverage-honest ('not collected') rather than silently green.
- test: tests/test_design_blueprint.py (or a new tests/test_dhcp_trust_detector.py): build a minimal snapshot whose interfaces include endpoint-bearing access ports with DHCP-snooping bindings present but no snooping/DAI/IPSG enablement evidence; assert the new detector emits a 'security-l2-access-edge-suite' decision with engine_actionable=True and the affected switch list, AND assert the inverse (a fixture with the suite enabled / no bindings) emits nothing — locking both the fire and the no-false-positive arms. Add a guard test asserting parse of 'show ip arp inspection'/'show ip verify source' populates the new per-port trust field once collection is wired.

## [Medium/high] SSOT-device-deliv-04
- file: cisco_toolkit/engagement.py:210 (n_devices defined L120)  (Engagement Workflow & Plan of Record — title page meta line)
- claim: engagement.py:120 sets `"n_devices": len(_as_dict(snap.get("devices")))` = 303 (inventory). engagement.py:210 renders `f"{f['n_devices']} devices in evidence scope"` = '303 devices in evidence scope'. Evidence scope = what was collected = 253. The same module elsewhere reads collection_completeness correctly: §4.3 status (engagement.py:276-277) says "Evidence collected; {len(f['blind'])} device(s) with blind spots" (50 blind) and _verdict (L130) computes `n_blind = partial + not_collected`. So the title-page 'evidence scope' count (303) is inconsistent with the doc's own blind-spot accounting (50 of the 303 have no/partial evidence).
- fix: On the title page use 'devices inventoried' / 'devices in scope' for 303, or render the collected count: `csum = f['csum']` is already available — use `csum.get('complete', f['n_devices'])` for an 'evidence scope' phrasing. Simplest: change the meta string to "{f['n_devices']} devices in scope".
- test: test_deliverables (webapp test_deliverables already pins engagement FAMILY size): assert the engagement title meta does not say 'devices in evidence scope' with the inventory total when complete != inventory; or assert the evidence-scope number == complete (253).

## [Medium/high] EX-engagement-001
- file: cisco_toolkit/engagement.py:210 (meta line); 120 (n_devices source); 273-279 (§2 Assess status)  (Engagement Workflow & Plan of Record .docx — title page + §2 Engagement Phase Tracker)
- claim: The title-page meta line renders `f"{f['n_devices']} devices in evidence scope"` where `n_devices = len(_as_dict(snap.get('devices')))` (engagement.py:120) = the INVENTORIED count (303 on AJ). But on the canonical AJ snapshot 50 of those 303 are status 'not collected' with data_quality=0 and health band 'Insufficient Data' — they have NO collected evidence at all. Calling all 303 'in evidence scope' overstates coverage, and the genuinely-collected count (collection_completeness.summary.complete=253) appears NOWHERE in the rendered document. Verified by rendering the doc on the canonical snapshot: the string '253' is absent; the only coverage figure is '303 devices in evidence scope'. This is the same coverage-honesty class as the flagged past_eos=0 defect: a not-observed/not-collected population silently reads as in-scope. The §2 Assess status (L277) also only says 'Evidence collected; 50 device(s) with blind spots' — it never states the 253-of-303 collected/inventoried split a real AS coverage statement requires. NOTE: `len(devices)` happens to equal the canonical `executive_brief.scale.n_devices` (both 303; scale.n_devices=len(health_scores), and all 303 inventoried hosts receive a health record, the 50 uncollected as 'Insufficient Data') — so this is a WORDING/coverage-honesty gap, not a numeric SSOT drift; the fix should additionally read the canonical scale rather than recompute len(devices).
- fix: Make the coverage figure evidence-honest. Either (a) change the meta line to read the collected/inventoried split from f['csum'] (already in scope), e.g. when f['coll_present']: `f"{f['csum'].get('complete','?')} of {f['csum'].get('inventory', f['n_devices'])} inventoried devices collected"` ("303 inventoried · 253 collected · 50 not collected"); when absent, keep '{n_devices} devices inventoried (collection completeness UNKNOWN)'. And (b) add the collected/not-collected split to the §2 Assess status line (L276-279) so the phase tracker states coverage explicitly. Prefer reading executive_brief.scale.n_devices for the inventoried figure to honour the SSOT contract instead of recomputing len(devices).
- test: Add tests/test_engagement.py::test_engagement_states_collected_coverage_not_just_inventory: build a snapshot with collection_completeness.summary={inventory:303, complete:253, not_collected:50} and 50 'not collected' device rows; render; assert the document text contains both '253' and '303' (the collected and inventoried counts) and that the bare phrase '303 devices in evidence scope' is NOT present (the overstated wording is gone). Add a complementary assertion that when coll_present is False the meta line says completeness UNKNOWN.

## [Medium/high] EX-engagement-002
- file: cisco_toolkit/engagement.py:469 (Risks `f["punch"][:8]`); 504-509 (Issues `f["blind"][:8]`)  (Engagement Workflow & Plan of Record .docx — §5.1 Risks, §5.3 Issues (the seeded RAID log))
- claim: Both RAID seed tables cap at 8 rows with no in-table disclosure of how many findings they are sampling. §5.1 Risks iterates `f['punch'][:8]` (engagement.py:469) then keeps Critical/High; on the canonical AJ snapshot there are 1128 Critical/High punch-list items (216 Critical + 912 High) but only 8 RSK rows render. §5.3 Issues iterates `f['blind'][:8]` (engagement.py:504/509) rendering 8 ISS rows out of 50 not-collected devices. Verified by rendering on the canonical snapshot: exactly 8 distinct ISS-NNN ids appear, and no remainder hint ('of 50', '42 more', or an ellipsis) is present in §5.3. A reader of the RAID log sees 8 risks / 8 issues and the table simply ends. This is a coverage-honesty gap for a management instrument whose own §5 preamble says 'Seeded rows are the assessment's own findings' without ever stating that 1120+ further Critical/High risks and 42 further uncollected-device issues exist. Mitigation (why this is Medium not High): the FULL totals do surface elsewhere in the same document — the §1 verdict condition cites '50 device(s) have partial or missing collection' and the §3 next-action cites '50 not collected', and §1 points to 'full detail in the workbook and runbook'. The §3 re-collect action also appends ' …' when len(blind)>6, so the truncation is signalled there but not in §5.3 itself.
- fix: Disclose the sample size on both tables. After the §5.1 Risks table, when the count of Critical/High punch-list items exceeds the rows shown, add a one-line caption like 'Seeded with the top 8 of N Critical/High findings — full register in the assessment workbook.' (N = f['sev_count'].get('Critical',0)+f['sev_count'].get('High',0)). After the §5.3 Issues table, when len(f['blind'])>8 add 'Showing 8 of {len(f['blind'])} devices with collection gaps — full list in the workbook.' Keeps the RAID log a seed while being coverage-honest about what it omits.
- test: Add tests/test_engagement.py::test_engagement_raid_discloses_truncation: build a snapshot with >8 Critical/High punch-list items and >8 not-collected devices; render; assert the §5 text discloses the totals (e.g. contains 'of 1128' / 'of 50' or 'full register'/'full list') and that the visible RSK/ISS row counts are bounded at 8. Conversely, with <=8 findings assert no truncation caption is emitted.

## [Medium/high] R2-6-03
- file: cisco_toolkit/eoldb.py:80, 89-92 (match); 43-44 (WS-C2960 row)  (eoldb.lifecycle_for longest-prefix match)
- claim: lifecycle_for does m.startswith(rec['pat']) over rows sorted longest-pattern-first, returning the first prefix hit. Any 2960 variant without its own row falls through to pat='WS-C2960' (Catalyst 2960, eos 2019-10-31 / ldos 2024-10-31, derived). I proved WS-C2960S-48TS-L and WS-C2960CX-8PC-L both resolve to platform='Catalyst 2960' band='Past-LDoS' ldos=2024-10-31 — but the 2960-S and 2960-CX are distinct Cisco platforms with their own (later) lifecycles, so this is a customer-facing wrong fact: a 2960-CX (a newer compact switch) would be mislabeled past-support. Same class affects any 3560/3750/3850 sub-variant lacking a dedicated row. The match has no guard that the matched family is plausibly correct and no 'low-confidence family match' flag.
- fix: Two options (do both): (a) add dedicated rows for the real divergent sub-families actually shippable in this estate (WS-C2960S ldos 2025-10-31, WS-C2960CX/2960C newer) so they don't collapse; (b) make broad family rows (e.g. base 'WS-C2960', 'WS-C3560') carry a 'match':'family' / lower-confidence marker, and have compute_lifecycle_risk flag per-device 'family_match'=True when the matched pat is a short family prefix, so the reader knows the date is a family approximation. Note this is latent (not firing) on the current AJ fleet — no 2960-S/CX present — so it is Medium, not Critical.
- test: tests/test_lifecycle.py: assert eoldb.lifecycle_for('WS-C2960S-48TS-L')['platform'] != 'Catalyst 2960' once a 2960-S row is added (or, if keeping the family-flag approach, assert the returned record is marked as a family-level match). Add a regression asserting WS-C2960CX is not classed Past-LDoS at asof 2026-06-21.

## [Medium/high] EX-workbook-002
- file: cisco_toolkit/excel.py:374-421  (workbook · VLAN Census sheet)
- claim: write_vlan_census_sheet renders one row per access/SVI VLAN and produces 172 data rows on the AJ fleet, while every other surface (explorer census, CRD, design, webapp) reports the canonical 202 'VLANs in use'. The 30 querier-only VLANs (whose gateway sits on uncollected DS/CS core) cannot populate a per-VLAN row and are simply omitted. The sheet displays no total, no '172 of 202 canonical' reconciliation, and no note pointing the reader to the canonical count — so a reader who counts VLAN Census rows gets 172, directly contradicting the 202 on the snapshot/explorer/CRD. The discrepancy is acknowledged ONLY in the function docstring (lines 374-379), which a workbook reader never sees.
- fix: Add a header/banner row to the VLAN Census sheet (above or in row 1 area, e.g. a merged note row) that reads canonical n_vlans from the brief: e.g. 'VLANs in use (canonical): {scale.n_vlans} — {len(agg)} have a collected access-port/SVI and appear below; the remaining {n_vlans-len(agg)} are querier-evidenced VLANs whose gateway sits on an uncollected device.' This requires threading the brief (or n_vlans) into write_vlan_census_sheet, which currently takes only all_interfaces.
- test: In a workbook test, assert the VLAN Census sheet contains a cell whose text includes str(snap['executive_brief']['scale']['n_vlans']) (202 for the fixture), so the canonical total is always visible on the sheet and the access-subset row count is explicitly labelled rather than silently presented as the total.

## [Medium/high] EX-workbook-003
- file: cisco_toolkit/excel.py:154-211  (workbook · Switch Inventory sheet)
- claim: The Switch Inventory sheet renders one row per inventoried device (303 rows) but INVENTORY_COLUMNS has no collection-status column. A not-collected device renders with its platform but blank serial/version/uptime/ports — visually identical to a collected device that failed parsing or a genuinely empty box. The coverage-honesty mandate ('Not observed never silently becomes healthy') is only met on a SEPARATE 'Collection Completeness' tab; the primary inventory sheet a reader opens first gives no in-row signal of the 50 assessment blind-spots, so an inventory export taken alone overstates collection coverage.
- fix: Add a leading 'Collected' (Yes/No) column to INVENTORY_COLUMNS sourced from the collection_completeness per-device status (or derive 'No' when the device has no serial AND no interfaces collected), and optionally shade not-collected rows. This keeps the always-produced inventory sheet self-honest without forcing a cross-tab lookup. Thread collection_completeness (already computed in main) into write_device_inventory_sheet.
- test: Workbook test: assert the Switch Inventory header includes a collection-status column and that the count of rows marked not-collected equals collection_completeness.summary not-collected (50 for the fixture) and equals len(devices.json) - n_collected (303-253).

## [Medium/high] EX-workbook-004
- file: cisco_toolkit/excel.py:423-454  (workbook · Endpoint Census + Move Groups vs canonical scale)
- claim: A reader sees three different endpoint-like totals across the workbook with no single reconciling figure: Endpoint Census = 5326 rows (one row per port with a learned MAC, mode!=Trunk), Move Groups Group 1 '# Endpoint MACs (per-switch sum)' = 5153, and the canonical evidenced-endpoint count (executive_brief.scale.n_endpoints) = 5127 — which is never printed in the workbook at all. The Move Groups column is honestly labelled '(per-switch sum)' and the Endpoint Census is a raw MAC list, so each is individually defensible, but without the canonical 5127 shown anywhere and without a census-summary note, the workbook presents conflicting endpoint magnitudes that a migration planner must reconcile by hand.
- fix: Print the canonical n_endpoints on the Executive Summary scorecard (see EX-workbook-001) and add a one-line note on the Endpoint Census sheet distinguishing 'rows = learned-MAC port records (5326); canonical evidenced endpoints = scale.n_endpoints (5127)'. The Move Groups column label is already adequate; no change needed there beyond the scorecard reconciliation.
- test: Assert (a) the canonical n_endpoints appears on the Executive Summary, and (b) the Endpoint Census sheet carries a label/cell that names its row count as MAC-port records distinct from canonical scale.n_endpoints, so the two magnitudes are never presented as the same quantity.

## [Medium/high] R2-4-03
- file: cisco_toolkit/excel.py:2899-2919  (deliverable: workbook Executive Summary sheet (first/landing tab))
- claim: write_executive_summary_sheet guards the whole Migration-brief section on `if eb.get("axes"):` (L2901). When executive_brief={} (R2-4-01), axes is falsy, so the posture_statement (L2903-2906), the 'Address first' top_gating row (L2907-2910), and the per-axis severity table (L2911-2918) are ALL skipped. The workbook's FRONT landing tab — explicitly the 'where to start without opening all 30+ detail tabs' synthesis — loses its decision-grade summary with no replacement marker. A reader sees a workbook that looks complete (every detail tab still present) but whose executive synthesis has silently vanished; nothing tells them it failed vs. was legitimately empty. This is omission-class false-health (less acute than the deck's rendered '0' because it shows nothing rather than a wrong number, hence Medium), but it still violates the coverage-honesty mandate that 'not observed' must never silently read as 'nothing to report'.
- fix: Add an else-branch (or a pre-check on the R2-4-01 integrity flag): when brief is empty/failed, write a single highlighted row 'Migration brief: cross-axis synthesis could not be generated this run (see log) — open the detail tabs directly.' so the landing tab discloses the gap instead of silently dropping the section.
- test: In tests/test_pipeline_golden.py or a new excel test, build the Executive Summary sheet with brief={} and assert the sheet contains a literal 'synthesis could not be generated' (or equivalent) marker cell, proving the omission is disclosed rather than silent.

## [Medium/high] R2-6-04
- file: cisco_toolkit/excel.py:2056-2067 (By-platform rollup) and 2070-2078 (By-device cols)  (workbook Lifecycle Risk sheet + runbook §4.1 + CRD + deck)
- claim: The conf flag is captured in per_device (analyze.py:4816) but is rendered nowhere as a column. The workbook 'By platform' rollup (the summary table that the runbook §4.1 and deck mirror) has columns [Platform, Devices, Band, LDoS] — no confidence (excel.py:2057,2062). The 'By device' table (excel.py:2070) shows Source (bulletin id) at col 9 but still no conf column. runbook §4.1 (runbook.py:302-305) and CRD (crd.py:215) and deck (deck.py:287-293) all headline n_past_ldos with only the generic note 'last-day-of-support is often derived = end-of-sale + 5yr' buried as a reference note, never per-row. So a reader cannot tell which platform's date is Cisco-confirmed vs EoS+5. This matters because the curated derived dates are demonstrably stale-prone: 16 KB rows already have a derived LDoS in the past as of today, and rows like Nexus 5600 (2026-04-30) cross the boundary essentially 'now', so the derived-ness is decision-relevant, not cosmetic.
- fix: Add a 'Confidence' column to the workbook Lifecycle Risk 'By device' table (render d.get('conf'), value 'derived (EoS+5yr)' vs 'confirmed') and ideally a derived-count line to the 'By platform' header. Mirror a one-line derived caveat into runbook §4.1 and the deck lifecycle slide when n_past_ldos_derived>0 (e.g. 'X of Y past-LDoS dates are derived EoS+5yr — verify before procurement'). The data already exists in per_device; this is pure render work.
- test: tests/test_compute.py or a workbook test: after write_lifecycle_risk_sheet, assert the 'By device' header row contains a 'Confidence' column and that a derived row renders a 'derived' marker. Add a runbook test asserting §4.1 text includes the derived caveat when the fixture has derived Past-LDoS devices.

## [Medium/high] R2-2-02
- file: cisco_toolkit/html.py:98-107, 304-326  (--compare diff workbook + --trend campaign workbook + webapp Campaign)
- claim: The entire diff/trend consumer pipeline tracks switches, interfaces, health, findings — but never endpoints or VLANs as a counted delta. compute_snapshot_delta returns no 'endpoints' and no 'vlans' key (proven: delta keys = ['findings','health','interfaces','switches','verdict','verdict_note']). _trend_point emits no n_endpoints/n_vlans (proven: keys lack both), and _TREND_METRICS (html.py:321-326) tracks avg_health/n_critical/n_punchlist/n_crit_high/n_not_ready/past_ldos only. So the migration-validation answer to 'did the cutover strand endpoints or lose VLANs?' is structurally unavailable at the count level, even though the engine publishes canonical scale.n_endpoints=5127 and scale.n_vlans=202. Endpoint changes appear ONLY as per-MAC appeared/gone rows in the Endpoint Changes sheet (html.py:196-210) with no roll-up count and no canonical total; VLANs have zero presence in the delta/trend at all. For a 5127-endpoint / 202-VLAN fleet this is a real coverage gap in the cutover-worse-or-not verdict.
- fix: Add an endpoint and VLAN scale delta to compute_snapshot_delta sourced from canonical: `endpoints={'old':_scale(old,'n_endpoints'),'new':_scale(new,'n_endpoints')}` and `vlans={'old':_scale(old,'n_vlans'),'new':_scale(new,'n_vlans')}` (None-tolerant). Surface them as rows in write_diff_workbook Summary ('Endpoints (canonical census)', 'VLANs in use') and add 'Endpoints' + 'VLANs in use' columns/metrics to _trend_point + _TREND_METRICS (lower-is-better=False for endpoints; a DROP in endpoints/VLANs post-cutover is the migration-loss signal worth flagging). This closes the stranded-endpoint / lost-VLAN blind spot the lane premise calls out.
- test: tests/test_snapshot_delta.py: two snapshots with scale.n_endpoints 5127->5000 and n_vlans 202->200; assert delta['endpoints']=={'old':5127,'new':5000} and delta['vlans']=={'old':202,'new':200}. tests/test_campaign_trend.py: assert 'Endpoints' (or 'VLANs in use') appears in the trajectory with the expected direction when scale shifts across the series.

## [Medium/high] R2-2-04
- file: cisco_toolkit/html.py:149, 434-438  (--compare diff workbook Summary vs --trend campaign workbook Timeline)
- claim: The two sibling outputs of the compare/trend pipeline disagree on the per-snapshot switch count. The diff workbook Summary 'Switches' row uses len(od)/len(nd) (html.py:149, raw) — same raw source as compute_snapshot_delta. The campaign workbook Timeline 'Switches' column reads key 'n_switches' from _trend_point (html.py:434 col 'Switches' -> html.py:437 key 'n_switches'), which is canonical-first (scale.n_devices). So for one snapshot fed to both, --compare shows the raw count while --trend shows the canonical count. This is a one-source-of-truth violation across the deliverable set: a reader comparing the diff workbook and the campaign workbook sees inconsistent fleet sizes. Latent on AJ (303==303) but structurally guaranteed wherever raw != canonical, and #1/#3 already establish the populations can differ.
- fix: Fixing R2-2-01 (route compute_snapshot_delta + the diff-workbook Summary through canonical n_devices) automatically reconciles this: both workbooks would then read scale.n_devices. Concretely, after R2-2-01, change html.py:149 to use delta['switches']['old']/['new'] and delete the local len(od)/len(nd) re-derivation so the Summary cannot drift from either the delta or the Timeline.
- test: Cross-output test: feed the same single-snapshot pair to write_diff_workbook and the same snapshot into write_campaign_workbook([s_prev,s]); assert the diff workbook Summary 'Switches' New cell == the campaign Timeline last-row 'Switches' cell == scale.n_devices, using a snapshot where canonical != len(devices) so the assertion is load-bearing.

## [Medium/high] EX-mop-2
- file: cisco_toolkit/mop.py:509-512  (MOP §x.6 Post-cutover validation (go/no-go))
- claim: Every validation_plan item carries a 'why' field (the consequence if the check fails), but the §x.6 rendered table emits only (Sev, Category, Device, Check, Command, Expected) and drops 'why'. On the AJ snapshot all 854 validation items carry a populated why. Without the rationale on the go/no-go table, an engineer correcting a failing check under window pressure cannot see WHY a given check is a hard stop vs a soft one — exactly the judgement a real change MOP's verification step is meant to support. The Severity column helps ordering, but the rationale is the load-bearing 'is this a rollback trigger' signal.
- fix: Add a 'Why it matters' column to the §x.6 table: change the headers to ['Sev','Category','Device','Check','Command','Expected (good) result','Why'] and append it.get('why') to each row tuple (truncate/wrap as needed; adjust widths). It is already on every item, so this is purely a render change — no new compute, no SSOT risk.
- test: Extend test_mop_validation_table_surfaces_severity_high_first (or add a sibling): assert the §x.6 table header row contains a 'Why' cell and that the wave-1 table body contains the fixture's why text (e.g. 'VLAN 10 endpoints lose their gateway if this SVI is down').

## [Medium/high] EX-mop-3
- file: cisco_toolkit/mop.py:533-534  (MOP §x.7 Rollback)
- claim: In the single-move-group case, the strategy-specific rollback line is appended to rb as a BARE STRING (line 534), while every other step in the same rb list — and the multi-group equivalent at 531 — is an (action, success_criterion) tuple. The steps() renderer only emits the bold 'Success: ...' verification sub-step for tuples (mop.py:150-156), so this one rollback step prints with NO success criterion, unlike all its siblings. The module explicitly markets 'a success criterion per step' (docstring/§ wording, test_mop_port_mapping_raci_and_success_criteria), so this is an internal inconsistency that drops the verification sub-step exactly on a rollback action — the highest-stakes place to omit it.
- fix: Make line 534 a tuple with a success criterion mirroring the multi-group sibling, e.g. rb.append(('Strategy-specific: ' + playbook['rollback'].rstrip('. ') + '.', 'this wave\'s endpoints are back on their legacy path and re-validated.')). This restores the per-step verification discipline for the single-group rollback path.
- test: Add test_mop_rollback_step_has_success_criterion(tmp_path): use the default single-group _snap() (no wave_plan), render, find the §x.7 Rollback paragraphs, and assert the paragraph beginning 'Strategy-specific:' (or the run that follows it) contains 'Success:'. A regression that reverts the tuple to a bare string fails it.

## [Medium/high] EX-mop-4
- file: cisco_toolkit/mop.py:342-343  (MOP §x.1 Scope & risk + §x.5 Cutover procedure)
- claim: The scope table exposes 'Maintenance window' and 'Rollback decision time' as bare placeholders ('<DATE> <START>-<END>', '<HH:MM> — if validation has not passed by this time, roll back') with no estimated cutover duration or outage budget to derive that decision time from. A real change MOP states an estimated elapsed time / expected outage window so the window can be sized and the go/no-go time is computed rather than guessed; for hard-cutover waves the procedure even says 'the outage clock is started' (line 483) without ever stating an expected outage duration. grep confirms mop.py contains no duration/elapsed/minutes/outage-budget emission.
- fix: Add an explicit timing scaffold the implementer fills: in §x.1 add rows 'Estimated cutover duration' = '<MM> min (est.)' and 'Expected endpoint outage (hard-cutover waves)' = '<MM> min'; tie the existing 'Rollback decision time' note to it ('= window start + estimated duration + buffer'). Optionally, for hard-cutover waves, add a §x.5 reminder to log outage start/stop against the budget. Keep them placeholders (deterministic, no invented numbers) consistent with the rest of the MOP.
- test: Add test_mop_scope_has_timing_and_outage_budget(tmp_path): render _snap(); assert the §x.1 scope table contains an 'Estimated cutover duration' field and (for the hard-cutover wave) an 'Expected ... outage' field placeholder; assert the rollback-decision-time row references the duration/buffer rather than standing alone.

## [Medium/high] SSOT-device-deliv-05
- file: cisco_toolkit/ops.py:115 (devices defined L31)  (Operations Handbook — title page meta line)
- claim: ops.py:31 `devices = _as_dict(snap.get("devices"))` = 303 (inventory). ops.py:115 renders `f"{len(devices)} devices in evidence scope"` = '303 devices in evidence scope', and the adjacent 'How to use' note (ops.py:121) says the baselines are "derived from this fleet's own assessed evidence". Evidence/assessed scope is the 253 collected, not the 303 inventoried (50 are Insufficient-Data blind spots). Note this same module DOES read collected correctly for the per-axis sections (e.g. ops.py:182/185 use `n_collected`), so only the title-page device figure is mislabeled.
- fix: Relabel the meta line to '{len(devices)} devices in scope' (inventory framing) or render the collected count from `(snap.get('collection_completeness') or {}).get('summary',{}).get('complete', len(devices))` for the 'evidence scope' phrasing.
- test: test_ops/test_deliverables: render Operations Handbook from AJ snapshot; assert the title meta line uses 'in scope' (not 'in evidence scope' with the inventory total), or that the evidence-scope number == complete (253).

## [Medium/high] EX-ops-01
- file: cisco_toolkit/ops.py:143-357  (ops.py (Operations Handbook DOCX))
- claim: The handbook covers monitoring (§3), drift control (§4), software governance (§5), a routine calendar (§6) and escalation/TAC (§7), but contains NO configuration backup/archive & restore procedure. A grep for backup|restore|archive|tftp|rollback|recover over cisco_toolkit/ops.py returns ZERO matches, and the same grep across ALL deliverable generators (cisco_toolkit/*.py) returns ZERO config-backup matches. runbook.py's 'rollback' (runbook.py:905) is change-time MOP-step reversal, NOT a Day-2 device-config backup/restore. A real Cisco Day-2 ops handbook (PPDIOO Operate) mandates a config-archive + restore-from-backup procedure: it is how the NOC recovers a device after hardware failure or an unauthorized change. §4 detects config drift but never tells operations how to back up or restore the config, and §7 even lists 'show tech-support' capture yet omits config archival.
- fix: Add a new section (e.g. '4.x Configuration backup & restore' or a standalone §) to ops.py: state the backup cadence (config archive on every change + scheduled snapshot), the restore procedure (recover running-config from the last known-good archive), and ground the 'known-good standard' in the existing golden_drift baseline (ev['gd']) so the restore target is the assessed standard, not a generic statement. Gate the evidence-backed part on gd_sum.get('n_baseline') like §4, with an honest absent() declaration otherwise.
- test: Extend tests/test_ops_handbook.py: assert the rendered DOCX text contains a backup/restore heading and the words 'backup' and 'restore' (or 'archive'); assert the golden-fixture render references the configuration baseline as the restore target; assert the empty-snapshot render still emits the section skeleton (or an honest absent() line) without inventing a backup target.

## [Medium/high] EX-ops-02
- file: cisco_toolkit/ops.py:208-227  (ops.py §3.2 Control-plane capacity baseline)
- claim: §3.2 is explicitly titled 'Control-plane capacity baseline' and renders only CPU 5-min % and memory-free % from platform_health (ops.py:208-224). It never surfaces the snapshot's `capacity` axis, which carries per-device port/PoE headroom for all 303 devices (fields total_ports / active_ports / free_ports / port_util / poe_capacity_w / poe_drawn_w / poe_remaining_w / poe_util). A real Day-2 ops handbook's capacity section tracks access-port and PoE-budget headroom so the NOC knows when a switch is running out of ports or power — this groundable fleet evidence sits entirely unused by the handbook.
- fix: Add a §3.x 'Port & PoE capacity headroom' subsection that reads snap['capacity']: surface the fleet's port-utilisation distribution and any devices flagged near port-exhaustion or PoE-budget limits (use the per-device flags / port_util), gated on the axis being present with an honest absent() otherwise. Keep §3.2 as control-plane CPU/mem; add interface/PoE as a sibling so 'capacity' is not silently CPU-only.
- test: Add a test feeding a snapshot whose 'capacity' axis has a high-port_util / low-free_ports device and assert the handbook text surfaces a port/PoE headroom row for it; add an empty/absent-capacity case asserting an honest absent() declaration and no invented utilisation numbers.

## [Medium/high] DET-vpc-03
- file: cisco_toolkit/parse.py:530-534 (out dict omits Peer Gateway) ; consumed-gap at design_advisor.py:306-342  (snapshot.vpc[].* (domain keys) - peer_gateway absent)
- claim: The lane asks whether the detector covers peer-gateway. It does not, and this is an EVIDENCE-PARSING gap, not just a detector gap: the raw 'show vpc' output carries a 'Peer Gateway : Enabled/Disabled' line on 53 of the collected vPC hosts (22 Enabled, 31 Disabled), but parse_vpc never reads it - the snapshot vpc domain records contain only {consistency, domain_id, keepalive_status, num_vpcs, peer_link, peer_status, role, vpcs}. peer_gateway is design-relevant (it affects whether the peer forwards for the partner's router MAC / FHRP behavior across the vPC); it is collected but invisible.
- fix: In parse.py parse_vpc, add out['peer_gateway'] = _kv(r'Peer Gateway') (normalized to enabled/disabled) and similarly capture 'Operational Layer3 Peer-router' and 'Type-2 consistency status' / 'Per-vlan consistency status' which are also present in the raw output. This is purely additive to the snapshot. Optionally extend _signals to surface peer_gateway posture; at minimum capture it so a future detector / NRFU check can read it.
- test: Extend tests/test_vpc.py _VPC fixture (which already contains 'Peer Gateway : Disabled' at line 17 but is not asserted) to assert parse_vpc(_VPC)['peer_gateway'] == 'disabled'. Add a second fixture with 'Peer Gateway : Enabled' asserting 'enabled'.

## [Medium/high] DET-vpc-04
- file: cisco_toolkit/parse.py:551-558 (single \S+ consistency token; no orphan-port parse)  (snapshot.vpc[].vpcs[] legs (only id/port/status/consistency/vlans) + missing show vpc orphan-ports)
- claim: The lane asks about orphan ports and type-1/type-2 consistency. Neither is represented: (a) the parser captures only a single per-leg 'consistency' token (parse.py:555 group 4 = one \S+), so it cannot distinguish a type-1 (must-match, brings the leg down) from a type-2 (should-match, traffic impact only) mismatch - the NX-OS domain header lines 'Per-vlan consistency status' (type-1-ish) and 'Type-2 consistency status' are present in the raw output but unparsed. (b) Orphan ports (single-attached devices on a vPC peer) come from 'show vpc orphan-ports', which is not in the collected command set for these hosts, so orphan-port risk at cutover cannot be assessed. These are coverage gaps the detector inherits from the evidence layer.
- fix: Two-part: (1) Parse the domain-level 'Per-vlan consistency status' and 'Type-2 consistency status' lines into out['per_vlan_consistency'] / out['type2_consistency'] (additive, already in the raw output) so the detector can name the mismatch class. (2) Add 'show vpc orphan-ports' to the NX-OS command list and a parse_vpc_orphan_ports parser; then extend _d_vpc_health (or a sibling detector) to flag orphan ports as a cutover risk (a single-attached device on a vPC peer loses connectivity if that peer is the one taken out of service). Until orphan-port data is collected, keep the detector silent on it (coverage-honest) rather than inferring.
- test: Add a parser test asserting parse_vpc captures type2_consistency/per_vlan_consistency from the domain header. For orphan ports, add a test for the new parse_vpc_orphan_ports against a canned 'show vpc orphan-ports' output, and a blueprint test that an orphan-port present in evidence raises the (new) orphan-port concern while absent evidence stays silent.

## [Medium/high] DET-fhrp-state-02
- file: cisco_toolkit/parse.py:137-169, 2046-2055  (collection/parse (build.py command set -> parse_hsrp_summary/_parse_fhrp -> model.hsrp_behavior))
- claim: Collection already gathers the data: build.py:628 loads 'show standby brief'/'show standby all'/'show hsrp brief'/'show hsrp all'. The brief format has a 'Pri' (priority) column and a 'P' (preempt) flag column -- parse_hsrp_summary's regex at parse.py:137-138 (r"^(Vl..)\s+(\d+)\s+\d+\s+[P\s]?...") explicitly MATCHES the priority digit (\d+) and the preempt char ([P\s]?) and then THROWS THEM AWAY, capturing only interface/group/state/VIP. 'show standby all'/'show hsrp all' additionally carry full priority, 'preemption enabled/disabled', and 'Track object N ... state Up/Down' lines, none of which are matched. _parse_fhrp (parse.py:2046-2055) returns only (proto, role, vip, group). model.hsrp_behavior (model.py:54) is a single string with no priority/preempt/track fields. Consequence: NO surface can detect (a) active running on the non-preferred/lower-priority box, (b) preempt DISABLED (after a failover the gateway never returns to the primary -- a latent suboptimal/asymmetric state), (c) a priority TIE (nondeterministic active election), or (d) MISSING interface/object tracking (the active SVI stays up while its upstream uplink is down -> first-hop blackhole). These four are precisely the evidence axes a 'fix split-active' detector and an informed 'introduce-FHRP' design recommendation need, and they are collected-but-unused.
- fix: Extend the FHRP capture to retain the operational-correctness fields. Minimum: in parse_hsrp_summary bind the Pri column and the 'P' preempt flag and append them to the behavior string (e.g. 'HSRP grp 1 Active pri 110 preempt VIP x'); extend _parse_fhrp's regex to surface (priority:int|None, preempt:bool|None) and, when 'show standby all'/'show hsrp all' is present, parse 'Track object/interface ... state' lines into a tracked-count. Then in compute_fhrp_consistency add issue checks: equal priorities across a group (tie / nondeterministic), preempt disabled on any member, and zero tracked objects on a member whose uplink set is non-trivial -- feeding the DET-fhrp-state-01 detector so the design layer can recommend 'set deterministic priority + enable preempt + track the uplink'. Keep all of it engine_actionable=False-safe: only emit when the 'all' evidence is actually present (coverage-honest), never infer preempt/track state from the brief alone.
- test: Add tests/test_parsers.py cases: (1) feed a 'show standby brief' line 'Vlan10  1  110 P  Active  10.0.10.2  10.0.10.3  10.0.10.1' and assert priority 110 and preempt True are surfaced (currently both are lost); (2) feed a 'show standby all' block with 'Preemption disabled' + a 'Track object 1 ... state Up' line and assert preempt False + tracked>=1 are captured; (3) a compute_fhrp_consistency test where two members share priority 100 and preempt is disabled asserts new issues 'priority tie' and 'preempt disabled' appear.

## [Medium/high] DET-mac-arp-001
- file: cisco_toolkit/parse.py:353-374; cisco_toolkit/build.py:575-580  (engine (analyze/design_advisor detectors))
- claim: Every device's full `show mac address-table` is collected (e.g. migration_collection_20260613_063201/10GSW01-BC-CA11F17/show_mac_address-table.txt = 236 dynamic MACs; 10GSW02 = 234; AAS01-BC-CA05R32-ENDOH = 45), but the engine never derives a per-device MAC-table population or compares it to the platform's L2-forwarding limit. parse.py:367 keeps only `type == dynamic` and dedupes per interface (`seen[if_n]`), then build.py:580 stores only the FIRST FIVE MACs per interface (`end_host_mac = ", ".join(macs[:5])`). No code aggregates a total MAC count, and the only capacity detector `_d_capacity` (design_advisor.py:857) + its producer `compute_capacity` (excel.py:565-591) read ONLY physical ports/PoE (keys: total_ports/active_ports/free_ports/port_util/poe_util) — confirmed: `tests/golden/snapshot.json` capacity[] has no mac/arp/tcam key, and the snapshot has zero top-level mac/arp/tcam keys. So a switch whose MAC table is near its hardware ceiling (a real migration risk: adding a consolidated VLAN/host load can overflow it and flood) is INVISIBLE.
- fix: Add a collected-but-unused detector. (a) In COLLECT_PARSE assembly, compute per-device `mac_table_count` = total dynamic MAC lines in `show mac address-table` (count rows BEFORE the per-interface dedupe/truncation; do not reuse end_host_mac which is capped at 5). (b) Map device model -> documented L2-forwarding (MAC) table size via the existing platform KB (the same place model port counts are known); leave it `""`/Unknown when the model isn't in the KB (coverage-honest — never assume a limit). (c) Add `_d_mac_table_pressure(snap, sig)` in design_advisor.py that fires engine_actionable=True when mac_table_count/limit >= e.g. 80%, citing `capacity[].mac_table_count` + the model limit, priority Medium, driver 'a near-full L2 table overflows to flooding when consolidated load is added at cutover — size the replacement / split the broadcast domain'. Register it in the detector list (design_advisor.py:1079-1080).
- test: tests/test_decision_layer.py: build a synthetic snapshot with one device model whose KB MAC limit is small and a mac_table_count at 90% of it; assert the blueprint gains a decision id 'mac-table-...' with engine_actionable True and evidence citing the count+limit; and a second device at 10% yields no such decision (proves it fires on the threshold, not unconditionally). Add a parse test that a 250-line dynamic MAC table yields mac_table_count==250 even though end_host_mac stays capped at 5.

## [Medium/high] DET-mac-arp-002
- file: cisco_toolkit/parse.py:1394-1413  (engine (analyze/design_advisor detectors))
- claim: `parse_show_ip_arp` drops every Incomplete ARP entry: line 1396 lists `incomplete` in the skip regex AND line 1401 has an explicit `if "incomplete" in line.lower(): continue`. Incomplete ARP entries (hardware addr = `Incomplete`, a host the switch is ARPing for but getting no reply) are present in the AJ data — 10 device `show_ip_arp.txt` files contain 'incomplete' lines (e.g. migration_collection_20260613_063201/ACS01-BC-CA05R30/show_ip_arp.txt). A high count of incomplete ARP entries on a gateway/SVI is a real pre-cutover signal (dead silent hosts, asymmetric routing, an over-large or mis-scoped subnet, or scanning) that an engineer would want flagged before baselining. The engine has no `arp_incomplete` count and no detector for it; the data is parsed away and lost.
- fix: Stop throwing the signal away: have the ARP collector also TALLY incomplete entries per device (e.g. `arp_incomplete_count`) while still excluding them from the mac->ip resolution map (keep that exclusion — an incomplete entry has no usable MAC). Surface a per-device count in the snapshot, then add `_d_arp_incomplete(snap, sig)` that fires engine_actionable=True when a device's incomplete-ARP count exceeds a threshold (absolute, e.g. >=25, or a fraction of total ARP), citing the field, priority Medium, driver 'unresolved ARP at scale on a gateway points at dead hosts / subnet mis-scope / asymmetric paths — investigate before trusting the L3 baseline'. Keep it Unknown-honest where ARP wasn't collected.
- test: tests/test_parser_robustness.py: feed `show ip arp` output containing 3 resolved + 4 Incomplete rows; assert the resolution map has 3 entries (unchanged) AND the new arp_incomplete tally == 4 (proves the count is captured without polluting mac->ip). tests/test_decision_layer.py: snapshot with arp_incomplete_count above threshold yields an engine_actionable 'arp-incomplete-...' decision; below threshold yields none.

## [Medium/high] DET-storm-001
- file: cisco_toolkit/parse.py:679-735  (engine (parse → build → design_advisor detectors))
- claim: Access-port storm-control coverage is NOT assessed as an actionable gap, and the underlying evidence is collected-but-unused — the classic pattern this lane targets. The interface running-config parser parse_run_config_interfaces() (parse.py:679) extracts bpduguard/rootguard/portfast/pc_mode/vrf/desc/ip_addr/acl_in/acl_out/mtu/helpers but has NO branch for 'storm-control', so the config line is silently discarded. Proof the raw evidence exists: `grep -rhi 'storm-control' migration_collection_20260613_063201/*/show_running-config_interface.txt` returns 121 matched lines (109x 'storm-control broadcast level 20.00', plus level 30/40 and multicast variants) across 36 COLLECTED switches in the canonical AJ fleet. The only storm-control code path that fires is a syslog SYMPTOM detector (analyze.py:4143, facility STORM_CONTROL from 'show logging'), which flags storm EVENTS that already happened — not the absence of storm-control CONFIG on access ports; and per the project's own history the bare-`show logging` axis is structurally dead on NX-OS ('% Incomplete command'). Every 'storm' hit in the canonical snapshot is narrative only (design_blueprint/doctrine/security[9] + decisions[12/17/29] recommended_action/alternatives/tradeoffs) — none is a parsed config field or a firing engine_actionable detector. Net: a reader is never told that broadcast/multicast storm-control is unconfigured on the access edge, even though the config to prove it (or prove its absence) is sitting in collected evidence. The switchport_mode field needed to classify access vs trunk already exists on InterfaceData (model.py:17) and is already used by _d_native_vlan (design_advisor.py:899), so the access/trunk gate is free once storm-control is parsed.
- fix: Wire the collected-but-unused evidence into a firing detector, mirroring _d_native_vlan exactly. (1) parse.py:716-722 region — add `if low.startswith('storm-control'): res[current]['storm_control']='Enable'` (and seed 'storm_control':'' in the dict literal at parse.py:689). (2) model.py InterfaceData — add `storm_control: str = ""`. (3) build.py:505-519 — add `if v.get('storm_control'): interfaces[p].storm_control = v['storm_control']`. (4) In design_advisor.py _build_signals (the sig dict), aggregate counts: for every interface whose switchport_mode == 'Access' (corroborated by the access-port census, ignoring trunk/uplink/L3 ports), count those with empty storm_control as `storm_unprotected_access`, and collect the host set. (5) Add `_d_storm_control(snap, sig)` next to _d_native_vlan: return None if `sig['storm_unprotected_access'] <= 0`; else `_decision('l2-storm-control-on-access-edge', f"{sig['storm_unprotected_access']} access port(s) across {sig['storm_unprotected_switches']} switch(es) have no broadcast/multicast storm-control — a single looped or chattering host can saturate the segment with broadcast/BUM traffic; the target access edge must apply storm-control (broadcast/multicast suppression) on user-facing ports.", sig['storm_unprotected_access'], ['availability','security'], ['interfaces[host][port].switchport_mode','interfaces[host][port].storm_control'], priority='Medium', driver='L2 edge hygiene: storm-control caps the blast radius of a broadcast/BUM storm on the access edge.', devices=sig['storm_unprotected_hosts'])`. Register it in the detector list so it reaches the blueprint/CRD/explorer/webapp like the other _d_* outputs. Keep engine_actionable=True only because it fires on collected config; honor coverage-honesty — only count ports on COLLECTED switches (50 not-collected stay UNKNOWN, never silently 'protected').
- test: tests/test_storm_control_detector.py: (a) parser unit — feed a `show running-config | section interface` snippet with one access interface containing 'storm-control broadcast level 20.00' and one without; assert parse_run_config_interfaces() sets storm_control=='Enable' on the first and '' on the second. (b) detector fires — build a minimal snap where 3 access ports lack storm_control across 2 switches; assert _d_storm_control returns a decision with engine_actionable True, priority Medium, scale==3, and the cited evidence paths. (c) no false positive — trunk/uplink ports (switchport_mode!='Access') without storm_control do NOT increment the count, and a fleet where every access port has storm_control returns None. (d) regression on real data — run against migration_collection_20260613_063201 and assert the firing count is > 0 and bounded by the access-port census on the 36 storm-configured switches (no double-count, collected-only).

## [Medium/high] DET-ntp-001
- file: cisco_toolkit/parse.py:1067, 1040, 1135  (engine (collection + parse_config_hygiene))
- claim: The engine never collects or parses actual NTP sync state. parse_config_hygiene only computes `ntp = has(r"^ntp (server|peer)\b")` (parse.py:1067) — pure config-line presence. No `show ntp status` / `show ntp associations` output is collected: across all 253 device dirs in migration_collection_20260613_063201 the only clock-related file is show_ptp_clock.txt (PTP, a different protocol); a grep for any NTP sync banner (`Clock is synchronized|stratum|reference time`) over the whole collection returns ZERO hits. The commands `show ntp status` / `show ntp associations` appear in the repo ONLY as NRFU verify text (design_advisor.py:2118/2175, mirrored into the snapshot JSON) and in questionnaire.json:99 — never in a collector command list and never in any parser fn. So synced?, stratum, and source are not in the snapshot at all.
- fix: Add `show ntp status` and `show ntp associations` to the base collection command set (NX-OS: `show ntp peer-status` / `show ntp peers`), then add a parser `parse_ntp_state(out_status, out_assoc)` in parse.py returning {synced: bool|None, stratum: int|None, source_count: int, sources:[{addr, refid, st, reach, selected('*'|'+'|None)}], oob_via_mgmt: bool|None}. Parse 'Clock is synchronized'/'unsynchronized' and the '*'/'+' selection markers + stratum column. Keep coverage-honest: when the command output is absent, emit status 'Not collected — NTP state commands absent from this collection' (mirror the platform_health/capacity 'Not collected' pattern at analyze.py:4676), never default to synced.
- test: tests/test_ntp_state.py: (a) feed a synthetic `show ntp status` containing 'Clock is unsynchronized' with `ntp server 10.0.0.1` present in config → assert parse_ntp_state(...).synced is False; (b) feed output with no '*'-selected peer → assert no redundant/selected source; (c) feed an empty/absent command → assert status field is the explicit 'Not collected' string and synced is None (not True).

## [Medium/high] DET-snmp-001
- file: cisco_toolkit/parse.py:1080  (engine/detector)
- claim: The 'no ACL on the SNMP community' design dimension the lane asks about is NOT fed into any detector. parse.py:1080 parses 'snmp-server community' with regex `^snmp-server community\s+(\S+)(?:\s+(ro|rw))?` which captures only the community string and the ro/rw keyword and DISCARDS the trailing ACL token (`snmp-server community <str> [view <name>] [ro|rw] [acl]`). As a result the insecure-snmp CIS finding (parse.py:1162-1177) and the firing _d_mgmt design decision (design_advisor.py:615, which reads insecure-snmp via _MGMT_FAIL_IDS at design_advisor.py:27) score a community that IS restricted to a management-station ACL identically to a naked, world-open community. The only KB principle that names the SNMP-server ACL allowlist, `security-management-plane-source-allowlist` (design_kb.py:1379), is engine_actionable:False (design_kb.py:1383), so no _d_* detector references it — confirmed by zero matches for allowlist/access-class/use-ipv4acl in design_advisor.py — even though its `observable` ('SNMP open to all sources, no source allowlist', design_kb.py:1392) is directly derivable from the config the engine already parses. Contrast: vty-hardening (parse.py:1113,1186-1188) DOES detect a missing VTY access-class, so the SNMP half of the same management-source-allowlist hardening layer is the asymmetric gap.
- fix: (1) In parse.py parse_security, extend the community regex to capture the optional trailing ACL: `^snmp-server community\s+(\S+)(?:\s+view\s+\S+)?(?:\s+(ro|rw))?(?:\s+(\S+))?` and record on each snmp_comm dict an `acl` field (the captured acl id, or None) — NEVER the community string (keep the existing redaction). (2) Add a per-community 'unACLed v2c community' bit to the insecure-snmp detail (e.g. 'N of M v1/v2c communities have no source ACL') so the management-source-allowlist gap is visible. (3) Flip security-management-plane-source-allowlist to engine_actionable:True and add a _d_snmp_acl (or fold into a management-source-allowlist signal) detector in design_advisor.py that fires when any collected device has a v1/v2c community with acl is None (mirror the existing vty-hardening access-class logic so SNMP and VTY are symmetric). Keep it coverage-honest: only fire on devices with a parsed running-config.
- test: Add tests/test_snmp_acl_detector.py: (a) parse_security('snmp-server community FOO ro 99') yields a snmp_comm/finding carrying acl='99' and the redacted detail still contains '<redacted>' but NOT 'FOO'; (b) parse_security('snmp-server community FOO ro') marks the community as no-ACL; (c) parse_security('snmp-server community FOO view V ro 10') captures acl='10' through the optional view qualifier; (d) a snapshot fixture with one host having a naked community and one host having an ACLed community makes compute_design_blueprint emit exactly one management-source-allowlist decision naming only the naked host, and asserting it carries engine_actionable lineage; (e) regression: a config with SNMPv3-only (no community) emits no such decision.

## [Medium/high] DET-poe-001
- file: cisco_toolkit/parse.py:2119-2136 (_parse_poe_watts); 1805-1843 (parse_show_environment_power); cisco_toolkit/excel.py:564-591 (compute_capacity); cisco_toolkit/design_advisor.py:256, 857-871 (_d_capacity)  (engine collection + detector layer)
- claim: The lane's target risk — PoE oversubscription threatening phones/APs at cutover — has NO actionable detector, even though all the inputs are collected. `show power inline` is collected for every switch (build.py:619, excel.py:3038) and its raw output carries the per-switch PoE budget on a `Module / Available / Used / Remaining (Watts)` summary line. PROOF: migration_collection_20260613_063201/AS22-BC-CA04R2-BCDOH/show_power_inline.txt lines 2-5 show `1  1120.0  77.0  1043.0` (Available 1120W, Used 77W). But the engine never parses this line: `_parse_poe_watts` (parse.py:2119) only reads per-port consumed watts via IFACE_TOKEN_RE and skips the Module summary row (no interface token). `compute_capacity`'s `poe_util` (excel.py:582) is derived from `dp.power_capacity_w`/`power_drawn_w`, which are populated ONLY from `parse_show_environment_power` (the CHASSIS power command, parse.py:1805-1829) — a different command that returns nothing usable on these PoE access switches. RUNTIME PROOF against the canonical snapshot Migration_Assessment_AUTOFILLED_20260613_063201.snapshot.json: 0 of 303 capacity records have `poe_util` or `poe_capacity_w` populated. Therefore the only PoE signal in the detector layer — `sig['cap_poe_hot'] = sum(... poe_util >= 85)` (design_advisor.py:256) — is always 0, and its sole consumer `_d_capacity` (design_advisor.py:857-871) gates on `cap_hot` (port-util) with PoE as a secondary string only (line 861), so a PoE-bound-but-port-cold switch can never raise a decision. Across 253 collected switches, 35 carry a parseable Module budget line and 32 have live PoE (2448 ports with poe_status, 134 actively Delivering, 3 Fault) — real first-hand evidence that is structurally unreachable by any detector. The engine even self-documents the gap: explorer blast_radius_explorer.html:8748 emits 'PoE budget was not reported by these platforms in collection', and design_kb.py:763 prescribes 'verify the PoE budget covers the powered-endpoint load' with no detector wired to fire it.
- fix: (1) Parse the budget: add `_parse_poe_inline_budget(output) -> {available_w, used_w, remaining_w}` to parse.py that reads the `Module/Available/Used/Remaining (Watts)` summary row (and the NX-OS `Power Supply ... Total Power ... Allocated` equivalent) from the already-loaded `show power inline` output, summing per-module rows for stacks. Populate it in build.py (alongside the existing poe_out load at build.py:619) onto new DevicePhysical fields `poe_budget_w`/`poe_used_w`, and prefer these over the chassis-power fields in compute_capacity so `poe_util` (excel.py:582) reflects the TRUE PoE budget rather than always being blank. (2) Add a dedicated, engine_actionable detector `_d_poe_oversubscription(snap, sig)` in design_advisor.py that fires when collected PoE draw plus the powered-endpoint count implies a target switch could exceed its PoE budget post-consolidation — grounded on `capacity[].poe_util`, `capacity[].poe_budget_w`, and the per-port Delivering count from interfaces[].poe_status. Make it move-group aware: sum the Delivering/Used watts of the access switches in each move-group and compare against the planned target's PoE budget; raise High when the consolidated PoE load would exceed a documented headroom (e.g. >80% of target budget). Register it in the DETECTORS tuple (design_advisor.py:1080) so it reaches the blueprint/punchlist like the other _d_* detectors. Keep it coverage-honest: when no budget line was parsed for a switch, emit 'PoE budget not observed — size from endpoint classes' (the existing explorer note at :8748) rather than asserting safety.
- test: Add tests/test_poe_oversubscription.py: (a) unit-test `_parse_poe_inline_budget` against the real AS22 fixture text, asserting available_w==1120.0, used_w==77.0, remaining_w==1043.0, and against a multi-module/stack sample asserting the rows sum; (b) feed a synthetic snapshot where a move-group consolidates 3 access switches each Delivering ~600W onto a 1 target with a 1120W PoE budget and assert `_d_poe_oversubscription` returns a High decision with engine_actionable=True naming the target host; (c) coverage-honesty test: a snapshot whose switches have poe_status ports but NO parseable budget line yields the 'budget not observed' wording, never a false 'within budget' claim; (d) regression-lock that on the canonical AJ snapshot the detector does NOT crash and reports the 134 Delivering endpoints as the at-risk powered load.

## [Medium/high] SSOT-endpoint-deliv-1
- file: cisco_toolkit/runbook.py:186 (value source: 137; §7 canonical at 769-773; test lock: tests/test_runbook.py:147)  (runbook (Migration Assessment Runbook .docx))
- claim: The runbook's §1 Assessment Header & Executive Summary 'Metric/Value' table prints the 'Endpoints' row from `ep_total` = the access-port host-MAC sum (runbook.py:186, value from _endpoint_census at runbook.py:137/40-61), which on the AJ fleet is 5292. The SAME document's §7 Endpoint & VLAN Census (runbook.py:769-773) prints the CANONICAL `executive_brief.scale.n_endpoints` = 5127 as 'Evidenced endpoints', explicitly distinguishing it from 'Access-port host MACs'. So the executive summary headlines 5292 while the canonical census downstream says 5127 — an internal SSOT drift of 165 within one deliverable. A reader skimming the exec-summary table sees 'Endpoints ... 5292' and reasonably takes it as THE endpoint count, contradicting the published canonical 5127 that every other generator (crd.py:77, deck.py:159) and the workbook reconcile to. Verified numerically against Migration_Assessment_AUTOFILLED_20260613_063201.snapshot.json: canonical scale.n_endpoints=5127; replicated _endpoint_census access-port MAC sum=5292.
- fix: Make §1 canonical-first like §7. In runbook.py read `ep_canon = ((snap_dict.get('executive_brief') or {}).get('scale') or {}).get('n_endpoints')` once near line 137, then at line 186 emit the canonical figure as the primary 'Endpoints' metric when present — e.g. row label 'Evidenced endpoints (canonical)' with value `ep_canon if ep_canon is not None else ep_total`, and either drop the bare MAC-sum row from the exec summary or add a clearly-subordinate '(access-port host MACs: {ep_total})' qualifier so the headline number matches §7 and the canonical SSOT. Do not recompute; reuse the same canonical read already present at runbook.py:769.
- test: Extend tests/test_runbook.py: build a fixture whose executive_brief.scale.n_endpoints DISAGREES with the access-port MAC sum (e.g. canonical 5127 vs MAC sum 5292 — make the fixture's MAC sum higher by adding a second access-port MAC on one host), generate the runbook, and assert the §1 exec-summary 'Endpoints' row equals the canonical 5127 (not the MAC sum) AND equals the §7 'Evidenced endpoints' value — so §1 and §7 cannot drift. Replace/repurpose the current assertion at test_runbook.py:147 which presently locks the MAC sum.

## [Medium/high] EX-runbook-01
- file: cisco_toolkit/runbook.py:186 (table at 182-195); compare §7 at 769-776  (runbook DOCX §1 Assessment Header & Executive Summary)
- claim: The §1 headline scorecard row 'Endpoints (access-port host MACs at snapshot)' renders ep_total (the locally recomputed access-port MAC sum), NOT the canonical executive_brief.scale.n_endpoints. On the canonical AJ snapshot these differ: ep_total=5292 vs scale.n_endpoints=5127. The SAME runbook's §7 (line 769-773) reads scale.n_endpoints and prints 'Evidenced endpoints: 5127 (canonical, from the assessment scale)'. So the document's Executive Summary table and its Endpoint Census disagree by 165, and the Exec Summary also disagrees with design.docx/crd.docx, which both publish 5127 (crd.py:77 reads scale.n_endpoints; design.py reads scale). The row IS labeled 'access-port host MACs', which is honest, but a reader of the headline table still sees a different fleet-endpoint number than every other canonical surface — the exact split-brain the SSOT contract exists to prevent.
- fix: In the §1 table, surface the canonical evidenced-endpoint count as the headline endpoints metric and demote the MAC sum to a clearly-labeled secondary row, mirroring §7. E.g. replace line 186 with two rows: ('Evidenced endpoints (canonical scale)', ((snap_dict.get('executive_brief') or {}).get('scale') or {}).get('n_endpoints') or ep_total) and ('Access-port host MACs at snapshot', ep_total). This makes §1 agree with §7, design.docx and crd.docx.
- test: Extend tests/test_runbook.py: set snap['executive_brief']={'scale':{'n_devices':2,'n_endpoints':99,'n_vlans':7,'n_domains':1}}; assert the §1 Metric table contains a row whose value is '99' (canonical endpoints) and that the access-port MAC sum '3' is only present under a row labeled as MACs — i.e. the headline endpoints row no longer shows the raw MAC sum. (Update the existing assertion at test_runbook.py:147 accordingly.)

## [Medium/high] EX-runbook-02
- file: cisco_toolkit/runbook.py:187  (runbook DOCX §1 (only VLAN figure in the whole document))
- claim: The runbook's sole VLAN metric is §1 'VLANs with endpoints' = len(ep_per_vlan) = 121 on AJ. The canonical fleet VLAN count executive_brief.scale.n_vlans = 202 (= analyze.vlan_inventory, 202 distinct in-use VLAN IDs) is NEVER rendered anywhere in the runbook (grep n_vlans cisco_toolkit/runbook.py returns zero hits). Sibling deliverables built from the same snapshot prominently publish 202: crd.py:209 ('VLANs in use', 202) and design.py:130 (n_vlan = scale.n_vlans). A reviewer cross-reading the runbook against the CRD/HLD sees 121 vs 202 with no reconciliation line. The endpoint-bearing subset (121) is a legitimate, smaller quantity, but presenting it as the runbook's only VLAN number without the canonical 202 is a coverage/SSOT gap — the migration must preserve all 202 VLAN IDs, not just the 121 that happen to carry an access-port MAC at snapshot time.
- fix: Add a canonical VLAN row to the §1 table (and/or §7 census) sourced from scale.n_vlans, keeping 'VLANs with endpoints' as an explicit subset. E.g. insert before line 187: ('Production VLANs in use (canonical)', ((snap_dict.get('executive_brief') or {}).get('scale') or {}).get('n_vlans') or len(ep_per_vlan)), and relabel line 187 to 'VLANs carrying an endpoint (subset)'. This reconciles the runbook to the CRD/HLD's 202.
- test: In tests/test_runbook.py, set snap['executive_brief']={'scale':{'n_devices':2,'n_endpoints':99,'n_vlans':7,'n_domains':1}}; assert the rendered text contains the canonical VLAN count 7 surfaced as a VLAN metric (e.g. a §1 row value '7'), and that the endpoint-bearing-VLAN row is labeled as a subset rather than as the total VLAN count.

## [Medium/high] HON-runbook-drop-1
- file: cisco_toolkit/runbook.py:803-809  (Runbook DOCX §8 Shared Infrastructure & Consolidation)
- claim: The 'Spare port capacity (consolidation candidates — lowest port utilisation)' table filters capacity rows only by `isinstance(c.get('port_util'),(int,float))` and sorts ascending by port_util, with NO guard on whether the port-count data was actually computed. In the canonical AJ snapshot, 6 capacity rows have active_ports='' (blank) and model='' yet port_util=0.0 — these are devices whose active-port count was never derived. Because the table sorts lowest-util-first, all 6 data-less rows are rendered as the TOP consolidation candidates: Active/Total renders as '/38' (blank numerator) and Port util %=0.0, which reads as 'this switch has all ports free — ideal to consolidate onto'. Absent utilization data is silently presented as 0% utilization (maximum spare capacity), the exact 0/absent-as-health pattern. A reader could target a switch for consolidation whose real utilization is Unknown.
- fix: In the low_util comprehension, additionally require the active-port count to be a real number, e.g. `low_util = sorted([c for c in cap if isinstance(c.get('port_util'),(int,float)) and str(c.get('active_ports','')).strip().isdigit()], key=lambda c: c.get('port_util') or 0)[:10]`. Alternatively keep them but render '—' / 'no data' for the util cell and exclude them from the 'lowest = best consolidation candidate' ranking, so an absent active-port count is never shown as 0% utilization. Mirror the false-health doctrine already used elsewhere in the file: a missing measurement is Unknown, not 0.
- test: Add `test_runbook_capacity_excludes_blank_active_ports` to tests/test_runbook.py: build a snapshot whose capacity list contains one genuine row {active_ports:10,total_ports:48,port_util:20.8} and one data-less row {hostname:'blankcap', model:'', active_ports:'', total_ports:38, port_util:0.0}; render the runbook; assert the §8 consolidation table does NOT list 'blankcap' as a 0%-utilization candidate (e.g. extract the docx table and assert no row pairs hostname 'blankcap' with a '0' / '0.0' port-util cell, or that 'blankcap' is absent). The existing fixture (test_runbook.py:47-48) only has a clean capacity row, so this path is currently uncovered.

## [Medium/high] HON-runbook-drop-2
- file: cisco_toolkit/runbook.py:762-776  (Runbook DOCX §7 Endpoint & VLAN Census (and §1 headline line 187))
- claim: The runbook's endpoint count in §7 was deliberately upgraded to read the canonical executive_brief.scale.n_endpoints (line 769-773, locked by test_runbook_endpoint_census_uses_canonical_not_mac_sum), but the VLAN count was NOT given the same canonical treatment. The only fleet-wide VLAN figure anywhere in the runbook is `len(ep_per_vlan)` (=121 on the AJ snapshot), i.e. the endpoint-bearing subset, surfaced in the §1 headline as 'VLANs with endpoints' (line 187) and in the §7 census prose as 'across {len(ep_per_vlan)} VLAN(s)' (line 772/775). The canonical published fleet VLAN count is scale.n_vlans=202 (analyze.vlan_inventory; injected at COLLECT_PARSE_V3_23_0.py:2053). CRD §VLANs-in-use, design.docx, the explorer census and the workbook all report 202; the runbook's 'VLAN Census' reports 121. 81 VLANs (L3-forwarding-only / IGMP-querier-only segments) are invisible in the runbook census. A 'census' implies completeness, so a reader reconciling deliverables sees a 202-vs-121 cross-document discrepancy with no canonical anchor in the runbook to explain it.
- fix: In §7, mirror the canonical endpoint line for VLANs: read `vlan_canon = ((snap_dict.get('executive_brief') or {}).get('scale') or {}).get('n_vlans')` and add a sentence such as 'Production VLANs (canonical, from the assessment scale): {vlan_canon}; of these, {len(ep_per_vlan)} carry at least one access-port endpoint.' Keep the §1 'VLANs with endpoints' label (it is honest) but add a 'Production VLANs (canonical)' row reading scale.n_vlans so the runbook headline reconciles to CRD/design/explorer. This removes the 202-vs-121 cross-deliverable drift while preserving the endpoint-bearing subset as an explicitly-labeled subset.
- test: Add `test_runbook_vlan_census_uses_canonical` to tests/test_runbook.py (sibling of the existing endpoint test at line 258): set snap['executive_brief']={'scale':{'n_devices':2,'n_endpoints':99,'n_vlans':202,'n_domains':1}} with interfaces whose access ports only populate ~3 distinct VLANs; render the runbook; assert the document text contains '202' as the canonical VLAN figure (not only the small endpoint-bearing subset count), proving §7 reads scale.n_vlans canonical-first.

## [Medium/high] R2-9-01
- file: webapp/backend/cutover_docx.py:105  (webapp DOCX generators (Cutover Plan))
- claim: The Cutover Plan DOCX renders a row labeled "Devices in scope" whose value is summary["n_devices"], which webapp/backend/cutover.py:363 computes as n_mbb + n_hard = sum of the make_before_break/hard_cutover list LENGTHS across waves (a raw recompute from the snapshot's wave_sequencing arrays). It does NOT read the canonical executive_brief.scale.n_devices. Three sibling deliverables a single reader receives use the IDENTICAL label for the canonical inventoried fleet instead: webapp/backend/nrfu_docx.py:91 (`["Devices in scope", scale.get("n_devices") or len(devices)]` -> 303, the C9 SSOT fix), cisco_toolkit/design.py:189 (`("Devices in scope", n_dev)` where n_dev = scale.n_devices = 303), and cisco_toolkit/crd.py:331 (`("Switches in scope", ev["n_devices"])` = 303). On the current AJ snapshot all 303 inventoried devices fall into move_groups so the cutover figure coincidentally equals 303 (verified: cutover summary n_devices=303 == canonical 303; 0 inventoried devices outside move_groups), so there is no wrong fact TODAY. But the two definitions diverge the moment any inventoried device is not placed in a wave (a normal scoping choice, e.g. excluding the 50 not-collected DS/CS-core devices from move groups): a refutation snapshot with canonical n_devices=10 but only 2 devices bucketed renders cutover "Devices in scope"=2 while NRFU/HLD/CRD render 10 under the same header. The cutover figure also double-counts any switch appearing in BOTH buckets of a wave (it sums list lengths, not a distinct set) — only reachable via uploaded/malformed wave_sequencing since the engine's own compute_wave_sequencing (analyze.py:914-916) assigns each host to exactly one bucket. This violates the "reconcile every shared fact to a single source" guardrail at the cross-deliverable level.
- fix: Disambiguate the labels so a reader who receives both deliverables cannot conflate two different facts. Either (a) rename the cutover row to "Switches migrating in waves" / "Devices in cutover scope" (its true meaning — switches actually placed in a wave) and ADD a separate canonical "Inventoried fleet" row read from (snap_dict.get("executive_brief") or {}).get("scale",{}).get("n_devices") so the in-wave count is visibly a subset of the fleet; or (b) if the intent is the inventoried fleet, change cutover_docx.py:105 to read the canonical scale.n_devices like nrfu_docx.py:91 does. Option (a) is preferred because the cutover scope is genuinely a different fact than the inventoried fleet. Independently, harden cutover.py to count DISTINCT switches (e.g. n_devices = len({s for w in waves for s in w["make_before_break"]+w["hard_cutover"]})) so an uploaded wave_sequencing row with a host in both buckets cannot double-count.
- test: Add webapp/tests/test_backend.py::test_cutover_scope_label_distinct_from_canonical_fleet: build a snapshot whose executive_brief.scale.n_devices=10 but whose wave_sequencing buckets only 2 distinct switches; render write_cutover_docx and assert the document either (a) carries BOTH a canonical fleet figure '10' AND a clearly-labeled in-wave '2' (no bare 'Devices in scope' row), or (b) after the canonical-read fix, that the 'Devices in scope' row shows '10'. Add a second case with a switch listed in both make_before_break and hard_cutover of one wave and assert the rendered count is the DISTINCT count (1), not 2.

## [Medium/high] EX-nrfu-002
- file: webapp/backend/nrfu_docx.py:201-214  (webapp NRFU/ATP DOCX generator — Phase II (logical config & connectivity))
- claim: A real ATP is partitioned per-site/per-wave so a field engineer runs the subset for the wave they just cut over. The engine already computes this: compute_validation_plan stamps every item with a `wave` field (analyze.py:3807) and publishes `validation_plan.by_wave` (analyze.py:3903-3909). On AJ this is 854 items across 3 waves (verified: validation_plan items=854, waves=3, keys 'Group 1'/'Group 15'/'Group 25'). The NRFU DOCX writer ignores `wave` and `by_wave` entirely (grep for 'wave'/'by_wave' in nrfu_docx.py returns no matches) and renders all 854 Phase II tests in a single undivided table (lines 206-212). The result is an 854-row monolith with no per-site/per-wave structure — the tester cannot extract 'the tests for the site I just migrated', and the document does not honor the prompt's required 'per-site' ATP part even though the data to do so is published.
- fix: Group Phase II rendering by wave: iterate `validation_plan.by_wave` (or sort items by `wave` then severity) and emit a sub-heading + table per wave (e.g. '4.x Wave: Group 1'), so the ATP can be torn off per site/wave. Keep a summary line of n_waves in §2.
- test: Add a webapp test seeding validation_plan with items in two distinct waves ('Group 1','Group 2') and assert the rendered DOCX contains a heading/cell naming each wave AND that each wave's tests render under it (e.g. wave label text present as a heading, not just inside one flat table).

## [Medium/high] R2-2-03
- file: webapp/backend/storage.py:138  (webapp Campaign page (wave cards) + SnapshotMeta API)
- claim: storage.add_snapshot persists n_devices = len(snapshot.get('devices') or {}) — the RAW device-key count — into the snapshots table (storage.py:138). That value is returned in SnapshotMeta and rendered verbatim by Campaign.tsx:216 as `{s.n_devices} dev` on every wave card. Meanwhile the same page's summary/cockpit (summary.summarize -> engine.trend_point) reads the canonical scale.n_devices for n_switches (summary.py:119, canonical-locked by test_summarize_projects_the_canonical_headline). Proven: for a snapshot with canonical scale.n_devices=42 but len(devices)=3, the wave card header shows '3 dev' while the cockpit shows 42 switches — two device counts for one snapshot on one page. On the AJ snapshot both are 303 (latent), but the divergence is real and unpinned wherever the DevicePhysical population differs from the health-scored/interface population.
- fix: In storage.add_snapshot prefer the canonical count: `n_dev = ((snapshot.get('executive_brief') or {}).get('scale') or {}).get('n_devices')` and INSERT `n_dev if n_dev is not None else len(snapshot.get('devices') or {})`. This makes the stored/wave-card count match the cockpit's canonical n_switches. (Cheap + already computed in summarize, so no extra work; the summary dict already holds n_switches and could be the source.)
- test: webapp/tests/test_backend.py: add a snapshot whose executive_brief.scale.n_devices=42 but devices dict size=3 via the upload/add path; assert the returned SnapshotMeta.n_devices==42 (canonical) and that it equals summarize(snap)['n_switches'] — locking the wave-card count to the cockpit count. Add a fallback case (no executive_brief) asserting ==3.

## [Medium/high] HON-webapp-backend-001
- file: webapp/backend/summary.py:108,115,69  (POST /api/campaigns/{id}/snapshots; POST /api/campaigns/{id}/ingest; POST /api/demo/seed; GET /api/campaigns/{id}/trend; POST /api/compare)
- claim: summary.summarize() is documented (L103: "Every field degrades gracefully") and partially hardened (L104-106 filter health_scores/punchlist/migration_readiness with isinstance(r, dict)), but three reads still assume engine-shaped types and raise AttributeError when a top-level section is present as a truthy WRONG type (a bare string, or a list of non-dicts): (1) L115 `lr = (snap.get("lifecycle_risk") or {}).get("summary")` crashes when lifecycle_risk is a non-dict (the classic `or {}` does-not-guard-a-truthy-non-dict class — 'str' has no .get); (2) L69-70 in _keystones, `eb = snap.get("executive_brief") or {}` then `eb.get("keystones")` crashes when executive_brief is a non-dict; (3) L108 hands the RAW snap to engine.trend_point() (= cisco_toolkit/html.py _trend_point, L286-296) which iterates health_scores / punchlist / migration_readiness rows and reads (lifecycle_risk or {}).get / (executive_brief or {}).get WITHOUT isinstance guards. Because the upload validator _parse_snapshot_bytes (app.py:109-117) only checks that top-level 'devices' exists, a hand-edited or partially-corrupted snapshot.json passes validation and the unhandled exception surfaces as an opaque HTTP 500 with a stack trace instead of a clean 400/422. The same _trend_point root also 500s GET /api/campaigns/{id}/trend (app.py:189 -> engine.campaign_trend -> compute_campaign_trend) and POST /api/compare (engine.snapshot_delta) for a stored malformed snapshot. Not an SSOT/correctness bug and not engine-produced (the engine always writes lists-of-dicts) — purely a malformed-input robustness gap, but it violates the function's own contract and turns user-fixable bad input into a server fault.
- fix: Make the dashboard projection total over any dict the upload path will accept. (1) In webapp/backend/summary.py harden the two local reads: L115 `_lr = snap.get("lifecycle_risk"); lr = (_lr.get("summary") if isinstance(_lr, dict) else {}) or {}`; L69-70 in _keystones `eb = snap.get("executive_brief"); eb = eb if isinstance(eb, dict) else {}`. (2) Stop handing the raw snap to the engine extractor — either pass a pre-sanitized copy, or wrap defensively: `try: head = engine.trend_point(snap) except Exception: head = {}` (summarize already supplies safe head.get(...) defaults at L119-127, so an empty head degrades cleanly). (3) Best/most complete: harden cisco_toolkit/html.py _trend_point (L286-296) to filter non-dict rows and coerce non-dict subsections — `hs=[r for r in (snap.get('health_scores') or []) if isinstance(r,dict)]`, same for punchlist/migration_readiness, and `_eb = snap.get('executive_brief'); _eb = _eb if isinstance(_eb,dict) else {}` (likewise lifecycle_risk) — which fixes /trend and /compare in one place (note: this file is the engine, technically outside the webapp/backend lane, but it is the shared root for the trend/compare endpoints). (4) Optionally tighten _parse_snapshot_bytes (app.py:109-117) to reject a non-list health_scores/punchlist/migration_readiness or non-dict executive_brief/lifecycle_risk with a 400 so the bad input is named at the boundary rather than swallowed.
- test: Add to webapp/tests/test_backend.py: `def test_summarize_total_over_malformed_sections(): from backend.summary import summarize; for f in ('health_scores','punchlist','migration_readiness','lifecycle_risk','executive_brief'): s = summarize({'devices':{'sw1':{}}, f:'x'}); assert s['n_switches']==1` (must not raise; today it raises AttributeError). And an HTTP-level guard: `def test_upload_malformed_section_is_400_not_500(client): cid=client.post('/api/campaigns',json={'name':'m'}).json()['id']; import json; raw=json.dumps({'devices':{'sw1':{}},'health_scores':'x'}).encode(); r=client.post(f'/api/campaigns/{cid}/snapshots', files={'file':('m.json',raw,'application/json')}, data={'label':'m'}); assert r.status_code != 500` (expect 400/422 after the fix; 500 today). Plus a trend guard mirroring the existing SSOT fixtures: store a snapshot whose health_scores is a list of non-dicts and assert GET /api/campaigns/{id}/trend returns 200, not 500.

## [Low/medium] DET-syslog-4
- file: cisco_toolkit/analyze.py:4143  (engine detector (compute_syslog_intelligence))
- claim: The storm-control branch is `elif fac.startswith("STORM_CONTROL"):` (analyze.py:4143). The Cisco storm-control log on IOS/IOS-XE is %STORM_CONTROL-3-FILTERED (facility STORM_CONTROL -> would match), but on NX-OS storm suppression surfaces differently and is not necessarily a 'STORM_CONTROL' facility. In the AJ corpus there are 0 occurrences of any %STORM*-* facility, so the branch is never exercised on real data, and test_syslog_intelligence.py has no storm-control test (the doctrine key exists but no fixture asserts it fires). This is a lower-confidence gap: the IOS form is plausibly correct, but there is no evidence it works and no NX-OS path. Flagging so it is either tested or documented as IOS-only.
- fix: Add a fixture that proves the IOS form fires ('%STORM_CONTROL-3-FILTERED: A Broadcast storm detected on Gi1/0/5'), and if NX-OS storm-control logging uses a different facility/mnemonic, add that variant (research the exact NX-OS message). If no NX-OS form exists, leave a code comment marking the branch IOS/IOS-XE-only for coverage-honesty.
- test: tests/test_syslog_intelligence.py: assert '%STORM_CONTROL-3-FILTERED: A Broadcast storm detected on GigabitEthernet1/0/5' yields kind=='storm-control' (severity Medium).

## [Low/high] DET-poe-002
- file: cisco_toolkit/analyze.py:2804-2838  ()
- claim: The single PoE-related finding the engine emits is gated by BOTH `POE_FAULT.search(ps)` AND `POWERED.search(desc)` (analyze.py:2830), where POWERED is a narrow regex `cam|camera|ptz|robot|light|on.?air|access.?point|\bap[-_ ]` (analyze.py:2804). On the canonical AJ snapshot this produces exactly 1 punchlist item (PoE fault on AS01-BC-STDOPS-CAR3R13-BCDOH) while the fleet actually has 3 Fault ports and 134 ports actively Delivering power to real endpoints. A PoE port in Fault on a switch slated for cutover is migration-relevant regardless of whether its description happens to match the regex (many ports are described 'IDS Device (On Air)', '*IDS Device', or left blank). This is a coverage gap: the detector is description-string-dependent rather than evidence-dependent, so genuine pre-cutover PoE faults on under-described ports are silently dropped — a 'not observed != healthy' adjacency.
- fix: Decouple the PoE-fault finding from the description regex: raise it for ANY port with poe_status in a fault/denied state (the POE_FAULT set), and ENRICH (not gate) with the powered-endpoint description when present. Keep severity High when the description indicates a powered endpoint and Medium otherwise, but never drop a real Fault. This complements DET-poe-001's budget detector: -002 covers per-port PoE health, -001 covers aggregate budget/oversubscription.
- test: Extend the false-health test: a snapshot with a poe_status=='Fault' port and an EMPTY description must still yield a PoE-fault finding (currently it does not); assert the count of PoE-fault findings equals the count of Fault ports, and that ports with a powered-endpoint description are labelled High while bare Fault ports are at least Medium.

## [Low/high] R2-6-05
- file: cisco_toolkit/analyze.py:4778-4782, 4764-4767  (compute_lifecycle_risk asof handling)
- claim: _to_date falls back to date.today() on any unparseable asof (4780-4781), and the function default asof=None -> today (4766). Combined with derived dates clustered near 'now' (Nexus 5600 ldos 2026-04-30, 4500-X 2025-10-31), the headline count drifts purely with the calendar: the on-disk AJ snapshot already shows asof=2026-06-21 (today) rather than the assessment/collection date 2026-06-13. So the '152 past end-of-support' figure is a function of when the snapshot is regenerated, not only of the evidence — a reproducibility/SSOT hazard for a number the deck calls the primary driver. The pipeline golden deliberately excludes lifecycle_risk for exactly this date-sensitivity (test_lifecycle.py:1-3), which confirms the fragility but also means no test guards the asof-source choice.
- fix: Pin asof to the collection/assessment date (the snapshot already knows it) rather than letting it default to today at render time, and make a malformed asof raise/log instead of silently using today (so a bad date is visible, not absorbed). At minimum, record the asof provenance in the note ('classified as of <date>, the assessment date') so a reader can see the basis date. This stabilizes the Past-LDoS count across re-renders.
- test: tests/test_lifecycle.py: assert compute_lifecycle_risk(devs, asof='not-a-date') does NOT silently equal the today-run result (either raises or is flagged); assert that passing the collection date yields a stable band set independent of the machine clock (freeze/inject 'today' and confirm the count tracks asof, not now).

## [Low/high] R2-7-02
- file: cisco_toolkit/analyze.py:4992-4994 (lc_tot = lc.get('n_devices') = 303; lc_pct = round(100*lc_pe/lc_tot))  (compute_executive_brief Hardware-lifecycle axis (analyze.py:5015-5019) -> executive_brief.axes -> deck/runbook/explorer/webapp lifecycle headline)
- claim: compute_executive_brief computes the EoL headline percentage as lc_pct = round(100 * lc_pe / lc_tot) where lc_tot = lifecycle summary n_devices = 303 (the INVENTORIED count, analyze.py:4992) and lc_pe = n_past_ldos + n_near = 152 + 61 = 213. This yields the published '70%' (snapshot axis: '152 past end-of-support, 61 within 1yr (70%)'). But the 56 Unknown-band devices (50 not-collected with no model + 6 collected models absent from eoldb: AS01/AS02-BC-CAR11RD22, DS17-BC-CA05R46-AJDOH, DS18-BC-CA05R47-AJDOH, DS19-BC-CA11D03, DS20-BC-CA11D04) can NEVER contribute to the past/near numerator -- they dilute the denominator only. Over the 247 devices that actually have a KB lifecycle date the figure is 213/247 = 86%; over the 253 collected it is 84%. The published 70% therefore UNDERSTATES the real end-of-support exposure by ~16 points. This is conservative (it never overstates risk), so it is Low, but it is a denominator-mismatch in a customer-facing posture number that a reader may quote.
- fix: In compute_executive_brief (analyze.py:4994), compute the percentage over the KB-DATED population, not the inventoried total: lc_known = lc_tot - lc.get('n_unknown', 0); lc_pct = round(100*lc_pe/lc_known) if lc_known else 0; and qualify the headline as '... (70% of all inventoried / 86% of devices with a known lifecycle)' so the dilution by the 56 Unknown is explicit rather than silently lowering the figure. Alternatively keep 303 but label it '% of inventoried (incl. 56 unknown-lifecycle)'.
- test: Add to tests/test_compute.py / test_lifecycle a case: lifecycle summary {n_devices:10, n_past_ldos:6, n_near:0, n_unknown:4}; assert the executive_brief lifecycle axis headline percentage is computed over the 6 KB-dated devices (100%) or is explicitly labelled, NOT 60% over the inventoried 10 with no disclosure. A revert to the raw /n_devices denominator without a label must fail.

## [Low/medium] R2-8-03
- file: cisco_toolkit/analyze.py:3101-3103  (engine (compute_punchlist) -> every downstream surface (explorer #priority, deck, runbook, design ✎))
- claim: compute_punchlist sorts items with key (-rank, category, title) at L3101 then assigns priority=enumerate index at L3102-3103. On the real AJ fleet, 940 of 1798 rows TIE on that 3-tuple key (e.g. 213 rows all '(Critical, Compound risk, CR-01: End-of-support keystone)', 117 '(High, Health, Critical-health switch)', 75 '(High, Protocol, EtherChannel high)') — they differ only by devices, which is NOT in the sort key. Their relative priority is therefore decided purely by Python's STABLE sort = insertion order. That insertion order is currently reproducible because every contributor list happens to be sorted with a total tiebreaker (Compound: dossiers per_device sorted by (band_rank,-risk_index,host) at analyze.py:5479; Health: health_scores sorted by (score,switch) at ~L1135; Protocol: protocol_health switch-sorted; L2 sublists via sorted(by_ip/by_sub) at excel.py:2335/2337 and via compute_topology_links' sort at analyze.py:255-256). So priority IS reproducible today — but it is an EMERGENT invariant spanning ~20 source lists with NO single test asserting 'same fleet -> identical priority sequence'. If any future contributor is added/changed to iterate an unsorted dict or set without a host tiebreaker, up to ~940 priority integers would silently churn between two assessments of the same fleet, and any artifact/script that references a finding by '#priority' would mis-point — with zero test to catch it.
- fix: Make the punch-list order self-sufficient instead of dependent on every caller: change the sort at analyze.py:3101 to add the row's own stable identity as the final tiebreaker, e.g. items.sort(key=lambda x: (-x['rank'], x['category'], x['title'], tuple(x.get('devices') or []))). This makes priority a TOTAL function of the finding content (independent of any upstream iteration order), so the 940 ties can never reorder.
- test: Add tests/test_punchlist_priority_stability.py: (a) call compute_punchlist twice with the SAME inputs but pass the contributor lists (security dict, device_dossiers per_device, health_scores, protocol_health, l2 sublists) in SHUFFLED order, and assert the resulting [(p['priority'], p['title'], tuple(p['devices'])) ...] sequences are identical — this fails today if any tie is broken only by insertion order, and passes once the devices tiebreaker is added; (b) assert priorities are a contiguous 1..N with no gaps.

## [Low/high] SSOT-explorer-posture-001
- file: cisco_toolkit/blast_radius_explorer.html:3229-3233, 7836-7859  (explorer (blast_radius_explorer.html embedded JS))
- claim: Two Health-view render paths recompute the fleet average health and the critical/poor switch counts directly from the raw SNAP.health_scores array via healthBandToken()/scoreToken(), rather than reading the already-published canonical posture object (executive_brief.posture.{avg_health,n_critical,n_poor}). The sibling narrative line at 7541-7542 was deliberately converted to read posture.avg_health SSOT-first; these two sites were not. On every real engine snapshot the numbers are currently IDENTICAL to canonical (verified below), so this is a latent maintainability/SSOT-discipline risk, not a present wrong fact.
- fix: Mirror the 7541-7542 pattern at both sites: read canonical first with the recompute only as fallback. Site A: `const _po=((SNAP||{}).executive_brief||{}).posture||{}; const avg=_po.avg_health??Math.round(...); const nc=_po.n_critical??(<existing crit count>); const np=_po.n_poor??(<existing poor count>);`. Site B (7836): `const avg=_po.avg_health??Math.round(hs.reduce((s,r)=>s+r.score,0)/Math.max(1,n));` and drive the `bands.crit` headline stat from `_po.n_critical??bands.crit` (keep the ok/watch/risk bucket counts as the local rollup for the good / fair-poor stat, which has no canonical scalar). This makes all three Health-view posture surfaces read one published value, so a future band rename in analyze.py cannot split the explorer's headline from the engine.
- test: Extend the explorer/pipeline SSOT test family: build a snapshot whose executive_brief.posture.{avg_health,n_critical,n_poor} are deliberately set to DISAGREE with what the raw health_scores bands would recompute (e.g. posture.n_critical=7 but only 3 rows carry band 'Critical'), assert the Health-view summary string (modeSummary('health')) and the fleet-health drawer headline render the canonical 7 (and avg from posture), and assert that reverting the reader back to the raw-array recompute makes the assertion fail (mutation-proof). This mirrors the existing webapp _trend_point canonical-first mutation lock referenced in test_pipeline_inprocess.py:80-81.

## [Low/medium] EX-crd-004
- file: cisco_toolkit/crd.py:119  (crd.py _verify_method)
- claim: _verify_method returns 'Design-driven NRFU (§8 + HLD §4)' for any REQ-D id. §8 is the CRD's own Design-Driven Requirements section — i.e. where the requirement is STATED, not where it is PROVEN. The Verification column is meant to declare HOW a requirement is proven (an acceptance test), so citing the requirement's own home section is a self-reference that weakens the testability the N5 column exists to provide. The §7 trace row for the same REQ-D already correctly points the NRFU column at 'design-driven NRFU' (an acceptance test), so the two surfaces are slightly inconsistent. Low severity: the intent (design-driven NRFU) is present; only the section pointer is imprecise.
- fix: Change the REQ-D verification string to reference the acceptance test rather than the CRD's own §8, e.g. 'Design-driven NRFU (HLD §4 → §7 NRFU)' or simply 'Design-driven NRFU acceptance test', keeping it consistent with the §7 trace row's 'design-driven NRFU' wording. The HLD §4 reference (design content) is fine; drop the §8 self-pointer.
- test: In test_crd_gains_design_driven_requirements_section, assert the rendered REQ-D verification cell does not contain '§8' (the requirement's own section) and does contain 'NRFU', locking the verification pointer to the acceptance test rather than the requirement's home section.

## [Low/high] HON-crd-overclaim-3
- file: cisco_toolkit/crd.py:194, 210, 334  (CRD .docx — §1/§2 "evidenced endpoints" vs §6 Future Plans "Evidenced endpoints")
- claim: §1 (crd.py:194) and §2 (crd.py:210) print ev['endpoints'] — a LOCAL recompute (crd.py:34-45: count of access ports with switchport_mode=access and a non-empty end_host_mac). §6 Future Plans (crd.py:334) prints ev['n_endpoints'] — the CANONICAL published value executive_brief.scale.n_endpoints (crd.py:77, with the local recompute only as fallback). On the canonical AJ snapshot both equal 5127 (the canonical scale.n_endpoints is itself the distinct access-port-host-MAC total, so they share a basis and coincide today), so this is NOT currently a wrong fact. But it is a latent single-source-of-truth violation: the same "evidenced endpoints" fact is sourced two different ways in one document, so a future snapshot where the published scale.n_endpoints diverges from the raw access-port recompute (the project's documented 5127-vs-5292 MAC-sum class) would make §2 and §6 disagree. Reported Low/honestly because it does not misreport on the current canonical data.
- fix: Use the canonical-first ev['n_endpoints'] consistently in §1 and §2 as well (it already falls back to the local recompute when scale is absent), so all three sections read one source. Keep the descriptive label "(access ports with a host MAC)" only where it matches the canonical basis, or relabel to "Endpoint census (distinct)".
- test: In tests/test_crd.py, build a snapshot where executive_brief.scale.n_endpoints deliberately differs from the access-port recompute (e.g. scale.n_endpoints=99 but only 3 access-port host MACs); render the CRD; assert the §2 evidenced-endpoints value equals the canonical 99 (not 3), proving §2 reads canonical scale rather than recomputing, and therefore cannot drift from §6.

## [Low/high] SSOT-posture-deliv-003
- file: cisco_toolkit/deck.py:169-171 and 175 (bar at 178-179)  (deck (pptx))
- claim: On the Fleet-posture slide, deck.py:175 prints the Critical-band stat callout from canonical `posture.get("n_critical",0)` (good), but the band-distribution bar recomputes `band_counts` locally from health_scores (deck.py:169-171) and paints the Critical segment from that local tally (deck.py:178-179). avg_health (L172) is also canonical. So a single slide mixes a canonical posture read with a local band recompute for the same Critical metric. On canonical AJ both equal 117, so no visible drift today; this is the same hygiene gap as the runbook (no publish-side lock guarantees the local band tally equals the published posture).
- fix: Drive the Critical bar segment from the canonical posture where the published posture already carries the figure (use posture.n_critical for the Critical segment, falling back to band_counts only when posture is absent), so the bar and the callout share one source. Simplest durable fix is the publish-side posture lock proposed in SSOT-posture-deliv-002, which makes the local band_counts provably equal to posture.
- test: In tests/test_deck.py, extend the existing posture fixture (test_deck.py:16 already sets posture.n_critical) so that posture.n_critical DISAGREES with the health_scores band tally, render the deck, and assert the Critical figure shown equals posture.n_critical (not the recomputed tally) — a mutation test mirroring webapp test_backend.py:1001.

## [Low/high] HON-deck-headline-2
- file: cisco_toolkit/deck.py:159  (deck)
- claim: The title slide's subtitle fallback (deck.py:158-160) is `f"{scale.get('n_devices','—')} devices · {scale.get('n_endpoints','—')} endpoints assessed"` — it reads canonical n_devices (303) and n_endpoints (5127) but omits scale.n_vlans (202), which IS published into the canonical scale (injected at COLLECT_PARSE_V3_23_0.py:2053). So when posture_statement is absent (older/degraded snapshots), the deck's only scale headline drops the canonical VLAN count. Severity is Low because: (1) this string is a fallback only used when `eb.posture_statement` is falsy, and on the canonical AJ snapshot posture_statement IS present (verified), so the fallback is not rendered in practice; and (2) the explorer's own exec headline (blast_radius_explorer.html:8886) likewise surfaces devices/endpoints/domains without VLANs, so the omission is consistent across surfaces rather than a unique deck drift. It is flagged only as a completeness/SSOT-read polish item, not a customer-facing wrong fact.
- fix: Append the canonical VLAN count to the fallback for completeness, e.g. `f"{scale.get('n_devices','—')} devices · {scale.get('n_endpoints','—')} endpoints · {scale.get('n_vlans','—')} VLANs assessed"`. (Leave the primary `posture_statement` path unchanged.)
- test: Add a test that calls write_executive_deck_pptx with a snapshot whose executive_brief has scale={n_devices:303,n_endpoints:5127,n_vlans:202} and NO posture_statement, re-opens the pptx, and asserts the title-slide subtitle text contains '202' (n_vlans) alongside '303' and '5127' — proving the fallback no longer drops the canonical VLAN count.

## [Low/high] EX-lld-05
- file: cisco_toolkit/design.py:404-414  (design.py LLD §3.4 Equipment list (Bill of Materials))
- claim: §3.4 emits a BoM of three columns — Model | Qty | Worst EoL band — aggregated by chassis model only. A real AS migration BoM enumerates the physical estate needed to rebuild/patch: line-cards/modules, transceiver/optic types and counts (per speed), and cabling. The snapshot has `num_modules` per device and per-interface `speed` (20504 ports) and `link_type` from which an optic/SFP BoM could be derived, but none of it reaches the BoM. The current 'BoM' is a chassis census, not a procurement bill a build team can order against.
- fix: Extend §3.4 (or add §3.4.1) with a module/optic BoM: count line-cards via num_modules and roll up transceiver demand by interface speed (e.g. N×10G-SFP+, M×100G-QSFP28) from the active-port speeds. Label it 'as-built physical BoM' and keep it evidence-gated (omit optic rows when speed data absent).
- test: Snapshot with a device num_modules=3 and several 10G active ports; render and assert §3.4 surfaces a module/optic count (e.g. contains '10G' transceiver demand or a 'Modules' figure) — fails today (only Model/Qty/Band columns exist).

## [Low/high] EX-lld-07
- file: cisco_toolkit/design.py:346-349  (design.py LLD §3.1 Device inventory & roles)
- claim: §3.1 columns are Hostname | Model | Serial | SW version | Role | EoL band | Active/Total ports. A real AS LLD device-inventory table carries the management IP (and ideally site/rack/location) so the device is addressable for the build and NRFU. The management IP is discoverable from interfaces[h][port].current_switch_ip for 253 of 303 devices, but §3.1 never reads it. Without a management IP column the inventory cannot be used to reach the devices during cutover.
- fix: Add a 'Mgmt IP' column to §3.1, resolved from interfaces[h][*].current_switch_ip (first non-empty) with '—' when unknown. Optionally add platform/NOS-family which is also collected.
- test: Snapshot where host H's interfaces carry current_switch_ip='10.0.0.9'; render and assert §3.1 text for H contains '10.0.0.9' — fails today.

## [Low/high] EX-lld-08
- file: cisco_toolkit/design.py:373-402  (design.py LLD §3.3 Per-device build detail)
- claim: Interface MTU / jumbo-frame configuration is a standard LLD build fact (cf. _aci_conv.txt Table 42 IS-IS MTU=1492; MTU is a first-class policy element in the reference). The snapshot carries `mtu` on 294 interfaces, but the LLD never renders it — neither per-interface nor as a fabric MTU summary. An MTU mismatch is a classic L2/L3 migration hazard; omitting it from the build record means a rebuilt link can silently differ.
- fix: Surface non-default MTU in §3.3 (e.g. annotate uplinks/SVIs carrying MTU≠1500 with '[MTU 9216]'), or add a short 'non-default MTU interfaces' table. Evidence-gate it (only emit where mtu present and non-default).
- test: Snapshot with an interface having mtu=9216 on an L3/uplink port; render and assert the design text contains '9216' — fails today.

## [Low/high] HON-design-unguarded-2
- file: cisco_toolkit/design.py:115-116  (HLD/LLD design DOCX (lc_by_host build feeding §3.1 inventory EoL band + §3.4/§3.5 BoM))
- claim: Lines 115-116 `for r in (lifecycle.get("per_device") or []): h = r.get("host") or r.get("hostname")` guard only the LIST (`or []`), not each ELEMENT. If any `per_device` entry is a non-dict (None / string) in a malformed snapshot, `r.get("host")` raises AttributeError and the whole render aborts. design_advisor.py reads the same `lifecycle_risk.per_device` and is exposed similarly (e.g. _replacement_bom line 1474), but the design generator additionally promises graceful degradation (docstring lines 16-18) and has a sibling test enforcing exactly this tolerance for stp_roots. PROVEN: a live call with `lifecycle_risk={'per_device':[None, {...}]}` raises AttributeError with the traceback at design.py:116. This crash fires EARLY (before line 178), so on such a snapshot it is the first failure.
- fix: Skip non-dict rows: `for r in (lifecycle.get("per_device") or []):` then `if not isinstance(r, dict): continue` before `r.get(...)`. (Optionally adopt design_advisor's `_as_dict` coercer for consistency.)
- test: Extend tests/test_design.py: set snap['lifecycle_risk'] = {'per_device': [None, 'garbage', {'host':'a','band':'Active'}]}, call write_design_doc_docx and assert the output file exists (no raise).

## [Low/high] HON-design-unguarded-3
- file: cisco_toolkit/design.py:111,128  (HLD/LLD design DOCX (SSOT scale: devices/VLANs in scope, §1 + title page))
- claim: Line 111 `eb = snap.get("executive_brief") or {}` substitutes `{}` only for FALSY values; a TRUTHY non-dict (e.g. `executive_brief: [..]`, or a stray string) passes through unchanged, and line 128 `_scale = eb.get("scale") or {}` then calls `.get` on a list/str and raises AttributeError. This is exactly the 'or {} does not guard truthy non-dicts' failure the lane targets. The canonical scale block (n_devices/n_vlans) is the SSOT the HLD is supposed to read here, so the crash sits on the single-source-of-truth read path. PROVEN: a live call with `executive_brief=[1,2,3]` raises AttributeError at design.py:128. design_advisor.py avoids this class by routing nested reads through `_as_dict()` (e.g. lines 157, 171, 201, 1308).
- fix: Coerce to dict rather than relying on truthiness: at line 111 use a helper, e.g. `eb = snap.get("executive_brief"); eb = eb if isinstance(eb, dict) else {}` (and likewise for the other top-level `or {}` reads that are later `.get`-chained: bp, svc, subnet_intel, lifecycle). Simplest local fix: `_scale = (eb if isinstance(eb, dict) else {}).get("scale") or {}`.
- test: Add a parametrized tolerance test feeding executive_brief in {None, [], 'x', {'scale': None}} and asserting write_design_doc_docx produces the file (no raise) and falls back to len(devices)/len(vlans) for the scale figures.

## [Low/high] HON-design-unguarded-4
- file: cisco_toolkit/design.py:119  (HLD/LLD design DOCX (cap_by_host feeding §3.1 active/total ports))
- claim: Line 119 `cap_by_host = {r.get("hostname"): r for r in capacity}` calls `.get` on every element of `capacity` with no isinstance guard; a non-dict element raises AttributeError and aborts the render. design_advisor.py reads the identical `capacity` list defensively — `_signals` line 251 `cap = [c for c in _as_list(snap.get("capacity")) if isinstance(c, dict)]` — so the two surfaces that read the same field disagree on robustness, and the design DOCX (which documents graceful degradation) is the weaker one. PROVEN: a live call with `capacity=[None]` raises AttributeError at design.py:119.
- fix: Guard the comprehension: `cap_by_host = {r.get("hostname"): r for r in capacity if isinstance(r, dict)}`, matching design_advisor.py:251.
- test: In tests/test_design.py, set snap['capacity'] = [None, {'hostname':'a','total_ports':1,'active_ports':1}] and assert write_design_doc_docx produces the file without raising.

## [Low/high] HON-design-unguarded-5
- file: cisco_toolkit/design.py:340,344  (HLD/LLD design DOCX (§3.1 Device inventory & roles row build))
- claim: Line 340 `d = devices[h] or {}` substitutes `{}` only when the value is falsy; a truthy non-dict device value (e.g. `devices={'a': 'not-a-dict'}` from a malformed snapshot) passes through, and line 344 `d.get('active_ports', ...)` raises AttributeError, aborting the render. Same 'or X does not guard truthy non-dicts' class as finding -3. PROVEN: a live call with `devices={'a':'not-a-dict'}` raises AttributeError at design.py:344. (The same `(d or {}).get(...)` idiom recurs at lines 407, 425-426, 642 over devices.values(), so the fix should be applied consistently.)
- fix: Coerce by type, not truthiness: `d = devices[h] if isinstance(devices[h], dict) else {}` at line 340 (and apply an isinstance coercion to the `(d or {})` reads at 407/425/642 — e.g. iterate `for h, d in devices.items(): d = d if isinstance(d, dict) else {}`).
- test: Add a tolerance test: snap['devices'] = {'a': 'garbage', 'b': {'model':'X','platform':'ios'}} and assert write_design_doc_docx writes the file without raising and still renders host 'b'.

## [Low/high] R2-3-04
- file: cisco_toolkit/docmeta.py:153  (All 7 DOCX 'Document Control' tables)
- claim: docmeta.py:153 renders `generated_at` verbatim, so the snapshot's full ISO value with microseconds prints on the cover — on-disk every Document-Control table shows 'Snapshot captured 2026-06-21T06:58:07.924237'. The very same page's cover line uses a clean `strftime('%Y-%m-%d %H:%M')` ('Generated 2026-06-21 06:58'), so one document displays two different formats for what a reader perceives as the same instant: a polished one and a raw machine timestamp with a 'T' separator and six fractional-second digits. Cosmetic, but it reads as unfinished/auto-dumped on a customer-facing deliverable.
- fix: Normalize the rendered value in docmeta.py:153: format the ISO string to '%Y-%m-%d %H:%M' (parse with datetime.fromisoformat when it is an ISO string, fall back to str() for already-formatted inputs) so it matches the cover's 'Generated' format and drops the microseconds/'T'. Best done together with R2-3-01 so the same display helper serves both the 'captured' and 'generated' rows.
- test: In tests/test_docmeta.py, pass generated_at='2026-06-21T06:58:07.924237' to add_document_control and assert the rendered text contains '2026-06-21 06:58' and NOT '.924237' and NOT a literal 'T06:58'.

## [Low/high] EX-engagement-003
- file: cisco_toolkit/engagement.py:141 (verdict condition); 277 (§2 Assess status); 312-315 (§3 next-action)  (Engagement Workflow & Plan of Record .docx — §1 verdict condition, §2 Assess status, §3 next-action)
- claim: engagement.py groups 'partial' and 'not_collected' devices together under f['blind'] (collected from coll['devices'] regardless of status) and describes them with partial-collection language. The §1 verdict condition (L141) reads '{n_blind} device(s) have partial or missing collection' and §2 (L277) reads '{len(blind)} device(s) with blind spots'. On the canonical AJ snapshot ALL 50 of these are status 'not collected' with data_quality=0 (Counter shows {'not collected': 50}, zero 'partial') — i.e. they are ENTIRELY uncollected, not partially collected. 'Blind spot' and 'partial or missing' soften this: a reader could infer most evidence was gathered with minor gaps, when in fact 50 devices (including, per the engagement context, the DS/CS core whose redundancy is therefore UNKNOWN) have NO evidence. This is a milder instance of the not-observed-vs-known coverage class. (The §3 next-action at L313-314 does correctly split the counts as 'X partial / Y not collected', so the underlying data is available to phrase §1/§2 honestly.)
- fix: Phrase §1 and §2 from the partial/not-collected split that is already computed. In §2 Assess status, when f['csum'].get('not_collected') is set, say e.g. f"Evidence collected for {complete} of {inventory}; {not_collected} not collected, {partial} partial" instead of the generic 'blind spots'. In the §1 verdict condition, when not_collected>0 and partial==0, say '{not_collected} device(s) were NOT collected (no evidence) — every verdict on them is provisional', reserving 'partial or missing' for the mixed case. No data change; wording only.
- test: Add tests/test_engagement.py::test_engagement_distinguishes_not_collected_from_partial: snapshot with collection_completeness.summary={inventory:10, complete:6, partial:0, not_collected:4} and 4 'not collected' device rows; render; assert the verdict/§2 text says the 4 were 'not collected' / 'no evidence' and does NOT call a 0-partial fleet 'partial'. A second fixture with partial>0 asserts the 'partial' wording still appears.

## [Low/high] SSOT-vlan-subsets-01
- file: cisco_toolkit/excel.py:374-421, 2864-2953  (workbook (XLSX))
- claim: Within the generated workbook artifact, the ONLY VLAN count a reader sees is the 'VLAN Census' tab's row count, which is the access/SVI SUBSET (len(agg) ~= 159 access-VLAN ∪ collected-SVI rows), NOT the canonical 202 in-use total. The docstring at excel.py:375-379 correctly states 'this is a labelled SUBSET, not the in-use total -- the canonical "VLANs in use" count (executive_brief.scale.n_vlans, from analyze.vlan_inventory) also includes querier-only VLANs', but that disclaimer lives only in the Python docstring and is NOT written into the rendered sheet: `_census_header` (excel.py:38-45) writes only the column-header row 1, no caption. The 'Executive Summary' landing sheet (write_executive_summary_sheet, excel.py:2866-2953) surfaces switches/health/punch-list but emits NO VLAN count at all (verified: grep for 'VLANs in use'/n_vlans across excel.py returns only the census docstring). So a customer opening the workbook sees the subset row count unlabeled and has no canonical 202 to anchor against -- unlike the design doc, CRD and explorer, which all show the labeled 202. This is a labeling/coverage gap confined to the workbook, not SSOT drift: the subset is a legitimate, correctly-computed population (only access/SVI VLANs can populate a per-VLAN row).
- fix: Surface the docstring's subset disclaimer in the rendered artifact: (a) add a caption row above the VLAN Census header (e.g. ws.cell(1,1,f"Access/SVI VLAN subset — {len(agg)} of {n_vlans} in-use VLANs (canonical count includes querier-only VLANs whose SVI is on an uncollected device; see Executive Summary)") then shift the header/data down one row, OR write it into a sheet-level note/comment; and (b) add a '_kv("VLANs in use", brief["scale"]["n_vlans"])' line to write_executive_summary_sheet's 'Fleet posture' block so the canonical 202 appears once in the workbook as the anchor. Pass the brief (already a param) to read scale.n_vlans canonical-first.
- test: Extend webapp/tests or tests/test_*.py: build the workbook from a fixture snapshot where vlan_inventory yields 202 but only ~159 VLANs have an access port/SVI; assert (1) the Executive Summary sheet contains a cell reading 202 next to a 'VLANs in use' key, and (2) the VLAN Census sheet's caption/first row contains both the subset count and the word 'subset' (so the rendered artifact, not just the docstring, labels it). Discriminating: a regression that drops the caption or the canonical cell turns it red.

## [Low/high] EX-workbook-005
- file: cisco_toolkit/excel.py:2426-2472  (workbook · FHRP Consistency + Executive Summary)
- claim: FHRP gateway redundancy is a canonical posture fact (0 of 209 gateway instances observed with FHRP — the headline migration risk for this fleet), but the workbook surfaces it only as 52 per-VLAN issue rows on the 'FHRP Consistency' tab with no summary headline (e.g. '0 of N gateways protected'), and the Executive Summary scorecard carries no FHRP line at all. A reader must count rows to learn that first-hop redundancy is entirely absent, even though the engine knows the aggregate and the design/CRD deliverables state it.
- fix: Add a summary row at the top of the FHRP Consistency sheet ('N of M gateway instances have an FHRP group — first-hop redundancy is absent on the fleet') computed from compute_fhrp_consistency / the gateway census, and add an FHRP/redundancy line to the Executive Summary scorecard's posture block. This is presentation-only; the aggregate is already derivable.
- test: Assert the FHRP Consistency sheet contains a summary cell stating the protected-vs-total gateway count, and that for a fleet with zero FHRP the figure renders as '0 of <n>' (matching the engine's canonical gate (fhrp or 'none') != 'none').

## [Low/high] DET-capacity-04
- file: cisco_toolkit/excel.py:584; design_advisor.py:38  (workbook Capacity sheet vs detector narrative)
- claim: CONSISTENCY GAP (real). compute_capacity flags a switch 'Port-bound (>=90%)' at excel.py:584 (putil >= 90), but the design detector fires its capacity decision at >=85% (design_advisor.py:38). A reader cross-referencing the design narrative against the Capacity sheet sees the narrative call AS26 (85.2%) 'hot / size for headroom' while the workbook's Flag column leaves it BLANK — two surfaces, two thresholds, for the same underlying fact. On AJ this is exactly the boundary case: both firing switches (85.2, 88.9) are >=85 (detector fires) but <90 (workbook unflagged), so the divergence is currently visible, not hypothetical.
- fix: Unify the two thresholds to a single named constant. Either import _PORT_UTIL_HOT into excel.compute_capacity so the 'Port-bound' flag uses the same 85% boundary (and update the flag label to '>=85%'), or, if 90% is the intended hard-bound and 85% the soft 'design-in-headroom' line, make the detector explicitly state the two-band relationship so a reader is not surprised. Prefer one constant in one module.
- test: Add a test asserting excel.compute_capacity's port-bound flag boundary equals design_advisor._PORT_UTIL_HOT (import both and compare), so the two cannot silently drift apart again.

## [Low/high] R2-11-3
- file: cisco_toolkit/html.py:301  (html.py _trend_point() -> campaign timeline avg_health (fallback path))
- claim: When a snapshot lacks the canonical posture.avg_health (older snapshots), _trend_point falls back to round(sum(scores)/len(scores),1) where scores (html.py:287) = [r.get('score') for r in hs if isinstance(r.get('score'),(int,float))] — with NO band filter, so Insufficient-Data rows (score~90) inflate the recomputed campaign-timeline average exactly as in R2-11-1. The primary path reads canonical posture.avg_health, which is itself inflated, so the timeline carries the inflation either way.
- fix: When fixing R2-11-1, apply the identical Insufficient-Data exclusion to the html.py:287 scores list used by the fallback: `scores = [r.get('score') for r in hs if isinstance(r.get('score'),(int,float)) and r.get('band')!='Insufficient Data']`. This keeps the canonical-first read and the fallback consistent.
- test: In tests/test_campaign_trend.py, build a snapshot WITHOUT executive_brief.posture but WITH health_scores containing two scored rows + one Insufficient-Data score=90 row; assert _trend_point(snap)['avg_health'] equals the mean of the two real scores, not the 3-row mean.

## [Low/high] EX-mop-5
- file: cisco_toolkit/mop.py:257-268  (MOP §2 Global Prerequisites + §x.5 Cutover procedure)
- claim: The MOP has a war-room brief in §2 prerequisites (line 266-267) and 'announce start in the war room' in §x.5, but no explicit communications-plan / stakeholder-notification step: who is notified at window start, at wave success, and on rollback (e.g. service desk, application owners, NOC), and via what channel. A standard change MOP carries a comms/notification line at the bound­aries (start, go/no-go, rollback, completion) so downstream teams and end-users are informed and the change record is auditable. grep for communicat|notif|stakeholder|comms|escalat|contact|bridge in mop.py returns no matches.
- fix: Add a communications step: in §2 prerequisites add 'Agree the communications plan: who is notified (service desk, application/business owners, NOC) and via which channel at window start, at each wave go/no-go, on rollback, and at completion (<contacts/channel>).' and add a short §x.5 step 'Notify stakeholders that the wave is complete / rolled back per the communications plan.' with a success criterion 'notification sent and acknowledged.' Keep contacts as placeholders.
- test: Add test_mop_has_communications_plan(tmp_path): render _snap(); assert the text contains a communications/notification step (e.g. 'communications plan' and 'service desk'/'stakeholders') in §2 and a 'Notify stakeholders' step in a wave's §x.5 procedure.

## [Low/high] HON-mop-precision-3
- file: cisco_toolkit/mop.py:533-534  (MOP §x.7 Rollback)
- claim: In §x.7 every rollback step is an (action, success_criterion) tuple so steps() renders a bold 'Success:' verification line (the MOP-per-step-success-criteria contract, mop.py:147-156) — the declare-rollback step (mop.py:524-526), the multi-group per-group steps (mop.py:531-532), the coupled-subwave step (mop.py:536-539), the make-before-break / hard-cutover steps (mop.py:541-547) and the final re-run step (mop.py:548-550) all carry one. The single-group strategy-specific branch is the lone exception: `rb.append("Strategy-specific: " + playbook["rollback"].rstrip(". ") + ".")` (mop.py:534) appends a bare STRING, so that rollback action prints with no success criterion to confirm the rollback actually restored service. It is not wrong, but it is the one rollback action a war-room reader cannot tick off against an explicit pass condition.
- fix: Make mop.py:534 a tuple consistent with its siblings, e.g. `rb.append(("Strategy-specific: " + playbook["rollback"].rstrip(". ") + ".", "this wave's endpoints are restored on the legacy path and pass the §" + f"{wi}.6 checks."))`.
- test: Extend tests/test_mop.py::test_mop_surfaces_blockers_and_rollback (single-group fixture, recommended_scenario hard-cutover with playbook.rollback='re-cable to legacy port') to assert that a 'Success:' run appears in the same paragraph block as the strategy-specific rollback action, so every §x.7 step carries a verification criterion.

## [Low/high] EX-ops-04
- file: cisco_toolkit/ops.py:351-357  (ops.py §7 Escalation & TAC readiness)
- claim: The escalation tier table (ops.py:353-357) leaves P1 as '<service-down condition>' entirely, even though the handbook has already computed ev['keystones'] — the top-5 devices whose loss strands the most endpoints — and rendered them with stranded counts in §2.1 (ops.py:167-175). A real Day-2 handbook anchors the P1 severity definition to the evidenced blast radius (e.g. 'loss of a keystone such as <host> strands ~N endpoints across M VLANs'). The evidence is in hand but the escalation matrix does not reference it, so the most consequential P1 trigger is left to the customer to rediscover from scratch.
- fix: Anchor the P1 row (or a sentence above the tier table) to ev['keystones']: name the worst keystone(s) and their stranded-endpoint count as a concrete example P1 condition, while keeping the contact/response-target columns as customer placeholders. Keep it evidence-gated (only when keystones exist).
- test: Assert that, for a snapshot with failure_impact keystones, the §7 escalation content references at least one keystone host and/or its stranded-endpoint count; assert that with no failure_impact the section still renders the placeholder tiers without inventing a P1 host.

## [Low/high] DET-qos-copp-002
- file: cisco_toolkit/parse.py:2179-2233 (global_attach handling); 2186-2194 (return shape)  (engine: parse.parse_qos_config)
- claim: The parser already SEES `service-policy` under `control-plane` (the comment at parse.py:2230 names control-plane explicitly) but records only the bare policy NAME into `global_attach`, with no notion that this is the control-plane CoPP attachment, no policer rate, and no class coverage. So even on the 38 IOS-XE devices that DO have CoPP, the engine cannot say anything about WHETHER the CoPP policy actually polices (rate) or merely classifies, which is exactly the rate/coverage dimension the exemplar reports. The return shape (parse.py:2193-2194) has no copp key, so downstream qos_audit/design have nothing to read even if they wanted to.
- fix: Extend parse_qos_config to detect the `control-plane` top-level block specifically and, for the policy-map it attaches, record {policy_name, n_classes, has_police} (scan the named policy-map body for `police`). Add a `copp` key to the return dict. This is the prerequisite enabling DET-qos-copp-001's actionable detector to also distinguish 'CoPP present but no policer' (un-tuned) from 'CoPP absent'. Keep it tolerant/never-raises like the rest of the parser.
- test: tests/test_parse_qos.py: feed a config containing `control-plane / service-policy input CP-POLICY` plus `policy-map CP-POLICY / class BGP / police 8000` and assert parse_qos_config(...)['copp'] == {'present':True,'policy_name':'CP-POLICY','has_police':True, ...}; feed a config with the attachment but a police-less policy-map and assert has_police False; feed a config with neither and assert copp.present False. Also assert the existing global_attach behavior is unchanged (no regression to inert-policy suppression).

## [Low/high] SSOT-posture-deliv-002
- file: cisco_toolkit/runbook.py:128 (rendered at 190-192)  (runbook (docx))
- claim: runbook.py:128 `bands = Counter(r.get("band","") for r in hs)` recomputes the Critical/Poor/Fair/Good/Excellent tally from `health_scores`, and §1 renders it at L190-192. The canonical SSOT for this is executive_brief.posture (analyze.py:5129 publishes n_critical/worst_band) computed at analyze.py:4980-4985. The SAME §1 also renders the canonical brief via `eb["axes"]` (runbook.py:210-212), whose Fleet-health headline carries the canonical n_critical. Today both equal 117 (verified: posture.n_critical==117, local Critical band tally==117) because the engine brief counts over the full unfiltered `health_scores` with identical logic — so there is no current wrong fact. But it is an SSOT-hygiene gap: a canonical read and a local recompute of the same metric sit on the same page, and unlike the VLAN count (locked by test_pipeline_inprocess.py:81 `scale.n_vlans == len(vlan_inventory)`) there is NO publish-side test asserting `posture.n_critical == band tally`, so any future change to how the brief counts bands would silently make the runbook self-contradict.
- fix: Read canonical first with the local tally as fallback only (the pattern already used by the explorer html.py:309 and webapp summary.py:122): `posture = (snap_dict.get("executive_brief") or {}).get("posture") or {}`; for the §1 "Health bands" row prefer posture-published per-band counts where available, and at minimum source n_critical from `posture.get("n_critical")` so the header and the brief table cannot disagree. If full per-band counts are needed and not published, leave the local Counter but add the publish-side lock below.
- test: Add to test_pipeline_inprocess.py (publish-side SSOT lock): assert `snap['executive_brief']['posture']['n_critical'] == sum(1 for x in snap['health_scores'] if x.get('band')=='Critical')` and the same for worst_band, mirroring the existing n_vlans==len(vlan_inventory) lock at L81. This guards every downstream recompute (runbook + deck) in one place.

## [Low/high] DET-mac-arp-003
- file: cisco_toolkit/runbook.py:17,768  (engine (collection + analysis))
- claim: The runbook honestly disclaims that endpoint presence is Confirmed only at snapshot time because 'MAC aging means absence is Unknown' (runbook.py:17 and runbook.py:768 `{_CONF_UNKNOWN} (MAC aging)`), but the engine never collects the actual MAC-address-table aging-time or the ARP timeout, so the size of that Unknown window is itself unknown and non-actionable. A non-default aging-time (e.g. shortened to seconds, or lengthened well past ARP timeout causing black-holing of moved hosts) is a genuine L2 hygiene finding for a migration, and the disclaimer would be sharper if it could cite the real aging value. The base command lists (COLLECT_PARSE_V3_23_0.py:485-530 NX-OS, 532+ IOS) collect `show mac address-table` and `show ip arp` but NOT `show mac address-table aging-time` / `show ip arp` timeout, and no parser reads an aging field.
- fix: Optional rigor add (lower priority than 001/002): add `show mac address-table aging-time` to the NX-OS+IOS base command lists and parse the global/per-VLAN aging seconds; record `mac_aging_seconds` per device. Then either (a) enrich the existing 'absence is Unknown' disclaimer to cite the real aging window, and/or (b) flag non-default aging (!= 300s) as an Info/Low hygiene note. Keep engine_actionable honest and Unknown when the command wasn't collected. Do NOT over-claim — this is a refinement of an existing honest disclaimer, not a new high-severity gap.
- test: tests/test_platform_variants.py: parse `show mac address-table aging-time` output (IOS '300' and NX-OS per-VLAN forms) -> assert mac_aging_seconds is captured; a device with aging-time 30 surfaces an Info-level hygiene note while a device at 300 does not.

## [Low/high] EX-nrfu-003
- file: webapp/backend/nrfu_docx.py:205-212  (webapp NRFU/ATP DOCX generator — Phase II rows)
- claim: Every validation_plan item carries a `why` field — the failure-impact rationale (e.g. 'VLAN N endpoints lose their default gateway if this SVI is down after cutover', analyze.py:3827; 'A dropped OSPF adjacency black-holes the routes it should exchange', analyze.py:3873). On AJ, all 854 items carry a non-empty `why` (verified: val items with why=854 of 854). The NRFU DOCX writer renders device/category/severity/check/command/expect but NOT `why` (lines 207-209), so the formal acceptance document states each test's command and expected output but omits WHY a deviation matters / what the failure mode is. A senior ATP couples each test's pass/fail criteria with its consequence-on-failure; the engine provides it and the writer discards it.
- fix: Add a 'Why / failure impact' column (or a sub-line beneath each Phase II row) sourced from it.get('why'). If table width is a concern, render `why` as a small italic note under the check rather than a column.
- test: Seed a validation_plan item with a distinctive why ('black-holes the routes') and assert that substring appears in the rendered Phase II DOCX output.

## [Low/medium] EX-nrfu-004
- file: webapp/backend/nrfu_docx.py:162-164  (webapp NRFU/ATP DOCX generator — §2.1 design-decision coverage)
- claim: §2.1 builds `phase_by` from design_nrfu items, then for each recommended decision emits 'Tested — ' + (phase_by.get(d.id) or 'post-cutover-functional') (lines 163-164). The `or 'post-cutover-functional'` fallback fires whenever a recommended decision has NO matching design_nrfu item, asserting the decision is 'Tested' (in the functional phase) when in fact no acceptance test was generated for it. Today compute_design_nrfu emits one item per recommended decision so the map is complete on AJ (30 rec == 30 items), making this latent rather than live. But it is a coverage-honesty hazard: if the two surfaces ever diverge (a recommended decision is filtered out of design_nrfu, or decision_id/id keying drifts), the ATP would silently claim test coverage that does not exist — the opposite of the 'not a silent gap' promise in the same section (line 159-160).
- fix: When phase_by has no entry for a recommended decision, render an honest label such as 'Covered by phase tests (no dedicated case)' or 'Not individually tested — see Phase II/III', not an unconditional 'Tested — post-cutover-functional'. Drive the coverage column from actual presence in design_nrfu, not a hard-coded default.
- test: Seed design_blueprint with a recommended decision whose id is ABSENT from design_nrfu.items; assert §2.1 does NOT print 'Tested — post-cutover-functional' for it (i.e. it is not falsely marked tested).

## [Low/high] HON-webapp-frontend-2
- file: webapp/frontend/src/pages/Dashboard.tsx:14  (AssessHub webapp — Campaigns dashboard PostureStrip)
- claim: `{s.avg_health || "—"}` treats a legitimate average-health value of 0 as 'no data' and prints an em-dash, because 0 is falsy in JS.
- fix: Guard on presence/type, not truthiness: `{typeof s.avg_health === "number" ? s.avg_health : (s.avg_health || "—")}/100`, or `{s.avg_health === "" || s.avg_health == null ? "—" : s.avg_health}/100`.
- test: Add a frontend unit/render test (or assert via summarize fixture) that a summary with avg_health===0 renders the string '0', not '—', in PostureStrip.

## [Low/medium] HON-webapp-frontend-1
- file: webapp/frontend/src/pages/Snapshot.tsx:245 (also 260); webapp/frontend/src/pages/Campaign.tsx:216; webapp/frontend/src/components/CutoverPlanner.tsx:249  (AssessHub webapp — Snapshot header, Campaign wave card, Cutover summary)
- claim: The fleet-size number rendered from SnapshotMeta.n_devices is a non-canonical recompute (the raw devices-dict length) that can disagree with the canonical executive_brief.scale.n_devices that the rest of the UI (and every deliverable) uses, and the two are shown on the SAME Snapshot page.
- fix: Display the canonical fleet size everywhere a 'devices'/'switches' total is shown for a snapshot. Easiest: in Snapshot.tsx header use `s.n_switches` (already canonical) instead of `meta!.n_devices`; in Campaign.tsx wave card use `s.summary.n_switches`; for the cutover Stat keep it labelled as 'devices in plan' (bucket membership) but reconcile it to scale or relabel so it is not read as the fleet total. Alternatively have storage.add_snapshot persist n_devices from executive_brief.scale.n_devices (falling back to len(devices)) so the column itself is canonical.
- test: Extend webapp/tests/test_backend.py: assert that the SnapshotMeta returned by add_snapshot for _divergent_snap() reports the canonical fleet size (n_devices==42, matching summary.n_switches), not len(devices)==3 — locking the meta count to the canonical scale so the header cannot drift from the KPI card.

## [Info/high] R2-7-03
- file: cisco_toolkit/analyze.py:4818 (by_band = Counter(d['band'] ...)); 4842-4845 (summary n_devices/by_band/n_unknown)  (compute_lifecycle_risk vs compute_health_scores vs devices dict vs collection_completeness)
- claim: The lane's primary reconciliation questions resolve WITH NO COUNTING-CONSERVATION DEFECT. (1) lifecycle summary by_band {Past-LDoS:152, Unknown:56, Near-LDoS:61, Active:34} sums to exactly 303 (analyze.py:4818,4842 -- Counter over per_device, which is one record per host in the devices dict, analyze.py:4790-4791). (2) len(per_device)=303 == len(devices dict)=303 == len(health_scores)=303 -- all three iterate the same inventoried device set. (3) The Unknown(56) reconciles precisely to the 50-not-collected + the in-KB-but-no-date set: all 50 collection_completeness not-collected hosts are Unknown (no model/sw to look up via eoldb.lifecycle_for, analyze.py:4793-4799), PLUS exactly 6 COLLECTED devices whose model is absent from eoldb (AS01/AS02-BC-CAR11RD22, DS17-20). 50 + 6 = 56, set-exact. (4) health_scores 'Insufficient Data' band (50) == the collection_completeness not-collected set, set-exact. (5) Inventory sheet population (excel.py:175-211, one DevicePhysical row per host) is also 303. No total is double-counted, dropped, or silently coerced. This is reported as a positive verification result so the orchestrator can mark this reconciliation closed rather than re-investigated.
- fix: No code change required for the conservation itself. OPTIONAL hardening to lock it: add an assertion/test that lifecycle summary by_band.values() sums to summary['n_devices'] and that n_unknown == |not_collected ∪ collected_without_kb_date|, so a future eoldb or collection change cannot silently break the partition (currently test_lifecycle.py:46-47 checks a 4-device toy but no sum-conservation or Unknown-decomposition invariant exists; test_collection_completeness.py:29 checks only the tier counts).
- test: Add tests/test_lifecycle.py::test_by_band_conserves_to_n_devices: for the golden snapshot (or a synthetic mix), assert sum(summary['by_band'].values()) == summary['n_devices'] == len(per_device); and a reconciliation test asserting n_unknown == count(not-collected) + count(collected devices whose model has no eoldb record), so the 56 = 50 + 6 decomposition is regression-locked.

## [Info/high] HON-archreview-passbysilence-OPS-4
- file: cisco_toolkit/archreview.py:793-814 (conforms branch L810; no not-assessable path)  (archreview.py compute_architecture_review · D6 Operational readiness · check OPS-4)
- claim: Unlike every other graded check, OPS-4 (L793-814) has only two outcomes: 'advisory' when ≥2 non-unknown NOS families are seen (L799-808) and 'conforms' when exactly one family is seen (L809-814). It keys on the device `platform` field; devices whose `platform` is empty are bucketed as 'unknown' and excluded (L796-798). If platform was not captured for any device, `_fams` is empty, so neither branch fires and OPS-4 simply emits no check at all (silent omission). When at least one platform is known but the genuine multi-NOS picture is unknown (partial collection), a single observed family yields 'conforms' ('The fleet is a single NOS family … minimises cross-platform interoperability risk') — a clean-bill assertion from incomplete platform evidence. This is a softer instance of the lane's pattern: there is no honesty fallback for 'platform/family evidence incomplete'. It is rated Info because `platform` is normally present on every inventoried device, so in practice the conforms branch reflects real single-family evidence rather than pure silence; but the missing not-assessable branch is a coverage-honesty gap relative to the module's stated doctrine (L17-19).
- fix: Add a not-assessable arm: when `not _fams` (no device reports a recognizable platform), emit OPS-4 'not-assessable' ('Switch NOS family was not captured for any device — the interoperability surface cannot be declared.'). Optionally, when platform is known for only a fraction of inventoried devices, downgrade the single-family 'conforms' wording to note the coverage caveat (single family *among collected* devices) so the clean bill is scoped to evidence.
- test: Test that a snapshot whose devices all have empty/absent `platform` yields an OPS-4 check with verdict 'not-assessable' (currently OPS-4 is omitted entirely). Test that single-family 'conforms' wording is scoped to collected devices when collection_completeness shows uncollected devices.

## [Info/high] SSOT-blueprint-decisions-001
- file: cisco_toolkit/deck.py:360-361  (deck.py (PPTX) vs blast_radius_explorer.html (explorer))
- claim: Both surfaces read the CANONICAL design_blueprint.summary (no recompute, no SSOT drift), but they headline DIFFERENT canonical fields under similar wording. deck.py:360 renders `{bsum.get('n_recommended', len(bp_rec))} design decision(s)` (=30 on the AJ snapshot), while blast_radius_explorer.html:6330 renders a stat card `{s.n_decisions} decisions` (=39) alongside a separate `{s.n_recommended} recommended` card (=30). A reader comparing the PPTX ("30 design decisions") against the explorer ("39 decisions") sees two different totals for the same blueprint. This is a labeling/altitude inconsistency, NOT a data-correctness or single-source defect: n_decisions=39 = n_recommended(30)+n_needs_requirement(9), both published once by compute_design_blueprint (design_advisor.py:2049-2057) and verified runtime-consistent on Migration_Assessment_AUTOFILLED_20260613_063201.snapshot.json (n_decisions=39, n_recommended=30, n_needs_requirement=9, n_critical=5; recompute matches).
- fix: Make the wording self-disambiguating so the numbers can't read as contradictory. Either (a) change deck.py:360 to `{n_recommended} recommended design decision(s)` (it already shows `· {n_critical} critical · {n_needs_requirement} need a requirement`, so adding "recommended" makes 30 unambiguous against the explorer's 39 total), or (b) relabel the explorer stat card at blast_radius_explorer.html:6330 from "decisions" to "total decisions". No data change — both already read the canonical summary.
- test: Extend tests/test_deck.py (the existing deck-blueprint fixture at L121 already provides summary={n_decisions:2,n_recommended:2,n_needs_requirement:1,n_critical:1}) to assert the rendered design slide text contains the word 'recommended' adjacent to the n_recommended figure (e.g. assert 'recommended design decision' in the slide's concatenated runs), locking the disambiguated label so a future edit can't silently revert to a bare 'design decision(s)' that collides with the explorer's total.

## [Info/high] DET-vpc-01
- file: cisco_toolkit/design_advisor.py:937-961 (detector); 302-342 (_signals vpc block); 941 in design_kb.py (engine_actionable:True); 1085 in _DETECTORS  (design_blueprint.decisions[id=dc-vpc-mlag-peer-fabric-integrity])
- claim: A firing engine_actionable detector DOES read vPC/MLAG peer health. _d_vpc_health reads domain-level consistency/peer_status/keepalive_status/peer_link.status AND per-vPC member status (down*) + per-vPC consistency. The backing KB principle dc-vpc-mlag-peer-fabric-integrity has engine_actionable:True (design_kb.py:941). Running _signals + the detector against the canonical AJ snapshot yields vpc_domains=49, vpc_legs=1350, vpc_legs_down=668, vpc_legs_incons=174, vpc_dom_bad=0, and the decision IS present in compute_design_blueprint(snap)['decisions'] with priority=High, status=recommended. The reported '668 down* / 174 inconsistent legs across 49 domains' is exact.
- fix: 
- test: 

## [Info/high] DET-snmp-002
- file: cisco_toolkit/design_advisor.py:615  (detector)
- claim: The 'v2c in use' half of the lane question is already correctly handled and should NOT be flagged as missing. _d_mgmt (design_advisor.py:615-625) is evidence-gated on sig['mgmt_devices'], which is computed (design_advisor.py:150-154) from security[host].findings[].status=='fail' for ids in _MGMT_FAIL_IDS={vty-hardening, insecure-snmp, no-aaa, telnet-enabled, ...} (design_advisor.py:27-28). It fires on the AJ fleet and emits the Critical 'mgmt-secure-protocols-and-rbac' decision whose driver is 'secure access, SNMPv3 and AAA/RBAC' (design_advisor.py:624). The underlying insecure-snmp CIS finding (parse.py:1162-1173) correctly classifies v1/v2c presence, escalates High when a read-write or default (public/private) community exists, passes only on SNMPv3 group/user (parse.py:1070,1174), and redacts the community value. software_risk independently screens v2c rw/ro (analyze.py:4535-4538) joined to advisory cisco-sa-snmp-x4LPhte/CVE-2025-20352; its CIS twins are de-duped from the punch-list (analyze.py:3081). So the v2c-in-use detector exists, is engine_actionable, and fires — the genuine gap is strictly the ACL/source-allowlist dimension in DET-snmp-001.
- fix: No code change. This finding documents that the v2c-in-use detector is intentionally already actionable; only DET-snmp-001 (the ACL dimension) needs work.
- test: Covered by the existing _d_mgmt / insecure-snmp tests; no new test required for this Info item.

## [Info/high] DET-capacity-01
- file: cisco_toolkit/design_advisor.py:38, 253; excel.py:574  (detector (design_advisor._d_capacity / signals))
- claim: VERIFICATION (no defect). The capacity detector reads port_util as a 0-100 PERCENT, not a fraction. compute_capacity() at excel.py:574 sets putil = round(100.0 * active / total, 1) (AJ values e.g. AS26=85.2, AS28=88.9). _PORT_UTIL_HOT=85.0 at design_advisor.py:38 is documented 'PERCENT' and the comparison at :253 (port_util >= 85.0) is percent-to-percent. Confirmed 2 of 303 switches fire on the canonical AJ snapshot (AS26-MGM-CA05R47 85.2%, AS28-MGM-CA11D17 88.9%); none >=90. The fix flagged in MEMORY (percent not fraction) holds and is regression-locked by test_design_blueprint.py:1154/1194 (port_util 95.0 fires, 40.0 excluded). poe_util is structurally empty on AJ because the switches reject 'show power inline' ('% Invalid command') — legitimate coverage-honesty, not a false negative.
- fix: No code change required for the percent semantics. Optionally tighten the inline comment at design_advisor.py:38 to also note poe_util is a percent (it already says capacity[].port_util/poe_util are percentages).
- test: Already covered by tests/test_design_blueprint.py:1154-1194; no new test needed for the percent contract.

## [Info/high] R2-10-01
- file: cisco_toolkit/design_advisor.py:815  (design HLD/LLD (design_advisor _d_oncrit_seg) vs runbook.py:595 vs excel.py:2201)
- claim: The single canonical metric segmentation.gateway_acl.coverage_pct (produced once at analyze.py:4886 as round(100.0*n_acl/n_gw, 1), an already-percent value with ONE decimal) is rendered with inconsistent precision across surfaces: design_advisor.py:815 formats it ':.0f}%' (integer, e.g. '67%'), while runbook.py:595 and excel.py:2201 emit it verbatim as coverage_pct (so it shows the producer's 1-decimal value, e.g. '66.7%'). Same underlying number, two displayed magnitudes that differ by rounding (67% vs 66.7%) when both documents are read side by side. This is a magnitude/rounding inconsistency, NOT a unit error and NOT a fraction-vs-percent confusion (both clearly read as %), so it cannot mislead a reader about order of magnitude; severity is Info/Low. design_advisor.py:173 reads it as float(seg_sum.get('gateway_acl_coverage') or 0.0) which is the same coverage_pct (analyze.py:4941), confirming it is the identical quantity.
- fix: Pick ONE display precision for coverage_pct fleet-wide. Simplest: change design_advisor.py:815 from '{sig['gw_acl_cov']:.0f}%' to '{sig['gw_acl_cov']:g}%' (or ':.1f}%') so the HLD shows the same value the runbook/workbook already show. Alternatively, if integer is the intended public precision, round at the producer (analyze.py:4886) to 0 decimals and update runbook.py:595/excel.py:2201 to match — but that loses precision and would churn the golden, so the design_advisor-side fix is preferred.
- test: Add a test in tests/test_design_blueprint.py (or test_design_addenda.py): build a fixture where gateway_acl coverage is fractional (e.g. n_acl=2, n_gw=3 -> coverage_pct==66.7), run the design-advisor decision set, find the 'security-isolate-oncritical-application-tier' decision, and assert its rationale string contains '66.7%' (matching the runbook/workbook rendering) rather than '67%'. The assertion fails on the current ':.0f}%' and passes after the fix.

## [Info/high] EX-ops-05
- file: cisco_toolkit/ops.py:210-216  (ops.py §3.2 Control-plane capacity baseline)
- claim: §3.2 builds btxt by joining platform_health.summary.bands, which on the AJ snapshot is {'OK':247,'Unknown':56}, then states 'Capacity sample at collection time (247× OK, 56× Unknown). These figures are the NORMAL for this fleet.' The 56 'Unknown' are the not-collected devices (n_not_collected=56, hosts_not_collected listed) — they have no capacity sample at all. Folding 'not sampled' into the same band string as a real sampled band, under a sentence asserting 'these figures are the NORMAL for this fleet', mildly conflates absence-of-evidence with an observed capacity state. Coverage-honesty nuance only — the literal word 'Unknown' is not a false 'healthy', so impact is low.
- fix: Exclude the 'Unknown' (not-collected) entry from the baseline band string, or render it separately as 'N device(s) not sampled' so it is plainly absence-of-evidence rather than a capacity band; keep the 'NORMAL for this fleet' framing scoped to the collected sample only.
- test: Feed a platform_health summary with bands {'OK':n,'Unknown':m}; assert the §3.2 text does not present the Unknown count inside the 'NORMAL for this fleet' band list (e.g. it appears as a separate 'not sampled' statement), so not-collected is never framed as a sampled capacity band.

## [Info/high] HON-ops-baseline-1
- file: cisco_toolkit/ops.py:120-121 (claim); 318-334 (hardcoded cadences)  (Operations Handbook (.docx) — title page 'How to use this handbook' note + §6 Routine Operations Calendar)
- claim: The title-page note (ops.py:120-121) states 'the monitoring thresholds, baselines and cadences below are derived from this fleet's own assessed evidence, not generic defaults.' The cadence column of the §6 table (ops.py:319-329) is a fixed literal tuple — ('Daily', ...), ('Weekly', ...), ('Monthly', ...), ('Quarterly', ...) — with NO fleet-evidence input; the intervals are invented constants, not derived from any snapshot field. So the word 'cadences' in the evidence-derivation claim is an overclaim. NOTE: this is a maintenance-interval framing nit, NOT a numeric threshold masquerading as a baseline (my strict lane). I report it at Info because it is the only groundable honesty wrinkle in the file, and the §6 intro itself (ops.py:316-317) is honest — it reframes them as 'the cadence that keeps the baselines above honest' and names each row's evidence SOURCE rather than claiming the interval is evidence-derived.
- fix: Tighten the title-page wording so the evidence-derivation claim covers only what is actually evidence-derived: change ops.py:120-121 to e.g. 'the monitoring thresholds and baselines below are derived from this fleet's own assessed evidence, not generic defaults; the operating cadences are standard PPDIOO-operate intervals.' This keeps the (true) baseline/threshold claim and stops over-attributing the (invented) cadence intervals to evidence.
- test: Add a docx-render assertion in tests/ (e.g. test_ops_handbook.py) that builds the handbook from a minimal snapshot and asserts the title-page note does NOT claim cadences are 'derived from this fleet's own assessed evidence' (assert 'cadences' not in the evidence-derivation sentence, OR assert the sentence text equals the corrected string), while still asserting §6 renders the four fixed cadences.

