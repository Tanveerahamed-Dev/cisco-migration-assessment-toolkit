generated: "2026-07-11 @ b50d17e base (P2-2 harness build, branch feat/p2-2-retrieval-eval, STACKED on the P2-1 branch claude/clever-agnesi-91bb95)"
stage: "PHASE 0 CLOSED — Phase 1 accrual continues; Phase-2 pre-tasks MERGED (P2-0a..0f all landed), P2-1 delivered as a PR (#329), P2-2 harness delivered as a stacked PR"
execution_phase: "Phase 2 — Retrieval layer falsification (docs/architect-master-plan-2026-07-10.md §5; owner protocol docs/d10-retrieval-eval-design-2026-07-08.md + DEC-006 amendment). P2-1 (eval set) delivered 2026-07-10 (#329, one ruff F401 pair fixed on the stacked branch); P2-2 harness delivered 2026-07-11 (cisco_toolkit/retrieval_eval.py + ollama_retrieval_judge.py + hermetic tests). The falsification RUN itself has NOT happened: it is gated on the human merging P2-1 (frozen set sealed on main first), then `python -m cisco_toolkit.retrieval_eval --run` on the owner machine. Phase-1 data accrual (P1-1) unchanged: REAL rows stay event-driven, never fabricated."
artifacts:
  - "docs/architect-understanding-2026-07-10.md"
  - "docs/architect-master-plan-2026-07-10.md"
qa_verdict: "APPROVE (round 2, all three artifacts; round-1 BLOCK dispositioned; verdict rows recorded)"
p1_3_status: >-
  P1-3 EXECUTED 2026-07-10/11 (DEC-004 ladder, 4 honest iterations, every measurement recorded) —
  outcome: floor NOT durably cleared, judge stays ADVISORY. Rungs: (1) forced per-condition walk ->
  detection up, localization broke (0.0 — first-enum binding); (2) HELD:-copy binding -> 0.4
  point-clear REFUTED by same-config rerun (clean control rejected -> null trust; 4b is run-to-run
  unstable at temp 0); (3) think=true probe-refuted (~360s/call, empty content — CPU-infeasible);
  (4) qwen3:8b worst-of-2 -> localized 0.8/0.6 (only judge to catch D-03) but NO specificity
  (rejects 'n/a - verification step' as missing-rollback, both runs) -> null trust. NOTHING pulled
  (egress); qwen3:4b/8b were already installed. Hardened into code: --runs N worst-run protocol
  (never promoted by the best run; incomplete protocol records nothing), --think knob (default
  off), and DEMOTION semantics — latest_judge_baseline is latest-row-wins so a null-trust
  re-baseline revokes a stale numeric one (was: null rows skipped -> the stale rung-2 0.4 would
  have stamped fresh /qa APPROVEs as GATING; found via refute-first rerun, fixed + pinned by
  test_latest_judge_baseline_null_trust_demotes; selfcheck judge_trust now reports NULL trust
  instead of TypeError->UNKNOWN). Live state verified: latest baseline = 2026-07-11 null row; a
  fresh APPROVE stamps provisional=true; selfcheck GREEN with demotion disclosed. Ladder log +
  small-N honesty (n=5: 2/5 point 0.4 has CP95 lower bound 0.076; 4/5 is the first bound-clearing
  outcome) in docs/quality/README.md judge section. Next-value paths (human decides): one more
  prompt rung on the 8b specificity failure (detection already 2x floor); a mid-size model pull
  (EGRESS — human-approved only); GPU/RAM host for think-mode/8b.
last_completed_task: >-
  P2-1 DONE 2026-07-10 (this branch/PR): the frozen 60-query D10 eval set + sealed anchors, built
  per owner-doc §2-§4 and the DEC-006 amendment. docs/quality/d10-eval-set.jsonl — 20 IDENTIFIER
  rows extracted deterministically from graphify-out/graph.json @ bc16a1c (extractor
  cisco_toolkit/d10_eval_set.py :: extract_identifier_queries; unique code-node labels ranked by
  CROSS-FILE reference in-degree, label tie-break, zero randomness; graph sha256 recorded in the
  fixture _meta) + 20 SEMANTIC (synonym-stripped, every row grounding-cited; 4 are digest-lane
  rows per A5 with requires_digest=true and per-config A2 honesty) + 10 MULTI-HOP (each declares
  its >=2 graph edges, all verified present in the live graph at build time) + 10 NEGATIVE (empty
  qrels; absence grep-verified 2026-07-10, nearest-distractor cited). Graded 0-3 qrels throughout
  (rubric + binarization grade>=2 recorded in _meta). d10-anchors.jsonl (judge-visible:
  aid/query/doc ONLY) + d10-anchor-key.jsonl (SEALED expected-grade bands 5/5/5, defect_panel
  separation pattern) for the P2-2 screening gate. Tests tests/test_d10_eval_set.py: strata counts
  reconciled to the P2-0d query_set owner, grounding-citation + synonym-strip + edge checks,
  anchor-whitelist seal, exact SHA-256 pins on all three fixtures, extractor determinism proven on
  a synthetic mini-graph (live-graph checks skip off the owner machine, saying so). Registered in
  docs/ssot.md (new row; placed clear of the freshness guard's single-row anchors) + documented in
  docs/quality/README.md.
next_task: >-
  HUMAN, in order: (1) review + merge the P2-1 PR #329 (its one red check — ruff F401 ×2 — is
  fixed as the first commit of the stacked P2-2 branch; cherry-pick that commit onto #329 or merge
  the stack in order); (2) review + merge the stacked P2-2 harness PR #331 (branch
  feat/p2-2-retrieval-eval; contains #329's content + the lint fix + the harness); (3) THEN run
  the falsification on the owner machine: `python -m cisco_toolkit.retrieval_eval --run`.
  KNOWN-IN-ADVANCE (gate dry-runs 2026-07-11, full numbers in the #331 comments): the judge
  screening gate FAILS on BOTH in-code ladder rungs — qwen3:4b kappa 1.00/acc 7/15, qwen3:8b
  kappa 0.87/acc 8/15 vs floors 0.70/0.80; direction = harsh-not-agreeable (clear-irrelevant 5/5
  both, relevant/borderline under-credited; likely excerpt-coverage: judge sees 1800 chars, key
  grades the doc). So the run as-is lands PARTIAL (identifier scored + negative diagnostics;
  semantic/multi-hop pooling INVALID; BM25 verdict UNDECIDED). Fork, human-owned: (a) accept
  PARTIAL and run; (b) pre-register an instrument v2 (larger/multi-window excerpts and/or a
  different local cross-family judge), re-gate from scratch, THEN run — never a quiet tweak
  after seeing anchor outcomes. The run was deliberately NOT executed pre-merge (frozen set
  sealed on main before any number exists). Also still open: #327 (P2-0a privacy regraft).
  Dense column stays DEFERRED — query_log.jsonl has 0 real rows (<30 floor); never fabricate the
  mix. Per-stratum power honesty: ~55%/~33% at n=20/10 (owner §7 corrected note). P1-1 accrual
  continues with every /qa; first REAL PIR stays event-driven. LESSON for parallel sessions:
  list open PRs before starting AND before creating one — this session found #329 mid-flight and
  STACKED on it instead of duplicating P2-1 (the #319/#321 lesson applied).
p0_gaps:
  - id: "G-001"
    description: "Protected memory tier -> P0-1"
    status: "closed (PR #308 @ a88c38d)"
  - id: "G-002"
    description: "Proposer != verifier -> P0-2"
    status: "closed (PR #311 @ 8d34df8)"
  - id: "G-003"
    description: "PPDIOO human gates -> P0-3"
    status: "closed (PR #313 @ bda5800)"
  - id: "G-004"
    description: "Merge gate -> P0-4"
    status: "closed (branch protection live, 10 checks + 1 review; red PR #306 BLOCKED)"
  - id: "G-005"
    description: "SSOT registry self-rot -> P0-5"
    status: "closed (PR #307 @ cf0d1af, hardened 91e81d1; retro #309 @ 425110f)"
  - id: "G-006"
    description: "Judge-trust -> P0-6"
    status: "closed (PR #312 @ 6806328, union with the P0-2 wedge)"
pending_human_review:
  - id: "HR-001"
    decision: "DEC-001 mission ladder"
    recommendation: "DECIDED 2026-07-10 by user delegation: ladder CONFIRMED (M0 personal -> M1 client -> M2 product; research = instrument layer)"
  - id: "HR-002"
    decision: "DEC-008 settings.local.json pre-approvals"
    recommendation: "DECIDED + EXECUTED 2026-07-10 (P0-7): python -c and cat> entries removed; revert = re-add + formally accept in HR-005(g)"
  - id: "HR-003"
    decision: "DEC-006 D10 eval corpus digest lane"
    recommendation: "DECIDED 2026-07-10 by user delegation; EXECUTED at P2-0e (PR #318, amendment + probe evidence). Probe finding for Phase-2 attention: the shipped Ollama digest arm interferes on identifier queries (delta -0.329 at n=8) — mitigation is a Phase-2 decision under §7, nothing tuned"
  - id: "HR-004"
    decision: "DEC-009 assumed answers to unanswered P0s"
    recommendation: "DECIDED 2026-07-10 by user delegation: defaults stand (generous-but-bounded resources; conservative risk; first REAL PIR event-driven) until corrected in one sentence"
  - id: "HR-005"
    decision: "P0-9 residual-bypass ledger: (a) Bash-holding agents bypass tool allowlists — floor is permission mode; (b) hooks fail-open by design; (c) graphify ranking engine external/unauditable; (d) out-of-repo consolidation plugin honoring protected: unverifiable — P0-1 wrapper detects loss after the fact; (e) P0-2 provenance identities self-reported; (f) enforce_admins=false — the human CAN bypass (the intended explicit-merge path); (g) settings.local.json — remaining narrow one-shots accepted post-P0-7"
    recommendation: "RATIFIED 2026-07-10 by user delegation ('proceed with best possible decision'): all seven accepted as documented residuals; refuse any item in one sentence to re-open it as a Phase-0 task"
  - id: "HR-006"
    decision: "P2-0c disposition: Ollama pin executed as read-live rather than a cached version"
    recommendation: "SATISFIED-AS-AMENDED 2026-07-10: the master plan asked for 'version + model recorded in ssot.md'; ssot.md:43 records install + nomic-embed-text model but deliberately reads the VERSION live (a cached 0.31.1 had already drifted to 0.31.2 — the exact rot class P0-5 closed). Veto in one sentence to reopen as a literal pin"
context_summary: |
  PHASE 0 CLOSED 2026-07-10 @ 520751d, same day the /architect-plan v5 master plan was written and
  2-round-QA-approved. All six doctrine gaps closed on main; the merge gate is live and proven
  (red PR #306 blocked); the protected tier has a runtime artifact pin; proposer!=verifier and the
  PPDIOO gates are enforced in code; the SSOT registry is de-rotted with a dual-mode freshness
  guard; judge trust is machine-readable (judge_tnr stamped, PROVISIONAL enforced at the
  append_row choke point, LLM judge advisory at TNR 0.2 < 0.25); the vault write-fence covers the
  Bash/PowerShell lane; the local allowlist erosion is removed; the seven irreducible residuals
  are ratified in HR-005. Front line = Phase 1 of docs/architect-master-plan-2026-07-10.md
  (hybrid data policy: N-floors now, sealed 70/30 at N>=50 REAL; 2026-08-05 checkpoint) — feed
  the loops REAL data, never fabricate. P2-0f closed 2026-07-10: owner errata fixed directly
  (D10 paired-power figure + node count; memory_guard + learnings ghost citations) — the two
  original fix-sessions never landed and are superseded; close their windows.

  2026-07-10 (later, this handoff): GitHub Actions billing broke account-wide ~11:35Z (jobs fail
  in 1-4s with ZERO steps + a payments/spending-limit annotation — the diagnostic signature; NOT
  a red diff; last green run 11:05Z). Under user delegation the P2 pre-task sweep above was
  executed as 4 independent PRs off 155ad84 (#318/#321/#322/#323, each locally green under the
  verify-green gate; ssot.md insertions placed in different table regions to merge cleanly in any
  order), plus this handoff PR. The DEC-006 cheapest probe was RUN (preconditions met), producing
  the one substantive new fact: the digest lane's LEXICAL arm is non-interfering (delta 0.000)
  but the shipped OLLAMA arm degrades identifier queries (delta -0.329, mechanism = thresholdless
  cosine ranking double-lists entries under RRF k=60) — recorded in the amendment with limits
  stated (n=8, interference-direction only, no significance claim); mitigation deferred to
  Phase 2 under the unchanged §7 bar. The eval-dep fork was resolved deliberately: rank-bm25
  (auditable) over bm25s, pytrec-eval-terrier (Windows wheels) over the original. Phase 2 remains
  blocked until the pre-task PRs land; P1-2 (holdout contract) is the next agent task, held back
  deliberately to keep the review queue sane during the outage.

  2026-07-10 (latest): billing recovered PARTIALLY at 13:54Z — Tests·py3.10 got a runner and
  passed the FULL suite on the docs-only #318; the other 9 jobs still zero-step-fail on the
  spending-limit annotation, so raising the limit is the remaining human step. All open-PR failed
  runs re-triggered at ~14:0xZ; a convergence sweeper re-runs --failed every ~15 min (successes
  stick) until all green or progress stalls. CONCURRENCY DISCOVERED: parallel sessions raced the
  same plan during the outage — #319 duplicates #321 (P2-0a twice, 19 min apart; comparison +
  authorship declared on #319; exactly one merges), #320 took P1-2 (do not re-implement), #325
  proposes a self-hosted-runner toggle (job-name/required-context wedge risk flagged above; the
  automode classifier correctly declined unsolicited comments on other sessions' PRs — the
  observations live here instead). Lesson: in a multi-session window, list open PRs before
  starting any plan task.

  2026-07-10 (P2-1, this branch): all Phase-2 pre-tasks are MERGED on main (P2-0a query log,
  P2-0b [eval] deps, P2-0d thresholds pin, P2-0e DEC-006 amendment + probe, P2-0f owner errata;
  P2-0c satisfied-as-amended). P2-1 built the falsification run's eval set as committed, frozen,
  SHA-pinned fixtures (see last_completed_task). Notable build decisions, for P2-2's author:
  identifier ranking uses CROSS-FILE reference in-degree (intra-file fan-in ranked test helpers
  above engine hubs) and label-uniqueness (an ambiguous label cannot be "exactly correct by
  construction" — 5 files define _snap()); the corpus contract in the fixture _meta widens the
  recall.py seed globs (qrels reference tests/*.py, root *.py, research_lane/*.py — the harness
  must index at least the union, listed in _meta.corpus_contract.required); binarization for
  binary metrics is grade>=2 (recorded in _meta, fixed BEFORE any run); anchor accuracy = judge
  grade in the keyed band, bands frozen with the key (clear_relevant [2,3] because the
  take-the-MIN ensemble biases down, clear_irrelevant strictly [0], borderline [1,2]). The anchor
  key is plaintext — the seal is separation + SHA-pin + pre-registration, stated plainly, not
  secrecy.
