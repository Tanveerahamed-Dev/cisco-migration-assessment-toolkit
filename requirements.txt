# Runtime dependencies for COLLECT_PARSE_V3_23_0.py (everything else is stdlib).
# Install with:  python -m pip install -r requirements.txt
netmiko>=4.1,<5
openpyxl>=3.1,<4
# Optional: enables the Assessment & Migration Runbook (DOCX) deliverable. Without it the run still
# produces the workbook + explorer + snapshot and logs that the runbook was skipped (or pass --no-docx).
python-docx>=1.1,<2   # optional
