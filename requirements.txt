# Runtime dependencies for COLLECT_PARSE_V3_23_0.py (everything else is stdlib).
# Install with:  python -m pip install -r requirements.txt
netmiko>=4.1,<5
openpyxl>=3.1,<4
# Default deliverables: runbook (DOCX) and executive deck (PPTX). Both are base
# dependencies because the default CLI enables their output paths.
python-docx>=1.1,<2
python-pptx>=0.6,<2

# Installed AssessHub entry point and multipart upload surface.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
python-multipart>=0.0.9,<1
