# Compatibility entry point for the canonical development contract.
# Install from the repository root with:
#   python -m pip install -r requirements-dev.txt
#
# Do not duplicate dependency pins here.  pyproject.toml
# [project.optional-dependencies].dev is the only development/test owner.
-e .[dev]
