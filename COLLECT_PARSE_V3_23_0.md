# COLLECT_PARSE_V3_23_0.py

> Builds on **V3.22.0** (protocol health) → … → V3.16. The final intelligence layer. See earlier `.md`s. This documents **only what V3.23.0 adds**: health scoring + migration readiness + the HTML Health mode.

## Purpose
Synthesises everything the run has computed into two decision-ready outputs:
- **`Health Scores`** — a per-switch **0–100** score with weighted deductions, banded Excellent / Good / Fair / Poor / Critical.
- **`Migration Readiness`** — a per **move-group** verdict **READY / CAUTION / NOT READY** from a 10-check pre-migration checklist.
Both are embedded in the snapshot and rendered by the explorer's new **🏥 Health** mode (nodes coloured by score band).

## Quick Start
Unchanged. Both sheets appear automatically; data also goes into `snap_dict["health_scores"]` and `snap_dict["migration_readiness"]`.

## Dependencies
Unchanged: `netmiko` + `openpyxl` + stdlib. **No new imports, no new collection** (pure synthesis of in-run records).

## Inputs & Outputs
- **Input:** unchanged.
- **Output:** unchanged, plus the `Health Scores` and `Migration Readiness` worksheets, two new snapshot keys, and the HTML Health mode.

## Health score (0–100)
Starts at 100; weighted deductions, **capped per category** so one noisy area can't zero the score:

| Category (cap) | Deductions |
|---|---|
| L1 (−30) | err-disabled −8, single-fiber-uplink −10, error-rate-high −5, half-duplex −8 |
| L3 (−30) | single-gateway −10, no-FHRP −3, tracked-object-down −12 |
| Cross-layer (−45) | Critical −18, High −10, Medium −4, Low −2 (attributed via each finding's `hosts`) |
| Protocol (−25) | High −10, Medium −4 |
| Security (−18) | high −8, medium −3, low −1 (per **failed** CIS config-hardening check from `parse_security`; **only** on devices with a captured run-config — missing posture is never scored as bad) — **NEW-V3.23.60** |

Bands: **≥90 Excellent** (#36e08a) · **≥75 Good** (#7adb8f) · **≥60 Fair** (#ffe566) · **≥40 Poor** (#ff9f45) · **<40 Critical** (#ff5775).

## Migration readiness — 10-check checklist (per move group)
Reuses `compute_move_groups` for the wave definition. Each check is `pass` / `warn` / `fail`; **any fail → NOT READY**, else **any warn → CAUTION**, else **READY**.

1. Redundant uplinks (warn on single-fiber) · 2. **Gateway redundancy** (fail on sole gateway) · 3. **No cross-layer Critical** (fail) · 4. No err-disabled (warn) · 5. STP consistency (warn) · 6. Port-channels healthy (warn) · 7. **Routing adjacencies up** (fail on down OSPF/BGP) · 8. No orphan VLANs (warn) · 9. Clean uplinks / no half-duplex (warn) · 10. **Device-health floor** (fail on a Critical-band switch, warn on Poor).

## How It Works (delta only)
- **Phase 28**: `compute_health_scores(all_interfaces, physical_health, l3_forwarding, cross_layer, protocol_health)` → `snap_dict["health_scores"]`.
- **Phase 29**: `compute_migration_readiness(all_interfaces, compute_move_groups(...), health_scores, …, dep_map)` → `snap_dict["migration_readiness"]`.
- **Cross-layer enrichment**: `compute_cross_layer_correlations` now attaches a **`hosts`** list to every finding, so cross-layer risk can be attributed to specific switches for scoring and the Health view.
- **HTML 🏥 Health mode**: `drawHealthCanvas` colours each node by band; `renderHealthMode` lists per-switch scores + top issues and the per-group readiness verdicts. Graceful empty state when a snapshot predates V3.23. See `blast_radius_explorer.md`.
- New symbols: `HEALTH_SCORES_SHEET_NAME`, `MIGRATION_READINESS_SHEET_NAME`, `_HEALTH_BANDS`, `_READY_FILL`, `_STATUS_FILL`, `_health_band`, `compute_health_scores`, `compute_migration_readiness`, `write_health_scores_sheet`, `write_migration_readiness_sheet`.

## Known Limitations / TODOs
- **Deduction weights and the band thresholds are a defensible default**, not calibrated against a labelled dataset — tune to taste.
- **Calibration aids (V3.23.5):** weights / caps / bands / criticality now live in the `ScoringConfig` dataclass, and the `Score Sensitivity` sheet runs an OAT ±25/±50% sweep showing which weights actually flip bands. Asset-criticality weighting exists but ships at factor **1.0 (opt-in)**, so the headline limitation above still holds for the *defaults* — they remain uncalibrated until you tune `ScoringConfig`.
- Readiness reuses the shared-VLAN connected-component move groups; if you migrate by a different grouping, feed your own groups to `compute_migration_readiness`.
- The single-fiber check is a **warn** (common in real access tiers), not a hard fail; gateway-redundancy / cross-layer-Critical / down-adjacency / Critical-health are the hard fails.
- NX-OS `show interface trunk` native-VLAN parsing under the 2-line header falls through the IOS branch (the `status` field and the switchport-derived mode are correct; the native VLAN may be blank). Cross-platform fixed-column drift like this is the next parser-correctness target (V3.23.6 made parsing fail-soft per section; this is a correctness, not a crash, gap).
- Everything is scan-bound (a redundant off-scan uplink or gateway would change a verdict).
- `data_quality` (V3.23.7) is a coarse completeness heuristic over a 4-command essential set (status / switchport / version / neighbors), not a per-command audit — a switch can read "sufficient" yet still miss a non-essential command. Tune `ScoringConfig.data_quality_threshold` (or the essential set) to taste.
- Inherits all V3.16–V3.22 limits.

## Testing
A `pytest` suite lives in `tests/` (added V3.23.2). It runs fully offline — no
SSH, no real device data:
- **Regression net:** `tests/test_pipeline_golden.py` runs the real
  `--no-collect` pipeline against synthetic fixtures and asserts the
  `snapshot.json` (the HTML explorer's contract) and the workbook's
  sheet-name/header schema against frozen goldens in `tests/golden/`.
- **Unit tests:** `tests/test_parsers.py` (show-command parsers) and
  `tests/test_compute.py` (`compute_move_groups`, `compute_cross_layer_correlations`,
  `compute_health_scores`, `compute_migration_readiness`, `compute_protocol_health`).
- **Fixtures:** `tests/synthetic_fixtures.py` — fully synthetic IOS + NX-OS
  output for 3 switches; safe to commit.

Run `python -m pip install -r requirements-dev.txt` then `python -m pytest`.
Regenerate goldens after a *reviewed* change with
`UPDATE_GOLDEN=1 python -m pytest tests/test_pipeline_golden.py`.

## Chat Context
**Problem being solved:** Roll the full L1→L4 + cross-layer + protocol picture up into a single per-switch health number and a go/no-go migration verdict per wave — the answer a migration lead actually asks for.
**Date:** 2026-06-03
**Related scripts:** `COLLECT_PARSE_V3_22_0.py` (prior version); consumes every prior intelligence layer; renders via `blast_radius_explorer.html` / `.md` (🏥 Health mode).

## Change Log
| Date | Change |
|---|---|
| 2026-06-07 | **V3.23.111** — *Code-review fixes (max-effort self-review of the session diff): 3 findings in the pre/post-cutover validation + PTP layers.* **(1, the important one)** `html._finding_key` digit-normalized the punch-list title (`\d+`→`#`), which collapsed DISTINCT per-identifier findings that differ only by an embedded id — e.g. `Fake FHRP redundancy (VLAN 20)` vs `(VLAN 21)` on the same gateways — into one key, so a real *fix-one / break-another* swap read as **no change** and the cutover verdict could show CLEAN through a regression; now keys on the **full title** (per-VLAN findings stay distinct; an aggregated finding whose count changes honestly shows as resolved+opened). **(2)** `parse_ptp_clock`'s `operational` flag required `num_ports>0`, so a genuine boundary clock on any platform whose `show ptp clock` omits the port-count line was false-flagged **dormant** → a cry-wolf "PTP not boundary-clocked" punch-list item; now a known device type that is not *explicitly* 0-port (or any grandmaster / measured offset) counts as operational, while the real dormant case (Device Type Unknown / 0 ports) stays dormant. **(3)** `compute_snapshot_delta`'s `opened`/`resolved` were built from set differences and sorted by severity only, so equal-severity rows were non-deterministically ordered in the diff workbook; now sorted by (severity, finding-key) for byte-reproducible output. No compute/snapshot/golden change (validation lives in `--compare`; PTP parser unaffected by fixtures). 3 new/updated tests. **176 tests pass.** `ruff` clean; mypy unchanged. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.110** — *Land the runbook §2.1 blind-spot table that V3.23.109's commit missed (+ regression-lock it).* The V3.23.109 `git add` omitted `cisco_toolkit/runbook.py`, so the **§2.1 Collection completeness** table (and the conditional fix to the now-stale "Multicast/PTP not collected" §2 dataset-limit bullet) never reached `main` — the snapshot/sheet/explorer all shipped, but the runbook didn't. This commits that runbook change and adds a `collection_completeness` block to the runbook test fixture with assertions on §2.1 ("Collection completeness", the not-collected device, "a missing device is not a healthy device"), so a missing runbook surface is caught next time. Pure documentation-surface completion — no compute/snapshot/golden change. **174 tests pass.** `ruff` clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.109** — *Pre-assessment collection-completeness / BLIND-SPOT report.* Every finding is only as trustworthy as the data behind it — but the un-/partially-collected inventory devices were buried in band counts. New **`analyze.compute_collection_completeness(inventory_hosts, all_cmd_to_files)`** tiers every device in the INVENTORY (`devices.json`): **complete** (all 4 essential commands returned usable output), **partial** (some essentials missing → Insufficient-Data band), or **not collected** (no usable essential output — unreachable / auth-failed / empty captures), and lists exactly which essentials are missing. Surfaced one-source-of-truth: snapshot `collection_completeness`, a **'Collection Completeness'** workbook sheet, runbook **§2.1** (blind-spot table; also fixed the now-stale "Multicast/PTP not collected" §2 limit when groups are present), and a **`collectionCard()`** in the explorer Health overview (`demoSnapshot()` block added). Evidence-disciplined: `Confirmed` (we know what was/wasn't collected) — "a missing device is not a healthy device". On the real AJ fleet: **303 inventory → 254 complete, 1 partial, 48 NOT collected** (16% blind spots, now explicit). **Additive golden** (`snapshot.json` +`collection_completeness`, `sheet_schema.json` +1 sheet). 2 new unit tests. **174 tests pass.** `ruff` clean; new code mypy-clean. Verified in-browser (0 console errors). In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.108** — *Promote PTP / media-timing readiness to a first-class punch-list finding.* V3.23.107 detected dormant PTP but it only lived in §6.6 / the explorer — not in the prioritized action list a migration lead actually works from. New **`analyze.compute_ptp_readiness(service_map)`** turns the PTP posture into punch-list-shaped finding(s): when PTP is present on the fabric but **no switch is an operational boundary/transparent clock** (timing distributed as plain multicast, not boundary-clocked) it raises a **Medium** "PTP enabled but not boundary-clocked (media timing at risk)" finding listing the affected switches and the PTP multicast groups in flight; a partial mix raises a **Low** finding listing only the dormant switches. Folded into `compute_migration_punchlist` (new `ptp_readiness` param, mirroring the `drift` fold) so it appears in the consolidated punch-list + Executive Summary + runbook risk register. Evidence-disciplined: the clock state is `Confirmed`, the media dependency is Inferred (hence Medium/Low, not High). On the real AJ fleet it flags **1 finding across all 13 PTP switches**. **No golden change** (synthetic fixtures carry no PTP → no punch-list delta). 3 new unit tests. **172 tests pass.** `ruff` clean; new code mypy-clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.107** — *PTP operational-state intelligence + fix the PTP shape bug (driven by the first REAL multicast/PTP re-collection).* The user re-collected the AJ fleet with the V3.23.102 commands and the broadcast goldmine lit up: **73 multicast groups classified** (43 broadcast/AV 239.x media groups + PTP 224.0.1.129/130), **1,310 IGMP queriers**, **PTP on 13 switches**. The real `show ptp clock` revealed (a) a **data finding** — every PTP switch reports `Device Type: Unknown` / `0 ports` / *"no parent clock"*, i.e. PTP is enabled but **NOT acting as a boundary/transparent clock** (PTP flows as plain multicast — a real ST 2110/AES67 timing-readiness gap), and (b) a **shape bug** — `service_map.multicast.ptp` is a `{host: clock}` map, but the runbook §6.6 + explorer were reading it as a single dict (so the PTP line was wrong on real data). Fixes: **`parse_ptp_clock`** now also captures `profile`, `clock_identity`, `num_ports`, and derives **`operational`** (a known device type with ≥1 active PTP port); runbook §6.6 + the explorer's Services & multicast section now consume the `{host: clock}` map and report **"N of M switches act as boundary clocks"** vs **"present but DORMANT (Device Type Unknown / 0 ports) — PTP flowing as plain multicast"**; stale "not collected yet" §6.6 text corrected to reflect the now-classified groups; `demoSnapshot()` PTP block switched to the real `{host: clock}` shape (one operational + one dormant). No golden change (synthetic fixtures carry no PTP). 2 new/expanded parser tests + a runbook PTP-dormant assertion. **169 tests pass.** `ruff` clean; new code mypy-clean. Verified in-browser (0 console errors) + against the real collection's `show ptp clock`. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.106** — *Pre/post-cutover VALIDATION: upgrade `--compare` from a raw diff into a migration-validation report.* `--compare OLD NEW` previously diffed only the raw `interfaces`/`devices` (Summary / Interface / Endpoint / SVI changes) — it ignored every computed analysis key, so it couldn't answer the real post-cutover question: *did anything get worse?* New **`html.compute_snapshot_delta(old, new)`** (pure, tolerant of older snapshots) diffs the COMPUTED state: per-switch **health-band shifts** (regressed vs improved, via `health_scores`) and the consolidated **punch-list findings opened vs resolved** (the punch-list already rolls up all 8 finding sources; matched by a count-/device-order-normalized key so finding-count churn doesn't read as resolved+opened), then derives an overall **CUTOVER VERDICT** — `CLEAN` (no regressions, no new findings) / `REVIEW` (new findings or a switch dropped out) / `REGRESSED` (a new High/Critical finding or a health-band drop). `write_diff_workbook` now leads the Summary with the colour-coded verdict + regressed/improved/opened/resolved counts and adds two sheets: **Health Shifts** and **Findings Delta**. Closes the migration loop (plan → cut → **prove**). Self-contained in the `--compare` path — **no main-pipeline / golden / snapshot-schema change**. 5 new tests (`tests/test_snapshot_delta.py`: finding-key normalization, health+findings+verdict, clean verdict, missing-key tolerance, end-to-end workbook sheets) + verified via the real CLI `--compare`. **168 tests pass.** `ruff` clean; new code mypy-clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.105** — *Bug-hunt hardening: tolerate malformed input across the V3.23.95–.102 intelligence code (found by adversarial fuzzing).* A correctness pass that fuzzed every new compute/parser/lookup with junk/None/non-string/non-dict inputs found one **reachable** crash and three defensive gaps: (1) **`portdb.service_for_port` / `classify_multicast` crashed** (`AttributeError: 'int' object has no attribute 'strip'`) on a **non-string** `proto`/`group_ip` — the public KB module already tolerates None/junk/too-short everywhere else, so the inconsistency is a real bug; now both coerce with `str(...)` and degrade to `None` (this is the reusable "brain", plausibly called with odd args). (2/3) **`compute_endpoint_dependencies` and `compute_subnet_intelligence`** did `g.get("switches", [])`, which returns `None` (crash on iterate) when the key is present-but-`None`, not just when missing — switched to the `g.get("switches") or []` idiom already used by `compute_migration_scenarios`. (4) **`compute_service_map`** crashed on a stray non-dict ACL-rule entry — now skips it. The 2–4 cases aren't reachable via the current pipeline (upstream parsers/computes always emit well-formed data) but make the functions uniformly defensive against future callers/refactors. Pure robustness — **no behaviour change on valid input**, golden untouched. 3 new tests (non-string lookups, malformed ACL rule, null move-group switches). **163 tests pass.** `ruff` clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.104** — *Type hygiene: keep the V3.23.99–.102 code mypy-clean (the "0 new mypy errors" convention).* A post-merge mypy pass of the new protocols/ports KB code found two type slips it introduced (the modules `portdb.py` / `protocol_kb.py` were already clean): `compute_service_map` sorted a mixed-type dict comprehension via `-c["refs"]` (mypy saw the value as `object`) — restructured to sort the typed `(category, count)` items first, then build the dicts; and `gen_port_registry.build()` rebound the loop variable `port` from `str` (IANA loop) to `int` (overlay loop) — renamed the overlay-loop vars (`oport`/`oproto`). Both are pure type-correctness clean-ups with **no behaviour change** (golden untouched; the shipped `port_registry.tsv.gz` is byte-identical). Package mypy 127 → 125. **160 tests pass.** `ruff` clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.103** — *Robustness: create the `--output` directory up-front instead of crashing at save time.* The pipeline emits the workbook, snapshot, topology diagram, HTML explorer and runbook **only at the very end** of the run, all derived from the `--output` path's directory. If that directory did not already exist, `openpyxl`'s `wb.save()` raised `FileNotFoundError` *after* all ~31 phases of multi-minute compute on the full fleet — wasting the entire run (hit in practice regenerating the deliverable into a fresh `IMPROVED_V3.23.102/`). Now `main()` runs `os.makedirs(os.path.dirname(os.path.abspath(out_xlsx)) or ".", exist_ok=True)` the moment the output path is resolved (same guard on the `--compare` diff path) — one guard covers every output. No behaviour change when the directory already exists (golden-safe). 1 new test (`test_missing_output_directory_is_created`). **160 tests pass.** `ruff` clean. In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.102** — *Collection extension: capture the broadcast goldmine (multicast groups, PTP, IGMP querier, ACL hit-counts) — inert until re-collection.* The service map ([[V3.23.101]]) could only see what was already collected. This adds six commands to `COMMANDS_NXOS`/`COMMANDS_IOS` — `show ip igmp groups`, `show ip igmp snooping groups`, `show ip igmp snooping querier`, `show ptp clock`, `show ptp parent`, `show ip access-lists` — plus four tolerant parsers (`parse_igmp_groups`, `parse_igmp_snooping_querier`, `parse_ptp_clock`, `parse_acl_hitcounts`) and four builders (`build_igmp_groups`, `build_igmp_queriers`, `build_ptp`, `build_acl_hits`), all fail-soft to `[]`/`{}` when uncollected (the [[V3.23.97]] BGP-received precedent). `compute_service_map` now folds them in when present: IGMP/snooping/mroute **group census** → classified multicast groups (PTP/Dante/ST-2110 light up); **PTP** grandmaster + offset → `locked` inference (`|offset|<1µs`) that gates ST 2110/AES67/Dante cutovers; **IGMP querier** per VLAN; **ACL hit-counts** upgrade a service's evidence from Inferred (design intent) → **Confirmed (active traffic)**. On the current snapshot all stay empty (Unknown) — the next re-collection with these commands populates them. Surfaced in the snapshot `service_map.multicast` (`ptp`, `igmp_queriers`), runbook §6.6 (PTP/querier lines when present), and the explorer's Services & multicast section (`demoSnapshot()` shows a locked PTP grandmaster + Dante group + queriers). **Additive golden** — `service_map.multicast` +`ptp`/`igmp_queriers` (+2/−0); no new sheet. 6 new unit tests (`tests/test_collection_parsers.py`). **159 tests pass.** `ruff` clean. Verified in-browser (PTP-lock + multicast groups + querier render, 0 console errors). In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.101** — *L4 service map: resolve ACL port references + multicast activity to named services (wire-now, populate-on-recollection).* New **`analyze.compute_service_map(acls, all_interfaces)`** uses the offline port registry ([[V3.23.99]] `portdb`) to turn every L4 port referenced in an ACL into a named `{service, category, broadcast}` and to count fleet multicast activity. Evidence-disciplined: an ACL port reference is **design intent (Inferred)** — *not* proof of active traffic (no flow telemetry); multicast **PIM/mroute presence** on an interface is **Confirmed**, but per-group (S,G)/IGMP classification awaits the richer collection ([[V3.23.102]]). On the real AJ fleet it honestly resolves the only L4 ports in the ACLs — **DHCP 67/68 (Infra) + BFD 3784 (Routing)** — and surfaces the genuinely useful signal: **48 PIM/mroute-active interfaces across 6 switches** (the broadcaster's multicast core), plus any multicast group IPs found in ACLs (classified via the registry). Surfaced one-source-of-truth: snapshot `service_map`, a **'Service Map'** workbook sheet, runbook **§6.6 "Services & multicast (observed vs intent)"**, and a **Services & multicast** section in the explorer's Protocols mode (`demoSnapshot()` block added showing a PTP-event broadcast service). **Additive golden** — `snapshot.json` +`service_map`, `sheet_schema.json` +1 sheet (+80/−0). 3 new unit tests + a runbook §6.6 assertion. **153 tests pass.** `ruff` clean. Verified in-browser (0 console errors). In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.100** — *Protocol-behaviour intelligence: turn an abnormal control-plane state into cause + remediation (the highest-yield protocol-layer win).* `protocol_health` flagged a state with a severity but never said WHY or what to do — so the fleet's 75 EtherChannel-High members read as bare "DEGRADED". New **`cisco_toolkit/protocol_kb.py`** encodes the *expected-vs-abnormal-state doctrine* (research-grounded: OSPF EXSTART/EXCHANGE → interface MTU mismatch; EtherChannel `s`/`I`/`D`/`w` flags; BGP Active/Idle/OpenSent → TCP-179/ASN/auth; VTP higher-revision overwrite of the VLAN DB; STP inconsistency), and **`analyze.compute_protocol_intelligence(protocol_health)`** joins each abnormal state against it → `{switch, protocol, state, severity, meaning, likely_cause, remediation, confidence}`. The observed state is a **fact**; the cause is **Inferred** per Cisco doctrine. Derived purely from `protocol_health` (no raw re-parse; `protocol_health` stays byte-stable). On the real AJ fleet it produces **84 advisories** — 74 down members, **2 stand-alone (silent half-bandwidth + loop risk)**, 8 suspended — and correctly stays silent on VTP (196 transparent, max rev 5 = no overwrite risk: honest, no false alarms). Surfaced one-source-of-truth: snapshot `protocol_intelligence`, a colour-coded **'Protocol Intelligence'** workbook sheet, runbook **§6.5 "Protocol behaviour & remediation"**, and a **Protocol intelligence** section in the explorer's Protocols mode (fleet overview + per-switch detail; graceful no-op on older snapshots; `demoSnapshot()` block added). **Additive golden** — `snapshot.json` +`protocol_intelligence`, `sheet_schema.json` +1 sheet (+21/−0). 3 new unit tests + a runbook §6.5 assertion. **150 tests pass.** `ruff` clean. Verified in-browser (Protocols mode renders cause+fix, 0 console errors). In-code version `3.23.0`. |
| 2026-06-07 | **V3.23.99** — *Ship an offline port / protocol / multicast knowledge base (the twin of the OUI registry).* The toolkit could resolve a MAC to a vendor offline (`ouidb`) but had no way to turn an L4 port or an IPv4 multicast group into a service/meaning. New **`cisco_toolkit/portdb.py`** + shipped **`cisco_toolkit/data/port_registry.tsv.gz`** (12,350 port records + 25 reserved/well-known multicast groups, ~135 KB) give fully air-gapped lookups: `service_for_port(port, proto)` → `{service, category, broadcast, note}`, `classify_multicast(group_ip)` → longest-prefix group match, `is_broadcast_av_port(...)`. The registry merges the **IANA** Service-Name/Port registry (RFC 6335, the long tail) with a **curated overlay** carrying the high-signal semantics IANA lacks — broadcast/pro-AV (PTP 319/320, Dante 4440/4444/4455/8700-8800, AES67/ST 2110 RTP 5004, NDI 5959-7960+, RTSP), OT/ICS (Modbus 502, DNP3 20000, BACnet 47808, OPC-UA 4840, EtherNet/IP), and IT infra/storage/DB/mgmt — each tagged with a `category` and a `broadcast` flag (the AJ media-fabric differentiator). Reproducible via **`python -m cisco_toolkit.data.gen_port_registry`** (mirrors the OUI refresh). Foundation only — **no snapshot / no golden change**; consumed by the protocol-intelligence + service-map layers that follow. 4 new unit tests (`tests/test_portdb.py`: overlay + IANA long-tail + multicast longest-prefix + missing-file resilience). **147 tests pass.** `ruff` clean. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.89** — *Deepen the Waves mode with cutover SEQUENCING (make-before-break vs hard cutover).* The Waves mode showed each move-group's readiness verdict but not HOW to cut it over. Per the migration doctrine (dual-homed endpoints migrate make-before-break; single-homed are hard cutovers), new **`analyze.compute_wave_sequencing(all_interfaces, move_groups)`** classifies each member switch by uplink redundancy from the physical topology (reusing `_topology_adjacency`): a **single-homed** switch (one inter-switch uplink — its uplink is a bridge) is a **HARD CUTOVER** (schedule a maintenance window; its endpoints take a hit), a **dual-homed** switch (≥2 uplinks) is **MAKE-BEFORE-BREAK** (migrate one path, validate, move the other — no outage). Returns per group `{make_before_break, hard_cutover, hard_cutover_endpoints, sequence}` (index-aligned with move_groups / migration_readiness). Surfaced both ways (one source of truth): a new **'Wave Sequencing'** workbook sheet and a **Cutover plan** section in each Waves-mode group card (hard-cutover + make-before-break switch lists, endpoints-at-risk). Embedded as `snapshot_state["wave_sequencing"]`. **Additive golden** — `snapshot.json` +`wave_sequencing`, `sheet_schema.json` +1 sheet. 1 new unit test. `demoSnapshot()` gained a matching `wave_sequencing` block. **124 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.88** — *Graph-analytics: rank the fabric's CHOKEPOINT LINKS (edge betweenness + bridge detection).* The toolkit ranked SPOFs per-**switch** (`compute_failure_impact` / cockpit keystones) but never analyzed the **links** structurally — which inter-switch links carry the most east-west traffic, and which are **bridges** (a link whose failure partitions the fabric). New **`analyze.compute_link_centrality(all_interfaces)`** builds the topology graph from `compute_topology_links` (scanned switches only) and computes **edge betweenness** (Brandes, unweighted — a link with two redundant equal-cost paths is correctly *not* flagged) + **bridges** (DFS low-link) with `pairs_cut` (switch-pairs severed if a bridge fails = product of the two component sizes). Surfaced both ways (one source of truth): a new **'Link Centrality'** workbook sheet (the LINK twin of 'Failure Impact', bridges highlighted) and a **`chokepointCard()`** in the explorer's Health overview (bridges first by pairs-severed, then top betweenness links; `data-jump` rows; "N bridges" / "meshed" badge). Embedded as `snapshot_state["link_centrality"]`. Deliberately **non-redundant** with the per-switch blast radius — this is link-centric. **Additive golden** — `snapshot.json` +`link_centrality`, `sheet_schema.json` +1 sheet. 2 new unit tests (`compute_link_centrality`: bridge + betweenness on a path-off-a-triangle, and the no-links empty case). `demoSnapshot()` gained an illustrative `link_centrality` block. **123 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.87** — *Surface port + PoE capacity headroom in the explorer (Python→HTML one source of truth).* The pipeline computed per-switch port/PoE utilization for the workbook's 'Capacity' sheet, but it lived **only** in the workbook — the explorer never showed which switches can absorb consolidated endpoints on a cutover vs. which are port-/PoE-bound (a core consolidation-planning question). Refactored the inline sheet logic into **`excel.compute_capacity(all_device_physical)`** (the single source of truth — `write_capacity_sheet` now renders its records, output byte-identical so the sheet schema is unchanged), embedded it as `snapshot_state["capacity"]`, and added a **`capacityCard()`** to the Health overview: a fleet headroom summary (free ports fleet-wide, # port-bound ≥90%, # PoE-bound ≥80%) plus the bound switches ranked by port utilization (model · ports % (active/total) · PoE % · flag pills), each a `data-jump` click-to-center row; clean "headroom" badge when nothing is bound. Back-compat: missing/empty key → no card. **Additive golden** — `snapshot.json` gains exactly the one `capacity` key; **`sheet_schema.json` untouched** (the Capacity sheet renders identically). `demoSnapshot()` gained a matching `capacity` block (ACC-1 port-bound, ACC-2 PoE-bound). **121 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.86** — *Add a Migration Waves mode: make the move-group dependency domains interactive, the central migration deliverable.* The pipeline computed move groups (`compute_move_groups` — connected components of the shared-VLAN graph; the switches that must cut over together) and the workbook had a 'Move Groups' sheet, but the explorer never showed the move-group **structure** — only a buried per-group readiness card at the bottom of the Health drawer. Now `snapshot_state` carries a **`move_groups`** key (the same structure the readiness verdicts already attach to — already computed in `main()` for `compute_migration_readiness`, so just embedded; one source of truth with the workbook sheet), and a new **Waves** mode (`drawWaves()` / `paintWaves()`) renders each group as a card: its **READY / CAUTION / NOT-READY** verdict (joined from `SNAP.migration_readiness` by index, switch-set fallback), blast radius (switches · endpoints · fail/warn counts), the **spanning VLANs** that bind it, its gateways, a VLAN-1-spans warning, and the failing/​warning pre-migration checks. **Click a group to spotlight its switches** on the canvas (reuses the Cross-Layer highlight pattern; toggle off via the card, background, or mode change). Headline tallies groups by verdict; sequencing guidance leads with NOT-READY / biggest blast radius first (per the migration doctrine: dependency groups cut over together). Mode fully wired (button, hint, tag, legend, summary, a11y announce; `WAVE_SEL` resets on mode-change / snapshot-load). **Additive golden** — `snapshot.json` gains exactly the one `move_groups` key. `demoSnapshot()` gained a matching `move_groups` block so the bundled demo exercises the mode. **121 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.85** — *Surface two embedded-but-unused snapshot keys (`l3_forwarding` + `score_sensitivity`) as Health-overview cards.* Both keys were already embedded in every snapshot by the Python pipeline but **consumed nowhere** in the explorer — dead weight the reader never saw. Added two cards to `drawHealth()`'s fleet overview: **`forwardingCard()`** renders `SNAP.l3_forwarding` (the gateway-per-VLAN forwarding-proof — the "gateway stays on the core" migration premise made auditable), one row per gateway SVI grouped by severity (High first), showing switch · VLAN · SVI IP · FHRP role/vIP · routing source/next-hop · the risk flags (`single-gateway` / `tracked-object-down` / `no-FHRP` / `ok`) as pills; rows reuse the `data-jump` click-to-center pattern. **`sensitivityCard()`** (rendered right next to its sibling `calibrationCard()`) renders `SNAP.score_sensitivity` (the one-at-a-time ±25–50% weight-perturbation sweep) — the perturbations that flip a health band, plus the set of **fragile verdicts** (switches whose band depends on the uncalibrated weights, each a `data-jump` chip), so the reader knows which bands to trust less than the structural SPOF list. Both omit themselves entirely when their key is absent/empty (back-compat with pre-V3.23 snapshots), guard every `SNAP.x` access, and `esc()` all interpolated strings; counts animate via the existing `.num[data-to]` count-up. `demoSnapshot()` gained small `l3_forwarding` + `score_sensitivity` blocks (referencing DIST-1/DIST-2/ACC-3/ACC-5 and VLANs 10/20/30/40/50) so the bundled demo exercises both cards. HTML-only — **no Python, no snapshot, no golden change** (the keys were already embedded); **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.84** — *Python↔HTML one source of truth: embed the four cross-switch L2/L3 consistency checks in the snapshot and have the explorer consume them instead of re-deriving in JS.* The explorer re-derived **addressing conflicts** (duplicate L3 IPs / overlapping subnets), **FHRP consistency** (fake first-hop redundancy on multi-gateway VLANs), **trunk native-VLAN mismatches**, and **link duplex/speed mismatches** with its own JS — so it could **disagree** with the workbook's 'Addressing Conflicts' / 'FHRP Consistency' / 'Trunk Native-VLAN' / 'Link Duplex-Speed' sheets. Now `snapshot_state` carries four new keys built from the same `excel.compute_*` functions that drive those sheets: **`addressing_conflicts`** (`{dup_ip,dup_subnet}`), **`fhrp`** (issue VLANs only), **`trunk_native`** (native mismatches only), and **`link_phy`** (duplex/speed mismatches). The explorer prefers them: `addressingConflicts()`, `fhrpConsistency()`, `trunkConsistency()`, and `linkPhyConsistency()` each map the Python shape to the JS the card renderers expect, falling back to the existing JS engine only when the key is absent (older snapshots / the in-page demo). FHRP keeps its clean-vs-absent semantics — Python lists *only* issue VLANs, so a VLAN absent from `SNAP.fhrp` falls through to the JS to render the benign ok/na case (never a real issue shown as clean, nor clean shown as an issue). **Trunk allowed-VLAN asymmetry stays explorer-only** (Python computes native mismatches only), so `trunkConsistency()` sources native entries from the snapshot but keeps computing the allowed-VLAN-asymmetry entries in JS. (These `compute_*` fns are already imported at module level — used since V3.23.64 to build the punch-list — so no local re-import is added; one would shadow that earlier use.) **Additive golden** — `snapshot.json` gains exactly the four new keys (regenerated + diff-verified: +8/−1, the −1 a structural brace; `sheet_schema.json` untouched). The demo snapshot is intentionally left without the new keys so it exercises the JS fallback path. **121 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.83** — *Fix a stuck Cross-Layer highlight: clear the canvas spotlight when a filter hides the selected card.* Code-review follow-up to V3.23.82. Selecting a finding card spotlights its switches on the canvas (`XLSEL`); applying a Layer/Severity filter that **excluded** that card left the spotlight stuck on with no visible card to toggle it off (the filter chips re-rendered the drawer but never repainted, and "All" clears the filter, not the selection). Now `drawXLayer()` drops `XLSEL` when the selected card no longer survives the active filter (same host-set test the card's `sel` state uses — preserved when the card still shows), and the filter-chip handlers `repaint()` after re-rendering so the canvas reflects it. Self-healing the other way too: clearing the filter brings the card back as `xlactive`. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.82** — *Make the Cross-Layer mode navigable with layer + severity filters.* The mode shows the top-60 of (on a real fleet) ~3,000 correlated findings, with no way to focus — so a specific layer or severity was buried. Added a filter bar (`Layer: All · L1 · L2 · L3 · L4` and `Severity: All · High · Medium · Low`, reusing the `.chip` style) that filters the finding set **before** the top-N cap, so the cards re-rank within the chosen slice; the heading shows `N of M (filtered from TOTAL)` and the "…more" note nudges to narrow further. Filters are drawer-only state (`XL_LAYER_FILTER` / `XL_SEV_FILTER`), toggle on click (click the active chip or All to clear), and reset on mode-change / snapshot-load. **Verified on real data** (60-switch AJ fleet, ~2,991 findings): unfiltered `60 of 2991`; **L3 → `28 of 28`**; **L3 + High → `1 of 1`**; clearing the layer (High still active) → `60 of 2414`; no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.81** — *Python↔HTML one source of truth: embed the causality chains + failure-impact rollup in the snapshot and have the explorer consume them instead of re-deriving in JS.* The explorer's Causality mode re-derived its structural chains with a separate JS engine (`causalityChains`), and the cockpit's keystone blast-radius used a JS `failureImpact` — so the explorer could **disagree** with the workbook's 'Causality Chains' / 'Failure Impact' sheets. Now `snapshot_state` carries two new keys: **`causality`** (`compute_causality_chains` → `{severity,trigger,mechanism,impact,mitigation}` dicts) and **`failure_impact`** (`compute_failure_impact` records). The explorer prefers them: `causalityList()` builds its structural entries from `SNAP.causality` (parsing the host names out of the trigger/impact text so the canvas still highlights), and `cockpitKeystones()` ranks by `SNAP.failure_impact[].stranded` — both falling back to the JS engine only when the snapshot lacks the keys (older snapshots / the in-page demo). The JS engine still drives the **interactive** Blast simulation (per-node removal, what-if remediation) — those are inherently client-side. (Local import in the snapshot assembler keeps `compute_causality_chains`/`compute_failure_impact` off this module's public surface, per the `test_package` re-export contract.) **Additive golden** — `snapshot.json` gains exactly the two new keys (regenerated + diff-verified: +56/−1, the −1 a structural brace; `sheet_schema.json` untouched). **Verified on real data** (60-switch AJ fleet, fresh run): snapshot carries **2,468 causality chains + 64 failure-impact records**; the Causality mode now shows the Python chains (first = `Gateway CS01-BC-CA05R54-AJDOH-central:Vlan1000 goes down`, "40 of 5591"), and the cockpit keystone for **CS01 reads 857** (matching the Failure Impact sheet exactly — it was 839 from the JS engine); diagram + selection still work; no console errors. `ruff` clean, **mypy unchanged** (101 baseline, 0 new). **121 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.80** — *Flag L1/L2 "path hazards" along a traced path in Path-trace mode — reachable no longer means clean.* Path trace reported hops + SPOF links/switches but said nothing about the silent link-level issues a packet actually crosses. Added `tracePathHazards()` which walks the links the path traverses (`TRACE.linkIdxs`) and flags, reusing the existing per-interface detectors (`_nativeVlan` / `_ifPhy` / `_ifMtu` — one source of truth with the Health-mode trunk/L1/MTU cards): **native-VLAN mismatch** (untagged traffic crosses VLANs — L2 leak / STP inconsistency), **duplex / speed mismatch** (late collisions / CRC errors, link up but degraded), and **MTU mismatch** (frames above the smaller MTU black-holed; jumbo-on-one-end). New **"L1/L2 hazards"** headline stat + a Path-hazards section (per-link rows, or a clean ✓ when none). **Verified on real data** (60-switch AJ fleet): a 4-hop `CS01 → AS05-MGM` path on VLAN 10 reports 0 hazards (clean ✓); on the demo the flagged path renders `Path hazards · 1 — ✗ native VLAN DIST-1 Gi1/0/1 ↔ ACC-1 Gi1/0/24` (30 hazardous pairs detected); no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.79** — *Add a "Device intelligence" dossier to the Blast-radius node detail — and finally surface `physical_health`, which the explorer had never consumed.* Selecting a switch in Blast mode previously showed only its removal impact + endpoints; it said nothing about the box's *own* condition. Added `deviceIntelSection(host)` that folds together, for the selected switch, every per-device finding the collector already computed: **health score + band** (`SNAP.health_scores`), **physical port errors / duplex** (`SNAP.physical_health` — 3,651 per-port records previously surfaced *nowhere* in the explorer; grouped by risk, e.g. `error-rate-high` with the offending ports), **protocol-health issues** (`SNAP.protocol_health`, reusing V3.23.78's row renderer), **CIS security gaps** (`SNAP.security`), **config-hygiene undefined refs** (`SNAP.config_hygiene`), and the **punch-list items naming it** (`SNAP.punchlist`). Rendered as a pill row of counts + grouped detail; omitted entirely when a device is clean. One source of truth — all read straight from the snapshot, no client re-derivation. **Verified on real data** (60-switch AJ fleet): selecting `AAS01-BC-CA05R32-ENDOH` shows `health 29/100 · Critical · 4 ports w/ errors · 2 security gaps · 44 punch-list items` with the `error-rate-high` ports and top punch-list items listed; no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.78** — *Surface the Python-computed `protocol_health` in the explorer's Protocols mode (one-source-of-truth + more intelligence).* The Protocols mode derived only routing *participation* from raw OSPF/EIGRP/BGP neighbor data and **never consumed `SNAP.protocol_health`** — the collector's per-(switch, protocol) STP / EtherChannel / VTP / FHRP / routing-protocol health with a severity (the same analysis the 'Protocol Health' workbook sheet shows). On the 60-switch AJ fleet that meant **25 High-severity issues were invisible in the explorer** (mostly EtherChannel members not bundled, plus VTP/STP risks). Added a **Protocol health** section to the Protocols overview (severity-ranked High/Medium rows: `severity · protocol · switch — summary (detail)`, top-20 + a "see the Protocol Health sheet" note, clean badge when all-Info), a **"proto issues"** headline stat, and the same per-host protocol-health list in the per-switch detail view (`protoHealthSection` / `protoHealthFor` / `protoHealthRow`). Also relaxed the mode's empty-state so a pure-L2 fleet with no routing but with `protocol_health` still shows the section. **Verified on real data** (60-switch AJ fleet): "Protocol health · 25 issues", 20 rows + "…5 more", first row `High · EtherChannel · ACS01-BC-CA05R30 — 6 bundle(s), 16 member(s); 8 not bundled`, the headline stat reads 25, no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. First of the per-mode intelligence additions. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.77** — *Redesign the VLAN selector from an always-open boxy strip into a compact dropdown + popover.* The V3.23.71 chip bar (a pinned header over a height-capped scrollable strip) was functional but ugly — a permanent ~100 px box with a cramped internal scrollbar floating over the canvas. Replaced it with a single small **trigger pill** (`VLAN · All ▾`) that opens a clean **popover** on click — a search box, a live VLAN count, and a roomy scrollable grid of uniform chips (populated VLANs bright, endpoint-less ones dimmed via `.vac`). Collapsed it costs almost no space; it dismisses on outside-click / Escape / selection; selecting updates the trigger label without rebuilding the list (so the filter text survives). `buildChips` now renders the trigger+popover; `setVlan`→`markActiveChip` updates the label; added `toggleVlanPop`/`openVlanPop`/`closeVlanPop`. **Bug fixed in passing:** the no-endpoint chip modifier was class `empty`, which collided with the global `.empty{padding:46px}` empty-state utility (later source order won) and inflated those chips to 112 px tall — renamed to `.vac`. **Verified on real data** (60-switch AJ fleet, 156 VLANs): collapsed trigger + popover render cleanly, all chips a uniform 26 px, `filter "23"` → `{ALL, 23, 232}`, selecting a VLAN updates the label and closes the popover, outside-click/Escape dismiss, no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.76** — *Land the explorer on the Risk cockpit (Health) instead of the raw topology, so the first thing a reader sees is the "where's the risk / fix-first" synthesis.* On a real fleet the default Blast-radius landing opened on a 60-node hairball with no framing; the Health view now leads with the Risk cockpit (risk-by-tier, keystone devices, punch-list triage), which is far more self-explanatory as a first impression. One-line default change (`MODE="health"`) plus moving the active-button highlight from Blast to Health in the toolbar markup. **A deep-link still wins:** `readHash()` returns early on an empty hash (so the new default stands) but applies `#mode=…` when present, so every shared/bookmarked link (e.g. `#mode=blast`) opens exactly where it points; loading a *new* snapshot in-session leaves you on whatever mode you're in. Back-compatible with any snapshot (a pre-health snapshot just shows the Health "no health_scores" empty state). **Verified on real data** (60-switch AJ fleet) and the demo: both open on Health with the cockpit + keystones rendered and only the Health button active; `#mode=blast` correctly overrides to Blast; no console errors. HTML-only — **no Python, no snapshot, no golden change**; **121 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.75** — *Add an "Executive Summary" landing sheet to the workbook — the Excel twin of the explorer's Risk cockpit.* Until now every explorer improvement this session (the unfrozen Health view, the Risk cockpit, keystone ranking) lived only in the HTML; the **workbook** — the standalone deliverable that's actually shared with management/clients who never open the HTML — opened cold on a **33-tab** wall of detail with no "start here". Added `write_executive_summary_sheet(wb, health_scores, punchlist, migration_readiness, all_interfaces)` (in `cisco_toolkit/excel.py`, wired as Phase 30e and **moved to the FRONT** via `wb.move_sheet`) that synthesises a one-pager from data the run **already computed** (no new analysis): **fleet posture** (switch count, average health, health-band distribution), **migration punch-list** breakdown (Critical/High/Medium/Low + top categories), **Keystone devices — fix-first** (the top 10 by `compute_failure_impact().stranded` blast radius — the same prioritisation that works when every per-switch score saturates to Critical, mirroring the explorer's keystone card), **per-group migration readiness**, and a plain-English **"Where to start"** headline. **Verified on real data** (re-ran the 60-switch AJ collection): Executive Summary is the first of 34 tabs; top keystone **CS01-BC-CA05R54-central at 857 endpoints stranded** (matching the Failure Impact sheet), posture/punch-list/readiness all populated. **Additive golden** — `snapshot.json` is **byte-unchanged** (no new snapshot key; this is a workbook-only view) and `sheet_schema.json` gains exactly the one new sheet entry (regenerated + diff-verified); new unit test `test_executive_summary_sheet_is_first_and_synthesizes`. `ruff` clean, **mypy unchanged** (101 baseline, 0 new). **121 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.74** — *Cap the Cross-Layer and Causality card lists so they stop building multi-MB DOMs on a real fleet.* After fixing the Health freeze (V3.23.69) I stress-measured **every** explorer mode on the 60-switch snapshot: blast/trace/compare/flow/protocols/health were all fast and small, but two modes dumped their entire finding set as cards — **Cross-Layer rendered all ~2,963 correlated findings (≈2.6 MB drawer DOM, 2,993 cards)** and **Causality rendered all 3,481 chains (≈1.7 MB, 3,485 cards)**. Not a hard freeze (≈250–325 ms to build) but a multi-MB DOM that's janky to scroll, heavy on memory, and impossible to actually read. Fix (pure explorer JS, matching the existing `punchlistCard` top-N pattern): `drawXLayer` now shows the **60 most-severe** findings and `drawCausality` the **40 most-severe** chains (both lists are already severity-ranked; the structural SPOFs sort ahead of the derivative cross-layer entries, so the cap surfaces exactly the genuine root causes), each with a "…and N more — see the workbook sheet / the cockpit distils all of them" note; and `crossLayerCorrelate(model)` is now **memoised** (per-model `WeakMap`, like the V3.23.69 detectors) so clicking a finding card no longer recomputes all ~2,963 correlations. **Verified on real data** (60-switch AJ fleet): Cross-Layer **2.6 MB/2,993 cards → 55 KB/60 cards (49 ms)**, re-render after the memo **20 ms** (was ~325 ms), card-click still highlights the spanned hosts on the canvas; Causality **1.7 MB/3,485 cards → 22 KB/41 cards (27 ms)**, the selected-chain diagram + flow animation still work; no console errors. HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.73** — *"Keystone devices — fix-first": a device-level priority ranking that works when every per-switch health score saturates to Critical.* Real-data problem: on the 60-switch Al Jazeera fleet (collapsed core, no FHRP) **every** switch scores **0/Critical**, so the cockpit's "Switches by score" table can't prioritise anything, and the genuine signal — *which few devices the whole fleet depends on* — is buried under **2,963** derivative cross-layer punch-list items. Added a **Keystone devices** card to the Risk cockpit (between the risk-by-tier matrix and the by-category triage) that ranks devices by how much the fleet **actually depends on them**: primarily the endpoints **hard-stranded if the device fails** (`failureImpact().hardStranded`, already memoised from V3.23.69), then the count of punch-list items and SPOF chains implicating it (`cockpitKeystones()` → top 6, animated blast-radius bars reusing the existing `.rc-row`/`data-rcw` pattern; each row `data-jump`s to that device's health detail). The blast metric discriminates sharply where the score can't — on real data the central core **CS01-BC-CA05R54** ranks #1 at **839 endpoints stranded** (150 findings · 32 SPOF chains) vs ~110 for the distribution pairs — turning "3,169 items, all switches red" into "harden these ~5 boxes first, starting with CS01." **Verified on real data** (60-switch AJ fleet): 6 keystone rows, correct blast/finding/chain counts (839→87), bars scaled to the worst, `data-jump` carries the full hostname (distinguishing the two CS01 VSS contexts), Health still renders in ~150 ms, no console errors (count-up shows its frozen `0` start only because of the known hidden-preview-tab artifact; `data-to` targets verified via `preview_eval`). HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.72** — *Link the exec Risk cockpit to the topology: click a role tier to highlight that whole tier's switches on the map.* Completes the "topology at scale" track without a risky layout rewrite. Investigation first (recorded so it isn't re-litigated): the 60-node graph is **already a Sugiyama layered layout with 4-sweep barycenter crossing-reduction** (`roleTiers`/`layeredLayout`) and Health mode **already tints every node by health band** (`paintHealth`), and site-filtering already works via the search box (typing `AJDOH` highlights that site) — so *spatial* node-clustering would be **redundant** (role ≈ the visible tier), **partial** (26/60 hosts carry no `XXDOH` site code), and **poorly-shaped** on this data (AJDOH 23 + Unsited 26 = two megagroups + slivers). The genuinely-additive, low-risk win is *interactive* role-tier grouping **on** the map: the cockpit's risk-by-tier rows (V3.23.70) previously jumped to a single switch (hiding the cockpit); they now **toggle-highlight that tier's switches on the topology**, dimming the rest, via a new `TIER_FOCUS={name,hosts}` state that `paintHealth` honors (dim every node outside the tier, only when no single switch is selected). Cleared on mode-change and snapshot-load; the active row gets an accent outline; the toggle updates row state in place (no full re-render, so the drawer scroll is preserved). **Verified on real data** (60-switch AJ fleet): clicking **Distribution** highlights its **15** switches and dims the other **45**, the row goes active, clicking again clears (0 dimmed, focus null), no console errors (visual opacity confirmed governed by the existing `.node.dim` rule — the only caveat being the known *hidden-preview-tab* artifact that freezes the entrance animation, so verification was via `preview_eval` per house practice). HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.71** — *Tame the VLAN chip bar so a large fleet's 150+ VLANs stop eating half the viewport.* Real-data gap: on the 60-switch Al Jazeera fleet the explorer carries **156 VLANs**, and the chip bar rendered them as one flat `flex-wrap` grid (`max-width:64vw`) that wrapped into ~12 rows — a **~300 px wall across the top of every mode** that squashed the topology into an unreadable strip (the 6-VLAN demo never showed this). Restructured the chip bar (pure explorer JS + one additive CSS block) into a **pinned header** (`VLAN` label · `ALL` chip · a numeric **filter box** · a live VLAN count) over a **height-capped (~2-row, 56 px), scrollable chip list**: the bar is now a compact ~100 px control regardless of VLAN count, every VLAN is one scroll or one filter-keystroke away (`applyChipFilter` prefix-matches the typed number; `.empty` VLANs — no endpoints — are dimmed). To keep the filter box's text/focus from being wiped on every chip click, `setVlan` no longer rebuilds the bar — a new `markActiveChip()` just toggles the `.on` highlight (the full `buildChips()` runs only on snapshot load). **Verified on real data** (60-switch AJ fleet): chip bar **~300 px → ~101 px**, list scrolls, `filter "23"` → `{23, 232}`, clears back to 156, topology reclaims the full canvas; demo (7 VLANs) renders a compact strip, filter + active-toggle work, no console errors. HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. First slice of the "topology at scale" track (node site/role clustering still to come). In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.70** — *Exec "Risk cockpit" at the top of the explorer's Health view — a graphical, self-explanatory landing summary that distills a real fleet's scale into two glanceable views.* Real-data gap: on the 60-switch Al Jazeera fleet the Health view is a long scroll of text cards over a **3,169-item** punch-list and a **156-VLAN** topology — impossible to read at a glance, exactly the "what do I fix first / where is the risk" question an exec assessment must answer up front. Added `cockpitCard()` (pure explorer JS, rendered between the fleet-posture headline and the per-switch table) with two graphics, **both built entirely from the Python-computed snapshot** (`health_scores` + `punchlist` — one source of truth, no client re-derivation): **(1) Risk-by-tier matrix** — every switch bucketed Core / Distribution / Access-Core / Access / Other from its hostname role code (`_cockpitTier`, tuned for the real `CS/DS/ACS/AS/AAS/VSS` naming *and* the demo's `CORE/DIST/ACC`), each row a health-band-distribution bar (good/fair/poor/critical) plus the Critical/High punch-list items attributed to that tier, so **where** the risk concentrates is obvious (core/dist faults carry the widest blast radius); clicking a tier jumps to its worst-scoring switch (reuses the existing `data-jump` wiring). **(2) Punch-list triage** — the whole punch-list as severity-stacked bars per category, sorted by volume, so the **shape** of the work is obvious without scrolling (on the real fleet: Cross-layer 2,963 of 3,169, dwarfing FHRP 42 / Inventory 43 / STP 17 / …). Reuses the existing design tokens, pills, `.bar`, `.num[data-to]` count-ups and `.sec` reveal animation (frontend house style — no new aesthetic); one small additive CSS block (`.ckp-*`). Back-compatible: a snapshot without `health_scores`/`punchlist` simply omits the relevant block (the card returns `""`). HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. **Verified on real data** (60-switch AJ fleet, embedded snapshot) and on the built-in demo: cockpit renders, 5 tiers (Core ×5 incl. 2 crit + 307 high, Distribution ×15, Access ×33 …) / 2 tiers on the demo, tier-click jumps, category triage matches the punch-list, no console errors. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.69** — *Unfreeze the explorer's Health view on a real fleet — it was blocking the browser's main thread for ~14 minutes on the 60-switch Al Jazeera snapshot.* Real-data gap: the synthetic 6-node demo renders Health instantly, but on 60 switches × 156 VLANs the view was unusable. Root cause, measured in-browser: the **what-if remediation simulator** (`_bestRemediation`) ran `simulateFix()` — a **13 MB snapshot deep-clone + full model rebuild + `compareModels` diff (≈1.7 s each)** — once **per fixable chain**, and the fleet has **518** of them ⇒ ~860 s; compounded by `rootCauseRank` re-running `causalityChains` and `buildNarrative` + the per-detector cards + `layerHeatmap` each re-deriving the same fleet-wide detectors. Two surgical, results-identical fixes (pure explorer JS): **(1) per-model memoization** — a `WeakMap`-keyed `_memo()` caches `failureImpact` / `causalityChains` and the fleet detectors (`addressing` / `trunk` / L1 / `fhrpConsistency`) on the active MODEL object (a fresh `load()` drops the stale cache automatically; `compareModels`' independent "after" model gets its own entry), so every repeat per render is a cache hit; **(2) the remediation simulator is now lazy + capped** — it renders an instant "▶ Simulate best remediation" button and, on click, evaluates only the strongest `REMEDIATION_SIM_CAP=6` candidate SPOFs (the card only ever shows one result, and the genuine best fix is reliably among the highest-impact chains). **Verified on real data** (60-switch AJ fleet): Health render **~14 min → ~120 ms**; the on-demand simulation completes in **~5 s** (6 capped sims) with a valid result; the built-in demo is unchanged (Health ~100 ms, lazy button works); no console errors. HTML-only — **no Python, no snapshot, no golden change**; **120 tests pass** unchanged. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.68** — *Flag inventory-name vs configured-hostname mismatches (the cause of phantom split nodes in the topology).* Real-data gap: one switch was collected under `AS08--BC-CR03R13-BCDOH` (double dash in `devices.json`) but its **own** configured hostname is `AS08-BC-CR03R13-BCDOH` (single dash). Those don't canon-match, so its neighbors' CDP entries (which carry the real name) couldn't reconcile to the inventory key — it surfaced as a duplicate/phantom node in the topology and would break any hostname-keyed cutover scripting, silently. Now caught: `parse_show_version` already extracts the device's reported hostname (the `<host> uptime is` line); `build.py` stores it on the new `DevicePhysical.reported_hostname`, and `analyze.compute_hostname_mismatches(all_device_physical)` flags any device whose `_canon_host(reported) != _canon_host(inventory)` (FQDN/case differences are canon-equal, so they're **not** flagged). Folded into the consolidated punch-list as a **Medium** `Inventory` item (via the same optional-arg pattern as the V3.23.64 `l2` fold), with remediation to fix the inventory name. **Additive golden** — only the new empty `reported_hostname` key is added to each device record (verified: the golden diff is exactly that key, nothing else); the synthetic fixtures' `show version` has no `uptime is` line so reported stays blank → no spurious mismatch, punch-list byte-identical. New unit tests: `test_compute_hostname_mismatches` (double-dash flagged, exact-match + FQDN/case + blank-reported all skipped) and a `hostname_mismatches` case in `test_compute_migration_punchlist`. **Verified on real data** (13-switch AJ fleet): exactly one Medium `Inventory` item — `AS08--BC-CR03R13-BCDOH vs AS08-BC-CR03R13-BCDOH` — and no false positives on the other 12. `ruff` clean, mypy unchanged. **120 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.67** — *Collect `show environment all` so IOS-XE (9300/3850) fan + temperature health stops coming back blank.* Real-data gap: on the 4 `*-BCDOH` Catalyst 9300s, the collector's bare `show environment` returned **`% Incomplete command`** (IOS-XE requires a sub-keyword), and no other captured file carried fan/temp — so `fan_status` / `temperature_status` were blank there (their `ps_status` was fine, via the working `show environment power`). Added `show environment all` to `COMMANDS_IOS`; `build.py` now feeds whichever of `show environment` / `show environment all` was collected (concatenated — an error line in either matches nothing, so combining is safe) to one `parse_show_environment`. The parser gained conservative IOS-XE markers: `Temperature State: GREEN/YELLOW/RED` → OK/Warning/Critical, the fan table row `1  1  8160  OK  front to back`, and `FAN PS-1 is OK`. **Honest scope / verification boundary:** the collector + wiring + parser are correct-by-construction and covered by two new `test_parsers` unit tests built from the **documented** IOS-XE format — but this is **not yet verified against a real 9300 `show environment all` capture** (the dataset only holds the rejected bare command; offline re-collection isn't possible). The next live collection will capture and validate it. **No regression / no fabricated data:** re-running on the current 13-switch fleet is byte-identical to V3.23.66 (the 9300s stay honestly blank because `show_environment_all.txt` doesn't exist yet; 4948E/VSS unchanged). **No golden change** (fixtures ship no environment output). `ruff` clean, mypy unchanged. **119 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.66** — *Recover power-supply / fan health from the Catalyst `show environment` PS table (4948E / 4500-X-VSS).* Real-data gap: on Catalyst 4948E and 4500-X(-VSS), `show environment power` returns `% Invalid input` — the PS health + fan-sensor actually live in the plain **`show environment`** Power-Supply table (`PS1  PWR-C49E-300AC-R  AC 300W  good  good  n.a.`), which `parse_show_environment` previously ignored (it read only fan/temp). So `ps_status`, `fan_status` and `num_power_supplies` came back **blank on the entire 4948E/VSS fleet** even though the device reported them. `parse_show_environment` now also parses the PS table (split on 2+ spaces, keyed off the watts column so leading-column drift is harmless) → `ps_status` (distinct, e.g. `OK` or `OK / FAIL`) + `num_ps`, and folds the per-PS **Fan Sensor** column plus the Catalyst `Fantray : Good` / `Fantray N : ... status : Good` lines into `fan_status`. `build.py` fills `ps_status` / `num_power_supplies` from this **only as a fallback** when the (authoritative) `show environment power` path left them blank — so 9300 / Nexus and every other platform stay byte-unchanged. **No golden change** — the synthetic fixtures ship no `show environment` output, so pipeline output is identical; locked by two new `test_parsers` unit tests (a healthy 4948E table → `ps OK / fan OK / 2 PSUs`; a `faulty` PSU → `OK / FAIL`). **Verified on real data** (13-switch AJ fleet): PS status + PSU count recovered on **all 13** (the VSS correctly shows **4** PSUs across its 2 chassis), and fan/temp on the 9 Catalyst devices whose `show environment` collected cleanly (4 BCDOH 9300s returned `% Incomplete command` for `show environment` — a separate collection issue — but already had `ps` via the working `show environment power`). Power capacity/draw stay blank on 4948E/VSS by design (those platforms expose no power *budget*, only per-PS nameplate). `ruff` clean, mypy unchanged. **117 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.65** — *Topology link map: resolve a scanned neighbor's FQDN/serial-suffixed CDP id back to its real hostname so it isn't drawn as a duplicate node.* On real fleets CDP advertises **domain-qualified** device-ids (e.g. `DS-VSS-CAR3-R13-ARDOH.broadcast.ajmn`), so `analyze.compute_topology_links` stored that raw string as `b_host` while `a_host` carried the short scanned name — **splitting one device into TWO nodes** in the **topology diagram** (`.dot`/`.mmd`) and the **Topology Links** sheet. (The explorer's `buildModel` already canonicalizes via its `canonHost()` port, so it was unaffected — this realigns the Python path to the same behavior.) Fix: build a `canon → real-hostname` map once and set `b_host = scanned_map.get(_canon_host(nbr), nbr)` — a **scanned** neighbor resolves to its short hostname; a **genuinely off-scan** neighbor (no canon match) keeps its raw advertised name. The dedup key was already canonical, so **link count is unchanged**, and every downstream `b_host` consumer either re-canonicalizes or only displays it, so none break. **No golden change** — the synthetic fixtures use short names that resolve to themselves, so output stays byte-identical; locked by a new FQDN-neighbor assertion in `test_package` (a domain-qualified CDP id yields a single short-named link, not a duplicate node). **Verified on real data** (13-switch Al Jazeera fleet): the VSS hub collapses from 2 diagram nodes → 1, external neighbors (`*.aljazeera.tv`) keep their raw names, and a true inventory/CDP hostname mismatch (`AS08--` vs `AS08-`) is correctly left **visible** rather than silently merged. `ruff` clean, mypy unchanged. **115 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.64** — *Complete the migration punch-list: fold in the four cross-switch L2 checks that previously lived only in the explorer.* The V3.23.63 punch-list rolled up the 8 Python-computed finding sources; the cross-switch L2 consistency checks (**addressing conflicts, FHRP consistency, trunk native-VLAN, link duplex/speed**) were computed only client-side for their explorer cards. They already exist as reusable Python functions (`compute_addressing_conflicts` / `compute_fhrp_consistency` / `compute_trunk_native_mismatches` / `compute_duplex_speed_mismatches` in excel.py, all sharing `analyze.compute_topology_links`), so `main()` Phase-30d now computes them and passes them to `compute_migration_punchlist(..., l2=…)`, which adds: duplicate L3 IP (**High**) / overlapping subnet (Medium), fake FHRP redundancy (**High**), trunk native-VLAN mismatch (Medium), and link duplex/speed mismatch (Medium) — each de-duplicated, wave-tagged, and ranked into the same list. The `l2` arg is passed as **data** (not an import), so analyze stays free of an excel dependency — no circular import. **No golden change** — the synthetic fixtures have no L2 issues, so the punch-list stays byte-identical; the fold is validated by the extended `test_compute_migration_punchlist` (a sample `l2` adds exactly its 4 items, wave-tagged) and by the demo (its punch-list now lists 11 items incl. FHRP / Addressing / Trunk). **Verified via Preview MCP `preview_eval`:** the demo Health card renders "11 items" (1 critical) with the FHRP / Addressing / Trunk rows present; no console errors. `ruff` clean, mypy unchanged (101). **115 tests pass.** The punch-list is now genuinely complete (every detection dimension the tool computes). In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.63** — *Consolidated migration punch-list — the executive "fix-this-first, in this order" roll-up.* The tool now detects across ~25 workbook sheets + a dozen explorer cards; this turns that scattered detection into **one prioritized action plan**. New `analyze.compute_migration_punchlist(...)` synthesizes EVERY actionable finding the run already produced — cross-layer SPOFs, security gaps (CIS), config-hygiene undefined refs, L1/L3 risks, protocol health, STP design (accidental / misaligned root), and device-health band — into one **severity-ranked** (Critical→Low), **de-duplicated** (like findings grouped one row + a device list, not one per port — so it does **not** cry wolf), **per-device, per-migration-wave** list with remediation. Pure synthesis of in-run records (no new collection). Wired in `main()` Phase-30d → `snap_dict["punchlist"]` (new top-level key) + a new **`Migration Punch-List`** Excel sheet (`write_punchlist_sheet`, excel.py; severity-banded rows). The explorer's 🏥 Health overview now **leads** with a new `punchlistCard()` (top-12 ranked — severity pill + category + device(s) + wave, with a "+N more in the workbook" footer). On the fixtures it consolidates to **19 items (2 Critical, 10 High, 5 Medium, 2 Low across all 8 categories)** — the Critical cross-layer SPOFs first, then the High security / hygiene / L1 / L3 / health items — a discriminating spread, not a wall of red. **Additive golden** — new `punchlist` key + new sheet, **every existing section/sheet byte-identical** (261 insertions / 0 deletions). New `test_compute_migration_punchlist` (aggregation across all 8 sources, Critical→Low ranking, move-group wave tagging, non-fail security findings excluded). **Verified via Preview MCP `preview_eval`:** the demo Health card renders "8 items" (1 critical) ranked with the Critical item at #1 and wave tags; no console errors. `ruff` clean, mypy unchanged (101). **115 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.62** — *STP root-bridge placement analysis — a cross-layer L2 design check (the root-bridge equivalent of FHRP/gateway alignment).* The tool parsed STP **port states** but never the **root-bridge identity**; new `parse_spanning_tree_root` (parse.py) reads the per-VLAN `Root ID` / `Bridge ID` blocks of the **already-collected `show spanning-tree`** → `{vlan:{root_priority,root_address,is_root}}` (`is_root` = this switch's Root ID address == its own Bridge ID address, or an explicit "This bridge is the root"). `build_stp_roots` (build.py) → `snap_dict["stp_roots"]` (new top-level key) + a new **`STP Root Bridges`** Excel sheet (`write_stp_roots_sheet`, excel.py). New `analyze.stp_root_findings(stp_roots, interfaces)` cross-references the root per VLAN against the L3 gateway SVIs to flag two design smells a migration inherits: an **accidental root** (a VLAN rooted on the **default** priority `32768 + sys-id-ext` — won on a MAC tiebreak, not deliberately elected, so it can move on a cutover) and **root / gateway misalignment** (the root bridge isn't a switch hosting the VLAN's gateway → intra-VLAN traffic to the default gateway hairpins through the root). The explorer's 🏥 Health overview renders a new `stpRootCard()` (misaligned = crit, accidental = watch, clean `b-ok` state). **The fixtures demonstrate both:** core1 is the deliberate, gateway-aligned root for VLAN 10 (clean), while **access1 is the default-priority root for VLAN 30** whose gateway lives on core1 → flagged **accidental and misaligned**. Adding the `Root ID`/`Bridge ID` blocks (and flipping the root switch's port *role*, which the detail parser ignores — it keys on FWD/BLK *status*) leaves every existing STP section byte-identical. **Additive golden** — new `stp_roots` key + new sheet (38 insertions / 0 deletions). New `test_parse_spanning_tree_root` / `_absent` + `test_stp_root_findings`. **Verified via Preview MCP `preview_eval`:** the demo card shows "2 issues" (VLAN 30 root ≠ gateway + accidental default-priority root) and the all-aligned path shows the `clean` badge; `parse_spanning_tree_states` output is unchanged by the fixture edits; no console errors. `ruff` clean, mypy unchanged (101). **114 tests pass.** Surfacing-only (an L2 design signal, deliberately **not** a health-score deduction). In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.61** — *Config hygiene: Batfish-style undefined-reference + unused-structure detection (research: [Batfish](https://batfish.org/)'s two flagship static checks — the config rot that silently breaks migrations).* New `parse_config_hygiene` (parse.py) does a tolerant, order-independent cross-reference over the **already-collected `show running-config`** (no new collection, stdlib `re` only): it gathers every **defined** ACL / route-map / object-group / prefix-list and every **reference** to one (interface `ip access-group`, `access-class`, `ip nat … source list`, route-map `match ip address`, `redistribute … route-map`, `neighbor route-map`/`prefix-list`, `ip policy route-map`, `object-group` / nested `group-object`, …), then reports **undefined references** (referenced-but-never-defined — silently does nothing, a real migration-breaker) and **unused structures** (defined-but-never-referenced cruft to drop). Conservative by design — only those four well-bounded kinds + high-confidence reference forms + a reserved-keyword guard — so it does **not** cry wolf; returns `{}` when the config has none. `build_config_hygiene` (build.py, fail-soft); `main()` Phase-5.x wiring → `snap_dict["config_hygiene"]` (new top-level key) + a new **`Config Hygiene`** Excel sheet (`write_config_hygiene_sheet`, excel.py); the explorer's 🏥 Health overview renders a new **`hygieneCard()`** (undefined = crit pills with the offending context, unused = neutral, clean `b-ok` state when all references resolve). **The detector immediately found real rot already in the fixtures:** core1's `ip nat inside source list 7` references ACL `7`, and `redistribute … route-map OSPF_TO_BGP` references a route-map — neither is defined → both correctly flagged **undefined**; `INET_RETURN` is defined but unapplied → **unused** (the other 3 ACLs were made *used* via added `ip access-group` lines that, being in the full run-config rather than the `| section ^interface` slice, leave the interfaces snapshot byte-identical). **Additive golden** — new `config_hygiene` key + new sheet, **every existing section/sheet byte-identical** (34 insertions / 0 deletions). New `test_parse_config_hygiene` / `_clean` / `_absent`. **Verified via Preview MCP `preview_eval`:** the demo Health card shows "2 undefined" (ACL `7`, route-map `OSPF_TO_BGP`, with context) plus the unused `INET_RETURN`, and the all-resolved path shows the `clean` badge; no console errors. `ruff` clean, mypy unchanged (101). **111 tests pass.** Surfacing-only (config rot is a correctness / cleanup signal, deliberately **not** a health-score deduction). In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.60** — *Fold config-compliance posture into the 0–100 health score (the deferred PR2 of V3.23.59 — research: "weight by … the worst unresolved high-weight finding"; config/security technical debt now lowers migration health, not just sits in a side sheet).* New **capped `SEC` category** in the score: `ScoringConfig.sec_weights` (`high −8 / medium −3 / low −1`) + `caps["SEC"] = 18` (kept below the cross-layer SPOF cap of 45 so genuine connectivity blockers still dominate). `compute_health_scores` gains an optional `security={host: parse_security()}` arg and a SEC deduction loop over **failed** checks only; `compute_score_sensitivity` gains the arg and now sweeps `sec_weights` as a 5th OAT group; `main()` threads `all_security` into both (Phase 28 / Phase 30). **Missing posture is never penalized** — a host with no captured run-config gets no SEC deduction (distinct from present-and-hardened), and a custom `ScoringConfig` whose `caps` predates SEC simply skips it (the cap loop now `continue`s on any category absent from `caps`, so older custom configs keep working). **Reviewed golden regen (intentionally moves a score):** in the fixtures only **core1** has a run-config, so only it moves — `21 → 3`, **still Critical (band unchanged)**; access1 (72/Fair) and core2 (100/Excellent) are untouched, so the **3-band spread is preserved (no cry-wolf reintroduced)**; the drop equals the SEC cap (18). snapshot.json +40/−7 (core1 score + SEC deductions + new `sec_weights` sensitivity rows); the `Health Scores` / sensitivity sheet *values* change but the sheet **schema is byte-identical**. Python-only — the explorer already renders whatever scores the snapshot carries (no JS/demo change). Updated 4 tests that encoded the old config shape / 16-row sweep; +4 new (`test_health_security_deductions_capped` / `_missing_data_not_penalized` / `_category_capped`, `test_score_sensitivity_sweeps_security_group`). `ruff` clean, mypy unchanged (101). **108 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.59** — *Config-compliance posture: a new CIS-aligned security assessment axis (research: the config-compliance / technical-debt dimension every 2026 assessment tool — CIS Cisco benchmark, IP Fabric, Forward Networks — centers on; the twin of the existing connectivity engine).* Adds a full-stack, **purely-additive** security/compliance layer over the **already-collected `show running-config`** (no new collection, no new dependency, stdlib `re` only). New `parse_security` (parse.py) runs 11 CIS-aligned checks — `service password-encryption`, `enable secret` vs `enable password`, Type-0/7 local users, `aaa new-model`, SNMPv1/2c communities, telnet on VTY, risky/unused services (HTTP server / Smart-Install / source-route / bootp / finger / `service config`), NTP, logging, login banner, and VTY hardening (`access-class` / `exec-timeout 0 0`) — each emitting `{id,title,severity,status(pass/fail/na),detail,cis_ref,remediation}` plus a per-device `summary{fail,pass,na,grade}`. **Secrets are never stored:** SNMP community strings and password hashes are redacted to `<redacted>` *in the parser* (the single-file HTML must be safe to share) — verified no fixture secret leaks into the snapshot. `build_security` (build.py, fail-soft `_safe_parse`); `main()` Phase-5.x per-device wiring → `snap_dict["security"]` (new top-level key) + a new **`Config Compliance`** Excel sheet (`write_security_sheet`, excel.py; named distinctly from the pre-existing operational `Security Posture` sheet to avoid a tab collision). The explorer's 🏥 Health fleet overview renders a new **`securityCard()`** — a severity-ranked gap list (high/medium/low pills, CIS refs) with a clean `hardened` state when no host fails; `demoSnapshot` gains a `security` block (DIST-1 weak / DIST-2 hardened). **Posture is surfaced only — folding it into the 0–100 health score (a capped `SEC` category) is a deliberate follow-up so this PR stays additive.** **Additive golden** — new `security` key + new sheet, **every existing section/sheet byte-identical** (119 insertions / 0 deletions, per `git diff`). `core1` fixture gains a deliberate posture block (synthetic secrets). New `test_parse_security` (each check fires; community/hash redacted), `test_parse_security_hardened` (all-pass → `hardened`), `test_parse_security_absent`. **Verified via Preview MCP `preview_eval`:** the demo Health card shows "5 gaps" with 3 high / 1 medium / 1 low severity pills + the redaction note, the all-pass path shows the `hardened` badge, no console errors. `ruff` clean, mypy unchanged (101). **104 tests pass.** In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.58** — *Lead each mode's screen-reader announcement with its single most important number (research playbook: "lead each mode with the single most important number").* `setMode` now appends a `modeSummary(m)` clause to the existing SR announcement: Health → "N switches scored, average X[, n critical][, n poor]"; Causality → "N root-cause chain(s)". Fleet-level signals only and fully guarded (`try/catch`, empty string when there's nothing to say); selection-driven modes (blast/trace) already speak their result on click, so they're untouched. **Verified via Preview MCP `preview_eval`:** `modeSummary('health')` on the 6-switch demo returns "6 switches scored, average 71, 1 poor."; no console errors. Pure `blast_radius_explorer.html` (JS) — **no Python/golden/pytest change (101 stay)**; back-compatible. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.57** — *Per-VLAN isolation: fade the switches that don't carry the selected VLAN (research playbook: "a per-VLAN view that isolates one of the 23 VLANs").* `baseEdge()` already dims off-VLAN edges; the new `applyVlanFocus` (run at the tail of `repaint`, after every paint) now also dims the **switches** that don't participate in the selected VLAN, so the fabric for one VLAN reads on its own. A switch participates if it has endpoints in the VLAN, is a gateway for it, or sits on a link that **carries** it (`linkCarries`, so trunk allowed-list / asymmetry pruning is respected). No-op when VLAN = "all". **Verified via Preview MCP `preview_eval`:** selecting VLAN 10 in the demo dims the non-participating switch; "all" lights every switch; no console errors. Pure `blast_radius_explorer.html` (JS) — **no Python/golden/pytest change (101 stay)**; back-compatible. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.56** — *Radial layout: actually bow the hub-spoke edges (fixes "curved" edges that rendered dead straight).* In the radial layout `updateEdges` put the quadratic-Bézier control point on the **chord midpoint**, so every curved edge collapsed to a straight line. The control point is now offset **perpendicular** to the chord (bow scales with link length, capped at 55px so short links stay subtle), and the edge badge rides the curve's true midpoint (`0.25·a + 0.5·ctrl + 0.25·b`) instead of the chord. Straight (layered) layout is unchanged. Pure `blast_radius_explorer.html` (JS) — **no Python/golden/pytest change (101 stay)**; back-compatible. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.55** — *Level-of-detail: shed secondary labels when zoomed out (research playbook: "flexible LoD … a notable performance boost with low implementation effort for SVG").* `applyTransform` now toggles a `#vp.lod-far` class when the zoom scale drops below `0.55`; under it the secondary node labels & badges (`.nsub/.nrole/.epbadge/.pcbadge/.ntag/.scorebadge`) fade out via a short opacity transition, leaving clean role-coded boxes — the node name, health ring, and link states (the at-a-glance cues) stay, and everything returns on zoom-in. **Verified via Preview MCP `preview_eval`:** `lod-far` is present at `k=0.4` and absent at `k=1.0`; no console errors. Pure `blast_radius_explorer.html` (JS/CSS) — **no Python/golden/pytest change (101 stay)**; back-compatible. In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.54** — *Health-mode readiness ring on the node border (research playbook: the Grafana node-graph "arc on the node border" idiom).* In Health mode each switch now draws a rounded-rect **score ring** around its node body — a faint full-perimeter track plus a banded arc that fills **score%** of the perimeter. `pathLength=100` normalizes the geometry so the fill is just the inline `stroke-dashoffset = 100 − score` (the resting value, robust); a CSS `@keyframes scoreFill` only grows it from empty on entry, so the resting fill is correct even in a background tab or under reduced-motion. The arc is `aria-hidden` (the numeric score already announces) and is torn down in `clearFx` like every other overlay. Legend gains a "◕ node ring fills with the 0–100 score" note. **Verified via Preview MCP `preview_eval`:** the 6-switch demo draws 6 tracks + 6 arcs, banded by health color; no console errors. Pure `blast_radius_explorer.html` (JS/CSS) — **no Python/golden/pytest change (101 stay)**; back-compatible (a record without a numeric score draws no ring). In-code version `3.23.0`. |
| 2026-06-06 | **V3.23.53** — *Activate asset-criticality weighting + surface it (research: "weight deductions by asset criticality; core/distribution carry higher weight than access").* The per-role deduction multiplier `ScoringConfig.criticality_factors` — added **dormant at 1.0** in V3.23.5 — is now **active by default**: `{core 1.5, distribution 1.2, access 1.0}`, so the **same** finding on a gateway/distribution switch (one that hosts an L3 SVI, per `_host_role`) deducts harder than on an access closet (a fault there has a wider blast radius). Each `compute_health_scores` record now also carries **`role`** + **`criticality`**, so the weighting is auditable next to the score (research: "every score shows its line-item contributions"); the `Health Scores` sheet gains a **`Criticality`** column (e.g. `distribution x1.2`). **Golden regen (reviewed, intended):** schema gains the column; in the fixtures only distribution switches **with findings** move — core1 **27→21** (still Critical), core2 **100** (clean → nothing to weight) and access1 **72** unchanged; **no band changes** (band-spread preserved — no cry-wolf reintroduced). Updated the 3 tests that encoded the old neutral default / exact record shape; +1 new test (`test_criticality_weighting_applies_and_is_surfaced`). `ruff` clean, mypy unchanged (**101**). **101 tests pass.** The explorer's Health drawer surfaces the same weighting as an **Asset-criticality card** (`role ×factor`, amplified-watch styling when >1.0), and `demoSnapshot` health records now carry `role`/`criticality` to exercise it. Recalibrate per-fleet via a custom `ScoringConfig`. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.52** — *Stateful-ACL: model `established` + `time-range` (the NAT / MTU / stateful-ACL frontier, 3 of 3 — completes it).* Fixes a real correctness bug: the ACL parser **silently dropped** the trailing `established` keyword, so `permit tcp any any established` was mis-parsed as a plain `permit tcp any any` and **wrongly permitted new/forward connections** — exactly inverting what a stateful return-only ACL does. `_acl_rule` (parse.py) now captures `established`, reflexive `reflect`, and `time-range NAME` into the rule. The explorer's `evalAcl` then treats an **`established`** rule as TCP **return-only** — it never matches a new/forward flow, so it's skipped (an `established`-only ACL now correctly **DENY**s an unsolicited inbound flow instead of the old false permit / indeterminate) — and a **`time-range`** rule that would otherwise match is reported **indeterminate** (its active window can't be known offline). Flow footnote updated (was "Reflexive / time-based rules aren't modeled"). core1 fixture gains an `INET_RETURN` ACL (`established` + `time-range` + plain deny); **additive golden** — new ACL only, every existing section byte-identical (verified by per-field diff). New `test_parse_acl_stateful`. **Verified via Preview MCP `preview_eval`:** an `established` rule + `deny ip any any` → **deny** on a forward TCP/443 flow (idx 1), the same rule **without** `established` → **permit** (idx 0, confirming the skip is the differentiator), and a `time-range` rule that matches → **indeterminate** (tr=BIZ); no console errors. `ruff` clean, mypy unchanged (101). **82 tests pass.** *(NAT/MTU/stateful trio COMPLETE: MTU #64, NAT #66 + NAT-B, stateful this.)* In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.51** — *NAT-B: surface NAT on flows in the explorer (consumes the V3.23.50 inventory).* The Flow view now flags **static NAT mappings that touch either endpoint** of a traced flow, from the embedded `SNAP.nat` inventory: a `watch` card lists each `local↔global` translation (with proto/ports for static PAT) and the device it lives on, plus an honest caveat — *past the NAT boundary an ACL sees the **global** address, so the L4 verdict (evaluated against the untranslated address) may not reflect it.* New `natStaticsFor(ips)` / `natCard()` JS; rendered in `drawFlow` after the MTU card; footnote updated. **Honest scope:** only static mappings touching an endpoint are surfaced (deterministic) — a broad "path crosses a NAT device" flag was deliberately **not** added, since the abstract L1–L3 path can't tell an inside→outside traversal and would over-claim. Pure `blast_radius_explorer.html` (JS) — **no Python/golden/pytest change (81 stay); back-compatible** (a snapshot without `nat`, or a flow whose endpoints aren't NAT'd, shows no card). `demoSnapshot` gains a `nat` block (static for the VLAN-30 server `10.0.30.9 ↔ 203.0.113.9` on DIST-1) so the VLAN10→VLAN30 flow surfaces it. **Verified via Preview MCP `preview_eval`:** `natStaticsFor` matches the server (and returns empty for non-NAT endpoints); the VLAN10→VLAN30 flow renders the NAT card (`203.0.113.9`); a VLAN10→VLAN20 flow shows no card; no console errors. *(With NAT-A + NAT-B, NAT is done; stateful-ACL is the last of the trio.)* In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.50** — *NAT inventory (the NAT / MTU / stateful-ACL frontier, 2 of 3 — data slice).* A migration must recreate every NAT rule on the new platform, so NAT is captured as an **inventory** first (the explorer flow-flagging consumes it next). New `parse_nat` (parse.py) reads IOS NAT from the already-collected `show running-config` — per-interface `ip nat inside` / `ip nat outside` roles, **static 1:1** and **static PAT** (`ip nat {inside\|outside} source static [tcp\|udp] LOCAL [LPORT] GLOBAL [GPORT]`, inside maps LOCAL→GLOBAL and outside swaps), **dynamic NAT/PAT** (`ip nat inside source list ACL {pool NAME\|interface X} [overload]`), and `ip nat pool` definitions; tolerant, returns `{}` when no NAT. `build_nat` (build.py); `main()` Phase 5.6 → `snap_dict["nat"] = {host:{static,dynamic,pools,inside,outside}}` (new top-level key) + a new **`NAT Inventory`** Excel sheet (`write_nat_sheet`, excel.py). No new collected command. core1 fixture gains a NAT block (inside/outside, a pool, a 1:1 static, a static PAT, a dynamic PAT); **additive golden** — new `nat` snapshot key + new sheet, every existing section/sheet byte-identical (verified by per-field diff). New `test_parse_nat` (all five forms) + `test_parse_nat_absent`. `ruff` clean, mypy unchanged (101). **81 tests pass.** *(NAT-B = explorer flow-flagging next; stateful-ACL follows.)* In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.49** — *Path-MTU mismatch detection (the NAT / MTU / stateful-ACL frontier, 1 of 3).* Flags **jumbo-frame black-holes** on a reachable flow's path — a classic post-cutover failure (storage / vMotion / backup traffic silently drops while pings succeed). New `InterfaceData.mtu` (model.py), parsed from run-config `mtu N` (preferred) / `ip mtu N` (fallback) in `parse_run_config_interfaces`, populated in `build_interfaces`, emitted to the snapshot (default stays blank). The explorer's Flow now computes the **path MTU** over the physical interfaces a reachable flow traverses (access ports + each trunk link's two ends + the routed egress) via new `_ifMtu` / `pathMtu` JS, and flags a mismatch — **≥2 distinct explicit MTUs**, or **jumbo set on only some hops** (rest default ~1500) — citing the effective path MTU; the verdict stays **REACHABLE** (the flow reaches — only large / jumbo frames drop), rendered as a `watch` card + footnote. Demo: jumbo on `ACC-3`'s uplink only, so flows via ACC-3 show the mismatch; fixture `core1` `Po1` gains `mtu 9216`. **Additive golden** — new `mtu` field on every interface, non-empty only on `Po1`, every other field byte-identical (verified by per-field diff). New `test_parse_run_config_interface_mtu` (L2 `mtu`, `ip mtu` fallback, default blank). **Verified via Preview MCP `preview_eval`:** a flow crossing the jumbo-mismatched ACC-3↔ACC-2 link flags the mismatch (jumbo on some hops, eff. 1500, still REACHABLE) and the card renders; a flow not via ACC-3 is clean (no regression); no console errors. `ruff` clean, mypy unchanged (101). **79 tests pass.** *(NAT and stateful-ACL follow as their own increments.)* In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.48** — *Scoring calibration (explorer): surface the diagnostic in the Health view.* The V3.23.47 calibration report was embedded in the snapshot + the `Score Calibration` Excel sheet but not shown in the interactive explorer. The 🏥 Health fleet-overview now renders a **"Score calibration"** card from `SNAP.calibration`: a discrimination-quality badge (good / fair / poor / n-a, colour-coded), the entropy-based discrimination metric with a plain-English gloss (0 = all one band, 1 = evenly spread), the score spread (range / median / stdev / modal band), the per-band distribution, and — when discrimination is poor — the relative re-banding suggestion. New `calibrationCard()` JS, wired into `drawHealth`'s fleet overview; `demoSnapshot` gains a matching `calibration` block (the demo's 6-switch fleet → "fair") so the card renders out-of-the-box. **Pure `blast_radius_explorer.html` (JS) — no Python/golden/pytest change (78 stay); back-compatible** (a snapshot without `calibration` simply shows no card). **Verified via Preview MCP `preview_eval`:** the demo card renders "fair" and appears in the Health drawer; a poor-discrimination fleet shows the `crit` badge + "Suggested re-banding" (Excellent≥79); absent calibration → ""; no console errors. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.47** — *Scoring calibration: fleet band-discrimination diagnostic + re-banding suggestion.* Addresses the standing "weights/bands are an uncalibrated default" limitation from a different angle than `compute_score_sensitivity` (which tests weight *robustness*): the weights can't be fit without a labelled dataset, but a scoring system that lumps every switch into one band gives no decision value **regardless** of the weights — so the new `compute_calibration_report` (analyze.py) surfaces whether the **bands discriminate this fleet at all**. Over the computed `health_scores` it returns: the band distribution, score spread (min / p25 / median / p75 / max / mean / stdev), a **discrimination** metric (normalized Shannon entropy of the band distribution — 0 = all one band, 1 = uniform) with a quality label (good / fair / poor / "n/a (too few switches)"), and — when discrimination is **poor** — quantile band thresholds as a **relative (curve-graded, norm-referenced) re-banding suggestion**, explicitly distinct from the absolute (criterion-referenced) default. `Insufficient Data` switches are excluded. New **`Score Calibration`** Excel sheet (`write_calibration_sheet`, excel.py) + `snap_dict["calibration"]` (new top-level snapshot key). Pure derivation, no new collection; **additive golden** — new snapshot key + new sheet, every existing section/sheet byte-identical (verified by per-field diff). 3 unit tests (poor→suggestion+0-floor, one-per-band→good+no-suggestion+excludes Insufficient-Data, empty); fixtures (n=3) → "n/a (too few)". `ruff` clean, mypy unchanged (101). **78 tests pass.** In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.46** — *PHASE 3 reachability (M): L4 ACLs on routed (non-SVI) transit interfaces.* Extends L4 truthfulness beyond SVI gateways: the Flow evaluator now also evaluates an ACL applied on the **routed egress interface** the source gateway uses toward the destination — the transit-uplink ACL that was previously invisible (only SVI `acl_in`/`acl_out` were checked). It reuses L's route data: when `_routeCovers(sgw, dst)` returns a route whose `out_intf` is **not** an SVI and that interface carries an `acl_out` (already in the snapshot — `acl_in`/`acl_out` is captured for *every* interface since the K-era model), it's added to the flow's ACL set and run through the same `evalAcl` (first-match, implicit deny). New `aclLoc(flag)` renders a flag's location as either `VLAN n` (SVI) or the interface name (routed), used by both `aclVerdictCard` and `aclFlagCard`; the Flow footnote now says SVIs **and the routed egress interface**. **Pure `blast_radius_explorer.html` (JS) — no Python/golden/pytest change (75 stay); no new snapshot data** (the interface ACL names were already emitted). Connected-route flows are unaffected (their egress *is* the SVI, correctly skipped). The bundled collapsed-core demo routes every in-scope subnet via a connected SVI, so it doesn't naturally traverse a routed transit interface (same situation as L2's no-route case) — **verified via Preview MCP `preview_eval`**: making the VLAN10→VLAN30 route egress a synthetic routed uplink `Gi1/1/1` (carrying `PROTECT_SERVERS out`) makes the flow evaluate that ACL *in addition to* the dst-SVI ACL, rendered `on Gi1/1/1`; the unmutated flow shows only the SVI ACL (no regression); `aclLoc` returns `VLAN 30` / `Gi1/1/1`; no console errors. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.45** — *PHASE 3 reachability (L3): route-aware per-endpoint reachability (the second surface).* Completes the route-aware story: L2 made the point-to-point **Flow** route-aware; this brings the same check to the per-endpoint **reachability panel** (`endpointReach`), mirroring how K made *both* surfaces VRF-aware. In the cross-subnet ("via L3 gateway") branch, after the existing VRF gate, it now asks whether a **reachable source-VLAN gateway has a route toward the destination VLAN's subnet** — tested with new JS `gwIp(host,vid)` (that gateway SVI's own address, a representative IP in the subnet) fed to L2's `_routeCovers`. If the gateway's table covers it → unchanged "via L3 gateway" reachability; if the table is present but **nothing covers** the subnet → a new **"no route"** group (0 reachable, cited reason); if **no route data** for that gateway → falls back to the prior behaviour (never a false-negative). The per-endpoint footnote now lists **routing tables** alongside VRF. Pure `blast_radius_explorer.html` (JS) — **no Python, no golden, no pytest change** (75 stay). **Verified live via Preview MCP** (`preview_eval`): a VLAN-10 endpoint's VLAN-30 group reads "via L3 gateway" (3 reachable) with full demo routes; stripping DIST-1's `10.0.30.0/24` flips it to **"no route"** (0 reachable, *"DIST-1 has no route to VLAN 30 (10.0.30.1) …"*); VLAN-40 still "different VRF" (K) and VLAN-20 still reachable — no regression, no console errors. `gwIp('DIST-1',30)`→`10.0.30.1`. **With K + L (L1+L2+L3), reachability is now VRF- and route-aware on both surfaces.** In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.44** — *PHASE 3 reachability (L2): route-aware Flow evaluator (consumes the L1 `routes` snapshot).* The second half of route-aware reachability (the G2/H2-style evaluator). The explorer's `flowBetween` cross-VLAN path no longer assumes any gateway routes to any subnet: after the gateway + VRF checks it now consults the source gateway's embedded routing table (`SNAP.routes`, from L1) via new JS `_routeCovers(host, ip)` — a **longest-prefix match** returning the matched route, `null` when the table is present but **nothing covers** the destination, or `undefined` when no route data was collected. Outcomes: a covering route → **REACHABLE** with the L3 hop now **citing the matched route** (e.g. *"via connected 10.0.30.0/24"*); routes present but none cover the destination → **NOT REACHABLE — no route to `<ip>`** (the new catch); **no route data → fall back to the prior assumption (never a false-negative on missing data)**, and same-device sgw==dgw stays trivially routed. The Flow footnote now states the path is VRF- *and* route-aware. Pure `blast_radius_explorer.html` (JS) — **no Python, no golden, no pytest change** (75 stay); the bundled demo gains `routes` for DIST-1/DIST-2 (their connected subnets) so the citation shows out-of-the-box. **Verified live via Preview MCP** (`preview_eval`): `_routeCovers` returns matched/`null`/`undefined` for known/uncovered/no-table; Flow `10.0.10.1`→`10.0.30.9` REACHABLE citing *via connected 10.0.30.0/24*; with DIST-1's `10.0.30.0/24` route removed the same flow flips to NOT REACHABLE — no route. No console errors. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.43** — *PHASE 3 reachability (L1): scoped routing tables → snapshot (route-aware reachability, data slice).* The first half of route-aware reachability (the G1/H1-style data slice; the explorer evaluator that consumes it is L2). The pipeline now embeds each switch's **routing knowledge** into the snapshot so the explorer can later verify a gateway actually has a route toward the destination — instead of assuming any gateway routes to any subnet. Parsed from the **already-collected `show ip route`** (no new collected command, via the existing `parse_ip_routes`), then **scoped** so the single-file snapshot stays small: new `build.py` helpers `build_routes` (full parse, transient), `inscope_subnets` (the set of gateway-SVI connected prefixes — the destinations the explorer asks about), and `scope_routes` (keeps only routes whose prefix **covers an in-scope subnet** + the **default route**; drops host `/32` local routes and out-of-scope prefixes; compact rows `{prefix, source, next_hop, out_intf}`, deduped). `main()` Phase 5.7 emits `snap_dict["routes"] = {host:[row,...]}` (NEW top-level key). `--redact` needs no change: route IPs are scrubbed by the existing `_walk` (no leak) and the deterministic `/24` remap + `/0` normalization preserve route-coverage relationships, exactly as it already does for subnets/ARP. core1 fixture route table gains a default, a covering `10.0.0.0/16` supernet, and an out-of-scope `192.168.99.0/24` static to exercise the scoping; **additive golden — one new `routes` key, every other section byte-identical** (verified by per-field diff before regen — core1 keeps default+supernet+3 connected, drops 3 local /32s + the out-of-scope static; core2 keeps its 2 connected). New `test_scope_routes_keeps_relevant_drops_noise` + `test_inscope_subnets_from_svis`. `ruff` clean, mypy unchanged (101). **75 tests pass.** In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.42** — *PHASE 3 reachability (K): VRF-aware Flow + endpoint reachability.* The explorer's offline reachability now honours **VRF membership**. `buildModel` records each gateway SVI's VRF (`svuVrf`, "host\|vid" → VRF name) from the **already-collected** `vrf` field — no new collection command — and `flowBetween` / `endpointReach` treat two subnets that live in **different VRFs** as **NOT REACHABLE** ("separate routing domains; route leak / VRF fusion isn't modeled"), via a `gwVrf()` lookup and a `_normVrf()` that collapses the global-table aliases `''` / `default` / `global` so they compare equal. This closes a **false-reachable** gap: a cross-VRF flow (or per-endpoint different-subnet group) previously showed REACHABLE whenever a gateway switch happened to be shared. The run-config interface parser also now captures **legacy IOS `ip vrf forwarding`** alongside IOS-XE `vrf forwarding` and NX-OS `vrf member`. The bundled demo gains a VRF-isolated VLAN 40 (`TENANT_RED`, gatewayed on DIST-1, 2 endpoints on ACC-4) so the new verdict is visible out-of-the-box; the two reachability footnotes now state the path is VRF-aware. One synthetic SVI (`core1` Vlan30) gains `vrf forwarding TENANT_RED` for snapshot-contract regression — **additive golden: exactly one field** (`interfaces.core1.Vlan30.vrf` `''`→`TENANT_RED`), every other section byte-identical. New `test_parse_run_config_interface_vrf` covers all three VRF syntaxes. Verified live in the explorer (Flow `10.0.10.1`→`10.0.40.13` = NOT REACHABLE; same-VRF VLAN 10→20 still REACHABLE — no regression). `ruff` clean, mypy unchanged (101). **73 tests pass.** In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.41** — *`--redact`: shareable, sanitized snapshot/explorer (opt-in, off by default).* New `cisco_toolkit.html.redact_snapshot()` consistently pseudonymizes IPs / MACs / serial numbers throughout the snapshot dict so a single-file HTML/JSON deliverable can be shared without leaking real addressing. Mappings are deterministic (same input → same output) and IPs keep their `/24` grouping, so ARP (MAC→IP), dual-homing, and subnet/flow-trace relationships the explorer relies on survive; hostnames are kept. Wired into `main()` behind a new `--redact` flag, applied to the augmented `snap_dict` before both the `.snapshot.json` write and the embedded HTML explorer. **Default off → the pipeline and the golden are unchanged.** Added `test_redact_snapshot_pseudonymizes_consistently` + an end-to-end check (real `10.0.10.x` → `10.0.0.x`, serials → `SN0001…`, no real IP leaks, subnet preserved). `ruff` clean, mypy unchanged (101). **68 tests pass.** In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.40** — *PHASE 2.7 step 30: move `snapshot_state` + `write_html_explorer` to the snapshot-reporting layer — HTML LAYER COMPLETE (behaviour byte-identical).* Moved `snapshot_state` (the JSON snapshot contract embedded in the HTML + written beside every workbook) and `write_html_explorer` (bakes the live snapshot into a copy of the read-only Blast-Radius Explorer template) into `cisco_toolkit/html.py`; both imported back for `main()`. Two couplings handled: **(a)** hoisted the single-source `__version__` from the monolith into `cisco_toolkit/__init__.py` and imported it back (so `LOG_FILE` / argparse / `cp.__version__` keep working and `snapshot_state` reads the same value — `test_version` still green); **(b)** `write_html_explorer` now finds `blast_radius_explorer.html` via `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))` (html.py's grandparent = the repo root where the template lives), preserving the original template resolution byte-for-byte. html.py gained `json` / `logging` / `os` / `datetime` / `typing` + the package `__version__` / model imports. No monolith F401 (`datetime` / `json` / `os` are all still used elsewhere). Extended `test_html_reexported_and_functional` with the `snapshot_state` / `write_html_explorer` re-exports + the `__version__` relocation check. **The whole snapshot-reporting (html) layer now lives in `cisco_toolkit/html.py` (~191 lines).** Golden **byte-identical** (snapshot.json unchanged), `ruff` clean, mypy unchanged (101). **67 tests pass**. Monolith **~1,290 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.39** — *PHASE 2.7 step 29: stand up the snapshot-reporting layer (`html.py`) with the diff workbook — behaviour byte-identical.* Created `cisco_toolkit/html.py` and moved the pure `--compare OLD NEW` diff-workbook writer `write_diff_workbook` (+ its `_macset` helper + `_DIFF_FIELDS` constant) into it; depends only on openpyxl + stdlib `re`, independent of the rest of the package. `write_diff_workbook` imported back for `main()`'s `--compare` path; `_macset` / `_DIFF_FIELDS` are package-internal. Dropped the now-unused `Font` / `PatternFill` / `Alignment` / `get_column_letter` from the monolith's hoisted openpyxl import (their last user moved; `load_workbook` stays for `main()`). Added `test_html_reexported_and_functional` (re-export identity + a functional smoke that writes a diff workbook and checks the Interface-Changes delta). **`snapshot_state` + `write_html_explorer` follow in step 30** (they carry the `__version__` + template-`__file__` couplings, kept to their own focused PR). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **67 tests pass**. Monolith **~1,344 lines** (from ~5,769); `html.py` ~122 lines. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.38** — *PHASE 2.7 step 28: move `build_interfaces` to the build layer — BUILD LAYER COMPLETE (behaviour byte-identical).* Moved the ~300-line per-device `InterfaceData` builder `build_interfaces` (deferred from step 27) into `cisco_toolkit/build.py` (which gained `re` + `_safe_parse` + ~22 parsers + `detect_link_type`); imported back for `main()` + conftest's `built` fixture. **Milestone: the monolith no longer imports the parse layer at all** — `build_interfaces` was the last parser consumer, so the entire `from cisco_toolkit.parse import (…)` (23 parsers) was removed; the cmdio import shrank to just `_CISCO_ERRORS` (dropped `_load_cmd_output` / `_safe_parse`), the textutils import to just `safe_fs_name` (dropped `normalize_ifname` / `detect_link_type`), and `Optional` (typing) went too. Largest test cascade of the split: repointed `test_parsers` (`cp.parse_*` → `parse.*`) + `test_platform_variants` (added the `parse` import) to import parsers from the package; `test_parser_robustness` now monkeypatches a failing parser on `cisco_toolkit.build` (where `build_interfaces` resolves the name) instead of the monolith, and pulls `_safe_parse` from cmdio; updated 5 stale `test_package` identity assertions (`normalize_ifname` / `detect_link_type` / `parse_show_interface_status` / `_load_cmd_output` / `_safe_parse` / `parse_run_config_interfaces` are now all package-internal) and flipped the step-27 `build_interfaces` deferral guard to a re-export check. **The whole build layer now lives in `cisco_toolkit/build.py` (~444 lines).** Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **66 tests pass**. Monolith **~1,451 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.37** — *PHASE 2.7 step 27: extract the model-construction layer (`build.py`) — behaviour byte-identical.* Moved `build_device_physical` / `build_switch_identity` (build switch-level `DevicePhysical` / switch-identity records from collected `show` output) + `collect_global_arp` / `apply_global_arp` / `detect_cross_device_dual_connections` (the network-wide ARP enrichment + dual-homing pass) into a new `cisco_toolkit/build.py` — depends on cmdio + parse + model + textutils; a layer above those, independent of analyze/excel. All five imported back for `main()`. **`build_interfaces` (the ~300-line per-device `InterfaceData` builder) deliberately stays in the monolith for now — its own step next**, to keep this PR reviewable + golden byte-exact. F401 cascade: dropped 8 now-unused parsers (`parse_show_version` / `parse_show_inventory` / `parse_show_environment_power` / `parse_show_environment` / `parse_show_module_count` / `parse_vtp_status` / `parse_switch_mgmt_ip` / `parse_show_ip_arp`) + `Tuple` (typing) + `PHYSICAL_IFACE_RE` (textutils) from the monolith re-imports — `parse_run_config_interfaces` stays (build_interfaces still uses it). Added `test_build_reexported_and_functional` + updated the step-17 `PHYSICAL_IFACE_RE` identity assertion (now package-internal). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **66 tests pass**. Monolith **~1,745 lines** (from ~5,769); `build.py` ~155 lines. In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.36** — *PHASE 2.7 step 26: move `append_interface_rows` to the Excel layer — Excel layer COMPLETE (behaviour byte-identical).* Moved the main 'Migration Assessment' interface-sheet filler `append_interface_rows` into `cisco_toolkit/excel.py` (its only sheet writer not yet moved). excel.py gained `MergedCell` (openpyxl) + `PHYSICAL_IFACE_RE`/`_TRUNK_STATUS_WORDS` (textutils); imported back for `main()`. Dropped the now-unused monolith `MergedCell` import + `_TRUNK_STATUS_WORDS` (textutils) + `sortkey` (excel) re-imports (ruff F401 + updated the step-20 sortkey identity assertion). **The whole Excel layer now lives in `cisco_toolkit/excel.py` (~1,203 lines).** This is the golden's interface sheet, so byte-exactness was the priority — golden **byte-identical**. `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~1,865 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.35** — *PHASE 2.7 step 25: move the fused compute-in-writer sheets to the Excel layer (behaviour byte-identical).* Moved `write_physical_health_sheet` + `write_l3_forwarding_sheet` (each computes its L1/L3 records inline and RETURNS them to `main()` for the snapshot) + `write_flow_trace_sheet` (renders a `trace_full_flow()` dict) into `cisco_toolkit/excel.py`, along with `_parse_track`/`_track_summary` (package-internal) and the `_RISK_FILL`/`_RISK_RANK`/`PHYSICAL_HEALTH`/`L3_FORWARDING` constants. excel.py now imports `build_network_model`/`_physical_uplink_index`/`_poe_device_util` (analyze) + the physical/FHRP parsers (parse) + `normalize_speed` (textutils). Triggered an 11-symbol F401 cascade — those deps + `_SEV_FILL` (its last monolith writers moved) were dropped from the monolith re-imports; repointed `test_parsers` (`parse_show_interface_counters`) + several `test_package` identity assertions. **All sheet writers now live in `cisco_toolkit/excel.py` (~1,107 lines) except `append_interface_rows`.** Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~1,945 lines** (under 2k; from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.34** — *PHASE 2.7 step 24: move the Tier-2 (file-reading) + intelligence sheet writers to the Excel layer (behaviour byte-identical).* Moved `write_interface_health_sheet` / `write_security_posture_sheet` / `write_routing_adjacency_sheet` (read collected output + call parsers) and `write_causality_chains_sheet` / `write_failure_impact_sheet` (call the blast-radius computes) into `cisco_toolkit/excel.py` — which now imports `_load_cmd_output` (cmdio) + 7 parsers (parse) + `compute_causality_chains`/`compute_failure_impact` (analyze). All five imported back for `main()`. Triggered the largest F401 cascade so far: the 6 routing/security parsers + the 2 blast-radius computes + `_census_header`/`_census_autofit` (no monolith writer uses them anymore) were dropped from the monolith's re-imports; repointed `test_parsers` (parse_ospf/bgp) + `test_package` identity assertions. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~2,197 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.33** — *PHASE 2.7 step 23: move the health/analysis sheet writers to the Excel layer (behaviour byte-identical).* Moved `write_cross_layer_sheet` / `write_protocol_health_sheet` / `write_health_scores_sheet` / `write_score_sensitivity_sheet` / `write_migration_readiness_sheet` (+ their `_CL_FILL` / `_READY_FILL` / `_STATUS_FILL` fill maps + `*_SHEET_NAME` constants) into `cisco_toolkit/excel.py`. These render already-computed records (main() computes via analyze, passes them in), so the only analyze dep is `_health_band` (now imported by excel.py for the health-scores fill). Relocated the shared `_SEV_FILL` severity fill-map to excel.py too (write_protocol_health_sheet needs it) — imported back for the causality/failure/physical writers still in the monolith. `_health_band` was its last monolith user → dropped from the monolith's analyze import (ruff F401 + step-15 assertion updated). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~2,395 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.32** — *PHASE 2.7 step 22: move the analysis sheet writers to the Excel layer (behaviour byte-identical).* Moved `write_move_group_sheet` / `write_topology_sheet` / `write_findings_sheet` / `write_capacity_sheet` / `write_topology_diagram` (+ their `*_SHEET_NAME` constants + `_to_float` / `_mermaid_id`) into `cisco_toolkit/excel.py`. These call the analyze computes, so excel.py now imports `compute_move_groups` / `compute_topology_links` / `compute_findings` from analyze (excel sits above analyze — one-way) + `os` (the diagram writer emits .mmd/.dot files). All five imported back for `main()`. `compute_topology_links` / `compute_findings` were their last monolith users → dropped from the monolith's analyze import (ruff F401); repointed `test_cry_wolf` at `analyze.compute_findings` + updated the step-12 identity assertions. `compute_move_groups` stays re-exported (main() calls it directly for migration-readiness). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~2,544 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.31** — *PHASE 2.7 step 21: move the census/inventory sheet writers to the Excel layer (behaviour byte-identical).* Moved `write_device_inventory_sheet` / `write_svi_gateway_sheet` / `write_stp_detail_sheet` / `write_vlan_census_sheet` / `write_endpoint_census_sheet` (+ their `*_COLUMNS` / `*_SHEET_NAME` constants + `_is_svi`) into `cisco_toolkit/excel.py` (which gained a module `logger`, `model` + `_split_macs` imports). Pure-render writers — no analyze compute. All five imported back for `main()`; the constants + `_is_svi` are package-internal. `_split_macs` was `write_vlan_census_sheet`'s last monolith user → its textutils re-import dropped (ruff F401 + step-11 assertion updated). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass**. Monolith **~2,719 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.30** — *PHASE 2.7 step 20: stand up the Excel layer's shared helpers (behaviour byte-identical).* New `cisco_toolkit/excel.py` (openpyxl + stdlib + textutils; a clean layer above analyze) holds the sheet-formatting helpers `_census_header`/`_census_autofit` and the template header-matching primitives `norm_header`/`HEADER_TO_FIELD`/`find_header_row`/`ensure_headers` + the port-row `sortkey`. The monolith imports back `_census_header`/`_census_autofit` (~12 sheet writers), `find_header_row`/`ensure_headers` (main() template-fill), `sortkey` (append_interface_rows); `norm_header`/`HEADER_TO_FIELD` are package-internal. This is the foundation for moving the `write_*` sheet writers in following PRs. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **65 tests pass** (+1 excel identity/smoke test). Monolith **~2,973 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.29** — *PHASE 2.7 step 19: flow-trace joins the analyze layer — analyze layer COMPLETE (behaviour byte-identical).* Moved `trace_full_flow` + its helpers `_ip_in_prefix` / `_find_endpoint_by_ip` / `_find_gateways_for` / `_bfs_forwarding_path` into `cisco_toolkit/analyze.py` (pure on InterfaceData + the shared model; no file I/O; deps `_is_physical_port`/`_link_carries`/`build_network_model`/`_physical_uplink_index` already in the package). `trace_full_flow` imports back for `main()` (`--flow-src`/`--flow-dst`); the four helpers are package-internal. `_RISK_FILL`/`_RISK_RANK` + `FLOW_TRACE_SHEET_NAME` + `write_flow_trace_sheet` stay (excel); the L3-forwarding writer stays too (it computes its records inline + writes — moves with excel). `_link_carries` was `_bfs_forwarding_path`'s last monolith user → import-back dropped (F401 + step-13 assertion updated). **The whole analyze layer now lives in `cisco_toolkit/analyze.py` (~1,344 lines).** Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **64 tests pass**. Monolith **~3,064 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.28** — *PHASE 2.7 step 18: dependency-map + cross-layer correlations join the analyze layer (behaviour byte-identical).* Moved `build_dependency_map`, `compute_cross_layer_correlations` (the CL-01..CL-10 rules), `all_hosts`, and the `_CL_RANK` constant into `cisco_toolkit/analyze.py` (now unblocked — `build_dependency_map`'s deps `build_network_model`/`_vlan_components`/`_physical_uplink_index`/`_is_physical_port` are all in the package). `build_dependency_map` + `compute_cross_layer_correlations` import back for `main()`; `all_hosts`/`_CL_RANK` are package-internal. `_CL_FILL` + `CROSS_LAYER_SHEET_NAME` stay (excel). `build_dependency_map` was the monolith's last `_vlan_components` user, so that import-back was dropped (ruff F401 + updated the step-13 identity assertion). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **64 tests pass**. Monolith **~3,270 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.27** — *PHASE 2.7 step 17: physical-health helpers move to the package (behaviour byte-identical).* Moved the pure physical-port parsers `_NON_PHYSICAL_RE` (internal) / `_is_physical_port` / `_classify_media` / `parse_interface_phy` / `_parse_poe_watts` into `cisco_toolkit/parse.py`, and the compute helpers `_poe_device_util` / `_physical_uplink_index` into `cisco_toolkit/analyze.py` (which now imports `DevicePhysical` too). The four parsers + two compute helpers import back for `write_physical_health_sheet` (+ `build_dependency_map`, which uses `_is_physical_port`/`_physical_uplink_index` — this unblocks moving the dependency-map cluster next). `IFACE_TOKEN_RE`/`VALID_IFACE_RE` were the phy parsers' last monolith users, so their textutils re-imports were dropped (ruff F401-fix); `PHYSICAL_IFACE_RE` stays. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **64 tests pass**. Monolith **~3,437 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-05 | **V3.23.26** — *PHASE 2.7 step 16: protocol-health joins the analyze layer (behaviour byte-identical).* Moved `compute_protocol_health` + its analyze-internal sub-parsers `_parse_stp_mode` / `_parse_stp_tcn` / `_parse_etherchannel_member_states` / `_parse_vtp_full` into `cisco_toolkit/analyze.py` (which now imports the STP/EtherChannel/routing parsers + `_parse_fhrp` from `parse.py`). Relocated the pure FHRP behaviour-string parser `_parse_fhrp` down to `cisco_toolkit/parse.py` since it's shared with the monolith's `write_l3_forwarding_sheet` (avoids a circular import); imported back. `compute_protocol_health` imported back for `main()`. `PROTOCOL_HEALTH_SHEET_NAME` stays (excel). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **64 tests pass**. Monolith **~3,536 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.25** — *PHASE 2.7 step 15: scoring + migration-readiness synthesis joins the analyze layer (behaviour byte-identical).* Moved `_ESSENTIAL_CMD_VARIANTS` + `compute_data_quality` (uses `cmdio._load_cmd_output`), `compute_health_scores`, `compute_score_sensitivity`, `compute_migration_readiness` into `cisco_toolkit/analyze.py` (analyze.py now imports `_load_cmd_output` from the cmdio leaf). The four `compute_*` import back for `main()`. `ScoringConfig` / `SCORING` / `_host_role` are no longer re-exported by the monolith (their last in-file users moved); `_health_band` stays (write_health_scores_sheet uses it). Pointed the PHASE-2.1/2.2/2.4 unit tests (`test_scoring_config`/`test_cry_wolf`/`test_data_quality`) at `cisco_toolkit.analyze` for those now-package-internal symbols. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **64 tests pass**. Monolith **~3,650 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.24** — *PHASE 2.7 step 14: home the command-output I/O glue (behaviour byte-identical).* Moved `_CISCO_ERRORS`, `_load_cmd_output` (fail-soft loader of collected show output) and `_safe_parse` (section-parser fail-soft wrapper) into a new `cisco_toolkit/cmdio.py` leaf (stdlib only: os/logging/typing; its own module `logger`). All three imported back — `_load_cmd_output`/`_safe_parse` are called from ~50 sites across build_*/compute_*/write_*, and `_CISCO_ERRORS` is reused by platform detection. This is the gate for moving the I/O-fed `compute_*`. Golden **byte-identical** (the loader/parse breadcrumbs now log under `cisco_toolkit.cmdio`, which doesn't touch the snapshot/Excel contract), `ruff` clean, mypy unchanged (101). **64 tests pass** (+1 cmdio identity/functional test). Monolith **~3,842 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.23** — *PHASE 2.7 step 13: network-model / blast-radius cluster joins the analyze layer (behaviour byte-identical).* Moved the contiguous pure cluster `_vlan_in_ranges`, `build_network_model`, `_link_carries`, `_vlan_components`, `compute_causality_chains`, `compute_failure_impact` into `cisco_toolkit/analyze.py` (added `Optional` to its typing import). Monolith imports back `build_network_model` / `_link_carries` / `_vlan_components` (the physical/L3/flow functions still here use them) + `compute_causality_chains` / `compute_failure_impact` (their write_* sheets). `build_network_model` was the last monolith user of `_canon_host`/`_canon_host_map`, so those step-12 import-backs were dropped (ruff-confirmed); updated the now-stale identity assertions. Causality/Failure `*_SHEET_NAME` + `_SEV_FILL` stay (excel). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **63 tests pass**. Monolith **~3,866 lines** (under 4k; from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.22** — *PHASE 2.7 step 12: topology-links + findings join the analyze layer (behaviour byte-identical).* Moved `compute_topology_links` and `compute_findings` plus their analyze-internal helpers `_canon_host`, `_is_infra_neighbor`, `_canon_host_map` and the `_SEV_RANK` constant into `cisco_toolkit/analyze.py`. Monolith imports back `compute_topology_links` (write_topology_sheet / write_topology_diagram), `compute_findings` (write_findings_sheet), and `_canon_host`/`_canon_host_map` (build_network_model). `MOVEGROUP_EXCLUDED_VLANS` went package-internal — its last monolith user (`compute_findings`) moved, so that import-back was dropped (ruff-confirmed, no F401). `TOPOLOGY_SHEET_NAME`/`FINDINGS_SHEET_NAME` stay (excel). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **63 tests pass**. Monolith **~4,149 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.21** — *PHASE 2.7 step 11: move-group computation joins the analyze layer (behaviour byte-identical).* Moved `compute_move_groups` + its union-find helpers `_uf_find`/`_uf_union` + `MOVEGROUP_EXCLUDED_VLANS` into `cisco_toolkit/analyze.py`. Relocated the pure one-line MAC-splitter `_split_macs` down to `cisco_toolkit/textutils.py` (the leaf) since it's shared by the VLAN census (excel) + `compute_move_groups`/`build_network_model` (analyze) — that breaks the would-be circular import. Monolith imports back `compute_move_groups` (for `write_move_group_sheet`), `MOVEGROUP_EXCLUDED_VLANS` (for `compute_findings`, still here), and `_split_macs`. `MOVEGROUP_SHEET_NAME` stays (excel). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **63 tests pass**. Monolith **~4,259 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.20** — *PHASE 2.7 step 10: extract the analyze layer's scoring foundation (behaviour byte-identical).* Moved `_HEALTH_BANDS`, the `ScoringConfig` dataclass (+ module-default `SCORING`), and the two pure helpers `_health_band` / `_host_role` into a new `cisco_toolkit/analyze.py` (depends only on `dataclasses`, stdlib, and `cisco_toolkit.model`); the `compute_*` functions still in the monolith import them back. As the **last dataclass user**, this also moved the top-level `from dataclasses import dataclass, field` into the package (monolith F401-fix). The Excel fill maps `_READY_FILL`/`_STATUS_FILL` + sheet-name constants stay — they belong to the excel layer. The `compute_*` functions follow in later steps (they entangle with the `_load_cmd_output` I/O helper still in the monolith). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **63 tests pass** (+1 analyze identity/smoke test). Monolith **~4,328 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.19** — *PHASE 2.7 step 9: extract the data model (behaviour byte-identical).* Moved the two passed-around record dataclasses `InterfaceData` and `DevicePhysical` into a new `cisco_toolkit/model.py` leaf (depends only on `dataclasses`); the monolith imports them back so every type hint and constructor keeps working. `ScoringConfig` deliberately **stays** in the monolith — it travels with the analyze layer, not the data model (and still needs `dataclass`/`_dcfield`, so no dropped import). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). **62 tests pass** (+1 model identity/smoke test). Monolith **~4,375 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.18** — *PHASE 2.7 step 8: extract counters + security parsers (behaviour byte-identical).* Moved `parse_show_interface_counters`, `parse_port_security`, `parse_auth_sessions`, `parse_dhcp_snooping_binding` into `cisco_toolkit/parse.py` (added `VALID_IFACE_RE`/`PHYSICAL_IFACE_RE` to its textutils import); all 4 import back. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. Monolith **~4,450 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.17** — *PHASE 2.7 step 7: extract version/inventory/environment parsers (behaviour byte-identical).* Moved `parse_show_version`, `_inv_commit` + `parse_show_inventory`, `parse_show_environment_power`, `parse_show_environment`, `parse_show_module_count` into `cisco_toolkit/parse.py` (added a module `logger` there for the power-calc debug breadcrumb). `_inv_commit` is package-internal; the 5 parsers import back. `normalize_mac` is now package-internal too (monolith import dropped, ruff F401). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. Monolith **~4,540 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.16** — *PHASE 2.7 step 6: extract run-config / etherchannel / ARP / VTP / mgmt-IP / multicast parsers (behaviour byte-identical).* Moved 10 more pure functions into `cisco_toolkit/parse.py` (run-config, `_proto_from_token` + the 3 etherchannel parsers, ip-arp, vtp, `_parse_ip_int_brief` + mgmt-ip, multicast). `_proto_from_token`/`_parse_ip_int_brief` are package-internal; the other 8 import back. `is_valid_iface` is now package-internal too (its monolith import dropped, ruff F401). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. Monolith **~4,740 lines** (from ~5,769). In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.15** — *PHASE 2.7 step 5: extract the bulk interface-command parsers (behaviour byte-identical).* Moved 11 pure parsers + 2 constants into `cisco_toolkit/parse.py`: `parse_show_mac_address_table`, `parse_spanning_tree_blockedports`/`_detail`/`_states` (+`_STP_STS_NAME`), `parse_vlan_brief`, `_compress_vlans`, `parse_show_vrf_interface`, `parse_show_power_inline`, `parse_neighbors_cdp`/`_lldp`, `infer_endpoint_type` (+`_DESC_EP_PATTERNS`); the monolith imports them back. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. Monolith now **~4,950 lines** (from ~5,769); `parse.py` ~510. In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.14** — *PHASE 2.7 step 4: extract the interface-table parsers (behaviour byte-identical).* Moved `parse_show_interface_status`, `parse_show_interface_switchport`, `parse_show_interface_trunk_table` into `cisco_toolkit/parse.py`. The monolith imports them back and no longer needs `extract_fixed_cols`/`slice_col`/`normalize_status`/`normalize_duplex` (now used only inside the package), so those import-backs were dropped (ruff F401). Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.13** — *PHASE 2.7 step 3: extract the pure routing/FHRP parsers (behaviour byte-identical).* Moved `parse_ip_routes`, `parse_hsrp_summary`, `parse_vrrp_summary`, `parse_glbp_summary` into `cisco_toolkit/parse.py` (pure; `re` + `textutils`); the monolith imports them back. Golden **byte-identical**, `ruff` clean, mypy unchanged (101). 61 tests pass. In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.12** — *PHASE 2.7 step 2: extract parse primitives + pure neighbor parsers (behaviour byte-identical).* Moved the column primitives (`extract_fixed_cols`, `slice_col`) and the self-contained routing-neighbor parsers (`parse_ospf_neighbors`, `parse_eigrp_neighbors`, `parse_bgp_summary`) into `cisco_toolkit/parse.py` (depends only on `re` + `cisco_toolkit.textutils`); the monolith imports them back. Verified: golden **byte-identical**, `ruff` clean, mypy unchanged (101); +1 boundary/identity test. **61 tests pass.** Step 2 of the gated split; the remaining `parse_*` (most are entangled with `build_interfaces` helpers) + analyze / excel / html layers follow. In-code version `3.23.0`. |
| 2026-06-04 | **V3.23.11** — *PHASE 2.7 step 1: begin the package split (behaviour byte-identical).* Created the `cisco_toolkit/` package and extracted the pure, leaf-level interface-name / text helpers (`normalize_ifname`, `is_valid_iface`, `normalize_status/duplex/speed/mac`, `detect_link_type`, `safe_fs_name`) + the interface regex constants (`IFACE_TOKEN_RE`, `VALID_IFACE_RE`, `PHYSICAL_IFACE_RE`, `_TRUNK_STATUS_WORDS`) into `cisco_toolkit/textutils.py` (depends only on `re`). The monolith imports them back, so every reference — and the `import COLLECT_PARSE_V3_23_0` entrypoint/tests — keeps working unchanged. Verified: golden **byte-identical**, `ruff` clean, mypy baseline unchanged (101); +2 tests (`tests/test_package.py`) incl. an identity check that the monolith uses the package objects (guards against re-definition). **Step 1 of the gated split**; the parse / analyze / excel / html layers follow in subsequent PRs. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.10** — *mypy baseline (PHASE 2.6 "code health"; report-only, behaviour byte-identical).* Stood up a report-only `mypy` baseline: added `mypy.ini` (lenient — `ignore_missing_imports`, py3.10 target; **not** gated) + `mypy` to `requirements-dev.txt`. `python -m mypy COLLECT_PARSE_V3_23_0.py` → **101 findings**, inspected and confirmed to be dynamic-typing strictness (union-narrowing on `float|str` values that are runtime-guarded by `!= ""`, `dict[str, object]` attribute/operator access, untyped openpyxl) — **not bugs**; tracked for incremental future typing rather than rewritten wholesale (per "don't rewrite working code for style"). Fixed the 2 cleanly-typeable `var-annotated` easy wins (`proto_high`, `nofhrp_vlans`). The `compute_*` functions already carry type hints + docstrings. `ruff check .` stays clean; **58 tests pass**, golden byte-identical. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.9** — *lint easy-wins (behaviour byte-identical; `ruff check` now clean).* Fixed the 5 pyflakes findings from the V3.23.8 baseline: aliased the dataclasses import (`field as _dcfield`) so the module-level `field` no longer shadows the `field` loop variables in the sheet writers (4× **F402**), and removed the write-only `ios_top` variable in `parse_show_interface_trunk_table` (1× **F841**; IOS is the implicit `else`, behaviour unchanged). The lone **E401** (multi-import line) is intentional compact style → now ignored in `ruff.toml`. `python -m ruff check .` → **All checks passed**; **58 tests pass**, golden byte-identical. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.8** — *loose ends: P4 + M2 + ruff baseline (behaviour byte-identical).* **P4:** `warnings.filterwarnings("ignore")` narrowed to `category=DeprecationWarning`, so genuine `UserWarning`/`RuntimeWarning` signals surface instead of being globally suppressed (netmiko imports clean on this env → low risk). **M2:** a single `__version__ = "3.23.0"` now feeds the argparse description, the snapshot `script_version` (still emits `"V3.23.0"` → **golden byte-identical**), and the log filename (fixing the stale `v3_14_6`). **ruff (report-only):** added `ruff.toml` that keeps correctness checks (`F`, `E4/E7/E9`) but ignores the intentional `E701/E702/E741` compact-style rules, taking the baseline from **310 → 6** real findings (4× F402 import-shadowed-by-loop-var, 1× F841 unused-var, 1× E401) — catalogued for a future targeted lint pass, **not** fixed here; `ruff` added to `requirements-dev.txt`. +1 test (`tests/test_version.py`); **58 pass**. |
| 2026-06-04 | **V3.23.7** — *"missing data ≠ healthy" (PHASE 2.4; closes audit C3).* Added `compute_data_quality` — per-switch fraction of an essential command set (status / switchport / version / neighbors) that returned usable output, via `_load_cmd_output` — and wired it into `compute_health_scores`: a switch below `ScoringConfig.data_quality_threshold` (default **0.5**) is reported **`Insufficient Data`** (neutral grey) instead of a misleadingly-high band, and each record + `Health Scores` row now carries a **Data Quality %**. A partial/failed collection can no longer masquerade as a healthy switch. **Opt-in by construction:** the override + `data_quality` field appear only when `compute_health_scores` is called with the new `data_quality=` arg (`main` always passes it; direct callers without it stay byte-identical). Golden diff is **additive-only** (the `data_quality` key per record + the sheet column; fully-collected fixtures stay 1.0 with unchanged bands). +5 tests (`tests/test_data_quality.py`); **57 pass**. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.6** — *parser robustness (PHASE 2.3; behaviour byte-identical).* Added `_safe_parse(fn, *args)` and routed every section parser call in `build_interfaces` through it: a parser that raises on a malformed/unexpected block now logs a breadcrumb and that section is skipped, while the **rest of the device still parses** (extends the FIX-V14-1 "guard and continue" idea from the device level down to the section level — resolves the deferred audit item **P1**). Happy path is unchanged because the parsers already return `{}` on empty input, so the wrap is value-preserving → golden byte-identical. Added NX-OS parser-variant unit tests (`tests/test_platform_variants.py`: interface status / port-channel / MAC / trunk) and resilience tests (`tests/test_parser_robustness.py`: one raising parser, and several at once, no longer drop the device). **52 tests pass.** Known gap (documented below): the NX-OS `show interface trunk` native-VLAN column under its 2-line header still parses via the IOS branch. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.5** — *cry-wolf calibration & transparency (PHASE 2.2).* **(1) Findings dedup:** `compute_findings` collapses per-port err-disabled / STP-inconsistent rows into one weighted row per switch with a count + port list (kills the "sea of red"; Findings-sheet rows only — snapshot & headers unchanged). **(2) Asset-criticality weighting:** `ScoringConfig.criticality_factors` scales a switch's deductions by its inferred role (`_host_role`: hosts an SVI → distribution, else access), applied via `round()` so integer scores are preserved; ships at **1.0 for every role**, so health scores stay byte-identical until tuned. **(3) Sensitivity sweep:** new `compute_score_sensitivity` + `Score Sensitivity` sheet + `snap_dict["score_sensitivity"]` — a one-at-a-time ±25/±50% sweep over each weight group reporting how many switches change band (OECD/JRC robustness; "which weights actually move verdicts"). Snapshot gains the **additive** `score_sensitivity` key (HTML ignores unknown keys). Verified: golden diff is **additive-only** (new sheet + key; existing scores/readiness byte-identical), +6 tests in `tests/test_cry_wolf.py`, **45 tests pass**. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.4** — *externalize scoring tunables (PHASE 2.1; behaviour byte-identical).* Lifted the health-score weights (`L1W/L3W/XLW/PW`), per-category `CAP`s, the score `bands`, and the 10 migration-readiness pass/warn/fail rules out of function-local dicts into a single frozen **`ScoringConfig`** dataclass (module default `SCORING`). `compute_health_scores` and `compute_migration_readiness` take an optional `config=SCORING` arg, so every existing call site is unchanged; recalibrate by passing a custom `ScoringConfig(...)`. Defaults reproduce the prior values exactly (**golden snapshot byte-identical**), verified by 5 new wiring tests (`tests/test_scoring_config.py`: a custom weight/cap/band/readiness rule changes output; defaults match the baseline). Also fixed a latent gap the tests surfaced: the band was derived from the module default, now from `config.bands`. Unblocks the PHASE 2.2 cry-wolf/sensitivity work. In-code version strings stay `3.23.0`. |
| 2026-06-04 | **V3.23.3** — *resilience + collection robustness (audit fixes C1, C2).* **C1:** every pre-save phase (interface rows + the ~20 analysis/sheet writers, Phases 6–29) now runs through a `_run_phase` guard that logs-and-continues on failure, so one writer/compute error can no longer abort the whole run **before `wb.save()`** (extends the FIX-V14-1 "guard and continue" idea fleet-wide); a failed `build_dependency_map` falls back to an empty skeleton (`_empty_dep_map`) so Phases 28–29 still produce. **C2:** `send_cmd` now prefers pattern-based `send_command` (waits for the device prompt) over `send_command_timing` (which could silently return truncated output that was then parsed as complete), falling back to timing-based then to `""`. Added a runtime `requirements.txt` (netmiko, openpyxl). Verified: golden snapshot + Excel sheet schema **byte-identical** (happy path unchanged) and 7 new resilience unit tests (`_run_phase`, `_empty_dep_map`, `send_cmd` pattern/fallback/empty). In-code version strings remain `3.23.0` (per the V3.23.1/.2 convention). |
| 2026-06-04 | **V3.23.2** — *test safety net (additive; no behavioral change to the script).* Added a `pytest` suite under `tests/`: synthetic IOS+NX-OS offline fixtures (3 switches: trunks, a 2-member port-channel, HSRP-redundant SVIs, a sole-gateway VLAN, a single-fiber uplink, an err-disabled port, a down OSPF neighbor); a **golden end-to-end regression** that runs the real `--no-collect` pipeline (`--workers 1`) and asserts the normalized `snapshot.json` + the Excel sheet-name/header schema (`tests/golden/`); and unit tests for the parsers and the `compute_*` intelligence functions (cap behavior + an explicit band-spread/anti-cry-wolf check). 27 tests, deterministic. The in-code version strings stay at `3.23.0` (same convention as V3.23.1). Run: `python -m pytest`; regenerate goldens after a reviewed change with `UPDATE_GOLDEN=1 python -m pytest tests/test_pipeline_golden.py`. |
| 2026-06-04 | **V3.23.1** — *connection resilience.* `connect_device` now retries transient/timeout SSH failures with linear backoff (`CONNECT_MAX_ATTEMPTS=3`, `CONNECT_BACKOFF_BASE=2.0s`); **authentication failures are never retried** (avoids account lockout). Platform autodetect runs once per device, not per retry. **No behavioural change on first-attempt success.** Set `CONNECT_MAX_ATTEMPTS=1` to restore single-shot. Tolerant `netmiko.exceptions` import (falls back to message sniffing if the class isn't importable). Verified by a 4-scenario stub test (first-try success, auth no-retry, timeout retry-to-max, recover-on-2nd). Note: in-file docstring/argparse version strings and filename left at 3.23.0 (release-naming is the maintainer's call). **Also (C):** hoisted `Font` / `PatternFill` / `Alignment` / `get_column_letter` to module scope, removing **28 duplicate local imports** across the sheet writers — pure cleanup, no behavioural change (verified: module imports + all writers resolve the names globally). **Also (E):** `--compare` now exits with a clear `ap.error` message on a missing or malformed snapshot file instead of a raw traceback (verified: exit 2 + message for not-found and bad-JSON). **Also (A) — credentials.** Password is no longer a required field in the devices file. It now resolves in order: explicit `"password"` (back-compat) → per-entry `"password_env": "VAR"` (read from `os.environ`) → global `$CISCO_PASS` → a secure `getpass` prompt (once per username) **only on an interactive TTY** — an unattended/batch run never blocks and instead warns + leaves blank (those devices fail auth visibly via the no-retry path above). SSH-key auth intentionally not added. Verified: 6 cases (explicit, `password_env`, `$CISCO_PASS`, non-TTY warn-no-hang, TTY prompt-once-per-user, missing required key still raises). **Also (D) — observability.** Four previously-silent `except` swallows (`autodetect_platform`, `detect_platform_from_files`, `_load_cmd_output`, remaining-power calc) now emit a `logger.debug` breadcrumb; control flow and exception types are unchanged, so behaviour at the default INFO level is identical and only `--debug-*`/DEBUG surfaces the cause. The `ipaddress` in-prefix predicates were left broad on purpose (returning `False` on bad input is the correct answer, not a hidden error). |
| 2026-06-03 | **V3.23.0** — `compute_health_scores` + `Health Scores` sheet (Phase 28); `compute_migration_readiness` + `Migration Readiness` sheet (Phase 29); cross-layer findings gain `hosts`; `snap_dict["health_scores"]` + `["migration_readiness"]`; HTML 🏥 Health mode. Verified: 22 Python + 19 HTML + 9 end-to-end embed checks (scoring bands, 10-check readiness, node colouring, panel, empty states, snapshot re-parse). |
| — | (V3.22.0 and earlier: see the prior `.md`s) |
