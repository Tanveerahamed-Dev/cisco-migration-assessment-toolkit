---
category: ui-kit
---
The loading state: a centred spinner with a faint label ("Loading…" by default). Use it as the pending branch of any data-bound panel so loading looks the same everywhere.

```tsx
<div className="panel" style={{ minHeight: 160 }}>
  <Loading label="Building topology…" />
</div>
```
