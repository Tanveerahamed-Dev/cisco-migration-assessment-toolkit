---
category: ui-kit
---
The error state: a panel outlined in the critical token with a bold "Something went wrong." lead and the dimmed message after it. Use it as the error branch of any data-bound panel — same placement as `Loading`.

```tsx
<ErrorBox msg="503 Service Unavailable — the engine is still parsing this snapshot" />
```
