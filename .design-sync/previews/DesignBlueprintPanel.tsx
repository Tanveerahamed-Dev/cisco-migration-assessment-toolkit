import { DesignBlueprintPanel } from "assesshub-frontend";

/** The CCDE blueprint: trade-off scorecard, requirements form, decision cards,
 *  target-state (BoM, IP plan, waves), open questions and the coverage map. */
export const TargetStateBlueprint = () => <DesignBlueprintPanel snapId={1} />;
