import { CutoverPlanner } from "assesshub-frontend";

/** The gated pilot-first run-of-show: verdict badge, stat strip, three wave cards
 *  (GO / CONDITIONAL GO / NO-GO) with blockers and blast radius. */
export const GatedRunOfShow = () => <CutoverPlanner snapId={1} />;
