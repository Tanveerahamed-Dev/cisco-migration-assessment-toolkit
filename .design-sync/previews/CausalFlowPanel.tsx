import { CausalFlowPanel } from "assesshub-frontend";

/** Full panel: filter chips, the selected flow's bowtie (the demo fleet's critical
 *  first-hop compound), evidence chips and the finding card list. */
export const AllFindings = () => <CausalFlowPanel snapId={1} />;
