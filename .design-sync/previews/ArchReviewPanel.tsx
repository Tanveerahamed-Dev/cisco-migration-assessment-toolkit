import { ArchReviewPanel } from "assesshub-frontend";

/** "Ask the engineer": the C-grade header with conformance statement and the
 *  question chips routing into ranked actions and per-domain verdicts. */
export const AskTheEngineer = () => <ArchReviewPanel snapId={1} />;
