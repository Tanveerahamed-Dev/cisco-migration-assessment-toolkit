# Data fixtures, not architecture: keep them out of the knowledge graph.
# (Graph audit 2026-06-10: fixture JSON owned 8 of the 10 god-node slots and ~40
# communities, drowning the real engine/webapp structure. Code in these dirs —
# e.g. webapp/sample_data/build_sample.py — still gets scanned.)
tests/golden/*.json
webapp/sample_data/*.json

# 2026-06-25 graph audit: stale duplicate engine copy + the graph's own output dumps were
# polluting ~30% of nodes (root cause of duplicate god-nodes / ambiguous path matches).
_ref/
graphify-out/
_aci_conv.txt
graphify-out/memory/

# 2026-07 audit: linked git worktrees under .claude/ are full repo COPIES, so scanning them
# re-imports every engine/webapp module as a DUPLICATE node (the same god-node/ambiguous-path
# pollution the _ref/ copy caused). Ignore the whole tree — worktree pruning still leaves the
# 3 named claude/* checkouts, and their copies must never enter the graph.
.claude/worktrees/

# 2026-07-03 audit: untracked scratch + side-engagement dirs were diluting the graph ~27% (~1.9k
# of 6.9k nodes) and outranking real engine symbols in queries. None is repo SOURCE:
#   ds-bundle/ + .ds-sync/  = AssessHub design-sync OUTPUT bundle (the real webapp source is webapp/)
#   [HISTORY-REDACTED]_DC_Design/       = Qatar DC side-engagement deliverables (its own SSOT: [HISTORY-REDACTED]/ssot.py)
#   compass_artifact_*.md   = deep-research artifact dumps
#   scratch_* / CHAT_SUMMARY = working scratch;  _verify_nrfu.py = a root scratch NRFU probe
# (The graph's own graphify-out/ dumps are already excluded above.)
ds-bundle/
.ds-sync/
.gstack/
[HISTORY-REDACTED]_DC_Design/
compass_artifact_*.md
scratch_*.js
scratch_*.txt
CHAT_SUMMARY.md
_verify_nrfu.py
